<p align="center">
  <img src="docs/logo/opcgw-horizontal.svg" alt="opcgw — ChirpStack to OPC UA Gateway" width="400">
</p>

<p align="center">
  <a href="https://github.com/guycorbaz/opcgw/actions/workflows/ci.yml"><img src="https://github.com/guycorbaz/opcgw/actions/workflows/ci.yml/badge.svg" alt="build and test"></a>
  <img src="https://img.shields.io/badge/version-2.0.2-blue" alt="Version">
  <img src="https://img.shields.io/badge/license-MIT%2FApache--2.0-green" alt="License">
  <img src="https://img.shields.io/badge/arch-amd64%20%7C%20arm64-brightgreen" alt="Architectures">
</p>

# opcgw — ChirpStack to OPC UA Gateway

Bridge LoRaWAN device data from ChirpStack to industrial automation systems via OPC UA.

**opcgw** is a production-ready gateway that connects ChirpStack 4 LoRaWAN Network Server with OPC UA industrial clients, enabling seamless integration of wireless IoT devices into SCADA, MES, and industrial edge systems.

> 📖 **Full Documentation**: Visit the [GitHub Pages](https://guycorbaz.github.io/opcgw/) for detailed guides, architecture diagrams, and real-world use cases.

## Quick Links

- 🚀 [Quick Start Guide](https://guycorbaz.github.io/opcgw/quickstart/)
- 🏗️ [Architecture & Design](https://guycorbaz.github.io/opcgw/architecture/)
- ⚙️ [Configuration Reference](https://guycorbaz.github.io/opcgw/configuration/)
- 📋 [Features & Roadmap](https://guycorbaz.github.io/opcgw/features/)
- 💼 [Use Cases](https://guycorbaz.github.io/opcgw/usecases/)

## What is opcgw?

opcgw solves a critical integration challenge: connecting wireless LoRaWAN IoT networks managed by ChirpStack to industrial automation systems that speak OPC UA. 

**The Problem**:
- ChirpStack manages LoRaWAN devices but doesn't speak industrial protocols
- SCADA/MES systems expect OPC UA but don't understand LoRaWAN
- Building custom integrations is time-consuming and fragile

**The Solution**:
```
ChirpStack Server (LoRaWAN) ──→ opcgw Gateway ──→ OPC UA Clients (SCADA/MES)
   (gRPC polling)                (Rust, async)      (Ignition, KEPServerEx, etc.)
```

## Key Features

✨ **Real-Time Data Collection**
- Polls device metrics from ChirpStack at configurable intervals
- Supports multiple applications and hundreds of devices
- Handles network failures with automatic retries

🏭 **OPC UA Server**
- OPC UA 1.04 compliant server
- Dynamically builds address space from configuration
- Support for Float, Int, Bool, and String metrics
- Compatible with any standard OPC UA client

🔐 **Enterprise-Grade**
- Configuration validation on startup with clear error messages
- Environment variable credential management (no hardcoded secrets)
- Structured logging (tokio-tracing) for operational visibility
- Graceful shutdown handling
- Comprehensive error handling (no panics in production code)

🐳 **Container-Native**
- Official Docker image with multi-stage build
- Docker Compose for quick local development
- Kubernetes-ready with health checks

## Installation

### From Source (Rust 1.94.0+)

```bash
git clone https://github.com/guycorbaz/opcgw.git
cd opcgw
cargo build --release
./target/release/opcgw -c config/config.toml
```

### Via Docker (published image)

Pre-built images are published to two registries on every `v*` tag. Both registries receive identical multi-architecture manifests covering `linux/amd64` and `linux/arm64`.

**Docker Hub** (primary):

```bash
docker pull docker.io/gcorbaz/opcgw:2.0
```

**GitHub Container Registry** (mirror):

```bash
docker pull ghcr.io/guycorbaz/opcgw:2.0
```

The container runs as non-root user `opcgw` (UID 10001) and exposes port `4855` (OPC UA). Minimal `docker run` example:

```bash
docker run -d \
  --name opcgw \
  --restart unless-stopped \
  -p 4855:4855 \
  -e OPCGW_CHIRPSTACK__API_TOKEN='<your-chirpstack-api-token>' \
  -e OPCGW_OPCUA__USER_PASSWORD='<your-opc-ua-user-password>' \
  -v "$(pwd)/config:/usr/local/bin/config" \
  -v "$(pwd)/pki:/usr/local/bin/pki" \
  -v "$(pwd)/log:/usr/local/bin/log" \
  -v "$(pwd)/data:/usr/local/bin/data" \
  gcorbaz/opcgw:2.0
```

The `data/` bind mount is **required** so the SQLite database (and its 7-day metric history) survives container restarts. Without it, persisted metrics live in the ephemeral container layer and are destroyed on every `docker rm`.

#### First-run wizard (Epic C C-0)

If you start the gateway WITHOUT `OPCGW_OPCUA__USER_PASSWORD` set and without `[opcua].user_password` in `config/config.toml`, opcgw enters **first-run mode**: the OPC UA server rejects every authentication attempt while the web UI at `http://<host>:<web-port>/` serves a one-shot setup wizard at `/setup`. Submit a password through the wizard and opcgw will:

1. Persist the password to `config/secrets.toml` (file mode `0600`, gitignored).
2. Signal a graceful shutdown.
3. Be restarted automatically by Docker / systemd (per your restart policy).
4. Boot in normal mode with the new password loaded via the figment provider stack (precedence: env-var > `secrets.toml` > `config.toml`).

**Important — the wizard collects ONLY the OPC UA `user_password`.** Every other configuration field (ChirpStack credentials, OPC UA endpoint + PKI dir + application name, web admin credentials, etc.) must still be present in `config/config.toml` (or provided via `OPCGW_*` env-vars) before the gateway starts. A truly-empty `config.toml` will fail validation BEFORE the wizard becomes reachable. The minimum bootstrap `config.toml` shape:

```toml
[chirpstack]
server_address = "127.0.0.1:8080"
api_token = "<your-token>"
tenant_id = "<your-tenant-id>"

[opcua]
endpoint = "0.0.0.0:4855"
application_name = "opcgw"
pki_dir = "pki"
# user_name defaults to "opcua-user" via serde default (iter-2 P23).
# user_password collected via /setup wizard on first boot.

[web]
bind = "0.0.0.0"
port = 8080
admin_user = "admin"
admin_password = "<your-admin-password>"
```

The wizard is one-shot: subsequent `/setup` requests return HTTP 410 Gone. To rotate the password later, either override via `OPCGW_OPCUA__USER_PASSWORD` env-var or hand-edit `config/secrets.toml` and restart. The shipped `config/config.toml` in the repo already contains a working baseline you can adapt — copy it, fill in your credentials, and leave `user_password` unset.

Bind-mounted host directories must be owned by UID 10001 before first start (the container can't `chown` host files):

```bash
sudo mkdir -p ./config ./pki/{own,private,trusted,rejected} ./log ./data
sudo chown -R 10001:10001 ./config ./pki ./log ./data
sudo chmod 700 ./pki/private
# Tighten any pre-existing private-key files (no-op on a fresh install):
sudo find ./pki/private -type f -name '*.pem' -exec chmod 600 {} +
```

### Via `docker compose`

```bash
git clone https://github.com/guycorbaz/opcgw.git
cd opcgw
cp .env.example .env
# Edit .env to set OPCGW_CHIRPSTACK__API_TOKEN + OPCGW_OPCUA__USER_PASSWORD
docker compose up -d
```

See [`docker-compose.yml`](./docker-compose.yml) for the canonical recipe; see [`docs/security.md`](./docs/security.md) for the secret-management contract.

### Documentation

The complete operator-facing user manual lives at [`docs/manual/opcgw-user-manual.xml`](./docs/manual/opcgw-user-manual.xml) (DocBook 4.5 XML). Build it to HTML or PDF via the bundled Makefile:

```bash
cd docs/manual
make html     # → docs/manual/out/html/index.html
make pdf      # → docs/manual/out/opcgw-user-manual.pdf
```

Prerequisites: `xsltproc` for HTML; `dblatex` (or `fop`) for PDF. See [`docs/manual/README.md`](./docs/manual/README.md) for build details.

Additional reference docs:

- [`docs/deployment-guide.md`](./docs/deployment-guide.md) — runbook + Epic A migration procedure
- [`docs/security.md`](./docs/security.md) — credential management, PKI, audit-event recipes
- [`docs/logging.md`](./docs/logging.md) — structured-log taxonomy
- [`CHANGELOG.md`](./CHANGELOG.md) — release notes

## Configuration

opcgw uses a single TOML configuration file:

```toml
[chirpstack]
server_address = "http://chirpstack.local:8080"
api_token = "your-api-token"
tenant_id = "your-tenant-id"
polling_frequency = 10
# Story C-1: server-side TTL cache for /api/inventory/{applications,devices}.
# Default 60. Set to 0 to disable caching (every inventory request hits
# ChirpStack — useful for development). Env-var override:
# OPCGW_CHIRPSTACK__INVENTORY_CACHE_TTL_SECONDS. Restart-required.
inventory_cache_ttl_seconds = 60
# Story C-1: max wait window for /api/inventory/uplinks (bounded read
# against InternalService.StreamDeviceEvents). Default 5; range 1..=60.
# Env-var override: OPCGW_CHIRPSTACK__INVENTORY_UPLINK_MAX_WAIT_SECONDS.
inventory_uplink_max_wait_seconds = 5

[opcua]
application_name = "My IoT Gateway"
host_port = 4855
user_name = "admin"
user_password = "secure-password"

[[application]]
application_name = "Farm Sensors"
application_id = "1"

[[application.device]]
device_name = "Field A"
device_id = "sensor_001"

[[application.device.read_metric]]
metric_name = "Soil Moisture"
chirpstack_metric_name = "soil_moisture"
metric_type = "Float"
metric_unit = "%"
```

**For complete configuration details**, see the [Configuration Reference](https://guycorbaz.github.io/opcgw/configuration/).

> 🔐 **Secrets:** the shipped `config/config.toml` ships with placeholder
> values for `api_token` and `user_password`. The gateway refuses to
> start with the placeholders in place — inject the real values via the
> `OPCGW_CHIRPSTACK__API_TOKEN` and `OPCGW_OPCUA__USER_PASSWORD` env vars
> (or via the `.env` file consumed by Docker Compose). See
> [`docs/security.md`](./docs/security.md) for the full env-var
> convention, Docker / Kubernetes recipes, and the migration path for
> existing deployments.
>
> 🛡️ **OPC UA security:** for production OPC UA setup (endpoint matrix,
> PKI layout, `0o600` private-key requirement, audit-trail recipe,
> anti-patterns), see
> [`docs/security.md#opc-ua-security-endpoints-and-authentication`](./docs/security.md#opc-ua-security-endpoints-and-authentication).
> For OPC UA session-cap sizing (`max_connections` knob, the
> `event="opcua_session_count"` gauge, and the at-limit warn), see
> [`docs/security.md#opc-ua-connection-limiting`](./docs/security.md#opc-ua-connection-limiting).
> For subscription / message-size limits (`max_subscriptions_per_session`,
> `max_monitored_items_per_sub`, `max_message_size`, `max_chunk_count`)
> and the `DataChangeFilter` contract for stale-status notifications,
> see [`docs/security.md#subscription-and-message-size-limits`](./docs/security.md#subscription-and-message-size-limits).
> For OPC UA `HistoryRead` (FR22) — `[storage].retention_days` floor /
> hard-cap, the `[opcua].max_history_data_results_per_node` per-call cap,
> the manual-paging recipe (continuation points are not implemented), and
> NFR15's <2s 7-day query budget — see
> [`docs/security.md#historical-data-access`](./docs/security.md#historical-data-access).
> For the embedded **Web UI** (Story 9-1; FR50, NFR11, NFR12, FR41) — the
> `[web]` config block (port / bind_address / auth_realm / enabled —
> defaults to off), Basic auth shared with `[opcua]` credentials, the
> `event="web_auth_failed"` audit event with direct source-IP via Axum's
> `ConnectInfo`, and the TLS-via-reverse-proxy stance — see
> [`docs/security.md#web-ui-authentication`](./docs/security.md#web-ui-authentication).

### Web UI (Story 9-1, opt-in)

Story 9-1 ships an embedded Axum web server gated by HTTP Basic auth.
**Off by default.** To enable, add the `[web]` block (or set the
`OPCGW_WEB__*` env vars):

```toml
[web]
enabled = true              # default false
port = 8080                 # default 8080; range 1024-65535
bind_address = "0.0.0.0"    # default "0.0.0.0"
auth_realm = "opcgw"        # default "opcgw"; max 64 chars
```

Credentials are **shared with `[opcua]`** (same `user_name` /
`user_password`); one rotation step covers both auth surfaces. HTTP-only
— deploy a reverse proxy (nginx, Caddy, Traefik) for TLS termination if
your environment requires it. See
[`docs/security.md#web-ui-authentication`](./docs/security.md#web-ui-authentication).

**Story 9-2 ships the gateway status dashboard.** Once enabled, browse
to `http://<gateway-host>:8080/` for a single-screen view of ChirpStack
connection state, last-poll timestamp, cumulative error count, and
configured application + device counts. The dashboard auto-refreshes
every 10 seconds (hard-coded in `static/dashboard.js` — edit the file
to change the cadence). Mobile-responsive layout (single-column ≤ 600 px,
two-column above) and OS-driven dark mode (`prefers-color-scheme: dark`)
ship out of the box; no in-page toggle. The underlying JSON shape is
exposed as `GET /api/status` for `curl | jq` / Prometheus textfile
exporters / custom dashboards (auth-gated identically to the HTML).

**Story 9-3 ships the live metric values page.** Browse to
`http://<gateway-host>:8080/metrics.html` for the per-application,
per-device grid of current metric values. Each row shows the metric
name, value, data type, last-updated timestamp, and a staleness badge
(`Good` / `Uncertain` / `Bad` / `Missing`) computed from the
`[opcua].stale_threshold_seconds` knob (default 120 s) and the
hard-coded 24 h "bad" cutoff. Configured-but-never-polled metrics
appear as "Missing" rather than being silently omitted — operators
spot mis-configured devices at a glance. Same 10 s refresh + responsive
layout + dark mode as the dashboard. JSON contract at `GET /api/devices`.

## Planning

> ### ⚠️ Production-deployment blocker
>
> **GitHub issue [#108](https://github.com/guycorbaz/opcgw/issues/108)** — surfaced during Story 9-3 code review (2026-05-03) — flags a pre-existing project-wide storage bug: the `MetricType` enum is payload-less, so every row in `metric_values` has `value == data_type` (literally the string `"Float"` / `"Int"` / `"Bool"` / `"String"`) instead of the actual measurement. **This means opcgw has never persisted real metric values.** Affects 4 epics (2, 5, 8, 9-3); SCADA clients see literal type-name strings via OPC UA, dashboards show `"Float"` instead of `23.5`, HistoryRead returns type-strings.
>
> **Fix is an Epic-1-scale storage-trait refactor** (provisionally tracked as "Epic A — Storage Payload Migration"). Until #108 lands, **opcgw is suitable for device-presence monitoring only** ("is the sensor reporting?") — not for actual measurement collection.
>
> Epic 9 retrospective is blocked on #108. See `_bmad-output/implementation-artifacts/sprint-status.yaml` for the formal annotation.

**Current Version:** 2.0.0-rc — last updated 2026-05-19 (Story B-1 CODE REVIEW iter-1 same-LLM (Opus 4.7) complete — `bmad-code-review B-1` ran 3 parallel reviewer subagents (Blind Hunter, Edge Case Hunter, Acceptance Auditor) against commit `e7931fe`; surfaced 45 raw findings → 26 PATCH applied + 6 DEFER + 7 DISMISS (3 Blind Hunter false-positives + 4 noise). **Phrase-harmonization-drift was the dominant failure class** — 7 HIGH + 11 MED operator-doc bugs where the manual / dockerhub-description grep recipes targeted structured-log strings the source never emits. Strongest catches: T2 `OPCGW_WEB__USER_PASSWORD` (env var doesn't exist; `WebConfig` has no `user_password` field — web auth shares `[opcua]` credentials); T3 `OPCGW_OPCUA__ENDPOINT` wrong field name (real `host_port` + `host_ip_address`); T4+T6+T7 three separate non-existent structured-log events (`chirpstack_poll_start/_end`, `poll_cycle_complete`, `chirpstack_auth_failed` — none emitted by `src/`); T5 `"OPC UA server started"` string doesn't exist (replaced with `opcua_limits_configured`); T9 manual's 2 `docker run` examples (Docker Hub + GHCR sections) missing the `data/` mount — same gap the smoke-test caught during implementation but the subagent-authored manual was written BEFORE that fix landed; T10 `peter-evans/dockerhub-description@v4` failure was unguarded → would have killed the entire workflow (incl. GHCR publish) when DOCKERHUB secrets were missing (now `if: secrets != ''` + `continue-on-error: true`). Plus 19 lower-severity fixes covering portability (T22 em-dash→hyphen for shell locale safety, T24 U+2026→explicit cmd, T13 `/nonexistant`→`/nonexistent`), correctness (T17 `chmod 600 *.pem` → `find -exec` no-op-on-empty, T16 useradd `--user-group`, T14 Makefile html-single output dir so logo path resolves, T15 Makefile drops silent `|| true`, T35 systemd `EnvironmentFile` syntax warning, T36 curl uses `$OPCGW_OPCUA__USER_NAME`, T20 systemd `install -m 0644 -t static/` for web UI, T37 Dockerfile `EXPOSE 8080`), and phrase fixes (T29 "Chirpstack"→"ChirpStack", T30 short-description 109→83 chars under Docker Hub's 100-char API limit). T18 reclassified as not-a-bug. **Doctrine validated AGAIN** — same-LLM iter-1 surfaced exactly the class of bugs the iter-2/iter-3 doctrine predicts for doc-dominant stories. **Status STAYS `review`** pending iter-2 different-LLM per CLAUDE.md "Code Review & Story Validation Loop Discipline" + memory `feedback_iter3_validation` 13-story pattern (B-1 would extend the streak to 14 as the first non-Epic-A doc/infra story under the doctrine). Iter-2 critical to catch any HIGH-REG fake-regression-guard / phrase-harmonization-drift bugs in iter-1's own patches (A-4 JR1, A-5 K1+K2, A-6 K1-K6, A-7 L1+L2 precedent — every Epic A story had iter-2 catch real iter-1 patch regressions). Deferred items (6): T8 Cargo.toml `2.0.0-rc → 2.0.0` (must be separate pre-tag commit; strict-zero forbids in B-1), T11 docker/build-push-action@v5→v6 (needs real CI test on a `v*` tag push), T23 OPC UA port 4840 reference loss (minor info), T25 logo zip in git vs PDF gitignored (policy inconsistency), T26 Docker Hub Overview links to raw XML (needs CI HTML publish), AC#23 GitHub issue `Refs #__` placeholder (spec-anticipated). Post-patch verification: `cargo test --all-targets` exit 0; `cargo clippy --all-targets -- -D warnings` exit 0; `cargo test --doc` 0 failed / 55 ignored; `xmllint --noout --valid docs/manual/opcgw-user-manual.xml` exit 0; `git status -- src/ tests/ Cargo.toml Cargo.lock migrations/ config/` returns ZERO entries (strict-zero AC#20 preserved). Next: `bmad-code-review B-1` iter-2 on a different LLM. PRIOR — 2026-05-19 (Story B-1 REVIEW — `bmad-dev-story B-1` implementation complete. All 23 ACs SATISFIED; all 10 Tasks (Task 0-10) checked. D1 chosen: (a) auto-sync via `peter-evans/dockerhub-description@v4` step in workflow. D2 chosen: (a) Makefile under `docs/manual/` wrapping xsltproc + dblatex for CLI/CI manual builds. **Local Docker smoke test PASSED** on the hardened Dockerfile + updated docker-compose.yml: container runs as `uid=10001(opcgw)` (verified via `docker exec ... id`), all v001-v008 SQLite migrations apply cleanly, OPC UA server initialises (5118 address-space nodes imported, `event="opcua_session_count" current=0 limit=10` gauge firing). The smoke test caught five progressive startup issues during implementation that became Dockerfile / docker-compose / docs fixes (most consequential: pre-existing `docker-compose.yml` had NO `data/` bind mount → in production every `docker rm` would have destroyed the SQLite database + 7-day metric history; fixed by adding `data/` to Dockerfile pre-create + chown list AND to `docker-compose.yml` volume list AND to the documented operator `chown -R 10001:10001 ./config ./pki ./log ./data` step). Out-of-scope additions documented in Story Dev Agent Record: `docker-compose.yml` image source switched from `image: opcgw` (local-build-only) to `image: docker.io/gcorbaz/opcgw:2.0` (published image); logo pack `docs/logo/*.svg` (provided by Guy mid-story) embedded in README header + Docker Hub Overview page (GitHub raw URL) + DocBook manual `<bookinfo>` title page (via `<mediaobject>`); `.gitignore` extended with `docs/manual/out/` + `git rm --cached` on the stale committed PDF (manual build outputs are regeneratable via the new Makefile). **Strict-zero invariant VERIFIED CLEAN** via `git status --short -- src/ tests/ Cargo.toml Cargo.lock migrations/ config/` returning ZERO entries — no Rust production code changes, as required by AC#20. **Regression gate green:** `cargo test --all-targets` exit 0; `cargo clippy --all-targets -- -D warnings` exit 0; `cargo test --doc` 0 failed / 55 ignored (matches AC#21 expected). **DocBook 4.5 DTD validation green:** `xmllint --noout --valid docs/manual/opcgw-user-manual.xml` exit 0. DocBook manual update is substantive: +1921 / -106 lines (now 3975 lines, 14 top-level chapters/appendices); new `ch-upgrade` chapter referencing the Epic A migration runbook (Path A in-place / Path B drop-and-recreate); rewritten `ch-installation` (Docker Hub, GHCR, Docker Compose, systemd, build-from-source, post-install verification); rewritten `ch-configuration` (config.toml schema with field-by-field tables for `[chirpstack]`, `[opcua]`, `[web]`, `[[application]]` + audit-event taxonomy section); rewritten `ch-troubleshooting` with seven new operator scenarios + structured-log-event grep recipes; logo embedded on title page; v2.0.0-GA `revhistory` entry. Next: `bmad-code-review B-1` on a **different LLM** per CLAUDE.md "Code Review & Story Validation Loop Discipline" + memory `feedback_iter3_validation` 13-story validated pattern (B-1 would extend the streak to 14 as the first non-Epic-A doc/infra-dominant story to test the iter-2/iter-3 doctrine). GitHub tracking issue: TBD (Guy opens out-of-band; commit references via placeholder `Refs #__`). GitHub repo secrets reminder: `DOCKERHUB_USERNAME=gcorbaz` + `DOCKERHUB_TOKEN` (PAT, Read/Write/Delete scope) must be added by Guy before the first `v*` tag fires — without them the Docker Hub legs fail (GHCR still publishes). PRIOR — 2026-05-19 (Story B-1 IN-PROGRESS — `bmad-dev-story B-1` running. Epic B opened as the v2.0 GA Release Packaging epic, single multi-deliverable story `B-1-docker-hub-publishing-and-user-manual`. Implementation lanes: (1) `.github/workflows/docker-build.yml` rewritten for dual-registry multi-arch publishing — adds `docker/setup-qemu-action@v3`, second `docker/login-action@v3` for Docker Hub (`secrets.DOCKERHUB_USERNAME=gcorbaz` + `secrets.DOCKERHUB_TOKEN`), `docker/metadata-action@v5` `images:` list extended with `docker.io/gcorbaz/opcgw` + `ghcr.io/${{ github.repository }}`, `docker/build-push-action@v5` gains `platforms: linux/amd64,linux/arm64`, and a final `peter-evans/dockerhub-description@v4` step auto-syncs the hub.docker.com Overview page from `docs/dockerhub-description.md` on every `v*` tag (D1 chosen: auto-sync over manual copy-paste). (2) `Dockerfile` hardened — runtime base pinned `ubuntu:latest → ubuntu:24.04`, non-root `opcgw` (UID 10001) user enabled, `COPY --chown=opcgw:opcgw` for binary + static/ assets, `USER opcgw` before `ENTRYPOINT`. (3) NEW `docs/dockerhub-description.md` (the canonical Markdown source for the hub.docker.com Overview page; auto-synced on v* tag). (4) `README.md` "Via Docker (published image)" section rewritten with both pull paths + concrete `docker run` example + UID-10001 chown notes; "Documentation" subsection added pointing at the manual; Planning table gains Epic B row. (5) DocBook user manual `docs/manual/opcgw-user-manual.xml` will gain new Installation + Configuration + Troubleshooting + Upgrade chapters; `docs/manual/Makefile` added per D2 chosen (Makefile for CLI/CI builds via xsltproc + dblatex). Strict-zero invariants honoured: NO changes to `src/**/*.rs`, `tests/**/*.rs`, `Cargo.toml`, `Cargo.lock`, `migrations/*.sql`, `config/*`. Local Docker smoke test in progress (`docker build -t opcgw:smoke .` running). Next within this story: DocBook manual update (Tasks 6-8) + Makefile (Task 9) + CHANGELOG.md (Task 10.1) + regression-gate verification (Task 10.2) + sprint-status `B-1: in-progress → review` (Task 10.3). After B-1 ships: `bmad-code-review B-1` on a different LLM per CLAUDE.md doctrine + memory `feedback_iter3_validation` 13-story pattern (B-1 would extend to 14 as the first non-Epic-A doc/infra story to test the iter-2/iter-3 doctrine post-Epic-A). PRIOR — 2026-05-19 (Epic A RETROSPECTIVE DONE — `bmad-retrospective` Epic A complete; `epic-A-retrospective` flipped optional → done. Two-part retro output: (1) Epic Review with deep-story-analysis synthesis surfaced 13-story iter-3 doctrine validation (A-7 first doc-dominant confirming case), new "fake regression-guard test" finding class (A-4 JR1 / A-5 K1+K2 / A-6 P12 — tests that purport to pin a dropped path but whose seeds satisfy both old and new paths), phrase-harmonization-drift codified as iter-N+1 reviewer rule (A-2 / A-3 / A-4 / A-5 K5 / A-7 L1+L2 — cross-reference every patched string against its source-of-AC location), and closed-enum doc-sync gap (A-6 K1 added new closed-enum value in code but `docs/logging.md` not updated in same commit). (2) v2.0 GA readiness assessment replaced "next epic preparation" since there is no Epic B — Epic A IS the GA gating epic per Epic 9 retro AI6. **Security review conducted inline per CLAUDE.md "Epic Completion Requirements" — CLEAN with 1 LOW finding** (`docs/deployment-guide.md:285` Gotcha #6 had `rm ./opcgw.db*` glob that could remove backup files like `opcgw.db.bak.YYYY-MM-DD`; patched inline to explicit triple-suffix form `rm ./opcgw.db ./opcgw.db-wal ./opcgw.db-shm`). Strict-zero invariant confirmed (no auth/security/main::initialise_tracing touched across Epic A); SQL parameterized (`?N` throughout); `scripts/check-schema-version.sh` clean (POSIX `/bin/sh`, `set -eu`, quoted args, no `eval`, no destructive ops, pre-flight table-existence check prevents misidentifying non-opcgw SQLite files); migration DDL clean (v007 pure DDL + v008 BEGIN/COMMIT-wrapped); new structured-log closed enums are static literals; web display path safe (`document.createTextNode` — no XSS sink). Epic 9 retro follow-through: AI6 (most important) ✅ done; AI5 #102 elevated to v2.x HARD COMMITMENT after 3 consecutive retros of deferral. 11 action items captured: AI-A-1/2/3 codify iter-N+1 / fake-regression-guard / closed-enum-doc-sync checks in `bmad-code-review` skill; **AI-A-4 CHANGELOG entry for `MetricValue.value` SemVer-major retire (GA-BLOCKING)**; AI-A-5 #102 tests/common extraction (v2.x HIGH); AI-A-6 #100 doctest baseline cleanup (v2.x MED); AI-A-7 IH1 migration-runner atomicity (before next migration story); AI-A-8 manual XML sync (v2.x MED); AI-A-9 v008 SLA tuning (LOW deferred). **Critical path before `v2.0` tag**: (a) CHANGELOG entry (AI-A-4) + (b) `git push` (5 unpushed commits become 6 after the retro commit lands) per CLAUDE.md "After an epic retrospective" rule. Final verification: `cargo test` 1256 passed / 0 failed / 10 ignored; `cargo clippy --all-targets -- -D warnings` clean; doctest 0 failed / 55 ignored. Retro doc: [`_bmad-output/implementation-artifacts/epic-A-retro-2026-05-19.md`](./_bmad-output/implementation-artifacts/epic-A-retro-2026-05-19.md). PRIOR — 2026-05-18 (Story A-7 DONE — `bmad-code-review A-7` closed through 2 iterations on the same LLM (Opus 4.7); loop terminated per CLAUDE.md condition #2 (only LOW remains). **Memory `feedback_iter3_validation` 12-story pattern extends to doc-dominant stories — A-7 is the 13th confirming case** (the doctrine was previously established on code-heavy stories; A-7's narrow code surface produced fewer iter-2 findings but the phrase-harmonization-drift class still fired). Iter-1 applied 9 K-patches: **K1** runner-sequence callout (v007 commits independently before v008 BEGIN/COMMIT); **K2** `event="poll_cycle_complete"` → `operation="poll_cycle_end"` (verified against `src/chirpstack.rs:1523`); **K3** cross-reference target fix (`_bmad-output/planning-artifacts/architecture.md` not `docs/architecture.md`); **K4** v008 CHECK positive-case assertion (pins constraint enforceability in both directions); **K5** POSIX `/bin/sh` conversion (Alpine portability); **K6** multi-deployment journalctl recipes (systemd + Docker Compose + plain Docker + foreground); **K7** explicit triple-suffix `rm $DB $DB-wal $DB-shm` (no wildcard); **K8** Variant::Double → Variant::Float (matches A-4 `convert_metric_to_variant`); **K9** exit code 2 → 1 for unrecognised version (spec skeleton alignment). Iter-2 surfaced 2 HIGH-REG phrase-harmonization-drift findings (iter-1's K2+K3 patched the runbook but did NOT sync the spec source-of-AC text at AC#1 step 4 + AC#4 bullet 1 — same class as A-4 JR8 / A-5 K5) + K6 inconsistency (Post-migration verification step 2 only got 2 of 4 deployment-shape recipes) + K4 in-test comment muddled (v007 has no cross-column CHECK so the K4 comment's disambiguation rationale was wrong; reclassified to a comment rewrite) + K8 partial (Int/Bool/String Variant claims not fact-checked) + DEF-iter1-A7-4 should-have-been-patched (script doesn't verify opcgw-schema, would misidentify Firefox `places.sqlite` as pre-Epic-A). **9 L-patches applied** (L1-L9): **L1** spec AC#1 step 4 sync; **L2** spec AC#4 path sync; **L3** spec File List exit-code prose sync; **L4** parallel multi-deployment recipes for verification step 2; **L5** K4 comment rewrite with fact-checked rationale (v007 has only column-level CHECKs `value_bool IN (0,1)` + `value_type IN (enum)`; ONLY v008's cross-column CHECK fires on the negative-case insert); **L6** Int/Bool/String Variant table replacing prose claim (`Int(i64)` → `Variant::Int32` if `i32::try_from` succeeds else `Variant::Int64`; `Bool` → `Boolean`; `String` → `UAString`); **L7** script queries `sqlite_master` for `metric_values` + `metric_history` tables and aborts with operator-friendly error if not an opcgw database; **L8** VERSION whitespace strip via `tr -d '[:space:]'` (defends against trailing CRLF from some sqlite3 versions); **L9** path-quoting (`'%s'`) in printf `rm` recommendation hint (defends against paths with spaces). 5 dismissed across iter-1 + iter-2 (K1 sequence claim fact-checked correct against `src/storage/schema.rs:215-258`; K4 reasoning partially valid → reclassified L5; K9 exit-code design-weakness inherited from AC#3; K1 SQLite ALTER ADD COLUMN qualifier minor edge for old distros; K5 non-atomic printf minor pipe-edge). 9 LOW deferred across iter-1 + iter-2 (script-side `tempfile::NamedTempFile` mass refactor, `cargo install --path .` operator-friendliness, `dnf` / `brew` install hints, `OPCGW_LOG_LEVEL=warn` filter operator-UX, markdown anchors, `-journal` sidecar mode, sprint-status.yaml `last_updated` multi-KB pattern, crash-during-migration recovery section, `cargo install --git --tag` form). Final verification: cargo test --all-targets **1256 passed / 0 failed / 10 ignored** (unchanged across iter-1 + iter-2 — A-7's only new test is in `src/storage/schema.rs::tests`; the K-patches and L-patches were doc + script edits + one test-comment rewrite + one positive-case `conn.execute` addition that doesn't change `#[test]` fn count); clippy clean; doctest 0 failed / 55 ignored. AC#5 grep contract returns 5 lines unchanged (`metric_history_read` + `metric_history_summary` + `metric_parse` + `metric_read` + `metric_view_serialize`); AC#7 grep contract returns 0 `metric_view_display_string` hits unchanged. Strict-zero invariants honoured: A-7 NARROW-MUTABLE on `src/storage/schema.rs::tests` only (production-code body of `run_migrations` zero-diff); MUTABLE on `docs/deployment-guide.md` + NEW `scripts/check-schema-version.sh` + `README.md` + sprint-status + this spec file. All other A-1 through A-6 strict-zero files remain zero-diff. **Issue #108 fully closed across all 7 stories.** **Epic A is 7/7 DONE.** **Per CLAUDE.md "Do not skip the retrospective" — the `epic-A-retrospective: optional` line is NOW MANDATORY as the very next BMad action, gating v2.0 GA release.** PRIOR — Story A-7 review — `bmad-dev-story A-7` implementation complete. Status flipped `ready-for-dev → in-progress → review`. All 9 ACs SATISFIED. **FINAL Epic A story.** Pure docs + tests + small shell script — ZERO Rust production-code changes per AC#7 strict-zero invariants. **Deliverables:** (1) `docs/deployment-guide.md § "Epic A migration"` operator-facing runbook (+186 lines) covering Path A (auto-migration on next gateway startup, preserves legacy rows as `value_type='legacy'`) / Path B (drop `opcgw.db*` and start fresh) / pre-upgrade checklist / post-migration verification / one-way rollback contract (only path: restore pre-upgrade backup) / SLA expectation (5s for ≤10k rows; multi-minute for ≥100MB databases — operator caveat documented) / 6 common gotchas (legacy-row BadDataUnavailable for one poll interval; metric_history same contract; new audit events on first startup; `run_migrations()` idempotent; v008 BEGIN/COMMIT crash-safe; Docker bind mounts preserve `opcgw.db`). (2) NEW `scripts/check-schema-version.sh` shell script (+103 lines, executable bit set) wrapping `sqlite3 PRAGMA user_version` with operator-friendly output + Path A/B recommendations + clear error paths (missing arg, missing sqlite3, missing file, unrecognised version). (3) NEW end-to-end regression test `test_v006_to_v008_full_upgrade_path_under_5s` in `src/storage/schema.rs::tests` (+165 lines) pinning the chained v006 → v007 → v008 migration via a single `run_migrations(&conn)` call (NOT the manual `execute_batch` shortcut — captures the A-5 docstring-vs-body drift lesson). Test seeds 10 000 pre-Epic-A rows in v006-shaped columns, calls the production `run_migrations()` once, asserts: SLA <5s wall-clock, `PRAGMA user_version == 8`, row counts preserved (5000+5000), all rows tagged `value_type='legacy'` with all 4 typed columns NULL (Story A-4 / A-5 contract), v008 exactly-one-non-NULL CHECK constraint enforceable (multi-non-NULL insert fails with `SQLITE_CONSTRAINT_CHECK`). Actual run completed in **~40ms** — well under the SLA ceiling. **Decision D1 resolved → option (a):** keep SLA test at 5s-for-10k-row baseline (operator-realistic for typical residential / small-scale deployments) + document the larger-DB startup-delay caveat explicitly in the runbook (`docs/deployment-guide.md § "Epic A migration" § "SLA expectation"`). Rationale: tuning v008's `CREATE TABLE … AS SELECT` pattern to meet the literal "5s for 100MB" target would re-open A-3's review loop and violate A-7's strict-zero invariant on `src/storage/schema.rs::run_migrations` body. AC#7 strict-zero invariants honoured: A-7 NARROW-MUTABLE on `src/storage/schema.rs::tests` only (production-code body untouched); MUTABLE on `docs/deployment-guide.md` + NEW `scripts/check-schema-version.sh` + `README.md` + sprint-status + this spec file. All other A-1/A-2/A-3/A-4/A-5/A-6 strict-zero files remain zero-diff per `git diff --stat`. Final verification: cargo test --all-targets **1255 passed / 0 failed / 10 ignored** (was 1254 A-6 baseline; +1 new test fn — matches AC#8 target ≥1255 exactly); clippy clean; doctest 0 failed / 55 ignored. AC#5 grep contract returns 5 lines unchanged (`metric_history_read` + `metric_history_summary` + `metric_parse` + `metric_read` + `metric_view_serialize`); AC#7 grep contract returns 0 hits unchanged. `scripts/check-schema-version.sh` smoke-tested across error paths (missing arg → exit 2 + usage; missing sqlite3 → exit 2 + install hints; missing file → exit 1; valid arg-only path operator-tested per AC#9). Tracking issue Task 0 deferred to user (gh CLI not authenticated for write — A-1/A-2/A-3/A-4/A-5/A-6 precedent). **Epic A completion + retrospective trigger:** after A-7 ships, Epic A is **7/7 done** (A-1 / A-2 / A-3 / A-4 / A-5 / A-6 / A-7 done); per CLAUDE.md "Do not skip the retrospective" + the existing `epic-A-retrospective: optional` line (which becomes mandatory once all stories are done), the very next BMad action after A-7's Code Review Complete commit MUST be `bmad-retrospective` for Epic A — **the gate to v2.0 GA release per the post-A-6 framing**. Recommend `bmad-code-review A-7` on a different LLM per CLAUDE.md "Code Review & Story Validation Loop Discipline" + memory `feedback_iter3_validation` 12-story validated pattern (A-7 would extend the streak to 13 — though A-7's doc-only nature may terminate review iter-1 cleanly under condition #1 or #2; documentation-only stories haven't been tested against the iter-3 doctrine yet). Next: `bmad-code-review A-7`. PRIOR — Story A-6 DONE — `bmad-code-review A-6` iter-2 same-LLM run complete; loop terminated per CLAUDE.md condition #2 (only LOW remains). Iter-2 surfaced 10 HIGH-REG / MEDIUM patches in iter-1's own patch round (memory `feedback_iter3_validation` 11-story pattern — A-6 iter-2 extends streak to 12). Iter-2 K1-K10 patches applied: **K1** `docs/logging.md` missing `int_precision_lossy` row (AC#14 violation — closed); **K2** P0-D1 f32 subnormal underflow producing 40-char decimal strings → new `f32_underflow` info reason + clamp to 0.0; **K3** P0-D1 f32 overflow path silently emitted `debug!` → new `f32_overflow` warn reason with full identifiers; **K4** P11 trim silently stripping NBSP (U+00A0) → switched to `trim_matches(c.is_ascii_whitespace())` preserving locale typography (French/Spanish industrial conventions); **K5** P1+P10 empty-string handling left a hole for non-String data_type → simplified `valueMissing` to treat ANY `value === ""` as missing; **K6** P12 grep guard structurally broken (regex over-match + git-missing silent-pass) → DELETED, compile-time `PartialEq` absence on the dropped-derive structs is the actual guard (any `==` is a build error); **K7** P3 wire-format length assertion + magnitude tolerance both passed for pre-fix and post-fix code → tightened to exact `assert_eq!(f, 23.6_f64)`; **K8** P2 `u32` → `usize` for the per-request counter (matches `Vec::len()` semantics, no overflow at scale); **K9** P7 path-ambiguous `body.contains` → JSON-key-prefixed `"value":9223372036854775807` grep; **K10** P5 extended unit-presence test to also pin `value` + `timestamp` always-serialized (closes the cross-Option-field hazard a future `skip_serializing_if` edit could introduce). 4 dismissed (P1 comment polish; `NaN.to_string()` doc cleanup; `-0.0` round-trip platform-dep; byte-length comparison theoretical). 6 LOW deferred (spec-table text update, persisted-vs-snapshot drift tracking, P6 dedicated wire-string test, aggregate-warn sample identifiers, client-side `console.warn` throttling, future-variant renderer coupling). Final verification: cargo test --all-targets **1254 passed / 0 failed / 10 ignored** (was 1255 pre-K6 grep-guard deletion; -1 net); clippy clean; doctest 0 failed / 55 ignored. AC#5 grep contract returns 5 lines unchanged; AC#7 grep contract returns 0 hits unchanged. The `metric_view_serialize` closed-enum reasons are now `{non_finite, int_precision_lossy, f32_overflow, f32_underflow}` (4 reasons post-iter-2; per-row severity: `non_finite` debug, `f32_overflow` warn, `f32_underflow` info, `int_precision_lossy` info; aggregate `non_finite` warn per request); all documented in `docs/logging.md`. **Issue #108 web-display side fully closed.** Epic A now 6/7 done (A-1/A-2/A-3/A-4/A-5/A-6 done; A-7 backlog). epic-A-retrospective stays `optional` until A-7 lands, then becomes required per CLAUDE.md. Strict-zero invariants honoured across iter-1 + iter-2 — diff confirms only the AC#13 MUTABLE files changed (`src/web/api.rs` + `src/web/mod.rs` + `static/metrics.js` + `tests/web_dashboard.rs` + `docs/logging.md` + `README.md` + the BMad artefacts). Next: `bmad-create-story A-7`. PRIOR — Story A-6 review iter-1 patches applied — `bmad-code-review A-6` iter-1 same-LLM run. Acceptance Auditor ELIGIBLE-FOR-DONE pre-patches. 1 decision-needed (D1 Float wire-precision contract) resolved by user → option (a) f32-cast in helper (promoted to **P0-D1** patch). 12 additional patches (P1-P12) applied covering: P1 String em-dash sentinel collision in formatValue; P2 aggregate per-request warn for non-finite (per-row demoted to debug!); P3 brittle `-0.0` wire-format assertion replaced with `is_sign_negative()` test; P4 split non-finite log-correlation test into 3 separate per-call `#[traced_test]` functions; P5 serde round-trip test pinning `unit` key always-present; P6 closed-enum `REASON_NON_FINITE` const extraction; P7 i64::MAX integration test via raw response-body grep; P8 new `int_precision_lossy` reason + telemetry for `|i| > 2^53`; P9 formatValue console.warn on unknown discriminant + visible `"?"` sentinel; P10 empty-String → "—" coalesce; P11 `metric_unit` trim + whitespace-normalise-to-None at `from_config` boundary; P12 grep-guard test against accidental equality on the dropped-derive structs (`MetricView` / `DevicesResponse` / `ApplicationView` / `DeviceView`). **P0-D1 implementation note:** the naive `*f as f32 as f64` cast does NOT strip f64 precision — the f64 retains all 53 significand bits of the f32 representation, serialising as `23.600000381469727`. The correct narrowing round-trips through Rust's `f32::to_string()` Display alphabet (which finds the shortest round-trip string at f32 precision) + `.parse::<f64>()` (lands on the f64-closest-to-that-string, a distinct bit pattern from the f32-back-cast value). Wire produces `23.6` as expected. 10 defers captured in `deferred-work.md`; **W2 + W3 RESOLVED-BY-P11 during iter-1** — empty-string `metric_unit` normalisation at the snapshot boundary closes both deferrals as a single structural fix (the orphaned `Some("")` vs `None` distinction is gone; the spurious-topology-rebuild trigger on `""` ↔ absent toggle is gone). 5 dismissed (taste / false-positive). Final verification: cargo test --all-targets **1255 passed / 0 failed / 10 ignored** (was 1242 pre-iter-1; +13 new tests from P3 + P4 split + P5 + P7 + P8 boundary/extremes/safe-range + P11 expanded normalisation + P12 grep-guard); clippy clean; doctest 0 failed / 55 ignored. AC#5 grep contract returns 5 lines unchanged (`metric_history_read` + `metric_history_summary` + `metric_parse` + `metric_read` + `metric_view_serialize`); AC#7 grep contract returns 0 hits unchanged. Status STAYS `review` pending iter-2 per CLAUDE.md "Code Review & Story Validation Loop Discipline" — iter-1 was a non-trivial patch round (P0-D1 production logic + P2 aggregate warn restructure + P11 boundary normalisation + new `int_precision_lossy` reason in audit telemetry) — exactly the change-class iter-2 historically catches HIGH-REGRESSIONs in (A-3 iter-2 caught Unknown+Int saturation gap; A-4 iter-2 caught JR1 fake-regression-guard; A-5 iter-2 caught K1+K2 docstring-vs-body drift). Recommend `bmad-code-review A-6` iter-2 on a **different LLM** per memory `feedback_iter3_validation` 11-story validated pattern (A-6 iter-2 would extend the streak to 12). PRIOR — Story A-6 review — `bmad-dev-story A-6` implementation complete. Status flipped `ready-for-dev → in-progress → review`. All 14 ACs SATISFIED. Closes the WEB UI side of Epic A — last READ-side consumer of typed payloads. `MetricView` JSON shape widened from stringly-typed `value: Option<String>` to typed `value: Option<serde_json::Value>` + new `unit: Option<String>` field. New helper `metric_type_to_json_value(&MetricType, &str, &str) -> Option<serde_json::Value>` in `src/web/api.rs` emits typed JSON primitives (number / bool / string); non-finite Float yields `None` + emits new `event="metric_view_serialize"` `reason="non_finite"` warn (defensive — A-3 poller filter makes this path unreachable in production). `web::MetricSpec` widened with `metric_unit: Option<String>` populated by `DashboardConfigSnapshot::from_config`; hot-reload propagation flows through Story 9-7 without `config_reload.rs` changes (existing `read_metric_lists_equal` destructure at `src/config_reload.rs:972` already considers `metric_unit` a topology diff). **Retired `metric_view_display_string` transitional shim** + its 2 unit tests (closes `DEF-iter1-A5-D1`); replaced with 5 typed-JSON unit tests covering Float finite / Float non-finite + warn / Int extremes / Bool / String + UTF-8. `static/metrics.js` gains new `formatValue(value, dataType)` helper + value-plus-unit composition at `renderMetricRow`; Bool wire shifts from `"1"`/`"0"` (A-5 transitional) to native `true`/`false`. **Task 1 decision (load-bearing):** verified that post-A-5 `src/storage/sqlite.rs::load_all_metrics` silently SKIPS legacy rows at line 1530-1540 (`legacy_skipped_count += 1; continue;`) — legacy rows never reach the dashboard handler. Consequence: AC#2 state (b) `legacy_row` reason for `metric_view_serialize` was dropped from the closed enum; the event has ONLY `non_finite` reason post-A-6. Legacy rows surface to the operator as the same "configured but never polled" outcome (`value: null, timestamp: null` → "missing" badge). Closes deferred-work `9-5-iter1-D4` (empty-string `metric_unit` coalesces to no-unit-suffix at the renderer via JS truthy check; both `None` and `Some("")` produce identical rendering). Strict-zero invariants honoured per AC#13: `src/web/auth.rs` / `src/web/csrf.rs` / `src/web/config_writer.rs` / `src/web/test_support.rs` / `src/opc_ua.rs` / `src/opc_ua_history.rs` / `src/opc_ua_auth.rs` / `src/opc_ua_session_monitor.rs` / `src/security*.rs` / `src/main.rs::initialise_tracing` / `src/storage/*` / `src/chirpstack.rs` / `src/config.rs` / `src/config_reload.rs` / `src/opcua_topology_apply.rs` all zero-diff. A-6 MUTABLE: `src/web/api.rs` (`MetricView` shape + `api_devices` handler + new `metric_type_to_json_value` helper + 5 new unit tests + retired shim+tests) + `src/web/mod.rs` (`MetricSpec` widening + `from_config` propagation + 2 new unit tests) + `static/metrics.js` (renderer) + `tests/web_dashboard.rs` (existing JSON-shape test extended + 2 new integration tests) + `docs/logging.md` (new `metric_view_serialize` row) + `README.md` (this line + Planning table). `MetricView` lost its `PartialEq, Eq` derives (cascaded to `DevicesResponse` / `ApplicationView` / `DeviceView`) because `serde_json::Value` does not implement `Eq` over the float NaN axis — no internal callers used the equality, so the derives were dropped rather than custom-impl'd. cargo test --all-targets **1242 passed / 0 failed / 10 ignored** (was 1230 A-5 baseline; +12 from: 5 unit tests for `metric_type_to_json_value` + 2 unit tests for `DashboardConfigSnapshot::from_config` propagation + 2 integration tests in `tests/web_dashboard.rs` + 3 implicit assertion-density additions to the existing JSON-shape test); exceeds AC#12 target ≥1240 by 2. cargo clippy --all-targets -- -D warnings clean; cargo test --doc 0 failed / 55 ignored. AC#5 grep contract verified: `git grep -hoE 'event = "metric_[a-z_]+"' src/ | sort -u` returns exactly **5 lines** post-A-6 (`metric_history_read` + `metric_history_summary` + `metric_parse` + `metric_read` + `metric_view_serialize`). AC#7 grep contract verified: `git grep -n 'metric_view_display_string' src/ tests/` returns ZERO hits. Issue #108 closure mapping: A-1 type-level → A-2 schema-level → A-3 WRITE-side → A-4 OPC UA Read → A-5 OPC UA HistoryRead (functionally closed) → **A-6 closes the WEB-DISPLAY side** → A-7 operator runbook + version-gated script. After A-6 ships, only A-7 (documentation + script) remains in Epic A backlog. Tracking issue Task 0 deferred to user (gh CLI not authenticated for write — A-1/A-2/A-3/A-4/A-5 precedent). Recommend `bmad-code-review A-6` on a different LLM per CLAUDE.md "Code Review & Story Validation Loop Discipline" + memory `feedback_iter3_validation` 11-story validated pattern (A-6 extends to 12). Next: bmad-code-review A-6. PRIOR — 2026-05-17 (Story A-5 done — bmad-code-review A-5 closed through 2 iterations on the same LLM (Opus 4.7); loop terminated per CLAUDE.md condition #2 (only LOW remains). Iter-1 applied 16 patches + 4 user-resolved decisions (3 accepted-deferred + 1 promoted to patch) + 5 defers. Iter-2 caught **2 HIGH-REGRESSIONs in iter-1's own patches** per memory `feedback_iter3_validation` 10-story validated pattern (A-5 extends the streak to 11): **K1** P15 i64-extremes test claimed to catch a `build_data_values` Variant::Int32-narrowing regression but never invoked `build_data_values` (fake regression-guard, A-4 JR1 class); **K2** P14 3-consecutive-legacy test claimed to catch a `build_data_values` N→1 collapse regression but never invoked `build_data_values` either. Both fixed by adding real unit tests at `src/opc_ua_history.rs::tests` (`test_build_data_values_int_extremes_narrow_to_int64` + `test_build_data_values_three_consecutive_legacy_rows_preserve_count`) that actually exercise the build_data_values boundary. Iter-2 also caught K3 (P11 negative-zero test was path-ambiguous — both underflow-fired and passthrough branches produced `Variant::Float(0.0_f32)`; fixed by adding `#[traced_test]` + log-absence assertion), K4 (P13 docstring factually wrong), K5 (spec orphan reference to pre-P2 `reason_detail` shape — A-4 JR8 phrase-harmonization-drift class), K6 (P3 trace! dropped `event=` field; restored as `event="metric_history_summary"` for ops dashboards), K7 (P0-D4 NaN/Inf wire-format unguarded — extracted `metric_view_display_string` helper + 2 unit tests pinning typed payloads + non-finite Float wire format). Final verification under `TMPDIR=/home/gcorbaz/.cache/cargo-tmp`: cargo test --all-targets **1230 passed / 0 failed / 10 ignored**; cargo clippy --all-targets -- -D warnings clean; cargo test --doc 0 failed / 55 ignored. AC#9 grep contract now returns exactly **4 lines** post-iter-2: `metric_history_read` + `metric_history_summary` (new from iter-2 K6) + `metric_parse` + `metric_read`. **Issue #108 fully closes with this commit** — the OPC UA HistoryRead read-path is the last storage-trait read path; the transitional `MetricValue.value: String` field is structurally gone across all 4 public storage structs; legacy rows surface as `BadDataUnavailable` in the response stream per architecture.md:182. Epic A now 5/7 done (A-1/A-2/A-3/A-4/A-5); A-6 (web UI live-metrics typed JSON shape) + A-7 (migration runbook) remain backlog; epic-A-retrospective optional. Next: bmad-create-story A-6. PREVIOUS NARRATIVE — Story A-5 review — OPC UA HistoryRead Value-Payload Pipeline implementation complete via `bmad-dev-story A-5`. Status flipped `ready-for-dev → in-progress → review`. **Closes the OPC UA HistoryRead read-path AND retires the transitional `MetricValue.value: String` field** across `MetricValue` / `MetricValueInternal` / `BatchMetricWrite` / `HistoricalMetricRow` (closes 10+ A-1 → A-4 carry-forward deferred-work entries: DEF1/DEF7/DEF9/DEF15/DEF16/DEF19, iter2-DEF5, iter3-DEF5/DEF11, DEF-iter1-A4-1). `SqliteBackend::query_metric_history` rewires SELECT to project v007 typed columns via the A-4-shared `metric_type_from_typed_columns` helper; `HistoricalMetricRow` restructured to `payload: Option<MetricType>` so legacy rows (`value_type='legacy'`) are a first-class outcome. `OpcgwHistoryNodeManagerImpl::build_data_values` rewritten to pattern-match the typed payload directly — no more `row.value.parse::<…>()`; legacy rows emit `DataValue { value: None, status: Some(BadDataUnavailable) }` per epic AC#1 (NOT silently dropped). Float narrowing-overflow/underflow guard mirrors A-4 IR7/JR13/JR14 in the live-Read path. New audit event `metric_history_read` joins the metric-event grep contract (`schema_drift` / `narrowing_overflow` / `narrowing_underflow`); post-A-5 the grep returns exactly 3 lines (`metric_history_read` + `metric_parse` + `metric_read`). `convert_variant_to_metric` signature simplified to `Result<MetricType, OpcGwError>` per AC#8 — the string half was only kept for the now-removed `MetricValue.value`. Compile-time field-shape pins (`const _: fn(&T)` pattern, no new crate dependency) for `MetricValue` + `MetricValueInternal` + `BatchMetricWrite` + `HistoricalMetricRow` close the A-1-era cross-field-invariant DEFs as a single structural fix. Strict-zero invariants revised: `src/opc_ua_history.rs` becomes A-5 MUTABLE (was A-5 territory under A-4); `src/storage/types.rs` + `src/storage/mod.rs` + `src/storage/memory.rs` MUTABLE for field removals; narrow `src/opc_ua.rs::convert_variant_to_metric` signature change band only; rest of `opc_ua.rs` + all other A-1-A-4 strict-zero files remain. No new migration; v008 from A-3 carries the exactly-one-non-NULL CHECK A-5 relies on. NEW integration test `tests/opcua_historyread_typed_payload.rs` with 4-variant round-trip + legacy outcome + mixed typed/legacy stream assertions. `cargo test --all-targets` **1217 passed / 0 failed / 10 ignored** (was 1214 A-4 baseline; +3 net incorporates the new integration test plus a JR13/JR14 unit-test consolidation); `cargo clippy --all-targets -- -D warnings` clean; `cargo test --doc` 0 failed / 55 ignored. AC#9 grep contract verified: `git grep -hoE 'event = "metric_[a-z_]+"' src/ | sort -u` returns exactly **3 lines** (`metric_history_read` + `metric_parse` + `metric_read`). `grep -rn 'TODO(A-5)' src/` returns **0 hits** — all transitional markers retired. **Issue #108 fully closes when A-5 ships** (the last storage-trait read path). Recommend running `bmad-code-review A-5` on a different LLM per CLAUDE.md "Code Review & Story Validation Loop Discipline" + memory `feedback_iter3_validation` 10-story validated pattern (A-5 extends to 11). Carry-forward GH issues unchanged: #88, #100, #102, #104, #110, #113, #117. Tracking issue Task 0 deferred to user (gh CLI not authenticated for write per A-1/A-2/A-3/A-4 precedent). Next: bmad-code-review A-5. PREVIOUS NARRATIVE — last updated 2026-05-17 (Story A-4 done — OPC UA Read Value-Payload Pipeline shipped through 2 code-review iterations + post-resume JR8-completeness fix; loop terminated per CLAUDE.md condition #2 (only LOW remains). Iter-2 (same-LLM Opus 4.7) applied 14 patches JR1-JR14: strongest catch JR1 HIGH-REG fake regression-guard tautology (iter-1 IR6 seed `value: "100"` would pass via both the new typed path AND the dropped legacy fallback). Post-resume verification (2026-05-17) surfaced a new finding class — **phrase-harmonization patch with downstream assertion drift**: JR8 harmonized helper error-message prefixes to "Schema drift:" but left 3 pre-existing IR2/IR8 test assertions still matching the OLD phrasing + 1 clippy `empty-line-after-doc-comments` violation on the JR8 preamble doc block. 4 mechanical fixes resolved everything (sqlite.rs:5761/5772/5780 assertions + sqlite.rs:359 doc-block paragraph break). Final under `TMPDIR=/home/gcorbaz/.cache/cargo-tmp` (tmpfs disk-quota workaround): cargo test --all-targets 1214 passed / 0 failed / 10 ignored; cargo clippy --all-targets -- -D warnings clean; cargo test --doc 0 failed / 55 ignored. End-to-end live OPC UA server proof: `tests/opcua_subscription_spike.rs::test_subscription_datavalue_payload_carries_seeded_value` passes — real OPC UA client receives `Variant::Float(42.5)` with `Good` status through the new typed-payload Read pipeline. AC#10 grep contract intact: `git grep -hoE 'event = "metric_[a-z_]+"' src/ | sort -u` returns exactly 2 lines (`metric_parse` + `metric_read`). 10-story `feedback_iter3_validation` pattern extended with two new finding classes from A-4: (1) fake regression-guard test (path-ambiguous seed values, caught by iter-2 Blind); (2) phrase-harmonization patch with downstream assertion drift (caught only by cargo test on resume — no review layer enumerated callsites of the old phrasing). Epic A now 4/7 done (A-1/A-2/A-3/A-4). Next: bmad-create-story A-5 (HistoryRead value-payload pipeline). PREVIOUS NARRATIVE — last updated 2026-05-16 (Story A-4 review — OPC UA Read Value-Payload Pipeline implementation complete via `bmad-dev-story A-4`. Status flipped `ready-for-dev → in-progress → review`. SqliteBackend readers (`get_metric_value` / `get_metric` / `load_all_metrics`) rewire SELECT to project v007 typed columns (`value_real` / `value_int` / `value_bool` / `value_text` / `value_type`) and build payload-bearing `MetricType` via new private free function `metric_type_from_typed_columns` in `src/storage/sqlite.rs` (NOT a method on `MetricType` per AC#11 strict-zero on `types.rs`). `OpcUa::convert_metric_to_variant` rewritten to pattern-match the typed payload directly — `metric.value.parse::<i64>()` / `parse::<f64>()` call sites all removed; the `value: "ignored"` literal in AC#4 unit tests is the regression guard. Legacy-row contract (architecture.md:182): pre-Epic-A rows tagged `value_type='legacy'` surface as `Ok(None)` from `get_metric_value`, mapping transitively to `BadDataUnavailable` via the existing `OpcUa::get_value:1493-1509` branch — no change to `get_value` itself, no new types, no `MetricValue` shape change. Float narrowing-overflow guard added per A-1-iter1-DEF17: after `f as f32` re-check `is_finite()`; on overflow emit `event="metric_read"` `reason="narrowing_overflow"` warn + return `Variant::Float(0.0)`. New audit event `metric_read` (closed enum: `reason ∈ {narrowing_overflow}`); grep contract `git grep -hoE 'event = "metric_[a-z_]+"' src/ | sort -u` now returns 2 lines (`metric_parse` + `metric_read`). AC#9 from A-3 (Counter monotonic typed-path preference, reverted in A-3 iter-1 IR1 because the reader returned zero-default) NOW closed at `chirpstack.rs:1707-1718` — prefers `prev_metric.data_type` via match-binding (`MetricType::Int(p) => Some(*p)`) with `prev_metric.value.parse::<i64>()` fallback retained for pre-A-3 rows where `batch_write_metrics` was the writer (real string in legacy `value` column). `convert_variant_to_metric` doc rewritten + plumbs real value into typed `MetricType` half (closes A-1-iter1-DEF3 + A-1-iter3-DEF7); caller `set_command` discards `_value_type` unchanged. `tests/opcua_subscription_spike.rs` seed call sites updated from zero-default `MetricType::Float(0.0)` to real-payload `MetricType::Float(<value>)` matching the legacy string field — these tests previously passed because the reader parsed `metric.value`; post-A-4 they only pass when the seed's typed payload carries the real measurement (closes the A-1-iter1-DEF13 "tests passing for wrong reason" risk for this surface). New A-4 tests: 6 `src/storage/sqlite.rs::tests` (typed-payload reader for all 4 variants + legacy-row Ok(None) for get_metric_value + get_metric + load_all_metrics-skip-legacy + helper schema-drift error path); 8 `src/opc_ua.rs::tests` (`convert_metric_to_variant` pattern-match per variant + Int32/Int64 narrowing + Float narrowing-overflow `metric_read` warn + `does_not_read_value_field` regression guard + `get_value` end-to-end with InMemoryBackend); 3 `tests/opc_ua_read_typed_payload.rs` integration tests (4-variant round-trip + legacy → Ok(None) + load_all_metrics-skip-legacy); 1 `chirpstack::tests::ac9_counter_monotonic_typed_path_extracts_payload` unit test pinning the AC#9 match-arm semantics on synthetic `MetricValue` constructions. cargo test --all-targets **1208 passed / 0 failed / 10 ignored** (+41 vs 1167 A-3-review baseline; exceeds AC#12 target ≥1175 by 33); cargo clippy --all-targets -- -D warnings clean; cargo test --doc 0 failed / 55 ignored (AC#13 preserved). Strict-zero invariants honoured: `src/storage/types.rs` + `memory.rs` + `mod.rs` + `pool.rs` + `schema.rs` + `web/*` + `opc_ua_history.rs` (A-5 territory) + `opc_ua_auth.rs` + `opc_ua_session_monitor.rs` + `security*` + `main.rs::initialise_tracing` + `config_reload.rs` + `opcua_topology_apply.rs` all zero-diff. A-4 MUTABLE per AC#11: `src/chirpstack.rs` + `src/opc_ua.rs` + `src/storage/sqlite.rs` + `docs/logging.md` + `tests/opcua_subscription_spike.rs` (contract update) + NEW `tests/opc_ua_read_typed_payload.rs` + `README.md`. No new migration in A-4; v008 from A-3 carries the exactly-one-non-NULL CHECK A-4 relies on for the typed-column projection (defensive `Option`-unwrap returns clean `OpcGwError::Database("schema drift")` if the invariant is ever violated). Issue #108 closure mapping: A-1 type-level → A-2 schema-level → A-3 WRITE-side → **A-4 closes OPC UA Read** → A-5 HistoryRead → A-6 Web UI; #108 fully closes when A-5 ships (the last storage-trait read path). Recommend running `bmad-code-review A-4` on a different LLM per CLAUDE.md "Code Review & Story Validation Loop Discipline" + memory `feedback_iter3_validation` 9-story validated pattern (now extending to 10). Carry-forward GH issues unchanged: #88, #100, #102, #104, #108 (production blocker), #110, #113, #117. Tracking issue Task 0 deferred to user (gh CLI not authenticated for write). Next: bmad-code-review A-4. PREVIOUS NARRATIVE — last updated 2026-05-15 (Story A-3 done — Poller Value-Payload Write Pipeline closed through 2 code-review iterations on a different LLM, loop terminated per CLAUDE.md condition #2 (only LOW remains). Iter-1 13 patches (3 HIGH + 8 MEDIUM + 2 LOW); iter-2 9 patches addressing 1 HIGH-convergent (Unknown+cfg=Int saturation gap — would silently saturate corrupt data into DB) + 3 MEDIUM-convergent (i64::MAX boundary off-by-one from f64 rounding, closed-enum violation in `store_metric` for cfg=None/String, Bool misattribution for kind=Unknown+cfg=Bool) + 1 MEDIUM-single-agent (SLA test missing `user_version=8` assertion) + 4 LOW fold-ins (SLA row-count scaled to AC#7 literal "10k+10k", revert comment counterexample corrected, per-reason emission-site docs in `docs/logging.md`, `metric_history` symmetric CHECK negative tests). Closed enum `expected_type ∈ {Float, Int, Bool, String, Unknown}` widens past iter-1's `{Float, Int, Bool}` per the iter-2 IR3-A patch; saturation guard predicate now `int_target = kind==Counter || (kind==Unknown && cfg==Int)` with `>=` on the upper bound. 8-story validated `feedback_iter3_validation` pattern extends to 9 (4-4/9-4/9-5/9-6/9-7/9-8/A-1/A-2/A-3) — iter-2 caught real bugs iter-1 missed. cargo test --all-targets 1167 passed / 0 failed / 10 ignored; clippy clean; doctest 0 failed / 55 ignored. AC#9 (typed-path Counter monotonic preference) DEFERRED-DOCUMENTED-TO-A-4 (user-accepted at iter-1). Next: Story A-4 OPC UA Read value-payload pipeline. PREVIOUS NARRATIVE — Story A-3 review — Poller Value-Payload Write Pipeline implementation complete via `bmad-dev-story A-3`. Real ChirpStack values (f32 → f64) now wrap into `MetricType` payload at all 7 `TODO(A-3)` sites in `src/chirpstack.rs::prepare_metric_for_batch`; NaN/Inf option-(a) filter at poller emits `event="metric_parse"` warn with `reason="non_finite"` + skips the metric (closes A-1-iter3-DEF8 NaN/Inf hazard). All 4 SqliteBackend writers (`set_metric`, `upsert_metric_value`, `append_metric_history`, `batch_write_metrics`) now populate the 4 typed value columns + `value_type` discriminant via inline 5-let pattern-match from `MetricType` payload; legacy `value`/`data_type` columns preserved per A-2-iter1-DEF1 staging contract. NEW `migrations/v008_typed_value_constraints.sql` (115 lines) adds exactly-one-non-NULL CHECK constraint to both `metric_values` and `metric_history` via CREATE TABLE … AS SELECT pattern wrapped in BEGIN/COMMIT (partially closes A-1-iter3-DEF6 for v008; closes A-2-iter1-DEF2). `src/storage/schema.rs::run_migrations` gains `MIGRATION_V008` const + `if current_version < 8` runner block + `LATEST_VERSION` bump 7→8; `create_v006_baseline_db` helper refactored from DROP-COLUMN rollback to manual v001-v006 SQL execution (v008's table-level CHECKs block the previous strategy). `chirpstack.rs::store_metric` body reinstated with real-payload wrapping + NaN/Inf guard (closes A-1 P9 / iter-2 IR5 `todo!()`). Counter monotonic check at `chirpstack.rs:1625` rewires to prefer typed payload from `prev_metric.data_type` when available; falls back to legacy string parse for pre-A-3 rows. `validate_bool_metric_value` warn updated from `operation="metric_parse"` → `event="metric_parse"` + `expected_type="Bool"` + `reason="invalid_bool"` (Stories 9-4 through 9-8 grep-contract pattern). New grep contract `git grep -hoE 'event = "metric_[a-z_]+"' src/ | sort -u` returns exactly `event = "metric_parse"` (1 line). `cargo test --all-targets` 1159 passed / 0 failed / 10 ignored (+16 vs 1143 A-2-review baseline; +8 new test fns covering writer typed-column population on all 4 variants × 4 writers + counter typed-payload round-trip). `cargo clippy --all-targets -- -D warnings` clean. `cargo test --doc` 0 failed / 55 ignored (AC#12 preserved). Strict-zero invariants honored: `src/storage/types.rs` + `memory.rs` + `mod.rs` + `pool.rs` + `web/*` + `opc_ua*` + `security*` + `main.rs::initialise_tracing` + `config_reload.rs` + `opcua_topology_apply.rs` all zero-diff. A-3 MUTABLE per AC#11: `src/chirpstack.rs` + `src/storage/sqlite.rs` + `src/storage/schema.rs`. Issue #108 closure mapping: A-1 closed type-level → A-2 closed schema-level → A-3 closes WRITE-side → A-4/A-5/A-6 close READ-side. Carry-forward: HIGH A-2-iter1-DEF-IH1 runner-wide atomicity remains user-confirmed deferral; v008 specifically IS BEGIN/COMMIT-wrapped (partial close). Next: Story A-4 OPC UA Read value-payload pipeline. PREVIOUS NARRATIVE — last updated 2026-05-15 (Story A-2 done — SQLite Schema Migration v007 (Typed Value Columns) shipped through 3 code-review iterations, loop terminated per CLAUDE.md condition #2 (only LOW remains). 16 patches applied across iter-1/2/3; 35+ defers in deferred-work.md (with user-explicit acceptance on HIGH A-2-iter1-DEF-IH1 migration-atomicity gap + AC#13 SPDX). All 13 ACs SATISFIED. 7-story `feedback_iter3_validation` pattern (4-4, 9-4, 9-5, 9-6, 9-7, 9-8, A-1) extends to 8 stories with A-2. cargo test 1143 passed / 0 failed / 10 ignored; clippy clean; doctest 0 failed / 55 ignored. Strict-zero invariants honored: `src/storage/sqlite.rs` and all other AC#10 listed files zero-diff across all 3 iterations. Next: Story A-3 Poller value-payload write pipeline. PREVIOUS NARRATIVE — last updated 2026-05-15 (Story A-2 review — SQLite Schema Migration v007 (Typed Value Columns) implementation complete via `bmad-dev-story A-2`. New `migrations/v007_typed_value_columns.sql` adds 5 typed value columns (`value_real REAL NULL`, `value_int INTEGER NULL`, `value_bool INTEGER NULL` with `CHECK(value_bool IS NULL OR value_bool IN (0,1))`, `value_text TEXT NULL`, `value_type TEXT NOT NULL DEFAULT 'legacy' CHECK(value_type IN ('legacy','Float','Int','Bool','String'))`) to both `metric_values` and `metric_history`. Pre-A-2 rows tagged `value_type='legacy'` via the column default — no UPDATE statement required. `src/storage/schema.rs` gains the v007 runner block + 7 new tests (`test_v007_preserves_pre_a2_rows` / `test_v007_writers_still_populate_legacy_columns` / `test_v007_readers_still_read_legacy_columns` / `test_v007_adds_all_typed_columns_to_metric_values` / `test_v007_adds_all_typed_columns_to_metric_history` / `test_v007_value_type_check_constraint` / `test_v007_migration_under_5s_for_10k_rows`). Scope is schema-only — `src/storage/sqlite.rs` writers + readers are strict-zero in A-2 per option-b staging; A-3 plumbs typed payload through writers. `cargo test --all-targets` 1143 passed / 0 failed / 10 ignored (cross-binary total; raw `#[test]` fn additions in `src/storage/schema.rs::tests`: 9 fns total = 7 from A-2 implementation + 2 from iter-1 review patches IM1 `test_v007_value_bool_check_constraint` + IL3 `test_v007_value_type_check_constraint_symmetric_on_metric_history`; the cross-binary delta from 1125 reflects test-binary re-counting at integration boundaries); `cargo clippy --all-targets -- -D warnings` clean; `cargo test --doc` 0 failed / 55 ignored (AC#12 preserved). Strict-zero invariants honored: src/storage/sqlite.rs / types.rs / memory.rs / mod.rs / pool.rs / web/* / opc_ua* / security* / main.rs / config_reload.rs / opcua_topology_apply.rs / chirpstack.rs all zero-diff. Carry-forward: A-1 iter-3 Edge F3 NaN/Inf hazard remains A-3 territory; exactly-one-non-NULL CHECK deferred to A-3 or A-7; legacy `value`/`data_type` columns retained until A-5/A-7. PREVIOUS NARRATIVE — last updated 2026-05-15 (Story A-1 done — MetricType Payload-Bearing Enum (FR51) shipped through 3 code-review iterations, loop terminated per CLAUDE.md condition #2 (only LOW remains). 23 patches applied across iter-1/2/3; 33 defers in deferred-work.md (with explicit user acceptance on D1 — Iter-0 scope revisions — and Edge F3 — NaN serialization hazard MUST-address-in-A-3); 13 dismissed. Iter-2 caught 1 HIGH-REG in iter-1's own P1 trait-doc backfill (split-brain LSP framing); iter-3 confirmed the 7-story validated `feedback_iter3_validation` pattern (4-4, 9-4, 9-5, 9-6, 9-7, 9-8, A-1). cargo test --all-targets 1125 passed / 0 failed / 10 ignored (+12 from new payload-bearing round-trip / boundary / Display-contract tests vs 1113 baseline); cargo clippy --all-targets -- -D warnings clean across all 3 iterations; cargo test --doc 0 failed / 55 ignored (was 56 — net −1 from iter-2 IR7 deliberately stripping a misleading rust,ignore doctest after `store_metric` body was swapped to `todo!()`; AC#12 spirit preserved per #100). Next: Story A-2 SQLite schema migration v007. PREVIOUS NARRATIVE — last updated 2026-05-14 (Story A-1 review — MetricType Payload-Bearing Enum (FR51) implementation complete via bmad-dev-story, opens Epic A (Storage Payload Migration, the v2.0 GA gate per Epic 9 retro AI6). `src/storage/types.rs::MetricType` refactored from a payload-less discriminant enum to a payload-bearing enum: `Float(f64)`, `Int(i64)`, `Bool(bool)`, `String(String)`. `Copy` derive dropped (incompatible with `String(String)`); call sites cascaded with `.clone()` where multiple consumers need ownership and `(_)` binding-discard on existing pattern matches. `Display` preserves discriminant-only rendering ("Float", "Int", "Bool", "String") so log volumes + the SQLite `value_type` discriminant column write path are unchanged. `FromStr` retained for TOML config parsing, produces zero-valued payloads — callers pair it with a separate value source (ChirpStack ingest). `MetricValue.value: String` field deferred to A-5 (kept as dual-storage with the new typed payload, marked `TODO(A-5)` — removal happens when read sites in `src/opc_ua_history.rs` + `src/opc_ua.rs` pattern-match the typed payload directly). SqliteBackend uses option-b staging: keeps the existing discriminant-string write path with `TODO(A-2)` markers — A-2 owns the typed-column schema migration v007. AC#10 strict-zero invariant revised pre-implementation (captured in story's Dev Agent Record § Iter-0 Scope Revision): `src/opc_ua_history.rs` removed from strict-zero (compile-fixup-only allowed with `TODO(A-5)` markers); remaining strict-zero list: `src/web/auth.rs`, `src/web/csrf.rs`, `src/web/config_writer.rs`, `src/opc_ua_auth.rs`, `src/opc_ua_session_monitor.rs`, `src/security*.rs`, `src/main.rs::initialise_tracing`, `src/config_reload.rs`, `src/opcua_topology_apply.rs`, `src/storage/pool.rs`, `src/storage/schema.rs`. Final: cargo test 1113 passed / 0 failed / 10 ignored across all targets (was 1112/0/9 baseline post-Epic-9); cargo clippy --all-targets -- -D warnings clean; cargo test --doc 56 ignored baseline unchanged (#100 invariant). Issue #108 behaviour preserved at runtime by A-1 (SqliteBackend still writes discriminant strings) — A-2 / A-3 / A-4 / A-5 / A-6 close it sequentially. GH tracking issue #118 filed. Recommend running bmad-code-review on a different LLM per CLAUDE.md "Code Review & Story Validation Loop Discipline" + memory `feedback_iter3_validation` 6-story validated pattern. Carry-forward GH issues unchanged: #88, #100, #102, #104, #108 (this epic closes), #110, #113, #117. PREVIOUS NARRATIVE — last updated 2026-05-13 (Story 9-8 review — Dynamic OPC UA Address Space Mutation (FR24) implementation + 5 integration tests complete via bmad-dev-story. Closes Story 9-7's documented v1 limitation ("dashboard updates but OPC UA stays frozen") and makes topology hot-reload end-to-end functional. New `src/opcua_topology_apply.rs` module (~1100 LOC inc. 6 unit tests) hosts `compute_diff` (7-axis fine-grained `AddressSpaceDiff`) + `apply_diff_to_address_space` (4-phase mutation envelope: Q2 `set_values(BadNodeIdUnknown)` transition → delete → add → DisplayName rename; single write-lock acquisition per phase per 9-0 Q3 finding). Story 9-7 stub seam wired in `src/config_reload.rs::run_opcua_config_listener`. 2 new audit events: `address_space_mutation_succeeded` info + `address_space_mutation_failed` warn. Critical empirical refinement of the 9-0 Q2 mitigation: async-opcua's default `MonitoredItemFilter::None` compares only `value.value` (`monitored_item.rs:514-517`), so the BadNodeIdUnknown DataValue must carry `value: Some(Variant::Empty)` as a distinct sentinel to trigger a DataChange notification — pinned by `tests/opcua_dynamic_address_space_apply.rs::ac2_remove_device_emits_bad_node_id_unknown_before_delete`. `RunHandles` extended additively with 3 new pub fields (storage, last_status, node_to_metric); `OpcUa::get_value`/`set_command` visibility widened to `pub(crate) fn`; `StatusCache` widened to `pub type`. Task 6 ships option (b) — async-opcua 0.17.1's `SimpleNodeManagerImpl` callback registry fields are private; stub `remove_read_callback`/`remove_write_callback` emit one-time deferred-leak info log. AC#9 strict-zero file invariants honoured. NEW `tests/opcua_dynamic_address_space_apply.rs` with 5 integration tests against a real OPC UA server. All grep contracts intact: `command_*`=4, `device_*`=4, `application_*`=4, `config_reload_*`=3; new `address_space_mutation_*`=2. v1 limitations documented in `docs/security.md § Dynamic OPC UA address-space mutation`. Recommend running bmad-code-review on a different LLM per CLAUDE.md doctrine. Carry-forward GH issues unchanged: #88, #100, #102, #104, #108 (production blocker, Epic 9 retro stays BLOCKED), #110, #113. PREVIOUS NARRATIVE — last updated 2026-05-12 (Story 9-6 review — Command CRUD via Web UI (FR36 + FR40 + FR41) implementation complete via bmad-dev-story, closing the FR34/35/36 cluster. Five new endpoints under /api/applications/:application_id/devices/:device_id/commands*. :command_id is i32 parsed by validate_path_command_id (rejects non-numeric/≤0). CSRF literal-arm completion adds "command" => warn at both rejection-emission match blocks per the explicit Story 9-6 hand-off comment; validate_path_device_id widened with resource parameter (parallel to iter-3 Blind#3 pattern). New helpers in src/web/api.rs: validate_path_command_id, validate_command_name, validate_command_port (delegates to DeviceCommand::validate_f_port at src/storage/types.rs:155 enforcing LoRaWAN 1..=223), validate_command_id_value, command_not_found_response, build_command_table. AppConfig::validate additive amendment: per-device command_id + command_name uniqueness HashSets (cross-device same-command_id remains allowed per device-folder-NodeId-namespace argument). TOML mutation preserves sibling [[application.device.read_metric]] byte-for-byte (Story 9-5 regression guard); DELETE-last-command leaves empty command ArrayOfTables in place (Task 6 pinned decision). 4 new audit events: command_created/updated/deleted (info) + command_crud_rejected (warn) with 1 new reason command_not_found. AC#10 strict-zero file invariants honoured. NEW static/commands.html + static/commands.js (vanilla JS, no framework); Commands nav links added to applications.html / devices-config.html / devices.html. NEW tests/web_command_crud.rs with 45 integration tests covering AC#1-12 + AC#11 cross-resource regression suite (Stories 9-4 + 9-5 grep contracts preserved); 7 new unit tests in src/web/api.rs::tests + 2 new csrf.rs unit tests + 3 new config.rs validate unit tests. No new dependencies (scopeguard crate is NOT a dep — chmod cleanup uses hand-rolled Drop-impl RAII guard). Grep contracts intact: command_*=4, device_*=4, application_*=4, config_reload_*=3. Next: bmad-code-review on a different LLM per CLAUDE.md "Code Review & Story Validation Loop Discipline". Previous narrative — Story 9-5 review — Device + Metric Mapping CRUD via Web UI (FR35 + FR40 + FR41) implementation complete via bmad-dev-story. Five new endpoints: GET/POST `/api/applications/:application_id/devices`, GET/PUT/DELETE `/api/applications/:application_id/devices/:device_id`. Reuses Story 9-4's CSRF + ConfigWriter + AppState + audit-event taxonomy without re-design; only addition is the path-aware audit dispatch in `csrf_event_resource_for_path` so `device_*` rejections emit `event="device_crud_rejected"` (NOT `application_crud_rejected`) — preserves Story 9-4's `application_*` grep-contract count = 4 while shipping Story 9-5's `device_*` count = 4. PUT-replaces-device contract preserves any existing `[[application.device.command]]` sub-table byte-for-byte (Task 6 anti-pattern guard against `toml::Value` round-trip). `AppConfig::validate` extended additively for per-device `metric_name` + `chirpstack_metric_name` uniqueness HashSets (modelled on existing `seen_device_ids` pattern); cross-device `metric_name` allowed (post-#99 NodeId fix at commit `9f823cc` makes that a valid scenario — verified in `src/config.rs::tests::test_validation_same_metric_name_across_devices_is_allowed`). New `static/devices-config.html` + `static/devices-config.js` (vanilla JS, mobile-responsive, separate from Story 9-3's read-only `/metrics.html` so live-metrics polling and editor state stay isolated). New `validate_path_device_id` + `validate_device_field` + `device_not_found_response` helpers in `src/web/api.rs`. AC#10 file invariants honoured: zero changes to `src/web/auth.rs`, `src/opc_ua.rs`, `src/opc_ua_*.rs`, `src/security*.rs`, `src/main.rs::initialise_tracing` (issue #99 already fixed at commit `9f823cc`). 38 integration tests in `tests/web_device_crud.rs` covering AC#1-AC#12 + 1 CRUD-driven AC#11 issue-#99 regression test (live-OPC-UA-server Read/HistoryRead variants deferred — orthogonal harness). cargo test all targets pass / 0 fail; cargo clippy --all-targets -- -D warnings clean. Grep contracts intact: `application_*` = 4 (Story 9-4 invariant), `device_*` = 4 (Story 9-5 target), `config_reload_*` = 3 (Story 9-7 invariant), `command_*` = 0 (no scope creep). Next: bmad-code-review on a different LLM. Previous narrative — Story 9-4 done — Application CRUD via Web UI (FR34 + FR40) shipped through 3 code-review iterations terminated under CLAUDE.md condition #2 (only LOW remains; deferred items have explicit rationale per condition #3). Iter-1: 33 patches; iter-2: caught 4 HIGH-REGRESSIONs in iter-1's own patches + 2 HIGHs + 9 MEDIUMs (10 applied, 5 deferred with rationale); iter-3: caught 3 iter-2 HIGH-REGRESSIONs (P40 IPv6 double-port URL, P41 P35 asymmetric to PUT/DELETE, P42 disk-dirty-after-persist) + 2 admin MEDIUMs (AC#8 reason-set drift, deferred-work.md sync); all 5 iter-3 patches applied. Final: 943 passed / 0 failed / 8 ignored; cargo clippy --all-targets -- -D warnings clean; AC#8 grep = 4 application_* events; AC#10 grep = 3 config_reload_* events (Story 9-7 invariant intact); AC#10 strict-zero files (src/web/auth.rs, src/opc_ua*.rs, src/security_*.rs, src/main.rs::initialise_tracing) unchanged. Same-LLM 3-iter pattern: Story 9-7's `feedback_iter3_validation` precedent holds — Acceptance Auditor's same-model blind spot consistently misses adversarial findings; over-reviewing pays off. Story 9-4 review pre-iter-3 — Application CRUD via Web UI (FR34 + FR40) implementation complete via bmad-dev-story. Five new endpoints: GET/POST `/api/applications`, GET/PUT/DELETE `/api/applications/:application_id`. New `src/web/csrf.rs` (Origin/Referer same-origin + JSON-only Content-Type defence; ships ahead of Stories 9-5/9-6 which reuse it). New `src/web/config_writer.rs` (TOML round-trip via `toml_edit`, atomic tempfile+rename writes, lock held across write+reload+rollback to prevent lost-update race). New `WebConfig.allowed_origins: Option<Vec<String>>` knob (restart-required in v1; same blocker as #113). `AppConfig::validate` extended additively: cross-application `application_id` uniqueness now enforced; empty `device_list` and empty `read_metric_list` demoted from hard-error to warn-level log so POST `/api/applications` can create empty applications that operators fill in via Story 9-5 endpoints. `AppState` extended with `config_reload` + `config_writer` fields. CRUD handlers call `app_state.config_reload.reload().await` after writing TOML; on reload failure roll back from in-memory pre-write bytes and map error to 422/500. 4 new audit events: `application_created`/`application_updated`/`application_deleted` (info) + `application_crud_rejected` (warn with `reason ∈ {validation, csrf, conflict, reload_failed, io, immutable_field, rollback_failed}`). New static/applications.html + static/applications.js (vanilla JS; reuses Story 9-2 `/dashboard.css` baseline; mobile-responsive). AC#10 file invariants honoured: zero changes to src/web/auth.rs, src/opc_ua*.rs, src/security*.rs, src/main.rs::initialise_tracing; src/config_reload.rs gains only the additive `web.allowed_origins` destructure-pattern guard (the Story 9-7 P28 landmine forces it). v1 limitations documented in `docs/security.md § Configuration mutations`: TOML-only persistence (no SQLite — single source of truth), no cookie-based CSRF token (Origin defence is sufficient for LAN single-operator threat model), no cascade-delete (DELETE on an application with devices returns 409 + operator-action hint), no last-application-delete (returns 409), best-effort rollback (operator must restore from backup if rollback IO itself fails), env-var overrides silently win over disk edits (operator-action documented). Tests: 23 integration tests in `tests/web_application_crud.rs` covering all 13 ACs + 4 unit tests in `src/web/config_writer.rs::tests` + 5 unit tests in `src/web/csrf.rs::tests` + 3 new validate-rule unit tests in `src/config.rs::tests`. cargo test 925 passed / 0 failed / 8 ignored across all targets; cargo clippy --all-targets -- -D warnings clean. AC#8 grep contract `git grep -hoE 'event = "application_[a-z_]+"' src/ | sort -u` returns exactly 4 lines; AC#10 grep `git grep -hoE 'event = "config_reload_[a-z]+"' src/ | sort -u` continues to return exactly 3 lines (no Story 9-7 regression). Next: bmad-code-review on a different LLM. Previous narrative — Story 9-7 done — Configuration Hot-Reload (FR39 + FR40) shipped through 3 code-review iterations, terminated under CLAUDE.md condition #2 (only LOW remains). Iter-1: 22 patches (5 HIGH + 11 MED + 5 LOW + 3 from 5 decisions resolved). Iter-2: caught 2 HIGH-REGRESSIONs in iter-1's own patches (P25 tautological subscription test, P26 topology_device_diff/classify_diff disagreement on `device_command_list`) + 3 HIGH (None≡Some([]), destructure landmines on `chirpstack_equal`/`opcua_equal`, NaN-safe `device_schemas_equal` via `to_bits()`) + 9 MED; 11 applied. Iter-3: caught 1 MED (P42 `param_type_equal` `_ => false` → exhaustive cross-variant pairs) + 9 LOW; P42 applied. Final: 876 passed / 0 failed; clippy clean; AC#7 grep contract = 3 lines; AC#8 file invariants intact. GH issues filed: #112 (tracker), #113 (live-borrow refactor for AC#10 + credential rotation), #114/#115/#116 (per-section future hot-reload). 5 v1 limitations backfilled to deferred-work.md per AC#11. Implementation surface: new `src/config_reload.rs` module with `ConfigReloadHandle` owning a `tokio::sync::watch::Sender<Arc<AppConfig>>`, validate-then-swap discipline, knob-taxonomy classifier; 3 new audit events `event="config_reload_attempted/succeeded/failed"`; SIGHUP listener in `src/main.rs`; poller `config_rx` outer-loop arm honouring Story 4-4 read-at-entry semantics; web `AppState` atomic swap of `dashboard_snapshot` + `stale_threshold_secs`; OPC UA listener stub logging `topology_change_detected` for Story 9-8. AC#8 invariants honoured: zero changes to `src/web/auth.rs`, `src/opc_ua_*.rs`, `src/security*.rs`, `src/main.rs::initialise_tracing`. v1 limitations documented in `docs/security.md § Configuration hot-reload`: credential rotation restart-required, OPC UA-side stale-threshold + address-space mutation deferred to follow-up stories, no HTTP trigger / filesystem watch. 19 integration tests in `tests/config_hot_reload.rs` + 6 unit tests in `src/config_reload.rs::tests` + 1 poller hot-reload unit + 3 clamp-helper unit tests. Previous narrative — Story 4-4 done — Phase A carry-forward auto-recovery from ChirpStack outages closes FR6/7/8 + NFR17 30s SLA; loop terminated under CLAUDE.md condition 1 after iter-3 code review. Iter-3 (over-reviewing pass) surfaced 1 HIGH + 4 MED/LOW that iter-1/iter-2 missed: P14 tightened shipped `config/config.toml` from `retry=10, delay=10` (worst-case 100s — violated AC#4) to `retry=30, delay=1` (worst-case 30s — satisfies AC#4); P10 preserves original gRPC outage cause separate from last probe error in cancel branch; P11 clarified retry/attempt semantics in `docs/logging.md`; P12 updated `chirpstack_outage` operator runbook; P13 replaced fragile field-order-coupled test assertion with token-boundary scan. cargo test 837 passed / 0 failed / 8 ignored across 17 suites; clippy clean. Epic 4 returned to done. Previous narrative — Story 4-4 review — Phase A carry-forward auto-recovery from ChirpStack outages; closes FR6/7/8 + NFR17 30s SLA. New `recover_from_chirpstack_outage` helper layered on Story 6-3's `chirpstack_outage` warn; 3 reserved log operations promoted to implemented (`recovery_attempt` / `recovery_complete` / `recovery_failed`); `Channel::connect()` 5s timeout wrap (resolves deferred-work.md:86 6-3 carry-forward); 7 new unit tests. Previous narrative — Story 9-0 done — async-opcua runtime address-space mutation spike, code-review iter-1 + iter-2 complete; loop terminated per CLAUDE.md condition 3. Three load-bearing questions resolved empirically: Q1 add path RESOLVED FAVOURABLY, Q2 remove path Behaviour B (frozen-last-good — 9-8 must arrange explicit cleanup), Q3 sibling isolation RESOLVED FAVOURABLY (write-lock-hold = 117 µs for 11 mutations). `OpcUa::run` split into `build` + `run_handles` to give 9-7 hot-reload a clean integration seam. Spike report at `_bmad-output/implementation-artifacts/9-0-spike-report.md`. Code review applied 10 iter-1 patches + 6 iter-2 patches (test-side hardening + spec/spike-report doc reconciliation); P1 (RunHandles missing Drop impl) blocked by rustc E0509 and tracked at GitHub KF issue #110 for Story 9-7 to revisit. Previous: Story 9-3 done after iter-1 + iter-2 code review; live metric values page at /metrics.html with per-row staleness badges; #107 duplicate-metric_name validation gap and #108 payload-less MetricType which BLOCKS production deployment until storage trait refactor lands.).

The roadmap is tracked in [`_bmad-output/implementation-artifacts/sprint-status.yaml`](./_bmad-output/implementation-artifacts/sprint-status.yaml). The table below mirrors the current state of every epic; story-level detail lives in the sprint status file and the per-story documents under `_bmad-output/implementation-artifacts/`.

| Epic | Status | Scope |
|------|--------|-------|
| **Epic 1 — Crash-Free Gateway Foundation** | ✅ done | Dependency refresh + Rust 1.94, `log4rs → tracing` migration, comprehensive error handling, graceful shutdown via `CancellationToken`, configuration validation. |
| **Epic 2 — Data Persistence** | ✅ done | `StorageBackend` trait, SQLite backend with WAL mode + per-task connection pool, batch writes, append-only history table, startup restore, graceful degradation, retention pruning. |
| **Epic 3 — Reliable Command Execution** | ✅ done | SQLite-backed FIFO command queue, parameter validation, command-delivery status reporting (sent / confirmed / failed / timed-out). |
| **Epic 4 — Scalable Data Collection** | ✅ done | Poller refactored onto `StorageBackend`, support for all ChirpStack metric types, gRPC pagination. **4-4 done (2026-05-06 — auto-recovery from ChirpStack outages; Phase A carry-forward closed after iter-3 code review tightened shipped `config/config.toml` defaults from `retry=10, delay=10` to `retry=30, delay=1` to satisfy AC#4's `retry × delay ≤ 30s` clause + 4 supporting patches).** Closes PRD FR6 (TCP connectivity check), FR7 (auto-reconnect without manual intervention), FR8 (configurable retry count + delay), and NFR17 (30s auto-recovery SLA). New `recover_from_chirpstack_outage` helper on `ChirpstackPoller` (~120 LOC at `src/chirpstack.rs`) layered on top of the existing `chirpstack_outage` warn (Story 6-3): reads `chirpstack.retry` + `chirpstack.delay` at loop entry, calls `update_gateway_status(None, error_count, false)` to surface the outage to OPC UA (Story 5-3) + web dashboard (Story 9-2), then probes `check_server_availability` up to R times with cancel-token-paired `tokio::time::sleep` budget (Ctrl+C aborts cleanly). Three reserved log operations from `docs/logging.md:240-242` promoted to implemented: `recovery_attempt` (info per attempt), `recovery_complete` (info on success with `downtime_secs` from `last_successful_poll` math, or `from_startup=true` on cold start), `recovery_failed` (warn on budget exhaustion). Picked up the 6-3 carry-forward from `deferred-work.md:86`: `Channel::connect()` now wrapped in a 5s `tokio::time::timeout` (smaller than NFR17's 30s SLA, larger than the TCP probe's 1s timeout). 7 new unit tests in `src/chirpstack.rs::tests::recovery_*` covering AC#1 (loop fires + emits operations), AC#2 (retry count + delay), AC#3 (gateway_status update with `chirpstack_available=false` + `error_count` propagation), AC#5 (cancel-safety), AC#6 (downtime_secs + cold-start `from_startup=true`). Existing Story 5-2 stale-data semantics + Story 7-2/7-3/8-3/9-0/9-1/9-2/9-3 invariants preserved (zero changes to `src/web/`, `static/`, `tests/web_*.rs`, `src/opc_ua_*.rs`). |
| **Epic 5 — Operational Visibility** | ✅ done | OPC UA server refactored onto SQLite backend, stale-data detection with OPC UA `Good`/`Uncertain`/`Bad` status codes, gateway health metrics (last poll timestamp, error count, ChirpStack availability) exposed under the `Gateway` folder. |
| **Epic 6 — Production Observability & Diagnostics** | ✅ done | **6-1 done** (structured logging, correlation IDs on every OPC UA read, staleness-transition logs, poller-cycle structured logs, storage-query timing, configurable log directory via `OPCGW_LOG_DIR` and `[logging].dir`); **6-2 done** (configurable log verbosity via `OPCGW_LOG_LEVEL` and `[logging].level`); **6-3 done** (microsecond UTC timestamps; performance-budget warnings on `opc_ua_read`/`storage_query`/`batch_write`; data-anomaly logs — NULL `last_poll_timestamp`, staleness-boundary, error-count spike; ChirpStack `chirpstack_connect` / `chirpstack_outage` / `retry_schedule` diagnostics; edge-case logs — `gateway_status_init`, `chirpstack_request` timeout, `metric_parse`; transient-failure logs — `device_poll`, SQLITE_BUSY (with `sqlite_error_code` sibling field for differentiating BUSY/LOCKED); end-to-end `request_id` correlation verified via integration test; expanded operations reference + symptom cookbook in `docs/logging.md`. Code review complete in 3 iterations: clippy-clean across the workspace, `Mutex<HashMap>` staleness cache replaced with `DashMap` for lock-free concurrent reads, 5 helpers extracted (`maybe_emit_error_spike`, `maybe_emit_chirpstack_outage`, `validate_bool_metric_value`, `classify_and_log_grpc_error`, `format_last_successful_poll`) so synthetic tests now drive production paths; 188 lib + 209 bin + 79 integration tests pass). |
| **Epic 7 — Security Hardening** | ✅ done | **7-1 done** — sanitized `config/config.toml` with `REPLACE_ME_WITH_*` placeholders + placeholder-detection in `validate()`, hand-written redacting `Debug` impls on `ChirpstackPollerConfig` / `OpcUaConfig`, `secrets_not_logged_when_full_config_debug_formatted` regression test, tonic 0.14.5 metadata-leak audit (clean — no `EnvFilter` mitigation needed today), `.env.example` + Compose recipe with `:?err` guards, `docs/security.md` with reversible-migration alternative, scrubbed `config/config.example.toml` (synthetic IDs only). **7-2 done** — `OPCUA_USER_TOKEN_ID = "default-user"` constant replacing the hardcoded `"user1"` token id (4 call sites), custom `OpcgwAuthManager` (`src/opc_ua_auth.rs`) implementing async-opcua's `AuthManager` trait with **HMAC-SHA-256-keyed credential digests** for fully-constant-time comparison (no length oracle) and sanitised-username `event="opcua_auth_failed"` audit logging (NFR12 via two-event correlation with async-opcua's accept event); `validate_private_key_permissions` (NFR9 — `0o600` file + `0o700` parent dir enforced at startup with both-violation accumulation, fail-closed) and `ensure_pki_directories` (FR45 — auto-creates `own/`, `private/`, `trusted/`, `rejected/` race-free via atomic `DirBuilder::mode()`) in new `src/security.rs` module; path-traversal guards rejecting absolute / `..` `private_key_path` and empty `pki_dir`; trim checks on `user_name` / `user_password` for copy-paste-from-`.env` resilience; shipped `create_sample_keypair = false` in both `config/config.toml` and `config/config.example.toml`, release-build warning when the flag is `true`; integration tests pinning the three endpoints (None / Basic256 Sign / Basic256 SignAndEncrypt) with line-scoped audit-event assertions and the wrong-password rejection path; smoke-test client at `examples/opcua_client_smoke.rs`; `async-opcua-client` moved to `[dev-dependencies]` to keep the production binary lean; extended `docs/security.md` with endpoint matrix + PKI layout + audit-trail recipe + log-level-required-for-NFR12 hard-statement + create-sample-keypair regen anti-pattern + Story-7-1 migration path. Code review closed all HIGH/MEDIUM findings over three iterations. **7-3 done** — `[opcua].max_connections: Option<usize>` config knob (default 10, hard cap 4096) wired through `ServerBuilder::max_sessions(N)` for FR44; `OPCGW_OPCUA__MAX_CONNECTIONS` env-var override; new `src/opc_ua_session_monitor.rs` module exposing a periodic `info!(event="opcua_session_count", current, limit)` gauge (5s tick) and an `AtLimitAcceptLayer` tracing-Layer that emits `warn!(event="opcua_session_count_at_limit", source_ip, limit, current)` correlated against async-opcua's `Accept new connection from {addr}` event (NFR12 two-event pattern); 8 unit tests + 4 integration tests; 581 tests / 0 fail / 7 ignored, clippy clean; extended `docs/security.md` with `## OPC UA connection limiting` section. **Epic 7 retrospective complete (2026-04-29)** — Phase A security baseline (FR42, FR44, FR45, NFR7-9, NFR12, NFR24) satisfied. Decided next steps before `bmad-create-story 8-1`: NFR12 silent-degradation startup-warn in `src/main.rs::initialise_tracing` (~5 LOC) + Epic 8 spec polish in `epics.md` (incorporates per-IP throttling #88, message-size / monitored-item limits #89, gauge tunability, NFR12 logging hard-statement). Eight follow-up GitHub issues (#82, #83, #85–#90) carry forward; none block Phase B entry. Doctest baseline (56 pre-existing failures) escalates to a defined cleanup story before Epic 9. See [`epic-7-retro-2026-04-29.md`](./_bmad-output/implementation-artifacts/epic-7-retro-2026-04-29.md). |
| **Epic 8 — Real-Time Subscriptions & Historical Data (Phase B)** | ✅ done (8-1 / 8-2 / 8-3 shipped; 8-4 descoped from Epic 8 on 2026-05-14 and tracked as a Known Failure — see retro § Known Failures + 2026-05-14 descope addendum) | **Epic 8 retrospective complete (2026-05-01)** — Phase B subscription + historical-data baseline (FR21, FR22) satisfied. Story 8-4 (threshold-based alarm conditions, FR23) classified as Known Failure (KF) with operator-visible diagnostics treatment per Story 6-3 convention; SCADA-side alarm thresholds in FUXA / Ignition is the documented operator workaround. Key decided action items before Epic 9 starts: NodeId metric-name-only collision bug fix (HIGH severity, pre-existing latent bug 8-3 surfaced via HistoryRead), `test_concurrent_write_read_isolation` flake fix (`#[serial_test::serial]`), doctest cleanup story (BLOCKING — 4th epic in a row), spike-test productionisation, `tests/common/mod.rs` extraction, CLAUDE.md per-iteration commit-rule clarification. Carry-forward GH issues #88, #93, #94, #95, #98. See [`epic-8-retro-2026-05-01.md`](./_bmad-output/implementation-artifacts/epic-8-retro-2026-05-01.md). **8-1 / 8-2 / 8-3 done.** **8-3 done (2026-04-30 after iter-1 + iter-2 code review)** — OPC UA `HistoryRead` (FR22) end-to-end: new `StorageBackend::query_metric_history` method on `SqliteBackend` + `InMemoryBackend` with half-open `[start, end)` interval semantics (start inclusive, end exclusive — matches OPC UA Part 11 §6.4), microsecond-precision UTC timestamps, partial-success on bad rows (NaN / unknown data_type silently skipped with `trace!`); new `src/opc_ua_history.rs` module wrapping async-opcua's `SimpleNodeManagerImpl` and overriding `history_read_raw_modified` (the wrap-don't-subclass pattern documented in Story 8-1's spike report); reverse-lookup `NodeId → (device_id, metric_name)` map built once at server-construction time; metric variables now carry `AccessLevel::HISTORY_READ` + `historizing = true`; new `[storage].retention_days` validation (FR22 floor of 7 days, hard cap 365) + new `[opcua].max_history_data_results_per_node` per-call cap (default 10000, hard cap 1_000_000) — both wired through env-var overrides; the configured retention is now written to `retention_config` at startup via `INSERT OR REPLACE` (was migration-default 90 days, now operator-config-driven); **continuation points NOT implemented** (manual-paging contract documented in `docs/security.md`); NFR15 release-build benchmark in `tests/opcua_history_bench.rs` (`#[ignore]` by default; targets 600k-row 7-day query <2s); NFR12 carry-forward intact (zero new audit events, src/opc_ua_auth.rs / src/opc_ua_session_monitor.rs production code unchanged); 11 unit tests on `query_metric_history` + 11 config-validation tests + 5 integration tests on the HistoryRead pipeline. See [`8-3-historical-data-access-via-opc-ua.md`](./_bmad-output/implementation-artifacts/8-3-historical-data-access-via-opc-ua.md) for the spec; the new docs section is `docs/security.md#historical-data-access`. **8-4** (threshold-based alarm conditions, FR23) **classified as Known Failure** — see retro § Known Failures + `deferred-work.md`. |
| **Epic 9 — Web Configuration & Hot-Reload (Phase B)** | ✅ done (closed 2026-05-14 — all 9 stories 9-0 through 9-8 done + retrospective complete; production deployment remains blocked on issue [#108](https://github.com/guycorbaz/opcgw/issues/108), to be addressed by Epic A — Storage Payload Migration) | **9-8 review (2026-05-13 — Implementation + 5 integration tests complete via bmad-dev-story; awaiting code review)** — Dynamic OPC UA Address Space Mutation (FR24). Closes Story 9-7's documented v1 limitation. New `src/opcua_topology_apply.rs` module hosts `compute_diff` + `apply_diff_to_address_space` (4-phase mutation envelope). Story 9-7 stub seam wired. 2 new audit events. Critical empirical Q2-mitigation refinement: `value: Some(Variant::Empty)` sentinel forces DataChange notification under async-opcua's default value-only filter. Task 6 option (b) — callback registry private upstream, stub functions emit one-time deferred-leak info log. AC#9 strict-zero file invariants honoured. 5 integration tests in `tests/opcua_dynamic_address_space_apply.rs` against a real OPC UA server. Recommend running bmad-code-review on a different LLM per CLAUDE.md doctrine. **9-6 done (2026-05-12 — Implementation complete via bmad-dev-story, all Tasks 0-11; awaiting code review)** — Command CRUD via Web UI (FR36, FR40, FR41). Five new endpoints under `/api/applications/:application_id/devices/:device_id/commands*` (GET list/by-id, POST, PUT, DELETE) closing the FR34/35/36 cluster. `:command_id` path segment is `i32` parsed by new `validate_path_command_id` helper (rejects non-numeric/≤0 with `event="command_crud_rejected" reason="validation" field="command_id"`). CSRF literal-arm completion in `src/web/csrf.rs` (adds `"command" =>` warn arm at both rejection-emission match blocks per the explicit Story 9-6 hand-off comment); `validate_path_device_id` widened with `resource: &'static str` parameter (parallel to iter-3 Blind#3 pattern for `find_application_index`) — all 9-5 device-handler call sites pass `"device"` byte-for-byte. New `validate_command_name` / `validate_command_port` (delegates to `DeviceCommand::validate_f_port` enforcing LoRaWAN 1..=223) / `validate_command_id_value` / `command_not_found_response` / `build_command_table` helpers in `src/web/api.rs`. `AppConfig::validate` additive amendment for per-device `command_id` + `command_name` uniqueness HashSets (modelled on Story 9-5's `seen_metric_names` pattern; cross-device same-`command_id` remains allowed per device-folder-NodeId-namespace argument). TOML mutation preserves sibling `[[application.device.read_metric]]` byte-for-byte (regression guard for Story 9-5); DELETE-last-command leaves empty `command` `ArrayOfTables` in place (Task 6 pinned decision — verified by `delete_last_command_leaves_clean_toml_round_trip` test). 4 new audit events: `command_created` / `command_updated` / `command_deleted` (info) + `command_crud_rejected` (warn) with 1 new reason value `command_not_found`. AC#10 strict-zero file invariants: `src/web/auth.rs`, `src/opc_ua.rs`, `src/opc_ua_auth.rs`, `src/opc_ua_session_monitor.rs`, `src/opc_ua_history.rs`, `src/security.rs`, `src/security_hmac.rs`, `src/main.rs::initialise_tracing` untouched. NEW `static/commands.html` + `static/commands.js` (vanilla JS, no framework, no build step); Commands nav links added to `static/applications.html` + `static/devices-config.html` + `static/devices.html`. NEW `tests/web_command_crud.rs` with 45 integration tests covering AC#1-12 + AC#11 cross-resource regression suite (Stories 9-4 + 9-5 grep contracts preserved); 7 new unit tests in `src/web/api.rs::tests` (3 `validate_path_command_id` + 2 `validate_path_device_id` widening + 1 already-existing widened); 2 new unit tests in `src/web/csrf.rs::tests`; 3 new unit tests in `src/config.rs::tests` (per-device command_id/command_name uniqueness + cross-device-allowed). docs/logging.md +4 command_* rows + path-aware dispatch note; docs/security.md "Command CRUD (Story 9-6)" subsection covering endpoint surface, CSRF, validate-side amendments, audit events, v1 limitations, anti-patterns. No new dependencies — `scopeguard` crate is NOT a dep (chmod-cleanup uses hand-rolled Drop-impl RAII guard per Story 9-5 `tests/web_device_crud.rs:1578` precedent). Final grep contracts intact: `command_*` = 4, `device_*` = 4, `application_*` = 4, `config_reload_*` = 3. Task 0 (open tracking GH issue) deferred to user — gh CLI not authenticated for write per Stories 9-4 / 9-5 precedent. **9-7 done (2026-05-06 — 3 code-review iterations terminated under CLAUDE.md condition #2)** — Configuration Hot-Reload (FR39 + FR40). New `src/config_reload.rs` module owns the `tokio::sync::watch::Sender<Arc<AppConfig>>` channel, the `ConfigReloadHandle::reload()` routine (validate-then-swap discipline), and the knob-taxonomy classifier that distinguishes hot-reload-safe from restart-required from address-space-mutating changes. `src/main.rs` spawns a SIGHUP listener (3 new audit events: `event="config_reload_attempted"` info, `"config_reload_succeeded"` info with `changed_section_count` + `duration_ms`, `"config_reload_failed"` warn with `reason ∈ {validation, io, restart_required}` + `changed_knob`), a web-config-listener task that atomically swaps `AppState.dashboard_snapshot` + `AppState.stale_threshold_secs` (now `RwLock<Arc<...>>` + `AtomicU64`), and an OPC UA config-listener stub for Story 9-8 that logs `topology_change_detected` with device counts. Poller's outer-loop `tokio::select!` gains a `config_rx.changed()` arm; Story 4-4's recovery loop unaffected (read-at-entry semantics naturally pick up new `retry`/`delay` values). AC#8 invariants honoured: zero changes to `src/web/auth.rs`, `src/opc_ua.rs`, `src/opc_ua_auth.rs`, `src/opc_ua_session_monitor.rs`, `src/opc_ua_history.rs`, `src/security.rs`, `src/security_hmac.rs`, `src/main.rs::initialise_tracing`. v1 limitations explicitly documented in `docs/security.md § Configuration hot-reload`: (1) OPC UA address-space mutation stubbed (Story 9-8 territory — the dashboard updates but OPC UA stays frozen), (2) credential rotation restart-required (auth-middleware refactor deferred), (3) `[opcua].stale_threshold_seconds` hot-reload affects only the web dashboard (OPC UA per-variable closures captured at startup), (4) no HTTP trigger (Stories 9-4/9-5/9-6 will add CRUD-driven reload), (5) no filesystem watch. 13 new integration tests in `tests/config_hot_reload.rs` covering AC#1 (validation-first / atomic swap), AC#2 (hot-reload-safe propagation), AC#3 (restart-required rejection with `changed_knob`), AC#4 (dashboard reflection), AC#9 (loose-perm rejection + secret-hygiene), AC#10 (stale-threshold propagation), plus io-reason path and equal-config NoChange. 6 new `src/config_reload.rs::tests` unit tests + 1 new `src/chirpstack.rs::tests::poller_picks_up_new_retry_at_next_cycle` + 3 new clamp-helper unit tests. AC#7 grep contract: `git grep -hoE 'event = "config_reload_[a-z]+"' src/ | sort -u` returns exactly 3 lines. Issue #110 (RunHandles missing Drop impl) verdict: keep open — listener tasks all cooperate with `cancel_token` explicitly via `tokio::select!`, RAII drop would be redundant. Tracking issue #112. **9-1 done (2026-05-02 after iter-1 + iter-2 code review)** — Axum 0.8 embedded web server gated by HTTP Basic auth (FR50 / NFR11), `event="web_auth_failed"` warn-level audit event with **direct** source-IP via Axum's `ConnectInfo<SocketAddr>` (NFR12 — strict improvement over the OPC UA path's two-event correlation), `tower-http::services::ServeDir` static-file mount with placeholder HTML for Stories 9-2+ (FR41 mobile-responsive viewport tag in place), `[web].enabled = false` master switch (opt-in to avoid surprise listening port on upgrade), credentials shared with `[opcua]` (single source of truth — one rotation step covers both surfaces), HMAC-SHA-256 keyed credential digest extracted from Story 7-2's `OpcgwAuthManager` into `src/security_hmac.rs` (Phase-B carry-forward rule per `epics.md:782`), `[web]` config validation (port range 1024-65535, parseable bind address, realm ≤ 64 chars + no `"`), `OPCGW_WEB__*` env-var overrides via figment's nested-key convention, `CancellationToken`-driven graceful shutdown joining the existing `tokio::select!` sequence in `src/main.rs`, 5 web-config validation unit tests + 10 web-auth middleware unit tests + 7 web integration tests + 4 security_hmac unit tests, clippy clean, TLS-via-reverse-proxy explicitly out of scope (tracked at #104). **9-2 review (2026-05-03 — implementation complete, awaiting code review)** — Gateway Status Dashboard (FR38) + responsive HTML/CSS/JS dashboard (FR41 fully closed at the content level). New `AppState` struct lands the deferred-from-9-1 shape (auth + per-task `SqliteBackend` + frozen `DashboardConfigSnapshot` + `start_time`); new `GET /api/status` JSON endpoint reads `get_gateway_health_metrics()` and returns the 6-field dashboard payload (`chirpstack_available`, `last_poll_time`, `error_count`, `application_count`, `device_count`, `uptime_secs`); new `src/web/api.rs` module hosts the handler (lands on the directory slot `architecture.md:417-421` reserved for "Stories 9-2 onwards"); `static/index.html` replaced with a real 6-tile dashboard backed by `static/dashboard.css` (mobile-first responsive grid + `prefers-color-scheme: dark`) and `static/dashboard.js` (`fetch('/api/status')` on load + 10 s `setInterval` + inline error banner on 401/5xx); `tests/web_dashboard.rs` adds 4 integration tests (auth carry-forward + JSON shape pin + HTML markup + CSS responsive marker); 4 new unit tests for `api_status` (success / 500-with-generic-body / first-startup defaults / `null`-serialisation pin); 3 new unit tests for `DashboardConfigSnapshot::from_config`. Audit-event surface: ZERO new web-side events; ONE new diagnostic event `event="api_status_storage_error"` (warn) on storage-read failure. AC#6 file invariants honoured: `git diff` shows zero changes to `src/opc_ua*.rs` and to `src/web/auth.rs` production code (only `tests/web_auth.rs` fixture wraps `WebAuthState` in `AppState` for the new `build_router` signature). **9-0 done (2026-05-05 — async-opcua runtime address-space mutation spike, code-review iter-1 + iter-2 complete).** Three load-bearing questions from `epics.md:784-787` resolved empirically against async-opcua 0.17.1 under live subscriptions: **Q1 add path RESOLVED FAVOURABLY** (a runtime-added variable's first subscription notification carries the registered sentinel; the `SyncSampler` honours post-init `add_read_callback` registrations without restart), **Q2 remove path Behaviour B (frozen-last-good)** (deleting a variable while subscribed produces no observable client error — Story 9-8 must emit an explicit `BadNodeIdUnknown` via `manager.set_attributes` before `delete()`), **Q3 sibling isolation RESOLVED FAVOURABLY** (bulk mutation of 11 nodes under a single `address_space.write()` = 117 µs hold time, ~850× shorter than the 100 ms sampler tick — no risk of starvation at typical 100-1000-node-reload scales). Implementation: split `OpcUa::run` into `build` + `run_handles` + backward-compat `run` wrapper (Shape B per AC#5; the `RunHandles` struct is the integration seam Story 9-7 hot-reload will reuse). All existing call sites compile unchanged; lib + bins test count holds at 322 + 345 = 667 / 0 fail / 5 ignored after iter-1 + iter-2 patches. New: `tests/opcua_dynamic_address_space_spike.rs` (3 spike tests), `_bmad-output/implementation-artifacts/9-0-spike-report.md` (12-section architecture-reference doc). Known limitation surfaced: `SimpleNodeManagerImpl` does not expose `remove_read_callback`, so deleting a node leaks the closure — 9-8 must extend `OpcgwHistoryNodeManager` with a remove method or file an upstream FR (precedent: Story 8-1's session-rejected callback FR at issue #94). Code-review iter-1 applied 10 patches (P2-P11: spike test asserts hardened, spike report § 7 restructured to "Pattern reuse", AC#1/#2/#3 measurement substitutions ratified in spec AC Amendments + spike report addenda); P1 (RunHandles missing Drop impl) blocked by rustc E0509 and tracked at GitHub KF issue #110 for Story 9-7 evaluation. Iter-2 re-ran all three reviewer layers per CLAUDE.md and applied 6 follow-up patches (IP1-IP6: residual "~110 LOC" claim, 30s sanity ceiling on lock-hold, header arithmetic, `assert_eq!` sentinel-comparison hardening, doc-comment cleanup, deferred-work.md path bracket). Remaining stories: 9-7 hot-reload, 9-8 dynamic address space, 9-4/9-5/9-6 CRUD. |
| **Epic C — Auto-Discovery and Web-First Configuration (post-v2.0 GA)** | 🔄 in-progress (3/6 done) — 6-story epic scoping commit `4f59592` + spec-drafting commit `afe6869` landed 2026-05-21. **C-2 done 2026-05-23** (inventory pickers in the web UI: `/applications.html` + `/devices-config.html` replace the free-form UUID/DevEUI inputs with cascading name-driven `<select>` dropdowns fed by C-1's `/api/inventory/*` endpoints; load-bearing manual-fallback toggle with localStorage mode persistence per-page; new shared `static/inventory-picker.js` module; new `POST /api/audit/picker-event` thin endpoint for client-attributable picker audit events with allowlist validation + per-event field sanitisation; new optional `picker_metadata` field on the metric-create path → emits `event="metric_wire_type_inferred"` per picker-attributed metric; new audit events `picker_opened`, `picker_manual_fallback`, `picker_audit_rejected`, `metric_wire_type_inferred`; 13 new integration tests in `tests/web_picker.rs`; see `docs/inventory-api.md § Picker-event audit endpoint`). **C-1 done 2026-05-22** (ChirpStack inventory query layer: 3 new GET endpoints `/api/inventory/{applications,devices,uplinks}` + server-side TTL cache + race-free fetch + CRUD invalidation hooks + wire-type inference + 5 audit events including `shutdown_cancellation` + `inventory_uplink_dropped`; see `docs/inventory-api.md`. Loop terminated after 3 review iterations — 17th iter-N+1 doctrine validation. Commits `b5df23f` + `372280f` + `3f35ddb` + `5840c04` + flip). **C-0 done 2026-05-22** (empty-config bootstrap + first-run wizard at `/setup`) — implementation `c200089` + iter-1 review fixes `7ec2fc1` + iter-2 review fixes `d1d1332` + iter-3 review fixes `d84fa7d`; 15th iter-N+1 doctrine validation; 1326/0/10 tests / clippy clean. Validator amended to accept empty `[opcua].user_password` when no env-var is set; new `is_first_run()` method; figment provider stack extended with sibling `config/secrets.toml` between main TOML and env-var layer; new `src/web/setup.rs` module providing the first-run gate middleware, wizard GET handler, POST persistence handler with HTTP 4 KiB body limit + strict `application/json` Content-Type + same-origin Origin check + race-free `Arc<AtomicBool>` compare_exchange one-shot guarantee; new `static/setup.html` wizard frontend with categorised error-reason UX (readonly_filesystem / disk_full / permission_denied / parent_directory_missing); OPC UA server emits `event="opcua_first_run_mode"` audit + rejects all auth via existing `OpcgwAuthManager::is_configured = false` path until restart; gateway shuts down gracefully via `CancellationToken` on successful wizard submit so the supervisor (Docker / systemd) restarts and the figment provider stack picks up the new `secrets.toml`. **C-5 (MQTT real-time path) intentionally removed from epic** — tracked as `CR-EPIC-C-MQTT` in `deferred-work.md`. Remaining stories (C-3 dup-prevention, C-4 drift view, C-6 TOML→SQLite migration) are spec'd and `ready-for-dev`. |
| **Epic B — v2.0 GA Release Packaging** | 🔄 in-progress — story `B-1-docker-hub-publishing-and-user-manual` is the single multi-deliverable story closing the final pre-v2.0-tag gaps surfaced in the Epic A retrospective. Scope: dual-registry Docker image publishing (Docker Hub `gcorbaz/opcgw` + GHCR `guycorbaz/opcgw`, multi-arch linux/amd64+linux/arm64), Dockerfile hardening (non-root `opcgw` UID 10001 + base pin to `ubuntu:24.04`), new `docs/dockerhub-description.md` for the hub.docker.com Overview page, repo `README.md` updates, and DocBook user manual update bringing `docs/manual/opcgw-user-manual.xml` current to v2.0 with new Installation + Configuration + Troubleshooting + Upgrade chapters (closes Epic A retro action item AI-A-8). NO Rust production code changes (strict-zero on `src/**/*.rs`, `tests/**/*.rs`, `Cargo.toml`, `Cargo.lock`, `migrations/*.sql`, `config/*`). After B-1 lands + `epic-B-retrospective` completes, the next mandatory step is `git tag v2.0` + `git push --tags` — the dual-registry workflow then auto-publishes the v2.0.0 images. |
| **Epic A — Storage Payload Migration (Phase B closure — v2.0 GA gating epic)** | ✅ done — **all 7 stories done (A-1 / A-2 / A-3 / A-4 / A-5 / A-6 / A-7) + epic-A-retrospective done (2026-05-19)**. **Issue #108 fully closed across all 7 stories.** Retrospective conducted inline security review (CLEAN with 1 LOW patched); 11 action items captured (AI-A-1 to AI-A-11). Critical path before `v2.0` tag: (a) CHANGELOG entry for `MetricValue.value` SemVer-major retire (AI-A-4); (b) `git push` per CLAUDE.md "After an epic retrospective" rule. | Closes issue [#108](https://github.com/guycorbaz/opcgw/issues/108) — the payload-less `MetricType` enum that flattened every persisted metric value to its discriminant string ("Float" / "Int" / "Bool" / "String") instead of the real measurement. **A-1 done** — `MetricType` refactored to payload-bearing (`Float(f64)` / `Int(i64)` / `Bool(bool)` / `String(String)`); `Copy` dropped; `Display` + `FromStr` preserved with zero-default contract; `MetricValue.value: String` retained dual-storage with `TODO(A-5)` marker. **A-2 done** — v007 SQLite migration adds five typed columns (`value_real REAL NULL` / `value_int INTEGER NULL` / `value_bool INTEGER NULL` / `value_text TEXT NULL` / `value_type TEXT NOT NULL DEFAULT 'legacy' CHECK(value_type IN ('legacy','Float','Int','Bool','String'))`) to both `metric_values` and `metric_history`. Pre-Epic-A rows tagged `value_type='legacy'` via column default; writers + readers strict-zero per option-b staging. **A-3 done** — Poller value-payload write pipeline: all 7 `TODO(A-3)` sites in `prepare_metric_for_batch` wrap real ChirpStack values into the typed payload; NaN/Inf option-(a) filter at poller with `event="metric_parse"` `reason="non_finite"` warn; all 4 SqliteBackend writers populate typed columns + `value_type` via private helper `typed_value_columns()`; new v008 migration adds exactly-one-non-NULL CHECK via CREATE TABLE … AS SELECT pattern wrapped in BEGIN/COMMIT; `chirpstack.rs::store_metric` body reinstated with NaN/Inf guard. **A-5 done (2026-05-17)** — OPC UA HistoryRead value-payload pipeline: `SqliteBackend::query_metric_history` rewires SELECT to project v007 typed columns via the A-4-shared `metric_type_from_typed_columns` helper; `HistoricalMetricRow` restructured to `payload: Option<MetricType>` so legacy rows are a first-class outcome. `OpcgwHistoryNodeManagerImpl::build_data_values` pattern-matches the typed payload directly; legacy rows emit `DataValue { value: None, status: BadDataUnavailable }` per epic AC#1 (NOT silently dropped). The transitional `MetricValue.value: String` field was retired across all 4 public storage structs (`MetricValue`, `MetricValueInternal`, `BatchMetricWrite`, `HistoricalMetricRow`); compile-time field-shape pins via non-exhaustive destructure patterns lock the shape against future re-introduction. New audit event `metric_history_read` (closed reason enum: `{schema_drift, unparseable_timestamp, narrowing_overflow, narrowing_underflow}`) + iter-2 `metric_history_summary` (trace-level aggregate skip telemetry). `convert_variant_to_metric` signature simplified to `Result<MetricType, OpcGwError>`. Story 9-3 dashboard wire-format preserved via `metric_view_display_string` shim (Bool `"0"`/`"1"`, Float f32-precision). 1230 tests / 0 fail / 10 ignored; clippy clean; doctest clean. **Issue #108 fully closes — Epic A now 5/7 done.** **A-4 done (2026-05-17)** — OPC UA Read value-payload pipeline: SqliteBackend readers (`get_metric_value` / `get_metric` / `load_all_metrics`) rewire SELECT to project typed columns via new private helper `metric_type_from_typed_columns()`; `OpcUa::convert_metric_to_variant` rewrites to pattern-match the typed payload directly (no more `metric.value.parse::<…>()`); legacy rows surface as `Ok(None)` mapping transitively to `BadDataUnavailable` per architecture.md:182. Float narrowing-overflow guard (A-1-iter1-DEF17 closure) emits new `event="metric_read"` `reason="narrowing_overflow"` warn (plus iter-1 IR7 `reason="narrowing_underflow"` and iter-2 JR4 `reason="no_payload"` siblings). AC#9 from A-3 (Counter monotonic typed-path preference) closed at `chirpstack.rs:1707-1727`. End-to-end live OPC UA server proof: `tests/opcua_subscription_spike.rs::test_subscription_datavalue_payload_carries_seeded_value` passes — real client receives `Variant::Float(42.5)` with `Good` status through the new typed-payload Read pipeline. 1214 tests / 0 fail / 10 ignored (post-iter-2 final); clippy clean; doctest 0 failed / 55 ignored. **A-6 done (2026-05-18)** — Web UI live-metrics typed value display. `bmad-dev-story A-6` implementation complete; status flipped `ready-for-dev → in-progress → review`. All 14 ACs SATISFIED. `MetricView` JSON shape widened from `value: Option<String>` to typed `value: Option<serde_json::Value>` + new `unit: Option<String>` field. New helper `metric_type_to_json_value` in `src/web/api.rs` emits typed JSON primitives; non-finite Float yields `None` + emits new `event="metric_view_serialize"` `reason="non_finite"` warn. `web::MetricSpec` widened with `metric_unit`; hot-reload propagation flows through Story 9-7 without `config_reload.rs` changes. **Retired `metric_view_display_string` transitional shim** + its 2 unit tests (closes `DEF-iter1-A5-D1`); replaced with 5 typed-JSON unit tests. `static/metrics.js::renderMetricRow` gets new `formatValue(value, dataType)` helper + value-plus-unit composition; Bool wire shifts from `"1"`/`"0"` to native `true`/`false`. **Task 1 finding (load-bearing):** post-A-5 `load_all_metrics` silently SKIPS legacy rows at `src/storage/sqlite.rs:1530-1540` (`legacy_skipped_count += 1; continue;`) — legacy rows never reach the dashboard handler. Consequence: `metric_view_serialize` event has ONLY the `non_finite` reason (legacy_row dropped from the closed enum). Closes deferred-work `9-5-iter1-D4` (empty-string `metric_unit` coalesces to no-unit-suffix at the renderer via JS truthy check). `MetricView` lost `PartialEq, Eq` derives (cascaded to `DevicesResponse` / `ApplicationView` / `DeviceView`) because `serde_json::Value` doesn't impl `Eq`. cargo test 1242 / 0 / 10 (was 1230 A-5 baseline; +12; exceeds AC#12 target ≥1240 by 2); clippy clean; doctest clean. AC#5 grep returns 5 lines (`metric_history_read` + `metric_history_summary` + `metric_parse` + `metric_read` + `metric_view_serialize`); AC#7 grep returns 0 `metric_view_display_string` references. Recommend `bmad-code-review A-6` on a different LLM. Next: `bmad-code-review A-6`. **A-7 done (2026-05-18)** — Migration runbook + version-gated script. `bmad-create-story A-7` produced spec at `A-7-migration-runbook-and-script.md` (~9 ACs, 6 tasks, 1 decision-needed). **FINAL Epic A story.** Pure docs + tests + small shell script — NO Rust production-code changes. Deliverables: (1) `docs/deployment-guide.md § "Epic A migration"` operator runbook covering Path A (auto-migration on next gateway startup, preserves legacy rows as `value_type='legacy'`) / Path B (drop `opcgw.db*` and start fresh) / pre-upgrade checklist / post-migration verification / one-way rollback contract (restore pre-upgrade backup) / SLA expectation / common gotchas; (2) NEW `scripts/check-schema-version.sh` shell script wrapping `sqlite3 PRAGMA user_version` with operator-friendly output + Path A/B recommendations (sqlite3 CLI dependency, introduces new top-level `scripts/` directory); (3) NEW end-to-end regression test `test_v006_to_v008_full_upgrade_path_under_5s` in `src/storage/schema.rs::tests` pinning the chained v006→v007→v008 path via a single `run_migrations(&conn)` call (NOT the manual `execute_batch` shortcut — captures the docstring-vs-body drift lesson from A-5). Decision D1 (load-bearing): epic AC's literal "5s for 100MB" target doesn't align with existing `test_v008_migration_under_30s_for_10k_rows` baseline; dev-agent picks (a) keep 5s-for-10k-row baseline + document larger-DB caveat in the runbook (recommended), (b) 500k-row `#[ignore]` test, or (c) tune v008 (out of A-7 scope — would re-open A-3 review loop). AC#7 strict-zero invariants: A-7 NARROW-MUTABLE on `src/storage/schema.rs::tests` only (production-code body untouched); MUTABLE on `docs/deployment-guide.md` + NEW `scripts/check-schema-version.sh` + `README.md` + `sprint-status.yaml` + this spec file. Test budget ≥1255 (was 1254 A-6 baseline + 1 new fn). Grep contracts unchanged. **Epic A completion + retrospective trigger:** after A-7 ships, Epic A is 7/7 done; per CLAUDE.md "Do not skip the retrospective" the `epic-A-retrospective: optional` line becomes mandatory — the very next BMad action after A-7's Code Review Complete commit MUST be `bmad-retrospective` for Epic A, the gate to v2.0 GA release. **v2.0 GA gated on A-7 + Epic A retrospective landing.** |

### How to read this section

- **Status legend:** ✅ done · 🔄 in-progress · 📋 backlog (and 📝 ready-for-dev / 👀 review for individual stories).
- **Phase A** covers Epics 1–7 — production hardening of the existing one-way (read) gateway.
- **Phase B** covers Epics 8–9 — adds real-time subscriptions, historical data access, and a web admin surface. Story 4-4 is deferred to a Phase B resilience epic.
- For the canonical, machine-readable view, see [`sprint-status.yaml`](./_bmad-output/implementation-artifacts/sprint-status.yaml). The sprint-status file is the source of truth; this table is updated alongside it.
- Per-story details, acceptance criteria, dev notes, and review findings live in `_bmad-output/implementation-artifacts/<epic>-<story>-<slug>.md`.

A long-form roadmap with marketing-friendly language is available at [Roadmap](https://guycorbaz.github.io/opcgw/features/#roadmap).

## Use Cases

- 🌱 **Smart Agriculture**: Monitor soil conditions across farms via wireless sensors
- 🏭 **Industrial IoT**: Asset tracking and equipment monitoring
- 🌍 **Environmental Monitoring**: Air quality, weather stations, environmental sensors
- 🏢 **Building Automation**: HVAC, occupancy, energy management
- ⚡ **Renewable Energy**: Solar + battery microgrid optimization

→ See [Real-World Use Cases](https://guycorbaz.github.io/opcgw/usecases/) for detailed scenarios.

## Logging

opcgw is built on `tracing` with per-module file appenders and a stderr console layer. The global verbosity is configurable at runtime — no rebuild required.

```bash
# Set verbosity for a single run
OPCGW_LOG_LEVEL=debug ./target/release/opcgw

# Or persist in config.toml
[logging]
level = "debug"
dir = "/var/log/opcgw"
```

Valid levels: `trace`, `debug`, `info` (default), `warn`, `error`. Per-module file appenders capture independently of the global level — see [`docs/logging.md`](./docs/logging.md) for the operator-facing reference, including the structured-field schema, correlation-ID tracing, and the env-var override convention.

## Architecture

opcgw consists of two main components running concurrently:

- **ChirpStack Poller**: Polls device metrics from ChirpStack via gRPC at configurable intervals
- **OPC UA Server**: Exposes collected metrics as OPC UA variables for industrial clients

Both components share thread-safe in-memory storage via `Arc<Mutex<Storage>>`.

→ [See full architecture](https://guycorbaz.github.io/opcgw/architecture/)

## Technology Stack

- **Language**: Rust 1.94.0+ with async/await
- **Protocols**: gRPC for ChirpStack, OPC UA 1.04 for industrial clients
- **Storage**: In-memory HashMap (v2.0) with SQLite persistence planned
- **Logging**: Tokio-tracing with structured fields and per-module log files
- **Async Runtime**: Tokio for high-performance I/O
- **Build**: Multi-stage Docker build for minimal image size

## Contributing

Contributions are welcome! Please:

1. Check [existing issues](https://github.com/guycorbaz/opcgw/issues) first
2. Open an issue to discuss your idea before implementing
3. Follow the code style and conventions in CLAUDE.md
4. Ensure tests pass: `cargo test && cargo clippy`
5. Submit a pull request with a clear description

## Development

```bash
# Build and test
cargo build --release
cargo test
cargo clippy

# Run with debug logging
RUST_LOG=debug cargo run -c config/config.toml

# Watch logs
tail -f log/*.log
```

## License

Licensed under either MIT or Apache-2.0 at your option.

---

## Support

- 📖 [Documentation](https://guycorbaz.github.io/opcgw/)
- 🐛 [Issues](https://github.com/guycorbaz/opcgw/issues)
- 💬 [Discussions](https://github.com/guycorbaz/opcgw/discussions)
- 📧 Contact: gcorbaz@gmail.com

## Contributing

Any contributions you make are greatly appreciated. If you identify any errors,
or have an idea for an improvement, please open an [issue](https://github.com/guycorbaz/opcgw/issues).
But before filing a new issue, please look through already existing issues. Search open and closed issues first.

Non-code contributions are also highly appreciated, such as improving the documentation
or promoting opcgw on social media.


## License

MIT OR Apache-2.0.
