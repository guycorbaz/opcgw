# Deferred Work

## Deferred from: code review of story-1.1 (2026-04-02)

- `command_id as u32` lossy cast at opc_ua.rs:592 — i32 cast to u32 without validation, negative values wrap. Target: Story 3.2
- `create_device_client().await.unwrap()` at chirpstack.rs:1084 — panic on client creation failure. Target: Story 1.3
- `flush_queue: false` hardcoded at chirpstack.rs:1081 — no mechanism to flush stale commands. Target: Story 3.1
- `try_into().unwrap()` at opc_ua.rs:824 — panic on out-of-range command value. Target: Story 1.3
- `command_port as u32` lossy cast at opc_ua.rs:823 — i32 port cast without validation. Target: Story 3.2

## Deferred from: code review of story-1.2 (2026-04-02)

- Console layer hardcoded to DEBUG with no runtime override (e.g., RUST_LOG). Future enhancement.
- Log file path "log/" hardcoded — no config option. Future enhancement.
- No fallback if log directory missing — logs silently drop. Same as old behavior.

## Deferred from: code review of story-1.3 (2026-04-02)

- Mutex poison silently drops metric writes — full fix when storage migrates to separate SQLite connections (Epic 2). Target: Story 4.1
- set_metric() on missing device returns () with no error signal — signature change to Result when StorageBackend trait introduced. Target: Story 4.1

## Deferred from: code review of story-1.4 (2026-04-02)

- Cancellation not checked inside poll_metrics() retry loop — long gRPC retries can delay shutdown. Target: Story 4.4

## Deferred from: code review of story-2-1b-core-storage-data-types (2026-04-19)

- **Missing Serde derives on DateTime-containing structs** — MetricValue/DeviceCommand need #[derive(Serialize, Deserialize)] if serde feature is used. Moot if serde feature removed per spec constraint. Target: Story 2-1c or later storage implementation.

- **Timestamp overflow risk in metric queries** — Duration addition in chirpstack.rs lacks bounds checking. Monitor when implementing full storage layer. Target: Story 4.1 (Poller refactoring).

- **Implicit UTC timezone assumption** — Code assumes ChirpStack API guarantees UTC timezones. Add validation when implementing full storage integration. Target: Story 2-2 (SQLite schema).

- **Chrono serde version compatibility** — chrono 0.4.x versions may have different serde behavior. Pin version and add integration tests for serialization round-trips. Target: Story 2-3 (Metric persistence).

- **Precision loss on prost-types conversion** — prost-types::Timestamp (seconds+nanoseconds) vs chrono::DateTime<Utc> (100-nanosecond precision) may lose precision. Add tests for round-trip conversion. Target: Story 2-3 (Metric persistence).

- **Option<DateTime> deserialization null semantics** — ChirpstackStatus.last_poll_time may need custom deserializer to distinguish "never polled" from "explicitly null". Address in full storage implementation. Target: Story 2-4 (Graceful degradation).

## Deferred from: code review of story-2-2b-schema-creation-and-migration (2026-04-19)

- **No prepared statement caching** [sqlite.rs:372-376] — Performance optimization. Each `get_pending_commands()` call parses SQL string via `conn.prepare()`. Use `prepare_cached()` for repeated queries. Target: Story 2-3 (Performance optimization).

- **Three separate queries instead of one** [sqlite.rs:225-276] — `get_status()` executes three separate SELECT queries for server_available, last_poll_time, and error_count. Combine into single query with multi-column SELECT. Target: Story 2-3 (Performance optimization).

- **device_id/metric_name length not validated** [sqlite.rs] — No explicit length constraints. May require validation when StorageBackend interface is stabilized. Monitor for extremely long IDs causing database bloat. Target: Story 2-2c or later.

- **Empty payload allowed in queue_command** [sqlite.rs:318-331] — No explicit constraint against zero-byte LoRaWAN payloads. May be acceptable per spec. Clarify intent with LoRaWAN specialists. Target: Story 3.1 (Command execution).

- **Config path validation incomplete** [config.rs] — StorageConfig struct created with database_path field, but full validation logic in config.rs not visible in diff. Verify that path validation is performed at config load time, not just at database open time. Target: Story 2-2c or concurrent review.

## Deferred from: code review of Story 5-1 (2026-04-24)

- **Trait object vtable indirection cost vs <100ms latency goal** — Converting SqliteBackend to Arc<dyn StorageBackend> adds vtable overhead. This is pre-existing architecture choice (all subsystems use trait objects), not introduced by this story. Monitor in production to verify <100ms latency is maintained. If latency concerns arise, consider concrete Arc<SqliteBackend> for OPC UA reads.

- **Pool cloning efficiency (double wrapping in Arc)** — SqliteBackend::with_pool(pool.clone()) then wrapped in Arc::new(). Double wrapping may be inefficient. This is pre-existing pattern (not introduced by this story). Candidate for refactoring: evaluate whether single Arc::clone() suffices or if separate pool instances are needed per subsystem.


## Deferred from: code review of story-6.1 (2026-04-27)

- **Unbounded growth of `last_status` HashMap** [src/opc_ua.rs] — bounded by configured device×metric matrix today; revisit when Epic 9 introduces dynamic config reload (Blind+Edge).
- **AC#10 `cargo clippy --all-targets -- -D warnings` not clean project-wide** — 58 pre-existing dead-code/unused-import warnings on `main` HEAD. Open a separate cleanup story.
- **SQLITE_BUSY `warn!` not emitted at storage layer** [src/storage/sqlite.rs] — covered today by parent retry `warn!` in `chirpstack.rs::poll_metrics`. AC#6 wording demands storage-layer `warn!`.
- **`info_span!` + `let _enter = span.enter()` is fragile if function ever becomes async** [src/opc_ua.rs:get_value, get_health_value] — currently sync-only; rewrite to `span.in_scope(|| ...)` if any `.await` is added inside.
- **`batch_metrics.clone()` on every retry attempt** [src/chirpstack.rs:822-848] — pre-existing pattern; performance optimisation only.
- **AC#5 `errors` field name vs AC#7 canonical-list `error` (singular) inconsistency** — spec ambiguity; track in 6-2 spec review or open a docs issue.
- **`error_count == i32::MAX` only catches saturated state once before wrapping in release** [src/opc_ua.rs:get_health_value] — proper fix is `saturating_add` at the increment site, not a comparison change. Open follow-up issue scoped to gateway health-metric overflow handling.

## Deferred from: code review of story-6.2 (2026-04-27)

- **`prepare_log_dir` fallback returns `"./log"` even when `create_dir_all("./log")` fails** [src/main.rs:152-178] — pre-existing from Story 6-1. Both fallback branches use `let _ = std::fs::create_dir_all("./log")` and return `"./log"` regardless of success. Read-only FS, `./log` existing as a file, or permission denied would all leave the non-blocking writer to fail silently. Probe + nested fallback (e.g., `/tmp/opcgw-log`) needed.
- **Stale `#[allow(dead_code)]` on `LoggingConfig`** [src/config.rs:132] — struct now has runtime consumers via `peek_logging_config` and `AppConfig`. Blanket allow will mask future genuine dead fields. Remove or scope to specific fields.
- **Init-time stderr warnings (invalid env, invalid config, log-dir fallback) never reach log files** [src/main.rs:127-130, 137-141, 156-160, 172] — by design (tracing not yet initialised at that point). Fix is buffer-and-replay: collect warnings into a `Vec<String>` during the bootstrap phase, then replay them via `warn!` immediately after `.init()`. Deferred as a design follow-up; affects post-mortem from log files only.
- **No automated test asserts the format of the post-init `logging_init` info line** [src/main.rs:308-313] — `docs/logging.md` advertises this line as the operator-visible signal; only verified by smoke test in Dev Notes. Capturing requires a test-only `Layer` that records events; out of scope for 6-2.
- **Test gap: `[logging].level = "INFO"` (uppercase in TOML)** [src/config.rs] — implementation lowercases internally so it works, but the contract isn't pinned by a test. One-line addition to existing config tests.
- **Test gap: `[logging]` block with `dir` only and no `level`** [src/config.rs] — no test exercises the `LoggingConfig { dir: Some(_), level: None }` path. Common operator config; deserves coverage.

## Deferred from: code review of story-6-3-remote-diagnostics-for-known-failures (2026-04-27)

- **`error_delta` oscillation re-fires spike warn** [src/chirpstack.rs:996-1004] — no hysteresis or debounce; oscillating error counts (0→6→0→6) re-trigger the warn every odd cycle. Design enhancement, not a bug.
- **`last_status` cache grows unboundedly** [src/opc_ua.rs:1462,1631-1641] — no TTL/LRU; long-running gateways with rotating device IDs accumulate map entries. Bounded eviction enhancement (also flagged in 6-1 review under a similar formulation).
- **`gateway_status_init` is per-process not per-instance** [src/opc_ua.rs:1456-1459] — spec authorizes process-wide latching; document the limitation in `docs/logging.md` so operators understand test/restart semantics.
- **NaN/Inf boolean parse falls into "invalid boolean" branch** [src/chirpstack.rs:1175-1200] — message technically correct; cosmetic refinement only.
- **`peek_logging_config` swallows TOML parse errors silently** [src/main.rs:745-752] — 6-2 carryover; downstream `AppConfig::from_path` does surface the error.
- **`prepare_log_dir` falls back to `./log` even when `create_dir_all("./log")` itself fails** [src/main.rs:853-879] — 6-2 carryover (already in 6-2 deferred list above).
- ~~**`Channel::connect()` has no explicit timeout** [src/chirpstack.rs:317-365] — pre-existing infrastructure; out of 6-3's instrumentation-only scope. Story 4-4 territory.~~ **RESOLVED 2026-05-05 in Story 4-4:** `create_channel` now wraps `builder.connect()` with `tokio::time::timeout(Duration::from_secs(5), ...)`; `chirpstack_connect` warn emitted on timeout with `timeout_secs=5` field. Budget chosen to be smaller than NFR17's 30s SLA so a single channel rebuild doesn't blow the recovery budget, larger than the TCP probe's 1s timeout so transient slow-but-reachable servers don't get falsely flagged.
- **`chirpstack_outage` reads `last_successful_poll` after the cycle has potentially updated it** [src/chirpstack.rs:892-901,1010-1015] — cycle-local consistency drift; minor.
- **`log_dir` mismatch warning compares strings without canonicalisation** [src/main.rs:376-395] — 6-2 carryover; produces false-positive "restart to apply" when paths are equivalent but not byte-identical.
- **`parse_log_level` eprintln may echo ANSI escape sequences from env** [src/main.rs:782-792] — 6-2 carryover; terminal injection via crafted `OPCGW_LOG_LEVEL`.
- **`NonBlocking` guards drop ordering** [src/main.rs:248-310] — 6-1 carryover; tracing-appender contract requires guards live to end of `main`.
- **Span re-entrancy in `OpcUa::get_value` via `add_read_callback`** [src/opc_ua.rs:608-625] — pre-existing; no recursion in current code, but `let _enter = span.enter()` is fragile if `get_value` ever becomes async (already in 6-1 deferred list).
- **`ChronoUtc` non-monotonic across NTP step-backward** [src/main.rs:307-320] — system-level; document as known limitation. Augment with monotonic counter only if grep-by-timestamp ordering becomes a real operator pain point.
- **Other tonic codes (`Unauthenticated`, `ResourceExhausted`) not classified** [src/chirpstack.rs:1708-1755] — follow-up enhancement; not on 6-3's instrumentation path.
- **`rollback_err` not classified as SQLITE_BUSY** [src/storage/sqlite.rs:2882-2898] — cascading busy on rollback path silently swallowed; minor.
- **`STORAGE_QUERY_BUDGET_MS` excludes commit/rollback paths** [src/storage/sqlite.rs:62-93] — slow commits never surface as `exceeded_budget=true`. Wrap commit/rollback in `StorageOpLog` or compare elapsed against the budget there.
- **Far-future `metric.timestamp` clock-skew handling** [src/opc_ua.rs:893-983] — current code treats negative ages as fresh, hiding the anomaly. Rare but worth a `clock_skew_extreme` debug for diagnosability.
- **`extract_request_ids` cursor advance after closing quote** [src/opc_ua.rs:2003-2032] — works for current emit format; brittle if quoting changes.
- **`microsecond_timestamp_format_matches_pattern` test missing monotonicity assertion** [src/main.rs:1378-1413] — AC#2 is about ordering, not just digit count. Add a monotonic-pair assertion alongside the regex.

## Deferred from: implementation of story-7-1-credential-management-via-environment-variables (2026-04-28)

- **`tenant_id` redaction in `Debug` impl** [src/config.rs::ChirpstackPollerConfig] — out of scope per epic spec (Story 7-1, AC#3 matrix); only `api_token` and `user_password` are classified as secrets. Story 7-1 substitutes the all-zeros placeholder UUID in the shipped `config/config.toml` template (so the operator's tenant identity isn't published) but does not redact the value at log time. Tracked at GitHub issue #83.
- **tonic / tower-http metadata redaction strategy** [src/chirpstack.rs::AuthInterceptor] — Story 7-1 AC#5 audit found tonic 0.14.5 does not log request metadata and opcgw has no `tower-http` / `TraceLayer` wiring, so no `EnvFilter` mitigation is needed today. The proactive mitigation (a `tower::Layer` that strips the `authorization` header before logging, so future `TraceLayer` additions are safe-by-default) is deferred. Tracked at GitHub issue #82.
- **Operator migration shim `scripts/migrate-config-7-1.sh`** — considered for the `git pull` / merge-conflict path on operators' local `config/config.toml`. Documented manually in `docs/security.md` "Migration path" instead; helper is deferred unless adoption signals demand it.
- **`secrecy::SecretString` newtype for `api_token` / `user_password`** [src/config.rs] — Story 7-1 Out of Scope. The redacting `Debug` impl achieves ~95% of the protection at ~5% of the diff cost. Adopting `secrecy` would add `.expose_secret()` calls at every consumer in `src/chirpstack.rs` and `src/opc_ua.rs`; defer unless a follow-up story requires zeroize-on-drop guarantees.

## Deferred from: code review of story-7-1-credential-management-via-environment-variables (2026-04-28)

- **Manual `Debug` impls have no compile-time pin against future field-add omissions** [src/config.rs:259-308] — `ChirpstackPollerConfig::Debug` and `OpcUaConfig::Debug` enumerate every field by hand. A future contributor adding a new (potentially secret) field will not get a compile-time warning; they must remember to update the AC#3 matrix. Procedural-macro or serde-roundtrip-driven test would close the gap at the cost of a larger refactor. AC#3 matrix is the explicit contract for now.
- **`tenant_id` all-zeros placeholder UUID bypasses startup validation** [src/config.rs::AppConfig::validate, config/config.toml:55] — the shipped `tenant_id = "00000000-0000-0000-0000-000000000000"` is a valid UUID format and passes `validate()`, so an operator who forgets to override it boots the gateway and only sees the failure on the first gRPC call. A 3-line check rejecting the all-zeros UUID with an actionable message would close the gap; deferred per Story 7-1 AC#3 matrix and tracked at GitHub issue #83.
- **Validation-error prefix duplicated between code constant and docs** [src/utils.rs::PLACEHOLDER_PREFIX, docs/security.md] — code uses the centralised constant, but `docs/security.md` hardcodes the literal `"REPLACE_ME_WITH_"`. If the constant ever changes, docs go stale. Cosmetic doc-rot risk; not a code defect.

## Deferred from: Story 7-2 (OPC UA Security Endpoints and Authentication, 2026-04-28)

- **Multi-user OPC UA token model** — Story 7-2 keeps the single-user model already in place (`OPCUA_USER_TOKEN_ID = "default-user"`). The OPC UA spec supports multiple `ServerUserToken` records with independent credentials and permissions. Out of Scope per Story 7-2; tracked at GitHub issue #85. Pick up when role-based access control becomes a requirement.
- **Rate-limiting OPC UA failed auth attempts** — Story 7-2 emits `warn!` events for every failed authentication so operators can spot brute-force probing in logs, but does not throttle. Out of Scope per Story 7-2; tracked at GitHub issue #86. Pick up when network-layer protection (firewall / fail2ban) is judged insufficient.
- **mTLS / X.509 user-token authentication** — Story 7-2 enforces username/password (FR20) only. The `ServerUserToken { x509: None, thumbprint: None, … }` shape stays. Out of Scope; revisit when an SCADA client requires certificate-based auth.
- **CA-signed certificate workflow integration** — Story 7-2 documents manual `openssl req` setup; an automated CA-signing pipeline integration is deferred. Operators with a real CA hierarchy can drop CA-signed certs into `pki/own/` manually today.
- **First-class source-IP in OPC UA auth audit log** — async-opcua 0.17.1's `AuthManager` does not receive the peer's `SocketAddr`; NFR12 is satisfied via two-event correlation (accept event + auth-failed event). File an upstream feature request to extend `AuthManager` with peer-addr; revisit when async-opcua releases such a hook.
- **Replace `String` `user_password` with `secrecy::SecretString`** — Story 7-1 deferred this on the rationale that the redacting `Debug` impls already cover NFR7. Same rationale holds in Story 7-2. Revisit if a future story introduces a third field whose Debug exposure is harder to audit.

## Deferred from: code review of story-7-2-opc-ua-security-endpoints-and-authentication (2026-04-28)

- **Endpoint path logged unsanitised in `OpcgwAuthManager`** [`src/opc_ua_auth.rs:101,107,115`] — defensive coding for future-proof; today only registered endpoint names (`null`, `Basic256/Sign`, `Basic256/SignAndEncrypt`) reach this field, so log-injection via endpoint is impossible in practice. Apply `sanitise_user`-style escaping to `endpoint.path` if/when dynamic endpoint registration becomes possible.
- **`tracing_test::internal::global_buf()` is a private API** [`tests/opc_ua_security_endpoints.rs:48`] — pre-disclosed deviation in Dev Agent Record; required because `traced_test`'s scope filter is incompatible with async-opcua's spawned-task spans. Pin `tracing-test = "=0.2.6"` exact-version or write a custom subscriber-layer if a patch release breaks the build.
- **TOCTOU between `validate_private_key_permissions` (startup) and async-opcua's runtime read** [`src/security.rs`] — relies on `<pki_dir>/private/` being `0o700` (which `ensure_pki_directories` does enforce). Re-stat-and-verify immediately before key open would close the race; not justified for current LAN threat model.
- **NFC vs NFD username normalisation** [`src/opc_ua_auth.rs:103`] — usability concern; configured user `"café"` (NFC) rejects client `"café"` (NFD). ASCII-only username convention is the workaround today.
- **`pki_dir` symlink-followed silently** [`src/security.rs:135-214`] — niche shared-host attacker threat; `O_NOFOLLOW`-style defence is non-trivial in stable Rust. Revisit if multi-tenant deployment becomes a use case.
- **`set_mode` discards setuid/setgid/sticky bits** [`src/security.rs:88,168`] — the `mode & 0o777` mask preserves only the basic perm triplet. Operators using `g+s` for group inheritance on `pki/private/` lose it after `ensure_pki_directories`. Preserve high bits with `(actual_mode & 0o7000) | expected_mode` in a follow-up.
- **`pick_free_port` race window in integration tests** [`tests/opc_ua_security_endpoints.rs:101-106`] — TOCTOU between listener-drop and OPC UA bind. Spec already recommends `serial_test` if flakes appear; `setup_test_server` doesn't yet hit them on dev hardware.
- **`ServerUserToken` keeps a duplicate plaintext password alongside `OpcgwAuthManager`** [`src/opc_ua.rs:1547-1548`] — async-opcua's `ServerConfig::validate` requires the entry; the `pass` field is decorative since `OpcgwAuthManager` is the gatekeeper. Pass `None` if a future async-opcua release allows it. Memory footprint is one extra clone of an already-redacted secret — minor.
- **`TestServer::Drop` race with `TempDir`** [`tests/opc_ua_security_endpoints.rs:2462-2467`] — `handle.abort()` is non-blocking; async-opcua may still be writing when `TempDir::Drop` deletes the dir. Cosmetic stderr noise on test panic; cleanup is automatic on next CI tmpdir sweep.
- **AC#2 ships only `null`-endpoint wrong-password sub-test (deviation accepted)** [`tests/opc_ua_security_endpoints.rs:402`] — spec required three named sub-tests (`_null`, `_basic256_sign`, `_basic256_sign_encrypt`); only `_null` shipped. **Accepted as deferred:** the auth path in `OpcgwAuthManager` is endpoint-agnostic (the manager does not see channel security, only `endpoint`/`username`/`password`); Basic256 client-side PKI handshake from the test harness adds significant brittleness without adding auth-rejection coverage. The Basic256 endpoints are still pinned by AC#1's discovery-based shape test.
- **Trojan-source Unicode (RTL overrides, zero-width joiners) in usernames not sanitised** [`src/opc_ua_auth.rs:76-78`] — `escape_default()` only escapes ASCII control chars + backslash + quote. Code points like `U+202E` (RIGHT-TO-LEFT OVERRIDE), `U+200B` (zero-width space), `U+2066/2067` (bidi isolates) pass through and can deceive RTL-aware log viewers. **Accepted as deferred:** LAN threat model + plain-text log readers (grep, tail) make the practical impact low; introducing `unicode-security` crate dependency is over-engineering for current deployment. Revisit when log analysis tooling expands or any client traffic crosses an untrusted boundary.
- **`create_sample_keypair=true` regen produces world-readable file for one boot cycle** [`src/security.rs:84-90`] — when the configured key file is missing and `create_sample_keypair=true`, validation short-circuits `Ok(())` and async-opcua regenerates the keypair with default umask (typically `0o644`). The next-restart validation catches it, but the gateway runs once with a world-readable key. **Accepted as deferred** — the runtime fix (post-create chmod or re-validation hook) is non-trivial and the documented anti-pattern note in `docs/security.md` is the operator-facing mitigation. Production deployments must run with `create_sample_keypair=false`; the boot-once-world-readable window only affects development workflows.
- **HMAC key not zeroized on drop** [`src/opc_ua_auth.rs::OpcgwAuthManager::hmac_key`] — pass-3 review finding E1: the per-process HMAC keying material lingers in heap memory until the allocator reuses the page. A memory-dump attacker who captures both the key and the digests can precompute candidate credentials offline. Defence is to add `zeroize` crate and wrap `hmac_key` in `Zeroizing<[u8; 32]>` (~5 lines + new dep). LAN threat model and the difficulty of obtaining process-memory access make this strategically marginal; revisit if the gateway is deployed in untrusted multi-tenant environments.
- **`getrandom` version not exact-pinned** [`Cargo.toml::getrandom = "0.2"`] — pass-3 review finding E4: the `^0.2` range allows minor version drift. `Cargo.lock` provides reproducibility today; if a future patch release introduces a regression on Linux x86-64 (the gateway's target), the lockfile keeps the build stable until intentional update. Pinning would be `getrandom = "=0.2.x"` once a known-good version is chosen.

## Deferred from: Story 7-3 (OPC UA Connection Limiting, 2026-04-29)

- **Per-source-IP rate limiting / token-bucket throttling** — Story 7-3 ships a flat global cap (`max_sessions`) per FR44. A single misbehaving SCADA client can saturate the cap on its own and starve other operators; a distributed flood is also unaddressed. Out of Scope per Story 7-3; tracked at GitHub issue #88. Pick up alongside the Story 7-2 follow-up (#86, "rate-limiting failed auth attempts") since both share the per-IP state machine.
- **Surface async-opcua subscription / message-size limits as config knobs** — `max_subscriptions_per_session`, `max_monitored_items_per_subscription`, `max_message_size`, `max_chunk_count`, etc. all default to async-opcua's library defaults. Out of Scope per Story 7-3; tracked at GitHub issue #89. Pick up when Epic 8 (subscriptions) lands.
- **Hot-reload of session cap** — `max_connections` is read at startup only; async-opcua's `Limits::max_sessions` is fixed for the server's lifetime via `ServerBuilder`. Out of Scope per Story 7-3; tracked at GitHub issue #90. Pick up alongside Epic 9 story 9-7 (configuration hot-reload). May also require an upstream feature request to async-opcua for runtime-mutable `Limits`.
- **First-class session-rejected event in async-opcua** — `SessionManager::create_session` rejects (N+1)th sessions with `Err(BadTooManySessions)` but does not log. Story 7-3 works around this via a tracing-Layer correlated against the library's pre-existing `Accept new connection from {addr}` event (NFR12-style two-event pattern). File an upstream feature request to extend `SessionManager` with a rejection callback or log emission; revisit when async-opcua ships such a hook. Same shape as the Story 7-2 deferred entry "First-class source-IP in OPC UA auth audit log".

## Deferred from: code review of 7-3-connection-limiting (2026-04-29)

- **`MessageVisitor` brittle to async-opcua emission style** [src/opc_ua_session_monitor.rs:142-153] — Visitor matches `field.name() == "message"` and uses `record_debug` to format. If a future async-opcua release adds a structured `message = "..."` field on the same target, or changes the format-string emission to use `record_str` with quote-wrapping, the at-limit warn silently breaks (the `starts_with("Accept new connection from ")` guard fails). Works today; revisit on async-opcua upgrade.
- **At-limit warn rate-limiting under TCP-accept floods** [src/opc_ua_session_monitor.rs:182-196] — When the cap is hit, every TCP accept produces one warn line, including port scans, healthchecks, and partial-handshake probes that never request a session. Spec already documents this trade-off (`docs/security.md` "Expected at-limit log noise"), but no in-process throttle / sampling exists. Subsumed by per-source-IP follow-up at GitHub issue #88; revisit when implementing per-IP token bucket.
- **`OPCUA_SESSION_GAUGE_INTERVAL_SECS` not tunable via env or config** [src/utils.rs] — Hard-coded compile-time constant. Operators wanting a different gauge cadence (e.g., 1s for short bursts, 60s for low-noise production) must rebuild. Spec did not require tunability. File a follow-up issue when the first operator asks; otherwise live with the compile-time default.
- **At-limit warn message wording: "will be rejected" misleading for partial-handshake peers** [src/opc_ua_session_monitor.rs:182-196] — When `current == limit`, the warn fires on the (N+1)th TCP accept BEFORE async-opcua's `CreateSession` rejection. If the peer disconnects mid-handshake (port scan, half-open connection), it never gets rejected — the warn's "will be rejected" phrasing is inaccurate for that case. Cosmetic; revisit when refining log copy or migrating to a true rejection callback (see "First-class session-rejected event in async-opcua" above).
- **Test count claim of 574 plausible but not directly re-verified during code review** — Completion Notes line 566 reports 574 pass / 0 fail / 7 ignored after the implementation run; reviewer counted the 12 net-new tests individually but did not re-execute the suite. Re-run `cargo test --lib --bins --tests` as part of the pre-commit verification before flipping the story to `done`.
- **Spec body still asserts `source_ip="127.0.0.1` (quoted) while integration tests use unquoted form** [_bmad-output/implementation-artifacts/7-3-connection-limiting.md:262] — Debug Log References (line 559) acknowledges the spec was wrong and the actual format is `source_ip=127.0.0.1` (no quote, since `tracing` formats `%val` via Display). Tests assert the corrected form. Spec body itself was not updated. Pure spec-hygiene, no code change required.

## Deferred from: Story 8-1 (async-opcua Subscription Spike, 2026-04-30)

- **Manual FUXA + Ignition / UaExpert verification (AC#3) deferred to post-Epic-9 single integration pass.** User decision 2026-04-30: rather than running the manual SCADA verification three times (once for 8-1 spike, once for 8-2 production subscriptions, once for Epic 9 web UI), batch into a single integration-validation pass after Epic 9 lands. Rationale: each manual verification consumes ~30–60 minutes of operator time configuring tags/screenshots; deferring until both Phase B epics ship gives a single end-to-end SCADA test (subscription + historical-read + alarms + web-UI hot-reload all together) instead of three partial validations. Risk-mitigation: Story 8-1 expanded the automated test surface to 9 integration tests (`tests/opcua_subscription_spike.rs`) covering Plan-A, multi-client, multi-monitored-item, sampling-interval revision, datavalue payload, teardown idempotency, sibling-session isolation, plus the Story 7-2/7-3 invariant pins — see spike report § 8 for the test inventory. **Acceptance:** explicitly accepted as deferred per CLAUDE.md "Code Review & Story Validation Loop Discipline" rule #3. **Tracked at GitHub issue #93** — pick up at the start of Epic 9 retrospective preparation, or before any production deployment cutover, whichever comes first.
- **Upstream feature request: session-rejected callback in async-opcua (AC#7).** Re-confirmed 2026-04-30 that async-opcua 0.17.1 still has no callback / event-listener / hook on `SessionManager::create_session`'s rejection path (the library's own `// TODO: Auditing and diagnostics` comment at `~/.cargo/registry/src/.../async-opcua-server-0.17.1/src/session/manager.rs:74` is the strongest possible upstream-FR hook). Story 7-3's `AtLimitAcceptLayer` workaround stays for Story 8-2. Suggested issue body and full re-confirmation evidence in `_bmad-output/implementation-artifacts/8-1-spike-report.md` § 9. **Action item carry-forward:** file the FR at `https://github.com/freeopcua/async-opcua/issues` and update the existing "First-class session-rejected event in async-opcua" entry above (Story 7-2 deferred block, line ~119) in place — replace "File an upstream feature request to extend `SessionManager` with a rejection callback or log emission; revisit when async-opcua ships such a hook." with "Filed upstream FR: <URL>". **Tracked at GitHub issue #94** — operator-action; not blocking Story 8-2 entry.
- **`--load-probe` 5-minute throughput run (AC#8) carry-forward.** The reference binary at `examples/opcua_subscription_spike.rs` implements the AC#8 throughput probe end-to-end (100 monitored items × 1 Hz × 5 min, JSON-on-stderr summary with p50/p95/p99 inter-notification interval, drop count, gauge-cadence usefulness verdict). Execution requires a running gateway and 5 minutes of wall clock; not run during the dev-story automation. The pull-vs-push recommendation in spike report § 6 is provisional pending these numbers; if p99 inter-notification interval > 3000 ms or drop rate > 5%, Story 8-2 must re-evaluate the push-model integration. Spike report § 7 has the operator's recipe + the empty results table. **Tracked at GitHub issue #95** — operator-action; not blocking Story 8-2 entry.


## Deferred from: Story 8-2 (OPC UA Subscription Support, 2026-04-30)

- **`OPCUA_SESSION_GAUGE_INTERVAL_SECS` gauge tunability (AC#7 conditional path, default — load-probe data absent).** Story 8-2 AC#7 prescribed a binary decision: promote the constant to a `[diagnostics]` config knob if the operator's `--load-probe` 5-minute run (issue #95) flagged the gauge as noise-buried (≥ 50 unrelated `info!` lines per gauge tick under representative subscription load), otherwise leave the constant hard-coded. As of 2026-04-30 the load-probe has NOT been run — issue #95 carries it as operator-action, deferred so 8-2 ships without blocking. **Default path taken:** the constant stays at `OPCUA_SESSION_GAUGE_INTERVAL_SECS = 5` in `src/utils.rs`. Revisit if a real subscription-flood operator scenario surfaces noise-buried gauge ticks; the promotion-path plumbing pattern is identical to the four mandatory knobs (~80 LOC additive change) and lives in the AC#7 spec block.
- **Five "advanced" subscription knobs surfaced by spike report § 5.** `max_pending_publish_requests`, `max_publish_requests_per_subscription`, `min_sampling_interval_ms`, `max_keep_alive_count`, `max_queued_notifications` — all reachable via `ServerBuilder::limits_mut().subscriptions.<field>` with the same plumbing pattern as Story 8-2's four mandatory knobs. **Deferred:** the four mandatory knobs cover the load-bearing flood / DoS / sizing risks; the advanced five are tuning surface for back-pressure scenarios that haven't materialised. Trigger to revisit: the operator's `--load-probe` numbers (issue #95) reveal back-pressure or queue-drop behaviour, OR a real SCADA deployment surfaces a configuration scenario the mandatory knobs can't shape. Most likely first candidate: `max_queued_notifications` if drops > 0 in the load probe.
- **Per-source-IP subscription-flood throttling.** Phase A carry-forward; Story 8-2 ships flat global subscription limits via `max_subscriptions_per_session` × `max_connections`. Per-IP throttling is a separate Phase B story (or upstream async-opcua FR) opened only if subscription-flood becomes a near-term operator concern. Tracked at GitHub issue [#88](https://github.com/guycorbaz/opcgw/issues/88). Same shape as the Story 7-3 deferred entry of identical name.
- **Upstream FR: rejection-time audit events for `BadTooManySubscriptions` / `BadTooManyMonitoredItems`.** async-opcua 0.17.1's `SubscriptionService::create_subscription` and `MonitoredItemService::create_monitored_items` return their respective Bad-status codes silently — no log emission, no callback. The contract is the OPC UA status code on the wire (pinned by Story 8-2 AC#3.1 / AC#3.2 tests), but operators get no audit trail of the rejection. Same shape as the Story 7-2 / 7-3 deferred entries pointing at upstream auth-rejection / session-rejected callback gaps; file alongside issue #94 once the operator has time. **Tracked at GitHub issue [#94](https://github.com/guycorbaz/opcgw/issues/94)** (combine with the existing session-rejected FR — both are the same shape of upstream gap).
- **`DataChangeFilter`-less subscription clients silently freeze on outage (AC#3.3 finding).** Story 8-2 AC#3.3 surfaced a load-bearing failure mode: async-opcua 0.17.1's `MonitoredItem::notify_data_value` (`subscriptions/monitored_item.rs:514-517`) dedupes on `value.value` only when `FilterType::None` (no `DataChangeFilter` supplied). Status-only transitions are silently suppressed — a non-compliant SCADA client that omits the filter would freeze on the last-good value during a ChirpStack outage. **Mitigation today:** compliant SCADA clients (FUXA, Ignition, UaExpert) supply a `DataChangeFilter` with the OPC UA Part 4 §7.17.2 default `trigger: StatusValue`, which routes through `is_changed()` and detects status changes — pinned by AC#3.3 test. **Documentation:** `docs/security.md#subscription-and-message-size-limits` calls out the contract explicitly. **Future work:** consider filing an upstream FR for async-opcua to use `DataChangeTrigger::StatusValue` as the implicit default when no filter is supplied; track alongside issues #94 / #88 if the operator chooses to file. No automated test pins the unfiltered-Plan-A path because it is intentionally documented as the contract for compliant clients.

## Deferred from: code review of 8-2-opc-ua-subscription-support (2026-04-30)

- **`tests/opcua_subscription_spike.rs:~1011` `clear_captured_buffer` swallows mutex `Err`** — pre-existing 8-1 spike-test infrastructure: a poisoned mutex (test panic) leaves the captured-log buffer non-empty, so subsequent assertions read stale data. Defer to a future spike-test productionisation pass, alongside the other 8-1 baseline test-infrastructure findings below.
- **`tests/opcua_subscription_spike.rs:~987-1008` `set_global_default` failure not surfaced** — pre-existing 8-1 spike-test infrastructure: subscriber-install failure passes captured-log assertions spuriously (auth/at-limit tests would pass even if no events were ever recorded). Defer; same productionisation pass.
- **`tests/opcua_subscription_spike.rs:~1244-1289` `open_session_held` returns `None` for ANY failure** — pre-existing 8-1 helper conflates auth/network/cap failures behind a single `Option<HeldSession>`. Auth-rejection regression tests pass even when the failure is actually transport/build/timeout. Defer; productionisation pass.
- **`tests/opcua_subscription_spike.rs:~1432-1467` 300ms sleep insufficient on loaded CI for tracing flush** — pre-existing 8-1 timing assumption. Auth-failed event present in buffer but assertion runs before flush completes on slow runners. Defer; productionisation pass (replace with bounded-retry poll loop).
- **`tests/opcua_subscription_spike.rs:~1232` `HeldSession::Drop` calls `event_handle.abort()` without await** — pre-existing 8-1 RAII helper. The aborted tokio task may continue briefly holding session state and bleed into the next `serial_test`. Defer; productionisation pass.
- **`tests/opcua_subscription_spike.rs::test_subscription_double_delete_is_safe` doesn't actually exercise server idempotency** — 8-1 baseline test, purely client-side state check (the second `delete_subscription` is a client-side error before the call reaches the server). Defer; productionisation pass.
- **`OpcUaConfig` `Debug` exhaustiveness not pinned by NFR7 regression test** — Story 7-1's NFR7 redaction matrix breaks silently if a future field is added without an `impl Debug` entry. `src/config.rs:~287-309`. Carry-forward NFR7 debt, not 8-2 scope. Defer alongside the other NFR7 follow-ups; pick up in a Phase B observability epic.
- **`OPCUA_DEFAULT_MAX_MESSAGE_SIZE = 65_535 * 5` is a hardcoded literal** — `src/utils.rs:~838`. The doc comment cites `opcua_types::constants::MAX_MESSAGE_SIZE` but the constant is computed locally rather than re-exported. A library bump that changes the formula silently diverges. Small follow-up: add `#[cfg(test)] static_assertions::const_assert_eq!(OPCUA_DEFAULT_MAX_MESSAGE_SIZE, async_opcua_types::constants::MAX_MESSAGE_SIZE);` (and the equivalent for `MAX_CHUNK_COUNT`) to fail the test suite if upstream drifts. Not blocking 8-2 ship; opportunistic fix during the next async-opcua bump.

## Deferred from: code review of 8-2-opc-ua-subscription-support iteration 2 (2026-04-30)

- **`src/storage/sqlite::tests::test_concurrent_write_read_isolation` flakes under heavy parallel test load** — writer/reader threads race on the same SQLite DB; the assertion `found > 0` requires the reader to see at least one written metric before the writer finishes. Under heavy parallel load (full `cargo test --lib --bins --tests` running multiple SQLite-touching integration tests concurrently), the reader can return `Err(SQLITE_BUSY)` for all 50 reads and `found == 0` triggers the assertion. Passes deterministically in isolation (`cargo test --lib storage::sqlite::tests::test_concurrent_write_read_isolation`). NOT caused by Story 8-2 iteration-2 patches — pre-existing race assumption. Fix path: make the test wait for at least one write to complete before starting reads, OR mark `#[serial_test::serial]` to remove the parallel-load contention. Defer to a future storage-test productionisation pass.
- **`dv.status: None` accepted as Good in AC#3.3 recovery branch** — `tests/opcua_subscription_spike.rs::test_subscription_survives_chirpstack_outage_with_stale_status` recovery loop currently accepts `dv.status: None` as a valid Good signal alongside the strict value-match check (`value_matches && status_ok`). The auditor flagged this as a LOW: a regression that strips status entirely while still delivering value 84.0 would pass. The pairing with strict value-match defends against this in practice (a stripped-status notification with the wrong value would still be rejected), so the iter-2 patch round did NOT tighten further to require `Some(StatusCode::GOOD)` explicitly — defensible design choice given the OPC UA encoding rule that Good may be omitted. Revisit if a real regression surfaces.

## Deferred from: Story 8-3 (Historical Data Access via OPC UA, 2026-04-30)

- **`HistoryReadProcessed` aggregation service (min / max / avg / sum, rate-of-change, interpolation)** — opcgw leaves async-opcua's default `BadHistoryOperationUnsupported` handler. SCADA clients that need aggregates compute them client-side from the raw rows the new `query_metric_history` returns. Tracked at GitHub issue **[#98](https://github.com/guycorbaz/opcgw/issues/98)** (open at story start; collects the three out-of-scope items below). Re-prioritise if multiple operators surface aggregate-dashboard requirements.
- **`HistoryReadAtTime` interpolated-at-timestamp service** — niche; same `BadHistoryOperationUnsupported` default. Tracked at issue **[#98](https://github.com/guycorbaz/opcgw/issues/98)** alongside `HistoryReadProcessed`.
- **OPC UA `ByteString` continuation-point round-tripping** (Part 11 §6.4.4) — Story 8-3's HistoryRead handler does not call `set_next_continuation_point`. Truncated responses surface as `data_values.len() == max_history_data_results_per_node` with `Good` status; SCADA clients page manually via `start = last_returned_row.timestamp + 1µs`. The manual-paging recipe is documented in `docs/security.md#historical-data-access`. Continuation points add server-side cursor lifecycle (memory cost, expiry handling) or self-encoding payload bloat; the manual-paging contract avoids both. Re-evaluate if a SCADA vendor surfaces a "needs single-call 7-day query without manual loop" requirement; tracked at issue **[#98](https://github.com/guycorbaz/opcgw/issues/98)**.
- **Per-metric retention overrides** — today the `retention_config` SQLite table is keyed by `data_type` (`metric_values`, `metric_history`), not by `(device_id, metric_name)`. A future requirement of "moisture metrics keep 30 days, temperature keeps 7 days" would need a schema migration + an operator UI. Out of scope for Story 8-3 since FR22 only mandates a global 7-day floor. Tracked at issue **[#98](https://github.com/guycorbaz/opcgw/issues/98)** if operators surface interest.
- **Dynamic retention reload at runtime** — `[storage].retention_days` is read once at startup and written to `retention_config` via `INSERT OR REPLACE`. Changing the value requires a gateway restart. Phase B Epic 9's hot-reload pipeline covers runtime reconfiguration; tracked at the same issue **[#98](https://github.com/guycorbaz/opcgw/issues/98)**.
- **NFR15 release-build benchmark not yet run on CI** — `tests/opcua_history_bench.rs::bench_history_read_7_day_full_retention` is `#[ignore]` by default and the seed phase takes ~30s, so the test isn't included in default `cargo test`. Latency contract (<2s for a 600k-row 7-day query) is documented but not pinned by automation. Run command: `cargo test --release --test opcua_history_bench -- --ignored bench_history_read_7_day_full_retention`. Add to a Phase B perf-CI lane when the lane exists.
- **AC#7 strict reading: test fixture in `src/opc_ua_auth.rs::tests` got an unavoidable `max_history_data_results_per_node: None` line** — the new `OpcUaConfig` field forced a single-line addition to the test fixture inside the `mod tests {}` block of `src/opc_ua_auth.rs`. Production code in that file is untouched. The strict letter of AC#7 ("git diff src/opc_ua_auth.rs is empty") is not met but the spirit is — the production auth path has zero LOC of change. Document in completion notes; don't re-open AC#7.
- **Test-harness extraction to `tests/common/mod.rs`** — `tests/opcua_history.rs` is the 4th integration-test file with significant overlap (TestServer / setup / pick_free_port / build_client) with `opcua_subscription_spike.rs` / `opc_ua_connection_limit.rs` / `opc_ua_security_endpoints.rs`. The Story 8-3 spec flagged this as the threshold trigger but allowed deferral if invasive — the four files diverge in subtle ways (different test users, different metric shapes, different `init_test_subscriber` requirements per spike-tests' tracing-test integration). Defer to a separate cleanup story.

## Known Failures (Epic 8 carry-forward, 2026-05-01)

- **Story 8-4 (Threshold-Based Alarm Conditions, FR23) — KF, descoped from Epic 8 on 2026-05-14.** Phase B feature gap classified as a Known Failure following the project's Story 6-3 (remote diagnostics for known failures) convention. **Descope decision (2026-05-14):** the Epic 8 retro (2026-05-01) originally kept `8-4-threshold-based-alarm-conditions: backlog` inside Epic 8's scope while flagging the work as KF, which left `epic-8: in-progress` dangling for one parked story. On 2026-05-14 the user explicitly descoped 8-4 from Epic 8 so the epic could close cleanly; the KF tracking lives here. Any future revival is filed under a new story name (NOT `8-4`) in a future Phase B epic — the original spec was scoped for the wrong epic and should be redrafted with the Stories 8-2 / 8-3 lessons baked in. **What's missing:** the gateway does NOT propagate `Bad` / `Warning` status codes when a metric value crosses a configured `low_alarm` / `high_alarm` threshold. SCADA clients see `Good` status for all metric reads regardless of threshold proximity. **What still works:** Story 5-2's stale-data status codes (`Uncertain` after 1× poll interval, `Bad` after 3× poll interval) function unchanged; Story 8-2's `DataChangeFilter`-driven subscriptions deliver these status transitions correctly. **Operator workaround:** define alarm thresholds in the SCADA application (FUXA / Ignition) rather than in opcgw — the operationally sound choice anyway, since alarm-condition state belongs in the SCADA client which has full context of acknowledged / suppressed / shelved state. **Rationale for deferral + descope:** Phase B's load-bearing functionality is real-time data (FR21 — done) + historical access (FR22 — done) + web configuration (Epic 9 — done). Threshold alarms are a value-add tier that compositions cleanly into the SCADA-client tooling. Production-deployment critical path is now Epic A (Storage Payload Migration, issue #108) — threshold alarms would need real metric values to alarm on anyway, so 8-4 is functionally blocked by #108 until Epic A lands. **Trigger to revisit:** an operator surfaces a deployment scenario where threshold-driven status-code propagation cannot be moved to the SCADA client (legacy SCADA without local alarm support, or compliance requirement for server-side alarm provenance). At that point, open a defined story in Phase B backlog that's NOT named "8-4" — the original spec was scoped for the wrong epic phase and should be redrafted using the Stories 8-2 / 8-3 lessons (specifically: the `DataChangeFilter` `trigger: StatusValue` path is the correct integration point), and the revival depends on Epic A having shipped. **Documentation surface:** `epic-8-retro-2026-05-01.md` § Known Failures has the canonical narrative; the 2026-05-14 descope addendum lives at the bottom of that file.

## Deferred from: code review of 8-3-historical-data-access-via-opc-ua (2026-04-30)

- **NodeId collision when two devices share `metric_name`** — `src/opc_ua.rs:~834-870`: `node_to_metric.insert(...)` (and likely `add_variables(...)`) silently overwrites when device A and device B both have a metric named "Moisture", since NodeId is constructed from the metric_name only. HistoryRead would silently return data from one device under a NodeId conflating two. Pre-existing latent bug (not introduced by Story 8-3) but Story 8-3 surfaces it. Open as separate GitHub issue.
- **UPSERT stale-`id` race** — `src/storage/sqlite.rs:~2113-2138`: `INSERT OR REPLACE INTO retention_config (id, ...) VALUES ((SELECT id FROM retention_config WHERE data_type='metric_history'), ...)` returns NULL `id` if the migration default row was removed. Pre-existing schema invariant assumes seeded; defer.
- **`retention_config` `metric_values` row not synced with `metric_history`** — pre-existing schema design: only `metric_history` is operator-configurable today. The two retention rows can drift; defer until per-metric retention work (issue #98).
- **`NaN` skip applies to `Float` only — `Int`/`Bool`/`String` rows with garbage pass storage layer** — `src/storage/sqlite.rs:~1409-1421`. Defense-in-depth at `build_data_values` skips them during conversion, so the user-facing contract holds. Pre-existing two-stage filter design; defer.
- **Pool-checkout 5-second timeout under sustained HistoryRead concurrency** — `src/storage/sqlite.rs:~1378-1389`: the pool exhaustion concern is pre-existing storage-layer work. Linked to the patch that fixes the BadHistoryOperationInvalid → BadInternalError mapping (which improves observability into this case).
- **Pre-1970 `SystemTime` arithmetic edge case** — `src/opc_ua_history.rs:~1505-1508`: negative `SystemTime` on non-Linux platforms is theoretical for opcgw's Linux-only deployment. Defer.
- **Pre-1601 OPC UA `DateTime` round-trip** — `src/opc_ua_history.rs:~1599-1635`: ChirpStack does not produce pre-1601 timestamps. Defer.
- **`OpcgwHistoryNodeManagerImpl` only forwards 10 methods — async-opcua trait surface not enumerated** — `src/opc_ua_history.rs:~1408-1488`: which methods `SimpleNodeManagerImpl` overrides requires a trait-by-trait audit of async-opcua 0.17.1. The wrapper currently passes all integration tests. Open follow-up to verify against upstream source.
- **`owns_node` not overridden on `OpcgwHistoryNodeManagerImpl`** — `src/opc_ua_history.rs:~1408`: relies on outer `InMemoryNodeManager` wrapper to dispatch correctly. Current architecture works (verified by passing tests). Defensive concern only.
- **`trace!` emits attacker-controlled NodeId field — log-injection class** — `src/opc_ua_history.rs:~1539-1546`: authentication is gated upstream; pre-existing log-injection class throughout the codebase. Defer to a project-wide log-hardening pass.
- **`Variant::String` round-trip allocates clone for every row — NFR15 budget concern** — `src/opc_ua_history.rs:~1622`: optimization. Re-evaluate after the actual NFR15 measurement runs in CI (depends on the deferred benchmark CI lane).
- **Test-harness boilerplate (`details_eo.inner_as::<...>` round-trip, `setup_test_server` duplication)** — `tests/opcua_history.rs`: tracked under the pre-existing `tests/common/mod.rs` extraction story already listed in this file under "Deferred from: Story 8-3".

## Deferred from: Story 9-1 implementation (2026-05-02)

- **User-manual chapter for the embedded web UI** — `docs/manual/opcgw-user-manual.xml` v2.0 covers Epics 2-6 only. Story 9-1's `[web]` config block + Basic auth surface should land a manual chapter, but per the Epic 7/8 retrospective pattern the manual update can be batched. The Story 9-1 changes are documented in `docs/security.md` § "Web UI authentication" and `README.md`; the manual sync is deferred to a future docs pass that batches Epic 7+8+9 manual updates together. Trigger: when the manual is next regenerated (or when an operator opens an issue noting the manual is out of sync with shipped behaviour).
- **Web TLS / HTTPS hardening** — Story 9-1 ships HTTP-only with reverse-proxy TLS termination as the documented stance. Tracked at GitHub issue [#104](https://github.com/guycorbaz/opcgw/issues/104). Trigger: an operator surfaces a deployment scenario where reverse-proxy TLS isn't viable (typically: edge deployments without an upstream proxy, or a compliance requirement for end-to-end TLS at the gateway boundary).
- **Per-IP rate limiting for the web auth surface** — issue [#88](https://github.com/guycorbaz/opcgw/issues/88) was a Phase A carry-forward (per-source-IP token-bucket throttling) that becomes structurally relevant once the web auth surface is exposed (HTTP rate is orders of magnitude faster than OPC UA session creation; the global session cap from Story 7-3 doesn't apply to web requests). Story 9-1 documents the deferral in its spec § "Out of Scope". Trigger: web-auth-flood becomes a near-term operator concern.
- **CSRF protection for Stories 9-4 / 9-5 / 9-6 mutating routes** — Story 9-1 ships only `GET` routes (no CSRF surface). Stories 9-4 (application CRUD), 9-5 (device + metric CRUD), 9-6 (command CRUD) will introduce `POST` / `PUT` / `DELETE` routes that need CSRF protection — either strict same-origin policy enforcement via CORS, or a double-submit cookie / synchronizer-token pattern. The decision belongs in those stories' specs; Story 9-1 documents the requirement in `docs/security.md` § "Anti-patterns" so the next-story author doesn't miss it.

## Deferred from: code review of 9-1-axum-web-server-and-basic-authentication (2026-05-02)

- **Drop-zeroize HMAC keys + digests** [`src/web/auth.rs::WebAuthState`, `src/opc_ua_auth.rs::OpcgwAuthManager`] — Story 9-1's `WebAuthState` (and pre-existing Story 7-2's `OpcgwAuthManager`) hold the per-process HMAC key and the credential digests in plaintext heap memory for the entire process lifetime. A heap dump, swap file, or core dump exposes them. Deferred from Story 9-1 review iter-1 (Blind Hunter [MED] finding) because AC#7 forbids modifying `src/opc_ua_auth.rs` beyond the allowed extraction — a Drop-zeroize impl on `WebAuthState` alone would be asymmetric. Track as a project-wide hardening pass that adds the `zeroize` crate as a direct dep and impls `Drop` consistently across both auth surfaces.
- **Per-request access log on the web surface** [`src/web/mod.rs`] — Story 9-1 logs auth failures (`event="web_auth_failed"`) and lifecycle events (`event="web_server_started"` + plain shutdown info), but not successful per-request accesses. Operators investigating "did user X load page Y at time Z" cannot answer the question from logs alone. Deferred from Story 9-1 review iter-1 (Blind Hunter [MED] finding) because the spec doesn't require it and successful auth is implicit (no audit-event on success). Add `tower-http::trace::TraceLayer` if a forensic-review request surfaces.

## Deferred from: Story 9-0 (2026-05-05)

- **Stale read-callback closure leak when a node is deleted** [leak surface: async-opcua `SimpleNodeManagerImpl` registry; mitigation surface: `src/opc_ua_history.rs` if extending `OpcgwHistoryNodeManager`] — Verified via source enumeration: `SimpleNodeManagerImpl` does not expose a `remove_read_callback` API. Deleting a node via `address_space.delete()` leaves the closure registered in the read-callback registry, holding clones of any captured state (`Arc<dyn StorageBackend>`, `StatusCache`, `device_id`, `metric_name`, `threshold` — ~120 bytes per closure). Operationally negligible at expected churn rates (~150 leaks over 30 days = ~18 KB) but strictly a leak. Originally claimed-to-be-tracked in `9-0-spike-report.md` § 8 but the entry was never added to this file (surfaced by Acceptance Auditor in code review iter-1, 2026-05-05). Story 9-8 mitigation options: (a) extend `OpcgwHistoryNodeManager` with a `remove_read_callback` wrap method (the wrap pattern from Story 8-3); (b) file an upstream FR to async-opcua to expose `SimpleNodeManagerImpl::remove_read_callback` (precedent: Story 8-1 issue #94 session-rejected callback FR); (c) periodic OPC UA server task restart as a "GC pause" if leak rate becomes operationally significant.

## Deferred from: code review of 9-0-async-opcua-runtime-address-space-mutation-spike (2026-05-05)

- **`RunHandles` lacks a `Drop` impl that fires `cancel_token.cancel()` + `gauge_handle.abort()`** [`src/opc_ua.rs:91-116`] — Tracked at [GitHub issue #110](https://github.com/guycorbaz/opcgw/issues/110) (Known Failure). Originally raised as patch P1 during code-review iter-1 but **the patch attempt failed with rustc E0509**: adding `impl Drop for RunHandles` forbids destructuring in `run_handles` because `server: Server` and `gauge_handle: JoinHandle<()>` are consumed by value (`server.run().await` and `gauge_handle.await` both take ownership). The existing destructuring `let RunHandles { server, gauge_handle, ... } = handles;` is incompatible with a Drop impl on the struct. Workarounds (`Option<T>` + `.take()`, `ManuallyDrop` with `unsafe`, guard-wrapper struct) are substantially more complex than the original one-line Drop sketch and risk introducing new bugs. The original author's doc-comment explicitly cited "cross-field Drop-order surprises" as the reason for omitting Drop — they were aware of E0509. **Mitigation today:** every current consumer has a separate cancel-fire path (the spike test's `DynTestServer::drop` fires the same token; `main.rs`'s shutdown handler will fire the gateway-wide token in 9-7/9-8 production), so the gauge task always observes cancel and exits naturally. The doc-comment on `RunHandles` was updated 2026-05-05 to spell out the consumer contract explicitly. Story 9-7 should revisit when designing the hot-reload listener's lifecycle — possible refactors: (a) wrap consumed fields in `Option<T>` so `Drop` can `.take()` them safely; (b) use `ManuallyDrop` with paired `unsafe` cleanup in `run_handles`; (c) split `RunHandles` into a `RunHandlesInner` (moveable fields) plus a `CleanupGuard` (with Drop) wrapper struct.
- **`MonitorStateGuard` global cleanup race** [`src/opc_ua.rs` RunHandles `state_guard` field] — Spike test creates a new caller of the Story 7-3 global session-monitor `OnceLock` pattern. When `RunHandles` drops, `state_guard` clears the global. With multi-binary `cargo test` runs (which serial_test does NOT serialize across binaries), or any code holding a `ServerHandle` clone obtained from that global, clearing produces cross-test contamination. Deferred because this is a pre-existing pattern from Story 7-3, not a 9-0 regression. Re-evaluate in Story 9-7 if hot-reload exposes the contention.
- **Weak `subscription_id != 0` assert in spike** [`tests/opcua_dynamic_address_space_spike.rs` `subscribe_one`] — async-opcua's CreateSubscription assigns nonzero subscription IDs by design; the assertion is tautological. Spike claims "regression-pin tests for 9-7/9-8" but weak pins undermine that. Cosmetic; replace with cross-call check (same ID in subsequent publish responses) when spike file is consumed by 9-7/9-8.
- **`add_read_callback` closure panic poisons SimpleNodeManagerImpl RwLock** [`tests/opcua_dynamic_address_space_spike.rs:1602-1611, 1706-1715, 1893-1902`] — async-opcua reads callbacks under sync RwLock; a panic in the closure poisons it and all subsequent reads on any node fail. Current closures are sentinel-only (`Variant::Float(42.0)`, `DateTime::now()`) with no realistic panic source — defer for 9-8 production callbacks where the closure body grows non-trivial.
- **`RunHandles` partial publicity (pub struct, pub(crate) cancel/gauge/state fields)** [`src/opc_ua.rs:91-116`] — External callers of the public type can read `server`/`server_handle`/`manager` but not three fields, and cannot destructure outside the crate. Today's only consumer (the spike test) lives in-crate so this works, but Story 9-7's hot-reload listener may live elsewhere and hit the trap. Defer to 9-7's spec: either make the whole struct `pub(crate)` or add accessor methods for the private fields based on what 9-7 actually needs.
- **DataValue 6-field literal duplicated 3 times in spike** [`tests/opcua_dynamic_address_space_spike.rs:1602-1611, 1706-1715, 1893-1902`] — At the 3-site DRY threshold but not over. When async-opcua adds a 7th DataValue field, three sites must update. Extract `fn sentinel_data_value(v: Variant) -> DataValue` when the spike file is consumed by 9-7/9-8 and the pattern grows.
- **`OPCGW_NAMESPACE_INDEX = 2` hardcoded with no runtime check** [`tests/opcua_dynamic_address_space_spike.rs:1183`] — async-opcua exact-pinned at 0.17.1 (issue #101) so namespace ordering is stable until the next deliberate bump. Runtime check (cache from `add_namespace` return value, assert `== 2`) is hardening for that bump. Deferred until the bump is scheduled.
- **Asserts inside write-lock-held block in spike** [`tests/opcua_dynamic_address_space_spike.rs:1582-1592`] — Comment says "drop write lock" but `assert!(added.len() == 1)` and `assert!(added[0])` evaluate while the guard is held. Failure path leaves address-space inconsistent for next test (callbacks not registered). `#[serial]` + per-test fresh server fixture mostly insulates but a one-line `drop(guard)` before assert is the clean fix when 9-7/9-8 grow the pattern.
- **HeldSession disconnect log + Drop without await** [`tests/opcua_dynamic_address_space_spike.rs:1416-1430`] — Disconnect errors swallowed via `let _ = ... timeout(...)`; Drop only aborts the event_handle without awaiting. Pre-existing pattern from Story 8-1's `tests/opcua_subscription_spike.rs`. Diagnostic improvement only — eprintln on disconnect error would aid flake triage.
- **`_dv` drain `Ok(None)` vs `Err` disambiguation** [`tests/opcua_dynamic_address_space_spike.rs:1646-1658`] — Drain loop lumps Ok(None) (channel closed — likely a bug) with Err(_) (timeout — expected). Informational drain only (per the AC#1/AC#2 baseline-stream demotion); diagnostic improvement only.
- **Q2 observation loop iteration cap** [`tests/opcua_dynamic_address_space_spike.rs:1740-1767`] — Loop is implicitly bounded by 3s deadline + 500ms slice. Multiple Bad notifications would only capture the first as `observed_status`; current Behaviour B (frozen-last-good) verdict is robust to this. Hardening only.
- **Gauge `JoinError::is_panic()` vs `is_cancelled()` disambiguation** [`src/opc_ua.rs:1083-1089` `run_handles` cleanup] — Current cleanup matches existing patterns in `OpcUa::run` body pre-refactor. Panic in gauge body before cancel-observe is conflated with normal abort termination. Diagnostic improvement only.
- **Stale `manager` Arc semantics post-shutdown** [`src/opc_ua.rs:103` `pub manager: Arc<OpcgwHistoryNodeManager>`] — Caller can clone the Arc and hold it past `run_handles` completion; mutations on a stale manager Arc after server stops are silent no-ops. Today's only consumer (the spike test) doesn't outlive the server. Story 9-7/9-8 hot-reload code will clarify the contract — either via state-checking facade or documented "drop before run_handles returns" rule.
- **`delete` returns None panic message** [`tests/opcua_dynamic_address_space_spike.rs:1729`] — `assert!(deleted.is_some(), ...)` with generic reference-leak message. Better diagnostic (capture pre-delete address-space state via `guard.find(...)`) is hardening only.
- **Q1/Q2 do not pin the known stale-callback leak** [`tests/opcua_dynamic_address_space_spike.rs` + spike report § 8] — A regression-pin test (delete a node → re-add same NodeId → verify which closure runs) would catch a future async-opcua patch that fixes the leak silently. Spike report § 8 documents the leak; pinning it would be additional scope. Pin in Story 9-8 alongside the planned `OpcgwHistoryNodeManager::remove_callback` extension.

## Deferred from: code review of 4-4-auto-recovery-from-chirpstack-outages (2026-05-05)

- **Cumulative `Channel::connect` 5s × N-devices blows NFR17** [`src/chirpstack.rs::create_channel`] — F8/EH13. With N=10 devices and a slow upstream, R × 5s = 50s could exceed NFR17's 30s SLA at the cycle level. NFR17 measures recovery-loop budget (R × delay), not full-cycle latency, so this is a tangential concern. Defer to Story 9-7 hot-reload work or a separate channel-pooling story when SCADA-side throughput pain surfaces. Possible mitigation: reuse a single `Channel` across devices in a cycle (the underlying `tonic::Channel` is `Clone` and pools internally).
- **Inner probe-loop in `get_device_metrics_from_server` is not cancel-safe** [`src/chirpstack.rs:1786-1847`] — EH5. The intra-cycle TCP probe retry loop sleeps `delay` between attempts but doesn't `tokio::select!` against `cancel_token`. A long-running poll cycle could delay graceful shutdown by R × delay seconds. Pre-existing pattern; not a Story 4-4 regression.
- **`#[serial_test::serial]` discipline on the 7 new recovery_* tests** [`src/chirpstack.rs::tests::recovery_*`] — EH17. Existing `chirpstack.rs::tests` don't use `#[serial_test::serial]` either; `tracing-test` global-buffer cross-pollination is theoretically possible but hasn't been observed in CI. Defer until a flake actually surfaces.
- **Port-rebind race in unit-test fixtures** [`src/chirpstack.rs::tests::recovery_*`] — F13/EH16. `bind('127.0.0.1:0')` + `drop` for the unbound case has a narrow race window where another process could grab the port. Pre-existing pattern from Story 8-1 / 9-0; never observed.
- **Recovery success doesn't reset `chirpstack_available=true`** [`src/chirpstack.rs::recover_from_chirpstack_outage`] — EH8. Spec explicitly says don't (avoid race with cycle-end write at `poll_metrics:1188-1195`). Documented design choice. Operator-visible: dashboard / OPC UA may show `chirpstack_available=false` for up to `polling_frequency` seconds after recovery_complete log. Acceptable.
- **`RecoveryOutcome` discarded by caller via `let _outcome`** [`src/chirpstack.rs:1235-1244`] — F3/EH11. Spec wire-in instruction explicitly says "control returns to the device for-loop... Do not break the cycle early." Returned enum is informational; helper signature is preserved for future callers (e.g., Story 9-7 hot-reload listener) that may want to branch.
- **`retry == 0` defense-in-depth** [`src/chirpstack.rs::recover_from_chirpstack_outage`] — F11/EH3. Config validation rejects `retry == 0` at startup (`src/config.rs:931-936`). Defense for a future caller that bypasses validation; not a present bug.
- **`error_count_at_entry: i32` could be `u32`** [`src/chirpstack.rs::recover_from_chirpstack_outage` signature] — F12. Style only; `update_gateway_status` accepts `i32` historically. Refactor when storage trait is rewritten (Epic-A storage payload migration per #108).
- **`update_gateway_status` entry-write Err is logged warn but proceeds** [`src/chirpstack.rs::recover_from_chirpstack_outage:846-854`] — EH9. Defensive choice: storage failure during outage is a secondary failure; logging warn + proceeding with the recovery probe is the right call. Documented in code comment.
- **Race between recovery's `update_gateway_status` write and cycle-end write** [`src/chirpstack.rs::recover_from_chirpstack_outage` + `poll_metrics:1188-1195`] — EH10. Eventually consistent: the cycle-end write is the authoritative state for the next cycle. Defer until SCADA-side observability gap surfaces.
- **Cancel fired BEFORE recover_from_chirpstack_outage entry** [`src/chirpstack.rs::recover_from_chirpstack_outage`] — EH19. Defensive guard would emit no `recovery_attempt` and skip the gateway_status write. Rare path; the cancel will be observed at the first `tokio::select!` inside the loop instead.
- **Originating device not retried after Recovered outcome** [`src/chirpstack.rs:1234-1243`] — EH20. Spec design choice: subsequent devices in the same cycle benefit from the restored channel; the originating device gets refetched on the next cycle (`polling_frequency` later — typically 10s). Acceptable user-visible gap.
- **NFR17 30s SLA has no regression test** [`src/chirpstack.rs::tests`] — Auditor concern #5. Spec called for `test_nfr17_recovery_within_30_seconds`; implementation assumes shipped defaults trivially satisfy. A future bump that breaks the SLA (e.g., shipping `retry=60, delay=1`) wouldn't be caught by `cargo test`. Add a simple test fixture asserting `R × D ≤ 30` for the shipped config when hardening pass surfaces.
- **Test-fixture comment in `make_recovery_test_poller` is misleading** [`src/chirpstack.rs::tests::make_recovery_test_poller`] — Auditor divergence #4. Comment says "Bypasses validation (delay=0 would fail validate())" but `ChirpstackPoller::new()` doesn't actually call `validate()`. Functionally wrong but harmless. Cosmetic.
- **End-to-end `poll_metrics` integration test for the recovery wire-in** [`src/chirpstack.rs::poll_metrics:1224-1243` + new test file] — D1 partial resolution. Story 4-4 review iter-1 P9 attempted a `poll_metrics`-driving end-to-end test against a stub TCP listener, but the design hit a dead end: tonic's `Channel::connect()` returns successfully once the kernel SYN-ACK completes (well before HTTP/2 SETTINGS), so the P2 5s channel timeout doesn't bound the request-level hang. A bound-but-non-responding listener causes `device_client.get_metrics(request).await` inside `get_device_metrics_from_server` to wait indefinitely for HTTP/2 SETTINGS; the inner probe-retry loop also runs forever on an unbound port (pre-existing issue — `if count == retry { warn!(...) }` emits a warn but doesn't `break`). Both paths block, so the wire-in is unreachable from `poll_metrics` in a unit-test fixture without further infrastructure. **Two viable mitigation paths for a future hardening pass:** (a) write a mock gRPC server (~200+ LOC of test infrastructure: tonic's `Server::builder` + a fake `DeviceServiceServer` that returns Err on every call); (b) add a request-level `tokio::time::timeout` wrap inside `get_device_metrics_from_server` around `device_client.get_metrics(request).await` (semantically distinct from Story 4-4's channel-connect timeout — the existing 5s budget bounds channel build, not request execution). **Today's coverage:** the wire-in's just_logged-gating + single-fire-per-cycle contract is pinned by the contract-pinning unit test `recovery_wire_in_just_logged_gating_single_fires_per_cycle` at `src/chirpstack.rs::tests`. The remaining wire-in surface (error_count post-saturating-add, no-cycle-break) is structural — visible in code review but not regression-pinned by any test. Re-evaluate when SCADA-side observability surfaces a real wire-in regression that the unit test missed, or when Story 9-7 hot-reload introduces additional poller mutations that benefit from the integration harness.

## Deferred from: code review of 4-4-auto-recovery-from-chirpstack-outages — iter-3 (2026-05-06)

- **Cancel-during-probe path untested; existing `recovery_aborts_on_cancel_during_sleep` is timing-flaky on slow CI** [`src/chirpstack.rs::tests::recovery_aborts_on_cancel_during_sleep`] — Blind M5. The test relies on a 50ms cancel landing during the first inter-attempt sleep. With probe-before-sleep semantics (P1) and a fast loopback connect-refused (microseconds), the cancel almost always lands during the inter-attempt sleep on dev hardware. On slow CI runners (>50ms connect-refused), the cancel would land during the synchronous probe; the cancel is not observable until the next `tokio::select!`, at which point the assertion `elapsed < 2s` may fail. Defer to a hardening follow-up; CI flake hasn't been observed yet. Mitigation when picked up: extract a test-controllable async probe seam, or assert structural state (`tokio::time::pause()` + `advance()` deterministic clock) instead of wall-clock budget.

## Deferred from: code review of 9-7-configuration-hot-reload — iter-1 (2026-05-06)

### v1 limitations introduced by Story 9-7 (AC#11 backfill — ref #112)

- **AC#10 downgrade — OPC UA `[opcua].stale_threshold_seconds` hot-reload is dashboard-only** [`src/opc_ua.rs:1017`] — Tracked in **#113**. Spec mandated per-NodeId staleness cache flush; OPC UA path captures threshold into per-variable read-callback closures at startup, AC#8 forbids modifying `src/opc_ua.rs`. Shipped propagate-only; AC#10 amended in iter-1 review. Future fix = closure-capture refactor (live-borrow via `Arc<AppState>` or `Arc<ArcSwap<AppConfig>>`).
- **Credential rotation requires restart** [`[opcua].user_name` / `user_password`, `[web].auth_realm`] — Tracked in **#113** (same root cause as AC#10 downgrade). Auth middleware captures `Arc<WebAuthState>` at router-build time; hot-swap requires `from_fn_with_state(app_state, ...)` refactor which would modify `src/web/auth.rs` (AC#8 forbidden). Future fix = same live-borrow pattern as #113.
- **`[global]` section hot-reload deferred** — Tracked in **#114**. v1 review iter-1 reclassified all `[global]` knobs (`prune_interval_minutes`, `command_delivery_*`, `history_retention_days`, `debug`) as restart-required to fix the silent-drop classifier hole. Future story wires per-task watch receivers.
- **`[command_validation]` section hot-reload deferred** — Tracked in **#115**. Same reclassification rationale; spec § "Hot-reload-safe" originally listed `commands.*` as hot-reload-safe but implementation never wired it. Future story = audit every read site of `Arc<CommandValidationConfig>` and live-borrow.
- **`[logging]` log-level hot-reload deferred** — Tracked in **#116**. Spec admitted "not in v1 if non-trivial"; reclassified to restart-required to fix silent-drop. Future story uses `tracing_subscriber::reload::Layer` for live `EnvFilter` swap.
- **No HTTP trigger for reload (POST `/api/config/reload`)** — Stories 9-4 / 9-5 / 9-6 will add programmatic reload triggers from CRUD handlers; no separate issue needed (covered by those stories' epics).
- **No filesystem watch (`notify` crate) auto-reload** — Out of scope by spec (editor-save races + dependency-surface expansion). No follow-up issue planned.

### Boundary-case deferrals from review findings

- **SIGHUP racing startup window before listener spawn** [`src/main.rs:960`] — The SIGHUP listener task is spawned after poller/web/OPC-UA boot. A SIGHUP delivered in that startup window hits the kernel default action for SIGHUP (terminate the process) instead of being queued. Mitigation would be early-boot `pthread_sigmask` / `signal-hook`-style block before `tokio::main`; out of scope for 9-7's "operator-driven reload at steady state" trigger model. Re-evaluate if operators report startup-time SIGHUP terminations. No issue filed (operationally rare; fix path = code-only adjustment in `main.rs` when triggered).
- **SIGHUP listener panic = no watchdog/restart** [`src/main.rs:960-1080`] — If the listener task panics for any reason (signal driver fault, future code change), the panic is captured in the JoinHandle, the gateway keeps running, but all subsequent SIGHUPs are silently ignored. No supervised restart loop; no panic-time log. General-resilience concern, not 9-7-specific. Add a supervised respawn pattern when any other listener task in the codebase needs the same treatment. No issue filed (general resilience pattern, not Story-9-7-specific).
- **TOML editor partial-write race with figment IO** [`src/config_reload.rs:226`] — If the operator's editor uses non-atomic writes (streaming `>>` or in-place truncate-write), figment can read a half-written file and fail with a parse error; the gateway logs `reason="io"` even though the file is fine seconds later. No retry-once logic. Most editors use atomic renames so this is rare. Mitigation if it surfaces: retry-N times on parse error before giving up. No issue filed (rare in practice).
- ~~**`_initial_reload_rx` retained for entire process life**~~ — **RESOLVED in iter-2 P38 (2026-05-06).** Binding rebound to bare `_` so the receiver drops immediately at end of statement; comment in `src/main.rs` updated to match reality. Entry kept here for audit-trail integrity (see iter-3 Edge L6).
- **`chirpstack.list_page_size` mid-pagination drift** [`src/chirpstack.rs` pagination loop] — `list_page_size` is classified as hot-reload-safe and read on each pagination call. If the poller is mid-page when the config swap lands, the next page request uses a different page size than the previous — pagination state machine assumption could be violated. Narrow boundary case; no test exercises it. Mitigation: snapshot `list_page_size` at the start of each `list_devices` invocation (or document that pagination is page-size-agnostic at the API level). No issue filed (narrow case).

## Deferred from: Story 9-4 implementation (2026-05-07)

### v1 limitations introduced by Story 9-4 (AC#12 backfill)

- **TOML-only persistence (no SQLite-side `applications` table)** — `epics.md:864` states changes are persisted "to both SQLite and the TOML config file"; Story 9-4 amends to TOML-only. Architecture's data-boundary table (`architecture.md:530-534`) reserves SQLite for runtime state (metric values, command queue, gateway status), not configuration topology. Adding an `applications` table would be Epic-A scale (similar shape to issue #108). Future revisit if data-corruption-recovery requirements demand a second sink. **No issue filed yet** — wait for operator concern.
- **No cookie-based CSRF token** — v1 ships Origin/Referer + JSON-only Content-Type defence, sufficient for the LAN single-operator threat model. Future v2 upgrade documented in `docs/security.md § Configuration mutations § Anti-patterns`: a double-submit pattern signed with the existing per-process `hmac_key` (Story 9-1 / 7-2 reuse). No cookies. Trigger: multi-operator deployment or hostile-LAN threat model.
- **No cascade-delete** — DELETE on an application with one or more `[[application.device]]` sub-tables returns 409 Conflict + a hint pointing at Story 9-5 device CRUD. Operator-deliberate two-step protects against accidental wipe of device topology. Future revisit when Story 9-5 lands and the recovery story can pivot.
- **`[web].allowed_origins` hot-reload is restart-required** — Tracked in **#113** alongside the Story 9-7 v1 limitations. The CSRF middleware captures `Arc<CsrfState>` at router-build time; live-borrow refactor is the same blocker as `[opcua].user_name/user_password` and `[opcua].stale_threshold_seconds`.
- **Best-effort rollback on reload failure** — If the rollback write itself fails (filesystem out-of-space, permission flip mid-flight), the gateway logs `event="application_crud_rejected" reason="rollback_failed"` and leaves the on-disk TOML in an inconsistent state. Manual operator action required. Mitigation when it surfaces: file-level snapshot before any CRUD write, restored synchronously on rollback. No issue filed (rare in practice; depends on filesystem-failure scenario).
- **No ChirpStack-side existence check at CRUD time** — v1 trusts the operator-supplied `application_id`. The next poll cycle surfaces a "device list lookup failed" log if the ID is wrong. Cross-checking against ChirpStack at CRUD-time would require a synchronous gRPC round-trip in the request hot path. Defer until operator complaints surface.
- **Env-var override silently wins over disk edit** — If `OPCGW_APPLICATION__N__APPLICATION_NAME=...` is set, a CRUD edit to that field via `PUT /api/applications/...` writes to disk but the post-write reload re-runs the figment chain (TOML + env-var overlay) and the env value silently overrides. Documented in `docs/security.md § Configuration mutations § Env-var-overrides-disk-edit gotcha`; operator-action only.

### Boundary-case deferrals from Story 9-4

- **`put_application_id_in_body_is_rejected` accepts 400 OR 422** — axum 0.8 maps `serde(deny_unknown_fields)` deserialisation rejection to 422 (Unprocessable Entity), not 400. The spec's AC#3 wording said 400. Test was relaxed to accept either; spec is technically correct under HTTP 422 semantics (well-formed JSON, semantically invalid). Cosmetic spec/impl misalignment; no operator-visible impact.
- **`Manual smoke test`** of the full applications.html flow (CREATE → EDIT → DELETE via browser) is not automated. The integration tests pin the structural contract (DOM IDs + viewport meta + JS file presence + `/api/applications` reference) but visual regression and mobile-responsive verification are operator-side.
- **No regression test for `concurrent_post_applications_do_not_lose_updates` (AC#4 lost-update fix)** — The spec called for a 2-task race test, but writing a deterministic race test in Rust requires either careful timing or an extra synchronisation primitive in the test fixture. The lock-based serialisation is exercised by `lock_serialises_concurrent_writers` in `src/web/config_writer.rs::tests` (proves `lock()` does serialise) + the production code uses the lock at every CRUD entry point. Defer the integration-level race test to Story 9-5 / 9-6 when the same lock pattern is reused; if a flake surfaces under multi-CRUD load, file an issue.

## Deferred from: code review of 9-4-application-crud-via-web-ui — iter-1 (2026-05-07)

### Boundary-case + cosmetic deferrals from review findings

- **`content_type_is_json` lowercases entire header value** [`src/web/csrf.rs:121-130`] — Smelly (parameter values like `boundary=...` may be case-sensitive per RFC 7231), but JSON's only standard parameter is `charset` which is case-insensitive. No current bug; revisit if Content-Type ever needs to dispatch on a case-sensitive parameter.
- **Referer extraction edge cases (IPv6 brackets, multi-value parsing)** [`src/web/csrf.rs:107-118`] — Cosmetic; current parser handles the common cases correctly. EH-7 IPv6 walkthrough confirms behaviour is correct, just fragile to future authority-component additions.
- **`tempfile + persist` cross-filesystem (tmpfs) failure mode** [`src/web/config_writer.rs:117-127`] — `tmpfs` parent + persistent target on different filesystems gives `EXDEV` on rename. Operational rarity (operators rarely host config on tmpfs); document in `docs/security.md` if it surfaces.
- **`load_document` blocking in async path** [`src/web/config_writer.rs:74-90`] — `std::fs::read_to_string` + `parse::<DocumentMut>()` are sync. Sub-millisecond on small configs; with the default multi-thread runtime, work-stealing absorbs the latency. Wrap in `spawn_blocking` if a current-thread runtime use case appears or config grows past ~10k applications.
- **TOCTOU on `config_path.parent()`; symlink-replace behaviour of `persist`** [`src/web/config_writer.rs:148-154`] — `persist` replaces a symlink with a regular file on most platforms. Operators using shared/multi-tenant setups that symlink config from a managed location lose the symlink on first CRUD. Document or canonicalize-non-symlink at construction.
- **Unused `_listener_handle` from listener task spawn** [`tests/web_application_crud.rs:1280-1287`] — Test-only; on `cancel.cancel()` the listener may continue briefly but the abortion is not awaited. Rare leak in test runs but not a correctness issue.
- **`make_test_reload_handle_and_writer` returns 3-tuple in unstable order** [`src/web/test_support.rs`] — Caller MUST keep the `dir` alive; tests that destructure `(_, _, _dir)` accidentally drop the tempdir. Cosmetic; struct return with explicit field names is safer.
- **`inject_allowed_origins` fallback appends a SECOND `[web]` block** [`tests/web_application_crud.rs:1208-1213`] — TOML allows multiple table headers but figment-style merge behaviour is implementation-defined. Dead code in current fixtures (every fixture has `[web].auth_realm`); future refactor could trip it.
- **Hard-coded `64 * 1024` body limit in CSRF unit tests** [`src/web/csrf.rs:538`] — Cosmetic. If production handlers ever return >64K, the test infrastructure silently truncates and masks bugs. A `const TEST_BODY_LIMIT` would clarify; deferred.
- **`inject_allowed_origins` writes header line without TOML escaping** [`tests/web_application_crud.rs:1196`] — Works because `base_url` is `http://127.0.0.1:NNNN` (no quotes/backslashes); a future bind that includes special characters would break TOML parse.
- **Frontend `onEdit` returns silently when name unchanged** [`static/applications.js:914`] — UX nit; user gets no feedback. Add a toast in a future UX-polish pass.
- **`extract_origin` parses Referer with no length cap** [`src/web/csrf.rs:107-118`] — DoS theoretical; axum has header-size limits upstream. Defer until upstream changes invalidate the assumption.
- **AC#1 / AC#9 test-coverage gaps (weaker assertions than spec wording)** [`tests/web_application_crud.rs::applications_html_renders_table` + `applications_js_fetches_api_applications`] — Tests check `<table` + `Application` substring presence and `fetch("/api/applications"`, but spec called for `Application ID` + `Application Name` + `<form action="/api/applications" method="POST"` markers + `Content-Type: application/javascript` header pin + a separate `applications_html_carries_viewport_meta` test + a separate `applications_css_uses_dashboard_baseline` test. Runtime behaviour is correct; coverage is just less granular than spec asked. Trigger: future review notes the gap or a regression slips through.
- **AC#3 spec deviation: PUT-with-immutable-field returns 422 (axum default), not 400 + spec body wording** [`tests/web_application_crud.rs::put_application_id_in_body_is_rejected`] — Acknowledged in Dev Agent Record + this deferred-work file already (line 245). Test relaxed to accept either 400 or 422; spec body wording (`"application_id is immutable; delete and recreate to change"`) is not pinned. Cosmetic spec/impl divergence; revisit during P5 (immutable_field audit-event addition) which will also surface a custom error body.

## Deferred from: code review of 9-4-application-crud-via-web-ui — iter-2 (2026-05-07)

5 iter-2 findings were deferred with documented rationale (per CLAUDE.md "Code Review & Story Validation Loop Discipline" condition #3). Each is also recorded inline in the story spec's "Code Review Iter-2" section.

- **P28: D1-P ambient-drift + rollback path not integration-tested** [`tests/web_application_crud.rs`] — The IO-failure test chmods BEFORE the write step, so it never exercises the `post-write-then-reload-fails → rollback` path that the iter-1 patches were designed to harden. Adding a true end-to-end test requires either a test-only hook in `ConfigReloadHandle` (architectural change) OR a malformed TOML payload that passes pre-write validation but fails reload (no such payload exists post-iter-1's pre-write checks). Rollback path remains unit-tested at `src/web/config_writer.rs::tests::poisoned_writer_rejects_subsequent_writes`. Defer until either an architectural test-hook or a synthetic-failure injection is added in a future story.
- **P34: `knob_in_delta` brittle `Item::to_string()` equality** [`src/web/api.rs::knob_in_delta`] — Cosmetic toml_edit re-formatting could flip the "ambient drift" determination on no real semantic change. The function is a defence-in-depth path that should NEVER fire under correct operation (application_list mutations don't trigger `RestartRequired` from `classify_diff`). Migrating to semantic compare via `toml::Value` would introduce another TOML library; cost > benefit for a path that's already dead in v1. Revisit if iter-2 P36 + future hot-reload work surfaces a real false-positive in the wild.
- **P37: Duplicate-id check is byte-equal, not case-folded** [`src/web/api.rs::create_application` pre-write check + `AppConfig::validate` HashSet] — `App-1` and `app-1` are distinct identifiers throughout the gateway (CRUD dup check, validate's HashSet, poller bookkeeping, OPC UA NodeId generation). Documented design call in `docs/security.md § application_id semantics`. Char-class doesn't have an obvious case-fold semantic for non-letters; case-sensitivity matches ChirpStack API behaviour. Revisit if operators report case-collision confusion.
- **P38: AC#3 spec body still pins `422` for duplicate-id; impl returns `409`** [story spec AC#3 + `tests/web_application_crud.rs::post_application_with_duplicate_id_returns_409`] — Iter-1 P2 amended the duplicate-detection path from post-write `validate()` (422) to pre-write semantic conflict (409). Behaviour is correct; the AC#3 spec body wording carries the historical 422 path. Add a one-line spec amendment in the next dev-story revision OR accept as documented divergence here.
- **P39: P30 relabel — `pub mod test_support` not feature-gated** [`src/web/mod.rs:39-48`] — Iter-1 P30 was originally meant to gate `test_support` behind `#[cfg(any(test, feature = "test-support"))]`. The dev agent's iter-1 resolution kept the module unconditional with a documented justification (Cargo cannot express integration-test feature flags without a circular self-dev-dep). Production-binary cost is ~150 LOC of TOML fixture strings — negligible. Defer until either (a) Cargo grows native support for integration-test feature flags OR (b) the binary footprint becomes operationally visible.

## Deferred from: code review of 9-4-application-crud-via-web-ui — iter-3 (2026-05-07)

3 iter-3 patches applied (HR2-1 IPv6 bracket logic, HR2-2 P35 symmetric to PUT/DELETE, EH3-H1 rollback on post-persist write failure) + 2 admin patches (AC#8 reason-set amendment, this deferred-work.md sync). Remaining iter-3 findings deferred:

- **H1 (Blind): `#[serde(default)]` on `device_list` / `read_metric_list` swallows valid TOML errors** [`src/config.rs:341-347, 354-360`] — A `[[application]]` block with a typo'd `[[application.devicc]]` (or any non-recognised sub-table) deserialises to an empty `device_list` because `#[serde(default)]` was added without `deny_unknown_fields`. Pre-9-4 the validate-error-on-empty caught typos at startup; post-9-4 typos slip through to runtime as warns. Pre-existing tension: `#[serde(default)]` was required for AC#3's empty-device-list demotion. Mitigation: the warn-log surfaces at startup; operators with structured-log analysis spot it. Revisit if operator reports surface a real typo-induced failure.
- **EH3-M1 (Edge): `dir.sync_all` skipped silently when `File::open(parent)` returns Err** [`src/web/config_writer.rs:217-225, 357-365`] — If File::open of the parent fails (concurrent removal, EMFILE, EACCES on confined cgroup), the durability guard is bypassed and no error is logged. Pre-existing in iter-2 P32; the risk is that a power loss right after persist + before the next sync loses the rename. Operationally rare; restart re-reads from disk, and atomic rename means either old or new content (never half). Defer to a future hardening pass; would require differentiating "open-for-fsync failure" from "open-for-write failure" with an audit warn.
- **EH3-M2 (Edge): TOML inline-array-of-tables `application = [{...}]` form bypasses P35 dup pre-flight** [`src/web/api.rs::create_application:691-741`] — Operators almost always write the conventional `[[application]]` block form; the inline AOT form is a TOML feature operators rarely use because it loses comment-readability. If an operator did use it, post-write reload's `validate()` still catches duplicates (just not pre-flight). Defer; revisit if any deployment surfaces inline AOT in `config.toml`.
- **EH3-M3 (Edge): `update_application` / `delete_application` "should-be-impossible" 404 reachable via external TOML edit** [`src/web/api.rs:953-963, 1163-1173`] — Pre-check reads live config; an operator-driven edit between pre-check and lock-acquire makes the post-lock TOML walk find no match. Client sees 404 with no diagnostic explaining the race. Defer; the failure mode is operator-action-driven and self-explanatory once they re-read the file. Add a `reason="external_edit_race"` audit event if it surfaces operationally.
- **EH3-M4 (Edge): `allowed_origins` validation accepts userinfo and unclosed IPv6 brackets** [`src/config.rs::AppConfig::validate` allowed_origins block] — `http://user:pass@host:8080` and `https://[::1` (no closing bracket) pass validation; they get stored, never match a real browser Origin → 403 CSRF on every request without an actionable hint. Pre-existing iter-1 validation gap. Mitigation: the operator's first CSRF-403 will surface in audit logs with `origin = <browser-supplied>` vs the malformed allow-list entry; `docs/security.md § CSRF defence` recommends bracketed-IPv6 + scheme-host-port-only entries. Defer to a future validation hardening pass.
- **Blind M1: Validation tests silently no-op when fixture has wrong shape** [`src/config.rs:541, 559`] — `test_validation_duplicate_application_id` is gated on `if config.application_list.len() > 1` and `test_validation_empty_read_metric_list_now_warns` is gated on `if !device_list.is_empty()`. If `get_config()` ever changes shape, both tests pass without exercising their assertions. Defer; the test-fixture contract is stable across the recent stories. Add `#[should_panic]` skeletons or unconditional assertions in a future hardening pass.
- **Blind M2: Warn-demote logspam on every reload** [`src/config.rs::validate` warn calls] — Empty `device_list` / `read_metric_list` warns fire on every `validate()` call. Story 9-7 reload calls validate, so each CRUD mutation, SIGHUP, or hot-reload event re-fires the warn for every long-standing empty entry. Defer; once-per-startup gating would require a `OnceLock<HashSet>` of already-warned IDs, which is a non-trivial addition. Operationally annoying but not incorrect; structured-log filters can suppress on duplicate operation+application_id pairs.
- **Blind M3: `bind_address = ":::"` malformed-input edge** [`src/config.rs::resolved_allowed_origins`] — Iter-3 HR2-1 patch refuses to construct a default if `bind` contains `[`/`]`. `:::` (3 colons) → bracketed → `[:::]:port`, malformed final URL. Browser will not match. CSRF rejects every state-changing request; operator notices via 403. Defer with the iter-3 HR2-1 defence-in-depth comment; the fix path is to tighten `WebConfig::validate` to reject any bind_address that doesn't parse as a pure `IpAddr` (it already does this in iter-1).
- **Blind M4: P35 false-positive 409 reason wording** [`src/web/api.rs::create_application` pre-flight] — Pre-flight rejects ANY `[[application]]` block with malformed `application_id`, even if the POST's id couldn't possibly clash. The `reason="conflict"` audit field is ambiguous (the operator's POST is well-formed; the conflict is with a pre-existing block they may not know about). Defer; consider adding a dedicated `reason="malformed_existing_block"` audit value in a future iteration.
- **Blind M5: Empty `allowed_origins` validate-vs-resolve disagreement** [`src/config.rs:371-377` validate, `:298-301` resolve] — Validate rejects `Some(vec![])` at startup; resolve treats `Some(vec![])` as equivalent to `None` (defensive fall-through to default). If validate ever loosens, resolve silently emits an unwanted default. Defer; the dual-defence is currently consistent and the code drift would be caught at code-review time.
- **EH3-L1..L4 + Blind L1..L5:** various cosmetic findings (dead `ReloadOutcome` import, leaked tempdirs in tests, brittle `reason()` string match, `UpdateApplicationRequest` doc-only struct, `ErrorResponse` mixed constructors, `wildcard bind` default useless without operator override, JSON duplicate-key collapse silent, `lock_serialises_concurrent_writers` extreme-starvation false-pass). Each is documented inline in the story spec's iter-3 review section. Defer to a future hardening / cleanup pass.

## Deferred from: code review of 9-5-device-and-metric-mapping-crud-via-web-ui (2026-05-08)

- **9-5-iter1-D1: CSRF "command" branch falls through to catch-all `crud_rejected`** [`src/web/csrf.rs:271-275, 296-306`] — Spec defers to Story 9-6; source comment acknowledges. No command routes wired today, so the helper's `"command"` return value is unreachable in any meaningful runtime emission. Will be replaced when 9-6 lands the literal `event="command_crud_rejected"` match arm.
- **9-5-iter1-D2: CSRF catch-all `crud_rejected` invisible to all grep contracts** [`src/web/csrf.rs:1949-1958, 1992-2001`] — When `resource = "unknown"`, the warn fires `event = "crud_rejected"` (no resource prefix), which is in NO documented grep contract (`application_*`, `device_*`, `command_*`, `config_reload_*`). SIEM filters keyed on the documented contracts silently drop these. No un-routed paths today; future-proofing concern. Add a generic `crud_rejected_*` family contract in a future hardening pass, or thread a `resource` tag through.
- **9-5-iter1-D3: `create_device` no intra-request metric_name uniqueness pre-check** [`src/web/api.rs:685-687`] — Duplicate metric_name within a single POST/PUT body is caught by `AppConfig::validate` at reload time + rollback (status 422). Functionally correct but wasteful: disk write + reload + rollback cycle on a case that could be rejected pre-write. Add a HashSet guard at validate-body-fields phase in a future polish pass.
- **9-5-iter1-D4: `metric_unit = ""` round-trips as `Some("")` to TOML** [`src/web/api.rs:1727-1729, 1638-1640`] — Empty string and absent are now two distinct states on the wire and on disk. Reload deserialises empty as `Some("")`. No hard rule today; defer until a downstream consumer expresses preference (or coerce on inbound: `value.filter(|s| !s.is_empty())`).
- **9-5-iter1-D5: `validate_device_field` length budget counts UTF-8 bytes, not chars** [`src/web/api.rs:1747`] — `metric_unit` allows non-ASCII (°C, m³, µT); a 22-char Cyrillic/Chinese unit name consumes ~44 bytes against the 64-byte budget, surprising operators with a length error on a visibly-short string. Other fields are gated by `is_valid_app_id_char` (ASCII-only) so they're effectively char-budget-equivalent. Defer to a UX-polish pass.
- **9-5-iter1-D6: PUT body with duplicate JSON keys silently last-wins (no audit)** [`src/web/api.rs:929-1012`] — `serde_json::Value::Object` deserialises duplicate keys with last-wins semantics (no error, no audit). Defer; this is project-wide policy and consistent with serde defaults; tightening would require a custom deserializer or a security-sensitive policy decision.
- **9-5-iter1-D7: PUT replaces inline `read_metric` with array-of-tables (TOML normalization)** [`src/web/api.rs:1218-1226`] — Operator-edited inline `read_metric = [{...}]` form is silently normalised to `[[application.device.read_metric]]` array-of-tables on PUT-replace. Spec mandates array-of-tables form for multi-entry; normalization is correct. Surprise diff for the operator. Defer.
- **9-5-iter1-D8: App-existence probe via 400/404 response-code differential** [`src/web/api.rs:1051-1090`] — Body validation runs BEFORE existence pre-check; malformed body → 400, well-formed body + non-existent app → 404. Authenticated operator can fingerprint application_ids by polling. Post-auth, minor info disclosure under operator-account compromise. Defer; would require running existence pre-check first then body validation, but that changes failure-mode ordering more broadly.
- **9-5-iter1-D9: README.md / sprint-status.yaml single-line narrative blocks reach 75 KB+** [`README.md:11`, `_bmad-output/implementation-artifacts/sprint-status.yaml:31, 49`] — Each story commit appends ~5 KB of narrative onto one line; line 11 of README.md is now 74 KB. GitHub PR diff renderer truncates at viewer width; `sed`/`awk` line-oriented edits become hazardous. Process hygiene rather than functional bug. Recommend wrapping at 80–120 chars in future commits.
- **9-5-iter1-D10: POST/PUT body limit not explicitly capped (default axum 2 MB)** [`src/web/mod.rs:396-405`] — Auth-gated surface (NFR11 Basic auth); default 2 MB is reasonable for v1; explicit cap warrants its own story (apply to `applications` Story 9-4 + `devices` Story 9-5 + `commands` Story 9-6 consistently). User reason: "auth-gated; default 2MB acceptable for v1; cross-resource cap deferred to dedicated hardening story."

## Deferred from: code review of 9-5-device-and-metric-mapping-crud-via-web-ui (iter-2, 2026-05-08)

- **9-5-iter2-D1: H1 fix loses `metric_index` from per-metric validation audit logs** [`src/web/api.rs::validate_metric_field_with_idx`] — The iter-2 H1 fix removed the double-emit by deleting the wrapper's outer warn; the inner `validate_device_field` warn does NOT carry `metric_index`. Operators debugging long metric_lists must guess which row failed. Future enhancement: thread `Option<usize>` through `validate_device_field` itself so a single warn carries `metric_index` when applicable.
- **9-5-iter2-M2: PUT/DELETE device existence-fingerprinting via 400/404 ordering** [`src/web/api.rs:1216-1402, 1620-1657`] — `update_device` and `delete_device` run body validation BEFORE existence pre-check; malformed body → 400, well-formed body + nonexistent device → 404. Authenticated operators can fingerprint device_ids by varying body validity. Same class as iter-1 D8 for applications, defer for consistency with that resolution.
- **9-5-iter2-M3: `update_device` intra-body metric_name uniqueness not pre-checked** [`src/web/api.rs:1363-1366`] — Body-field validation only invokes `validate_metric_mapping_fields` per row; intra-body `metric_name` duplicates traverse to disk write + reload + rollback (caught by `AppConfig::validate`'s HashSet at `src/config.rs:1593+`). Same gap as iter-1 D3 deferred for `create_device`; defer for consistency.
- **9-5-iter2-L1: `post_device_emits_topology_change_log` bypasses OPC UA listener wiring** [`tests/web_device_crud.rs::post_device_emits_topology_change_log`] — Test calls `log_topology_diff` directly per the source comment's intent (`src/config_reload.rs:1186-1188`: *"Public so integration tests for AC#4 can drive the emission without standing up a full `OpcgwHistoryNodeManager`"*). A regression that breaks the listener-to-helper wiring would still pass this test. Listener integration coverage is orthogonal harness work.
- **9-5-iter2-L6: tempdir guard panic surface excludes reqwest-send panics** [`tests/web_device_crud.rs::device_crud_io_failure_does_not_log_secrets`] — The iter-1 L12 guard restores perms before the assert (protects against assert-panic). If `json_request(...).send().await.expect("send")` panics on a network-side error, perms remain at `0o000` and `TempDir::drop` fails. Network panics in test infra are rare and the failure mode is recoverable manually.
- **9-5-iter2-L7: csrf empty-segment paths produce two audit warns (csrf + path validator)** [`src/web/csrf.rs::csrf_event_resource_for_path`, `src/web/api.rs::validate_path_application_id`] — `/api/applications//devices` triggers `device_crud_rejected reason="csrf"` from CSRF middleware AND `device_crud_rejected reason="validation"` from path validator on the next request attempt. Consistent rejection chain (CSRF rejects first; path validator only fires post-CSRF). Mild audit-log row inflation; not load-bearing.
- **9-5-iter2-L8: PUT-replace strips `read_metric` block operator decor** [`src/web/api.rs::update_device`, line ~1531] — `tbl.remove("read_metric")` then re-insert means any operator comments inside individual `[[application.device.read_metric]]` blocks are dropped. The handler comment promises preservation only for the sibling `[[application.device.command]]` table; metric-block comments are best-effort. TOML mutation fundamental; would require `toml_edit::ArrayOfTables`-aware merge instead of remove+rebuild.

## Deferred from: code review of 9-5-device-and-metric-mapping-crud-via-web-ui (iter-3, 2026-05-09)

- **9-5-iter3-D1 (Blind #2): `closeEditModal` ESC-close doesn't reset `editModalLoading`** [`static/devices-config.js`] — Native `<dialog>` ESC-key fires a `close` event; the DOMContentLoaded handler binds only the explicit Cancel button via `document.getElementById('edit-cancel').addEventListener('click', closeEditModal)`, not the dialog's native `close` event. Functional impact bounded by iter-2 M4 try/finally (flag is reset synchronously by the time `showModal()` returns). The `closeEditModal` flag-reset is now defensive dead code. Future enhancement: add `modal.addEventListener('close', closeEditModal)` for full coverage of all close paths (Cancel button + ESC + parent navigation).
- **9-5-iter3-D2 (Blind #4): 120ms `tokio::time::sleep` test pattern is flake-prone on slow CI** [`tests/web_device_crud.rs` — 6 tests: `delete_device_without_content_type_returns_415`, `post_device_emits_device_created_event`, `post_device_without_origin_returns_403_with_device_event`, `post_application_csrf_event_unchanged`, `auth_required_for_post_devices`, `device_crud_does_not_log_secrets_success_path`, `device_crud_io_failure_does_not_log_secrets`] — Tests wait 120ms for tracing's async dispatch to flush. Under heavy CI load (debug-mode parallel test runs on shared runners), 120ms can be insufficient. Future enhancement: implement a polling helper `async fn wait_for_log_substring(needle: &str, deadline: Duration) -> bool` that polls `captured_logs()` until either the substring appears or the deadline elapses. Adopt across all log-asserting tests.
- **9-5-iter3-D3 (Blind #5): `fetchJson` Content-Length=0 check doesn't cover HTTP/2 chunked transfer** [`static/devices-config.js::fetchJson`] — HTTP/2 servers / HTTP/1.1 chunked-transfer-encoded responses may omit `Content-Length` entirely. `res.headers.get('content-length')` returns null; `null === '0'` is false; falls through to `res.json()` which throws SyntaxError on empty body. Caught by inner try/catch and surfaces as "empty or non-JSON body" error to user. No crash, but misleading message. Niche today (opcgw is HTTP/1.1 + Content-Length-set). Future enhancement: also treat null Content-Length + 0-byte body as no-body (requires checking `res.body` after consumption attempt).
- **9-5-iter3-D4 (Edge H1): NodeId test pins format string locally, not production-side** [`tests/web_device_crud.rs::issue_99_regression_node_id_format_includes_device_id`] — Test contains `let dev_a_node = format!("{}/{}", "dev-A", "Moisture");` and asserts string equality against that locally-constructed string. Does NOT invoke `src/opc_ua.rs:978`'s production NodeId construction. A regression that flips line 978 to `format!("{}::{}", ...)` or swaps argument order would NOT fail this test (the test's local `format!` literal is unchanged). User accepted this trade-off in the iter-2 H2 decision (full OPC UA harness was deferred). Companion CRUD test #37 + storage tests #35/#36 partially cover the invariant via the storage layer's `(device_id, metric_name)` keying. Future enhancement: expose `pub fn metric_node_id(device_id: &str, metric_name: &str) -> String` in `src/opc_ua.rs` and have both line 978 and the test use it (binds the test to the production format).
- **9-5-iter3-D5 (Edge H2): `serial(captured_logs)` doesn't prevent bleed-through from non-grouped tests** [`tests/web_device_crud.rs`] — `#[serial(captured_logs)]` only serialises tests within the captured_logs group. Non-grouped tests in the same binary that emit `event=device_crud_rejected reason=csrf` could satisfy the captured-logs assertion via parallel emission. Iter-2 L3 fix added `clear_captured_logs()` + serial but the cross-group bleed-through path remains. Real but rare flake (`serial` group + 120ms sleeps make collision unlikely in practice). Future enhancement: refactor `init_test_subscriber` to register a per-test-instance subscriber at fixture-spawn and tear it down at fixture-shutdown, replacing the global `tracing_test::internal::global_buf()`.

## Deferred from: code review of 9-6-command-crud-via-web-ui — iter-1 (2026-05-12)

27 findings deferred after iter-1 review. See `_bmad-output/implementation-artifacts/9-6-command-crud-via-web-ui.md` § "Review Findings (Iter-1, 2026-05-12)" for full triage detail. Summary:

- **9-6-iter1-D1 (Blind H1): `command_port = -1` confusing error message** — `src/web/api.rs::validate_command_port` produces "out of u8 range" for negative values; the same effective bound emits "not in LoRaWAN range" for in-u8 but out-of-1..=223 values. Cosmetic.
- **9-6-iter1-D2 (Blind H4): Lost-update / reload deadlock concern** — under-specified by reviewer; no concrete repro of a deadlock between `config_writer.lock()` held during `reload().await` and a parallel SIGHUP-driven reload. Defer pending concrete repro.
- **9-6-iter1-D3 (Blind M7): `update_command` malformed-block check positional** — pre-flight only fires for malformed blocks BEFORE the target, not after. Symmetric to Story 9-5's update_device pattern.
- **9-6-iter1-D4 (Blind M8): `let _guard` pattern fragility** — Rust drops `_guard` at scope end as expected, but a future refactor to `let _ = ...lock()` would silently introduce a TOCTOU window. No regression test forces lifetime.
- **9-6-iter1-D5 (Blind M10): 120ms hardcoded sleeps race on slow CI** — inherited from Story 9-5 iter-3 D2.
- **9-6-iter1-D6 (Blind M11): `io_error_response` could echo file paths via `std::io::Error` Display** — tempdir paths in tests; in production deployments, paths under `/etc/opcgw/` or similar might leak. Pre-existing Story 9-4 / 9-5 pattern.
- **9-6-iter1-D7 (Blind M12): Rollback-after-rollback-failure not tested** — the iter-1 D3-P poison-flag mechanism handles outcome; test surface missing.
- **9-6-iter1-D8 (Blind M13 + D4-decision): Frontend defence-in-depth gap** — `static/commands.js` interpolates `c.command_id` / `c.command_port` raw into HTML without `escapeHtml(String(...))`. Same gap exists in Stories 9-4 / 9-5 frontend code. Cross-resource frontend hardening pass.
- **9-6-iter1-D9 (Blind M14): `closeEditModal` rapid-double-click race** — UX state-machine flaw, no security impact.
- **9-6-iter1-D10 (Blind L15): `post_command_emits_topology_change_log` triple-`||` assertion** — defensive design (multiple valid reload markers); accept as-is.
- **9-6-iter1-D11 (Blind L18): `internal_error_response` 11 paths emit no audit event** — Pre-existing Story 9-4 / 9-5 pattern.
- **9-6-iter1-D12 (Blind L19 + Edge L8): `inject_allowed_origins` dead fallback** — test fixture hygiene.
- **9-6-iter1-D13 (Blind L20): `OPERATOR_COMMAND_COMMENT_MARKER` test does not assert inter-block comment preservation** — test enhancement.
- **9-6-iter1-D14 (Edge M6): Command CRUD does not pre-check duplicate `device_id` blocks within target application** — silent first-match. Same gap pattern as `find_application_index`.
- **9-6-iter1-D15 (Edge L9): `set_global_default` failure in `init_test_subscriber` silently swallowed** — `let _ =` pattern; pre-existing Story 9-5 inheritance.
- **9-6-iter1-D16 (Blind L17 + Edge L10): `validate_command_port` two distinct error wordings for same effective bound** — cosmetic.
- **9-6-iter1-D17 (Edge L11): `inject_allowed_origins` drops trailing newline** — `.lines()` discards terminators; TOML parsers accept either.
- **9-6-iter1-D18 (Edge L12): Command `command_name` cross-device uniqueness undocumented at API surface** — operator-visible but disambiguated by device folder hierarchy. Documentation enhancement.
- **9-6-iter1-D19 (Edge L13): Catch-all `_` arm in CSRF / validate_path_device_id violates grep contract** — currently unreachable; defensive future-proofing guard documented in source comments.
- **9-6-iter1-D20 (Edge L14): `update_command` PUT body `command_port = i32::MIN` funnels through u8::try_from instead of negative-port semantics** — cosmetic error-message lossiness.
- **9-6-iter1-D21 (Edge L15): `update_command` accepts duplicate JSON keys via `serde_json::Map` last-wins** — standard serde_json behaviour.
- **9-6-iter1-D22 (Edge L16): `command_id` upper bound not capped below `i32::MAX`** — operator-hostile (a `command_id = 2_147_483_647` is unreasonable) but functionally fine.
- **9-6-iter1-D23 (Auditor A-L3): `mutate_command_preserves_read_metric_subtable` unit test missing despite spec checkbox** — integration test `put_command_preserves_read_metric_subtable` covers it; spec checkbox mismarked.
- **9-6-iter1-D24 (Auditor A-L4): Pre-flight on malformed sibling command blocks only checks `command_id`, not other 3 fields** — symmetric to Story 9-5 device pre-flight pattern.
- **9-6-iter1-D25 (Auditor A-L5): `commands_html_renders_per_device_table` assertions weaker than spec** — page is JS-driven so the static markup the spec mentioned doesn't apply.
- **9-6-iter1-D26 (Auditor A-L6): Dev Agent Record "45 tests" vs actual count drift** — narrative drift; fixed in this iter's verification block.
- **9-6-iter1-D27 (Auditor A-L7): `UpdateCommandRequest` struct has dead `command_id: Option<i32>` field** — cosmetic; could remove.

## Deferred from: code review of 9-6-command-crud-via-web-ui — iter-2 (2026-05-12)

22 findings deferred after iter-2 review. See `_bmad-output/implementation-artifacts/9-6-command-crud-via-web-ui.md` § "Review Findings (Iter-2, 2026-05-12)" for full triage detail. Summary:

- **9-6-iter2-D1 (H2-decision): Axum Json extractor rejects malformed JSON before handler audit emission** — cross-resource gap in Stories 9-4 / 9-5 / 9-6 (both POST + PUT). Fix requires a custom Axum `JsonRejection` middleware that emits `event="<resource>_crud_rejected" reason="malformed_body"` with the correct resource literal. File as cross-resource hardening story alongside 9-6-iter1-D8 (frontend defence-in-depth) and the inherited 422-vs-400 cosmetic divergence.
- **9-6-iter2-D2 (B2-HR4): B-H3 canonical decimal emits same error wording for `/commands/0` and `/commands/-1`** — single "no leading '+' or '0' allowed" message instead of distinguishing leading-sign vs leading-zero vs negative semantics. Cosmetic.
- **9-6-iter2-D3 (B2-M9): E-M2 emits cosmetic-duplicate error on TOML with two `command_id = 0`** — both `command_id <= 0` + duplicate-detection fire, producing 3 error messages for 1 logical issue.
- **9-6-iter2-D4 (B2-M10): E-M5 leading/trailing whitespace doesn't cover internal whitespace** — `"open valve"` vs `"openvalve"` are still distinct command_names. Acceptable per char-class allowing spaces.
- **9-6-iter2-D5 (B2-M11): Duplicate command_name 409 audit `?command_name` Debug-format with surrounding quotes** — SIEM log parsers may need to handle quoted form.
- **9-6-iter2-D6 (B2-M12): `post_command_with_unknown_field_returns_400_with_audit` short command_name="x" fragility** — audit-reason assertion catches regression.
- **9-6-iter2-D7 (B2-M14): D2 walk-and-reject extraction-vs-validation interleaving fingerprinting** — defence-in-depth.
- **9-6-iter2-D8 (B2-M15): `find_application_index` double-lookup minor perf regression** — `doc.get("application")` called twice per CRUD request. Acceptable.
- **9-6-iter2-D9 (B2-L16): Unknown-field error message duplicated** — `create_command` and `update_command` hardcode the same string; future renames will drift.
- **9-6-iter2-D10 (B2-L17): D1 NodeId comment cross-references metric pattern at src/opc_ua.rs:976-979** — separator (`/`) consistency verified at impl time.
- **9-6-iter2-D11 (B2-L18): 9-6-iter1-D14 asymmetric fix scope** — find_application_index shape-check fix doesn't extend to nested device-shape mismatches; documented for future cleanup.
- **9-6-iter2-D12 (B2-L19): Fixture coupling `post_command_with_duplicate_command_name_within_device_returns_409` to `"reboot"` in APP_TOML_TEMPLATE** — implicit; relies on fixture stability.
- **9-6-iter2-D13 (E2-9): B-H3 negative `/commands/-1` returns "no leading '+' or '0' allowed" message** — confusing for operators passing negative. Same class as 9-6-iter1-D16.
- **9-6-iter2-D14 (E2-10): B-H3 whitespace / non-ASCII path returns canonical-form error** — misleading diagnosis class as 9-6-iter2-D13.
- **9-6-iter2-D15 (E2-12): `find_application_index` `_` catch-all dead code** — defensive future-proofing.
- **9-6-iter2-D16 (E2-13): No explicit test for `/commands/0`** — covered indirectly by `/01` test.
- **9-6-iter2-D17 (E2-14): `pre == post` byte-equality flake risk on slow CI** — inherited from 9-5 iter-3 D2.
- **9-6-iter2-D18 (E2-15): E-M4 case-sensitivity `"Reboot"` vs `"reboot"` not documented** — operator-visible behavior contract.
- **9-6-iter2-D19 (E2-16): `obj.keys()` reports only first-alphabetical unknown field** — minor reconnaissance friction.
- **9-6-iter2-D20 (E2-1/2/3 partial): Read-path application handlers (list/get) don't need shape-check** — figment validates first; documented.
- **9-6-iter2-D21 (A2-1): AC#3 prose lines 176-189 still narrate the post-write rollback 422 path** — iter-1 D3 amendment renamed the test but didn't update upstream AC narrative.
- **9-6-iter2-D22 (A2-2): Dev Agent Record narrative "45 integration tests" vs actual 56 (post-iter-2)** — narrative drift fixed in the iter-2 verification block.

## Deferred from: code review of 9-6-command-crud-via-web-ui — iter-3 (2026-05-12)

9 LOW findings deferred after iter-3 review. Memory pattern feedback_iter3_validation.md VALIDATED — 0 HIGH-REGs, 0 HIGH/MED, 12 LOW total (Blind 5 + Edge 3 + Auditor 4). Loop terminated per CLAUDE.md condition 2. See `_bmad-output/implementation-artifacts/9-6-command-crud-via-web-ui.md` § "Review Findings (Iter-3, 2026-05-12)" for full triage.

- **9-6-iter3-D1 (Blind B-LOW-1):** `check_top_level_application_shape` `_` catch-all match arm dead code today (only "device" / "application" / "command" passed). Defensive future-proofing.
- **9-6-iter3-D2 (Blind B-LOW-3):** E2-8 token assertion `logs.contains("command_name")` could match unrelated substrings. Mild test fragility.
- **9-6-iter3-D3 (Blind B-LOW-4 + Edge E3-2):** Iter-2 E2-11 response-body Debug format produces `"key"` (double-quoted) where prior was `'key'` (single-quoted). Wire-contract change symmetric to audit-log; documented but no external SDK reported as affected.
- **9-6-iter3-D4 (Blind B-LOW-5):** H1 well-formed-shape regression test covers only POST create_application; PUT update_application / DELETE delete_application not exercised. Helper is shared so single-site regression unlikely.
- **9-6-iter3-D5 (Edge E3-3):** `post_command_with_duplicate_command_name_within_device_returns_409` token assertion could be tightened to `logs.contains(r#"command_name="reboot""#)`.
- **9-6-iter3-D6 (Auditor A3-1):** Integration test count narrative drift in Dev Agent Record. Cargo aggregate verified (1082); per-category sub-breakdown imprecise.
- **9-6-iter3-D7 (Auditor A3-2):** AC#3 prose at spec lines 176-189 still narrates the post-write-rollback 422 path; iter-1 D3 renamed tests to `_returns_409` but upstream AC narrative not fully updated.
- **9-6-iter3-D8 (Auditor A3-4):** Audit-trail completeness — Edge Case Hunter iter-2 finding IDs gap (no E2-4 line item in iter-2 findings list). Documentation/audit-trail concern, no spec defect.
- **9-6-iter3-D9 (Multiple LOW carry-forwards):** Various inherited LOW findings from Story 9-5 iter-3 deferral set (9-5-iter3-D1..D5).

## Deferred from: code review of 9-8-dynamic-opc-ua-address-space-mutation — iter-1 (2026-05-13)

### v1 limitations introduced by Story 9-8 (also covered in `docs/security.md § Dynamic OPC UA address-space mutation § v1 limitations`)

- **9-8-iter1-D1 (Blind B-H6):** `stale_threshold` captured by-value into runtime-added read-callback closures (`src/opcua_topology_apply.rs:1210-1218` + `src/config_reload.rs:333`). A reload that changes `[opcua].stale_threshold_seconds` leaves existing closures with the old value; new metrics added by the same reload use the new threshold. Acknowledged in spec AC#13 + dev notes as #113 carry-forward (live-borrow refactor — same blocker as web `allowed_origins`). Long-term fix: thread `&AppConfig` into the closure via `Arc<watch::Receiver>` so each invocation reads the live value.
- **9-8-iter1-D3 (Blind B-H15):** `metric_unit` change forces full metric variable remove+add (drops subscriptions, resets historizing) — heavy-handed for a unit-string change. The OPC UA `EngineeringUnits` property is a separate node attribute that could be mutated in-place. v2 candidate. `src/opcua_topology_apply.rs::metric_equal:1648-1653`.
- **9-8-iter1-D6 (Edge E4):** Application rename (same `application_id`, different `application_name`) silently dropped by `compute_diff`. v1 scope intentionally limited to device renames; application-folder DisplayName change in `set_attributes` Phase 4 would mirror the device-rename path. Defer until operator demand emerges. `src/opcua_topology_apply.rs::compute_diff` (no walk for app-name changes).
- **9-8-iter1-D7 (Edge E6):** Pure-remove path leaks the read-callback closure in `SimpleNodeManagerImpl.read_cbs` — the closure holds Arc clones (storage, status_cache, device_id, chirpstack_metric_name, threshold) ~120 bytes each. Operationally negligible at expected churn rates (per 9-0-spike-report.md:183 — ~150 leaks / 30 days = ~18 KB) but strictly a leak. Task 6 option-(b) explicitly authorized by spec; documented in `docs/security.md § Stale read-callback closure leak (option-b limitation)`. Long-term fix: upstream FR to async-opcua to expose `SimpleNodeManagerImpl::remove_read_callback` (precedent: Story 8-1 issue #94 session-rejected callback FR). `src/opc_ua_history.rs::remove_read_callback` (stub).
- **9-8-iter1-D11 (Auditor A3):** `trigger ∈ {sighup, http_post}` field missing from `address_space_mutation_succeeded` / `…_failed` events. Spec AC#7 line 202 requires it. Cross-cutting Story 9-7 change — would require `ConfigReloadHandle` to thread the trigger source through the watch channel state (since the listener task doesn't know who triggered the reload, only that the channel changed). Out of v1 scope; revisit if operator forensics need to distinguish SIGHUP-triggered from CRUD-POST-triggered reloads.

### Lower-impact / rare edge cases (not v1 scope; pre-existing or defensive)

- **9-8-iter1-D2 (Blind B-H14):** `command_port: i32` accepted in `AddedCommand` without defence-in-depth validation. Config-layer `validate_command_port` already enforces LoRaWAN 1..=223; 9-8 trusts validated input. Defence-in-depth addition (re-validate at the apply layer) would be belt-and-braces hardening. `src/opcua_topology_apply.rs:913, 1182-1193`.
- **9-8-iter1-D4 (Edge E2):** Phase 2 (delete) drops the address_space write lock before Phase 3 (add) acquires; concurrent browse request between phases can observe vanished folders before replacements added. Rare race window (μs); not a correctness violation, just transient inconsistency. Single-lock optimization across both phases would close the window. `src/opcua_topology_apply.rs:1098-1141 (Phase 2)` → `:1161-1197 (Phase 3)`.
- **9-8-iter1-D5 (Edge E3):** Phase 3 adds variable via `add_variables` BEFORE registering the read-callback via `add_read_callback`. A client Read in the narrow window receives the initial 0.0 (per the variable's DataType default) instead of the storage-derived value. Operationally indistinguishable from a normal first-sample scenario; clients must handle initial-value semantics regardless. `src/opcua_topology_apply.rs:1196 (add_variables)` → `:1207-1218 (add_read_callback)`.
- **9-8-iter1-D8 (Edge E10):** Renamed device whose application is ALSO being added/removed in the same diff — Phase 4 `set_attributes` targets a folder that's about to be deleted or just-created. Causes BadNodeIdUnknown failure → entire Phase 4 returns Failed. Rare edge (rename + app-mutation in single diff); P1 fix (Phase-level error reporting) covers the operator visibility once it lands. `src/opcua_topology_apply.rs::compute_diff` (no cross-axis filter) → Phase 4 `:1265-1281`.
- **9-8-iter1-D9 (Edge E11):** `added_devices` with `application_id` NOT in `added_applications` NOR in `prev_apps` — orphan add. `add_folder` on missing parent silently inserts; child device floats orphan in address space. `compute_diff` structure prevents this in normal flow (added_devices only generated for apps in new config); defensive parent-exists check would be belt-and-braces hardening. `src/opcua_topology_apply.rs::Phase 3 :1163-1175`.
- **9-8-iter1-D10 (Edge E12):** `stale_threshold == 0` yields immediate-stale on every read for newly-added metrics (interpreted as "0 second threshold = always stale"). Config validation should prevent 0 at the schema layer; if it does pass through, operator-acceptable. `src/config_reload.rs:333 stale_threshold unwrap_or(DEFAULT_STALE_THRESHOLD_SECS)`.

## Deferred from: code review of 9-8-dynamic-opc-ua-address-space-mutation — iter-2 (2026-05-14)

- **9-8-iter2-D1 (Edge E-H2-iter2):** Conditional regression on hypothetical future `compute_diff` change — if a future revision ever emits `removed_applications` or `removed_devices` WITHOUT also pushing children to `removed_metrics` / `removed_commands` (bypassing `push_removed_device`), iter-1 P1's `delete().is_none()` check would misroute cascaded grand-children to `Failed{REMOVE_FAILED}`. NOT a current runtime defect; iter-2 IP2 added the load-bearing flatten-invariant doc-comment to `compute_diff` to defend against this regression. `src/opcua_topology_apply.rs::compute_diff` doc block.
- **9-8-iter2-D2 (Edge E-H3-iter2):** Closure-replacement strict pin missing in `ac3_modified_device_metric_swap` — the post-modify fresh subscription proves the closure FIRED (via IP3-tightened `value_typed || status_non_good` check) but doesn't strictly distinguish "new closure with new metric_type" from "stale closure HashMap-overwritten by new closure". Verifying closure-content equivalence would require introspecting the private `SimpleNodeManagerImpl.read_cbs` registry — option-(a) impossible (see 9-8-iter1-D7); upstream FR pending. `tests/opcua_dynamic_address_space_apply.rs::ac3_modified_device_metric_swap`.
- **9-8-iter2-D3 (Edge E-H4-iter2):** Callback-teardown side-effect detection — `ac4_unaffected_subscription_continues_across_remove` does not assert that the removed device's leaked read-callback closure stops firing (it can't easily — stub returns `false`, closure persists in registry). Detecting stray storage reads on the removed device's metric_name would require InMemoryBackend instrumentation. Covered by 9-8-iter1-D7 (option-b leak doc). `tests/opcua_dynamic_address_space_apply.rs::ac4_unaffected_subscription_continues_across_remove`.
- **9-8-iter2-D4 (Blind B-H3-iter2):** `OnceLock` warn-once is process-global, not per-server. Across `cargo test` binaries running multiple test servers, only the first test-binary's first deletion fires the leak-observed log. Operationally correct for production single-server lifetime; deferred for tests where the once-only log gating could in theory mask coverage gaps but no test asserts on the log today. `src/opc_ua_history.rs::remove_read_callback` + `::remove_write_callback`.
- **9-8-iter2-D5 (Blind B-H4-iter2):** `ac3_modified_device_metric_swap` test commentary about "old subscription bound to deleted variable" is conceptually fragile because the NodeId is shared with the re-added variable post-Phase-3. Test correctness is intact (assertions verify the correct state); only the explanatory comment's framing is loose. `tests/opcua_dynamic_address_space_apply.rs::ac3_modified_device_metric_swap:760-765` comment.
- **9-8-iter2-D6 (Blind B-H5-iter2):** Phase 4 multi-rename failure not per-NodeId attributed — `manager.set_attributes` packs N renames; a single set_attributes failure returns one combined error without per-NodeId breakdown. Per iter-2 IP1 Phase 4 is now warn-and-continue (does not fail apply), so operationally fine — operators see the aggregate `address_space_rename_failed` event and retry. Per-NodeId attribution would require splitting `set_attributes` calls. `src/opcua_topology_apply.rs::apply_diff_to_address_space Phase 4 block`.

## Deferred from: code review of 9-8-dynamic-opc-ua-address-space-mutation — iter-3 (2026-05-14)

- **9-8-iter3-D1 (Blind B-H2-iter3):** Listener `mutation_succeeded` guard wildcard arm forward-maintenance risk — the current `Failed { .. } => true` wildcard correctly advances prev for all current Phase 2/3 failure reasons (REMOVE_FAILED, ADD_FAILED). However if a FUTURE Phase 1 failure path emits a NEW `failure_reason::*` constant (other than SET_ATTRIBUTES_FAILED), the wildcard would incorrectly route to "advance prev" creating the same replay-loop hazard iter-2 IP1 was designed to prevent. Mitigation paths: (a) exhaustive match on each reason constant (fragile against future reason additions), (b) add a `phase_id: u8` field to `AddressSpaceMutationOutcome::Failed` and route prev-advancement based on phase, (c) inline doc-comment + test asserting the wildcard never sees a NEW reason. Deferred — current code path doesn't trip; defensive concern for future maintainers. `src/config_reload.rs::run_opcua_config_listener mutation_succeeded match`.

- **9-8-iter3-D2 (Blind B-H4-iter3):** `failure_reason::SET_ATTRIBUTES_FAILED` constant name misleading for Phase 1 `set_values` call. Phase 1 actually calls `manager.set_values(...)` (not `set_attributes`); `set_attributes` is Phase 4's API. The shared constant name was a pre-iter-1 naming choice that became more confusing after iter-2 IP1 demoted Phase 4 (which is now the ONLY caller of `set_attributes` that could conceivably warrant this name). Cosmetic; renaming would break the iter-1+iter-2 audit-event reason-string contract (operators reading prior runbooks would see broken grep patterns). Deferred to v2 if operator forensics confusion surfaces — likely candidate name: `PHASE_1_SET_VALUES_FAILED` or `Q2_TRANSITION_FAILED`. `src/opcua_topology_apply.rs::failure_reason` module.

## Deferred from: code review of A-1-metrictype-payload-bearing-enum (2026-05-15)

- **A-1-iter1-DEF1 (Blind F1 + Edge F18):** `InMemoryBackend::load_all_metrics` reconstructs `MetricValue.value` from `metric_type.to_string()`, which post-A-1 is the discriminant string (`"Float"`, `"Int"`, etc.) rather than a real value. Warm-restart recovery produces degenerate `MetricValue { value: "Float", data_type: MetricType::Float(0.0) }` rows. Pre-existing pattern; A-5 rewrites read sites and removes `value: String`. `src/storage/memory.rs:204-222`.
- **A-1-iter1-DEF2 (Blind F2 + Edge F11):** Counter monotonic check `prev_metric.value.parse::<i64>()` is fragile under value-column encoding drift — only works for rows written via `batch_write_metrics` (real string), not rows written via `upsert_metric_value` (discriminant string). Pre-existing; A-3 plumbs typed payload through and the check can switch to inspecting `data_type` directly. `src/chirpstack.rs:1626-1636`.
- **A-1-iter1-DEF3 (Blind F4):** `opc_ua.rs::convert_variant_to_metric` discards the inbound Variant payload — typed `MetricType` side is hardcoded to zero defaults (`Int(0)`, `Float(0.0)`, `Bool(false)`, `String(String::new())`). The string side carries truth via `value.to_string()`. Explicit `TODO(A-4)` markers. A-4 closes when typed payload is wired through OPC UA Read. `src/opc_ua.rs:2018-2024`.
- **A-1-iter1-DEF4 (Blind F9 + Edge F20):** `chirpstack.rs::process_metrics` Bool arm at line 1640 binds `Some(s)` from `validate_bool_metric_value` but constructs `MetricType::Bool(false)`, dropping the parsed truth value. String-side `s.to_string()` carries truth. Explicit `TODO(A-3)`; A-3 closes. `src/chirpstack.rs:1640`.
- **A-1-iter1-DEF5 (Edge F3):** `SqliteBackend::set_metric` writes `serde_json::to_string(&value)` to the `value` column (`{"Float":0.0}` post-A-1); `upsert_metric_value` writes `value.to_string()` (`"Float"` post-A-1). Two encodings on the same column. Pre-existing — but post-A-1 their formats diverge (pre-A-1 both produced `"Float"`). A-2 schema migration v007 unifies via typed value columns. `src/storage/sqlite.rs:539-544 vs 839-840`.
- **A-1-iter1-DEF6 (Blind F5):** Divergent fractional-warn policy across chirpstack Counter arms — kind-driven path (line 1602-1622) emits no fractional warn, fallback path (line 1755-1760) does. When A-3 lifts the `TODO(A-3)` markers, the developer must unify the policy. Housekeeping concern. `src/chirpstack.rs:1602-1622 vs 1755-1760`.
- **A-1-iter1-DEF7 (Blind F6 + Edge F19):** `Storage::new` startup-defaults at `src/storage/mod.rs:998-1015` build a `MetricValueInternal` with parallel value-string arms (`"false"`, `"0"`, `"0.0"`, `""`) AND typed payload arms (`Bool(false)`, `Int(0)`, etc.) that must move together. A future contributor changing one and forgetting the other creates silent drift. Transitional; A-5 removes the value field. `src/storage/mod.rs:998-1015`.
- **A-1-iter1-DEF8 (Blind F8 + Edge F6 + Edge F10):** `FromStr` has no compile-time enforcement of the "pair with value source" contract. A caller that uses `let mt: MetricType = "Float".parse()?;` straight into a write path stores `Float(0.0)` permanently until overwritten. Spec option-(b) explicitly chose this trade-off; a newtype `DiscriminantOnly(MetricType)` refactor is out of A-1 scope. `src/storage/types.rs:67-77`.
- **A-1-iter1-DEF9 (Blind F10 + Edge F2 + Edge F7 + Edge F8 + Edge F22):** `MetricValue.value: String` / `MetricValue.data_type: MetricType` / `BatchMetricWrite { value, data_type }` are independent fields with no constructor enforcing consistency. Post-A-1 production writes deliberately set `value` from real input and `data_type` to a zero placeholder. Transitional; A-5 removes the value field and constructor pattern becomes natural. `src/storage/types.rs:96-117 + src/storage/mod.rs BatchMetricWrite + src/chirpstack.rs:1671-1677`.
- **A-1-iter1-DEF10 (Blind F15):** README "Current Version" entry is now a ~4000-char single-line paragraph with recursive PREVIOUS NARRATIVE / PRIOR / PRIOR-PRIOR nesting. CLAUDE.md mandates README mirror sprint-status, but the implementation is degrading reviewability. Housekeeping cleanup — possibly compress to bullet list or move detail to a CHANGELOG. `README.md:198`.
- **A-1-iter1-DEF11 (Blind F16):** `chirpstack.rs:1745-1781` four `OpcMetricTypeConfig::*` arms are structurally identical except for the variant placeholder. A-3 refactor risk: combining arms can silently lose per-variant validation (`"Counter metric has fractional value"`). Housekeeping. `src/chirpstack.rs:1745-1781`.
- **A-1-iter1-DEF12 (Blind F17 + F21):** `SqliteBackend` NaN filter at line 1444 uses `matches!(data_type, MetricType::Float(_))` + separate `value.parse::<f64>()`. Once A-2 makes the payload real, the natural rewrite is `matches!(data_type, MetricType::Float(f)) if !f.is_finite()` — but the diff's structure encourages keeping the string-parse path. Refactor landmine for A-2. `src/storage/sqlite.rs:1444`.
- **A-1-iter1-DEF13 (Blind F19):** Test assertions of the form `assert_eq!(actual, MetricType::String(String::new()))` (or `::Float(0.0)`, etc.) currently pass because all producers stamp zero payloads. Once A-3/A-4 wire real values, every such assertion will silently regress without compile-time signal. ~20+ assertions across test files. Bulk maintenance with A-3. `tests/metric_types_test.rs + src/storage/sqlite.rs::tests + others`.
- **A-1-iter1-DEF14 (Blind F24):** `cargo test --doc` has a 56-ignored baseline (issue #100). Doctests added in A-2/A-3 using zero-payload `MetricType::Float(0.0)` examples would harden the temporary placeholder contract. Hypothetical; monitor in subsequent stories. `src/storage/types.rs doc comments + future doctests`.
- **A-1-iter1-DEF15 (Blind F25):** `MetricValue.value` is `pub` and `TODO(A-5): remove` is a public-API SemVer break. The 2.0.0-rc bump in README arguably covers it, but the field's doc comment itself should note the breaking-change intent. Revisit at A-5 with formal CHANGELOG entry. `src/storage/types.rs:104-117`.
- **A-1-iter1-DEF16 (Blind F27):** `opc_ua_history.rs:386-414` trace-level row-skip log is too quiet for what is operationally a row-loss event (HistoryRead returns fewer rows than expected). After A-3 lands and `value` may genuinely disagree with `data_type`, this path could silently drop history rows. Promote to warn with an event-name field at A-5 rewrite. `src/opc_ua_history.rs:386-414`.
- **A-1-iter1-DEF17 (Edge F4):** `opc_ua.rs::convert_metric_to_variant` Float arm narrows f64 → f32 via `value as f32` without re-checking `is_finite()` after narrowing. The sibling path in `opc_ua_history.rs:390-397` does check (post-narrowing). Pre-existing; A-4 OPC UA Read hardening. `src/opc_ua.rs:1845-1859`.
- **A-1-iter1-DEF18 (Edge F5):** `chirpstack.rs` Int Counter arm casts non-finite f64 to i64 via `as` (saturating: NaN→0, Inf→i64::MAX). The fractional warn fires for NaN with misleading message ("Counter metric has fractional value; precision lost"). Pre-existing; A-3 should add an `is_finite()` guard. `src/chirpstack.rs:1654-1660`.
- **A-1-iter1-DEF19 (Edge F12):** `Storage::set_metric_value(&mut self, _, _, value: MetricValueInternal)` accepts any caller-built struct without cross-field invariant check (e.g., `value = "999.9"` + `data_type = MetricType::Int(42)` would be accepted). With payload-bearing data_type now present, a third disagreement axis is possible. Constructor-pattern refactor with A-5. `src/storage/mod.rs:1268-1300`.
- **A-1-iter1-DEF20 (Edge F14):** `Variant::String(value) → value.to_string()` for an async-opcua null `UAString` returns `""`, indistinguishable from a legitimate empty string. Discriminant-only payload makes the difference unrecoverable. Pre-existing; A-4. `src/opc_ua.rs:2020`.
- **A-1-iter1-DEF21 (Edge F15):** `Variant::Int32` and `Variant::Int64` both collapse to `MetricType::Int(0)` — the bit-width distinction is permanently lost in the typed enum. The reverse path uses `i32::try_from(value)` to recover. Post-A-3 once `MetricType::Int(i64)` carries the real value, an OPC UA write of Int32 followed by a read can yield Int64 if magnitude exceeds i32::MAX. `src/opc_ua.rs:2016-2017`.
- **A-1-iter1-DEF22 (Edge F16):** Boolean variant from invalid `value` string (e.g., "yes", "1", "on") in `convert_metric_to_variant` defaults to `false` with `warn!` but no event-name field for grep. Pre-existing. `src/opc_ua.rs:1862-1873`.

## Deferred from: code review of A-1-metrictype-payload-bearing-enum — iter-2 (2026-05-15)

- **A-1-iter2-DEF1 (Blind F3):** New `InMemoryBackend` round-trip tests in `src/storage/memory.rs::tests` never assert the "missing-device returns error" path documented in the trait doc. The 4 tests call `set_metric` on `"dev1"` without registering the device first; the test relies on `InMemoryBackend::set_metric` auto-creating the device entry. Edge case unexercised: missing-device contract drift between trait doc and impl. `src/storage/memory.rs:517-607`.
- **A-1-iter2-DEF2 (Blind F4):** `test_set_then_get_bool_metric_roundtrips_payload` carries an assertion message claiming it "guards against the discriminant-only regression," but the `Bool(true)` half alone cannot catch a backend that always returned `Bool(true)`. The combined `on`/`off` pair catches the regression; the message attached only to the `on` half is technically overstated. `src/storage/memory.rs:543-548`.
- **A-1-iter2-DEF3 (Blind F6):** `test_metric_type_payload_roundtrip_boundary_values` NaN check only exercises `Clone`, not an `InMemoryBackend` round-trip. The test name suggests storage coverage but the body validates only the enum's own Clone implementation. A backend that flattened NaN→0.0 on write would not be caught. `src/storage/types.rs:319-323`.
- **A-1-iter2-DEF4 (Blind F7):** `Cargo.toml` `version = "2.0.0-rc"` lacks numeric suffix (e.g. `-rc.1`). Per SemVer precedence rule, `2.0.0-rc < 2.0.0-rc.1` because the right-hand side has more dot-separated identifiers — so subsequent RCs sort correctly. But many cargo registry indexers and docs.rs display rules assume dot-numeric form. Phase B Closure framing in the Dev Agent Record explicitly chose `2.0.0-rc` (no suffix) — confirmed acceptable. Revisit before publishing if a second RC ships. `Cargo.toml:3`.
- **A-1-iter2-DEF5 (Blind F9):** `MetricValueInternal` doc claims `MetricValue` and `MetricValueInternal` "must move together to preserve the harmonisation contract" but no test enforces the invariant. A future A-5 patch that removes one field but not the other would silently desync. `src/storage/mod.rs:802-808`.
- **A-1-iter2-DEF6 (Blind F10):** 4 `TODO(A-2)` comments in `sqlite.rs` (lines 540, 845, 964, 1070) are near-verbatim duplicates of the same staging concern. When A-2 removes one, the migrator may miss the others and leave orphan TODOs claiming option-(b) is still in effect. Consider extracting to a single canonical const + cross-references. `src/storage/sqlite.rs`.
- **A-1-iter2-DEF7 (Blind F14):** `String("\0\u{FFFD}embedded")` test payloads run only against `InMemoryBackend::set_metric` → `get_metric` and `MetricType::clone()`. They never exercise SQLite's NUL-handling (some SQLite drivers treat `TEXT` as NUL-terminated; `rusqlite` does not, but bindings vary). For meaningful embedded-NUL preservation coverage, A-2/A-3 should add a SQLite-backed integration test. `src/storage/memory.rs:595-602` + `src/storage/types.rs:339-343`.
- **A-1-iter2-DEF8 (Blind F15):** New trait doc comments name `src/opc_ua_history.rs::history_read_raw_modified` as a downstream consumer that A-5 will rewrite. The path was not grep-verified during the patch round — stale cross-references rot fast in a multi-epic codebase. A-5 should grep the actual function name + add a doc test or assertion if drift is detected. `src/storage/mod.rs + src/storage/types.rs`.
- **A-1-iter2-DEF9 (Blind F17):** P9's `unimplemented!()` → `todo!()` body deletion (~80 lines of `store_metric` dispatch) moves the original kind→variant + bool 0/1 validation + int fractional warn logic into git-history-only storage. Story A-3 must `git show 16e7811:src/chirpstack.rs` and manually reconstruct. Risk: A-3 re-implements differently and possibly worse. Mitigation: A-3 dev agent should be briefed to reference the historical commit. `src/chirpstack.rs:1714-1736`.
- **A-1-iter2-DEF10 (Blind F18):** `test_metric_type_display_pins_discriminant_only_contract` covers Float/Int/Bool/String only. If a future patch adds a `MetricType::Bytes(Vec<u8>)` variant, the contract test silently passes while the new variant's Display output is uncovered. Hardening: use `match v { Float(_) | Int(_) | Bool(_) | String(_) => {} }` exhaustive match somewhere in the test to force test updates when variants change. `src/storage/types.rs::test_metric_type_display_pins_discriminant_only_contract`.
- **A-1-iter2-DEF11 (Blind F19):** `TODO(A-N)` marker form varies — some sites use `TODO(A-4/A-6):`. A grep for the exact `TODO(A-4)` pattern won't find the slash-form. Standardise to one canonical form (e.g. `TODO(A-4): ...` + separate `TODO(A-6): ...` lines when multiple stories close the same site). Future-story-owner ergonomics. Multiple files.
- **A-1-iter2-DEF12 (Edge F6+F7+F8+F9):** Boundary coverage holes — subnormals (`f64::from_bits(1)`), signaling NaN, `f64::MIN` / `f64::MIN_POSITIVE`, `set_metric` overwrite (write same key twice), cross-device same-metric-name (composite-key independence), empty `device_id` / `metric_name` (asymmetric InMemoryBackend vs SqliteBackend validation). Each is a meaningful boundary class the iter-1 P4 patch didn't cover. A-3/A-5 test pass to address holistically. `src/storage/memory.rs::tests + src/storage/types.rs::tests`.

## Deferred from: code review of A-1-metrictype-payload-bearing-enum — iter-3 (2026-05-15)

- **A-1-iter3-DEF1 (Blind F3):** `get_metric_value` trait doc says "single-row write methods (`set_metric`, `upsert_metric_value`) may not [populate the `MetricValue`-shaped store]"; SqliteBackend impl doc says they DO write a row (with discriminant content). Different failure modes — "row absent" (InMemoryBackend) vs "row present, wrong content" (SqliteBackend). Tighten trait doc at A-5 cleanup with explicit per-backend table. LOW. `src/storage/mod.rs::StorageBackend::get_metric_value`.
- **A-1-iter3-DEF2 (Blind F5):** `todo!("store_metric body to be reinstated by Story A-3 — see review patch P9 in A-1-metrictype-payload-bearing-enum.md § Review Findings")` references internal review-cycle doc path that operators can't resolve. Replace with public-facing breadcrumb (issue # or Story A-3) at A-3 implementation time. LOW. `src/chirpstack.rs::store_metric`.
- **A-1-iter3-DEF3 (Blind F7):** `InMemoryBackend::set_metric` auto-creates devices on first write; trait doc says "If the device does not exist, returns an error." Trait/impl divergence — either tighten the trait doc to "may auto-create per impl" or harden the InMemoryBackend impl to reject missing devices. A-5 cleanup target. LOW. `src/storage/mod.rs::StorageBackend::set_metric` vs `src/storage/memory.rs::InMemoryBackend::set_metric`.
- **A-1-iter3-DEF4 (Blind F9):** `tests/metric_types_test.rs` tautological AC#1/AC#2/AC#3 assertions (`MetricType::Float(0.0) == MetricType::Float(0.0)`) marked with TODO(A-3) comments in iter-1 P8 — but tests still PASS and contribute to the AC#1/2/3 green-bar signal. A-3 should mark them `#[ignore = "A-3 to replace with real assertion"]` until the real kind→variant mapping is exercised. LOW. `tests/metric_types_test.rs:67-106`.
- **A-1-iter3-DEF5 (Blind F10):** Three A-1 staging caveat blocks (MetricValue.data_type doc, MetricValueInternal struct-level doc, BatchMetricWrite struct-level doc) plus the StorageBackend trait method docs each restate the dual-storage transitional contract independently. At A-5 cleanup, extract to a module-level rustdoc anchor (e.g., `## A-1 Transitional Storage Contract` section in `src/storage/types.rs` module docs) and link from each field doc. LOW. `src/storage/types.rs + src/storage/mod.rs`.
- **A-1-iter3-DEF6 (Blind F11):** README:77 references `docker run ghcr.io/guycorbaz/opcgw:2.0.0-rc` but no evidence the image tag has been built and pushed to GHCR. Out of A-1 scope (release-process concern). Mark the README as "after release" or coordinate with the CI workflow that publishes ghcr images. LOW. `README.md:77`.
- **A-1-iter3-DEF7 (Blind F12):** `convert_variant_to_metric` top-line summary reads "Converts an OPC UA Variant to a `(value_string, MetricType)` tuple"; the fact that the typed `MetricType` payload is zero-defaulted (the load-bearing data lives in the `value_string` half) is buried in a transitional caveat block. Move the warning to the top-line or `# Returns` header at A-4 cleanup. LOW. `src/opc_ua.rs::convert_variant_to_metric`.
- **A-1-iter3-DEF8 (Edge F3 — MEDIUM, MUST address in A-3):** `SqliteBackend::set_metric` calls `serde_json::to_string(&value)` which rejects NaN/Inf by default with `Error: NaN is not a valid JSON number`. Iter-2's IR8 boundary roundtrip test established `MetricType::Float(f64::NAN)` as a valid payload of the post-A-1 enum, but no test exercises the SqliteBackend persistence path on NaN. Today unreachable because the poller stamps `Float(0.0)`. **Once Story A-3 wires real ChirpStack readings (`metric.datasets[0].data[0]: f32 → f64`), any sensor emitting NaN (out-of-range, calibration error, missing reading) will cause `set_metric` to return `OpcGwError::Database("Failed to serialize metric value: ...")` — silent metric loss with noisy log.** A-3 must decide policy: (a) filter NaN/Inf at the poller before constructing `MetricType::Float(...)`, (b) use a `serde_json` mode that allows NaN/Inf (potentially fragile — JSON-extended text), or (c) add an explicit `!value.is_finite()` guard at `SqliteBackend::set_metric` that returns a clean operator-facing `OpcGwError::Storage(...)` before reaching serde_json. **User explicitly accepted iter-3 deferral on 2026-05-15. Severity MEDIUM preserved for A-3 attention.** `src/storage/sqlite.rs::set_metric:549`.
- **A-1-iter3-DEF9 (Edge F4):** `store_metric` function-level doc retains a `# Arguments` section describing `device_id` and `metric` as if they were used by the body, but the body is `todo!()` and parameters are underscored. Minor cosmetic voice mismatch — could remove the `# Arguments` section entirely when A-3 reinstates the body (which will redefine the contract anyway). LOW. `src/chirpstack.rs::store_metric` doc.
- **A-1-iter3-DEF10 (Edge F6):** `MetricValueInternal::ToSql`/`FromSql` (`src/storage/mod.rs:846-869`) serialize the full enum via serde_json; post-A-1 `data_type: MetricType` round-trips as `{"Float":0.0}` (externally-tagged). No migration shim for pre-A-1 JSON rows that may exist in sidecar tables or backups. Hazard is dormant because `MetricValueInternal::FromSql` is currently `#![allow(dead_code)]`-gated. A-2's schema migration must verify no production deployment has pre-A-1 JSON rows before activating this path. LOW. `src/storage/mod.rs:846-869`.
- **A-1-iter3-DEF11 (Auditor CF4 — already DEF-iter2-5):** "MetricValueInternal and MetricValue must move together" invariant not enforced by any test. A future A-5 patch that removes one field but not the other would silently desync. Add a test pin at A-5 if possible (e.g., reflection-based field-name comparison, or a build script that fails if the two structs' field names diverge). LOW. `src/storage/mod.rs::MetricValueInternal` + `src/storage/types.rs::MetricValue`.

## Deferred from: code review of A-2-sqlite-schema-migration-v007 — iter-1 (2026-05-15)

- **A-2-iter1-DEF-IH1 (Blind F9 + F10, HIGH — user-confirmed deferral 2026-05-15):** Migration execution is not atomic. `schema::run_migrations` calls `conn.execute_batch(MIGRATION_V0NN)` followed by a separate `conn.pragma_update("user_version", N)` — same shape across v001 through v007. If `execute_batch` succeeds but `pragma_update` fails (e.g. concurrent connection holding a lock at the moment of update, process kill, disk full), the schema is upgraded but `user_version` remains at the prior version; the next runner invocation re-applies the migration and fails with "duplicate column name" on `ALTER TABLE ADD COLUMN`. Similarly, if the 10 `ALTER TABLE` statements in v007 partially succeed (statement N fails after N-1 committed), the migration leaves the DB in a half-upgraded state. The right fix is to wrap each migration block in an explicit `BEGIN TRANSACTION` + `COMMIT` and include the `pragma_update` inside that transaction. Out of A-2 scope because the same fix touches v001-v006 runner blocks; wrapping only v007 while leaving v001-v006 unwrapped would be asymmetric and lock in technical debt. **Recommended for a focused runner-hardening story (or A-7's cleanup migration).** `src/storage/schema.rs::run_migrations` + every `MIGRATION_V0NN.sql` file.

- **A-2-iter1-DEF1 (Edge F11):** Pre-existing heterogeneous legacy `value` lexemes across the three SqliteBackend writers. `set_metric` writes `serde_json::to_string(&value)` (e.g. `{"Float":0.0}`); `upsert_metric_value` and `append_metric_history` write `value.to_string()` (the discriminant string, e.g. `"Float"`); `batch_write_metrics` writes `BatchMetricWrite.value` (the real string-encoded sensor reading, e.g. `"23.5"`). Pre-A-3 reads tolerate this only because `query_metric_history` skips rows that fail to parse (Story 8-3 partial-success contract). A-3 will face all three shapes in `value_type = 'legacy'` rows; the typed-column write must handle each shape consistently. The A-2 test `test_v007_preserves_pre_a2_rows` only seeds the third shape (real-string format); A-3 must add coverage for the JSON and discriminant shapes when designing the writer migration. `src/storage/sqlite.rs::set_metric:540 + ::upsert_metric_value:839 + ::append_metric_history:951 + ::batch_write_metrics:1062`.

- **A-2-iter1-DEF2 (Blind F7 + F13 + Edge F13 convergent):** No cross-column CHECK constraint enforces the typed-column / `value_type` consistency invariant (e.g. `value_type = 'Float' AND value_real IS NOT NULL AND value_int IS NULL AND value_bool IS NULL AND value_text IS NULL`). The current schema allows `value_type = 'Float'` with ALL typed columns NULL (defeats type-fidelity) or `value_type = 'legacy'` with `value_real` non-NULL (semantic contradiction). Spec § Dev Notes "Why NOT to enforce exactly-one-non-NULL in A-2" explicitly defers this — adding it now would force every legacy-shaped INSERT to fail because writers don't yet populate typed columns. **Right place is A-3 (when writers populate typed columns and the invariant becomes provable) or A-7 cleanup migration.** `migrations/v007_typed_value_columns.sql`.

- **A-2-iter1-DEF3 (Blind F23):** No index on `value_type`. Stories A-4 (OPC UA Read) and A-5 (HistoryRead) will read by `(device_id, metric_name)` and post-filter on `value_type`; with growing `metric_history` the post-filter becomes expensive. Spec § Out of Scope explicitly defers indexing to A-4/A-5. Recommended partial index: `CREATE INDEX idx_metric_history_typed ON metric_history(device_id, metric_name, recorded_at) WHERE value_type != 'legacy';` once typed rows accumulate. `migrations/` future v008+.

- **A-2-iter1-DEF4 (Blind F8):** Dual source of truth for the variant discriminant — legacy `data_type TEXT NOT NULL` (no CHECK) and new `value_type TEXT NOT NULL CHECK(...)`. Pre-A-3 writers populate `data_type` only; A-3 writers will populate BOTH (the legacy `data_type` for backwards compat + the new `value_type` for typed reads). Drift between the two is undetectable until A-5/A-7 retires the legacy column. **Mitigation in A-3:** add a `DEBUG-ASSERT` or per-write invariant `assert_eq!(data_type, value_type)`. **A-7 retires `data_type` once readers move.** `src/storage/sqlite.rs` write paths.

- **A-2-iter1-DEF5 (Blind F11 + F12, pre-existing issue #102):** `temp_db()` uses `uuid::Uuid::new_v4()` for path generation (verified non-colliding for practical purposes), but tests rely on best-effort `let _ = fs::remove_file(&path)` cleanup that's never reached on assertion-panic paths. Issue #102 tracks `tests/common/mod.rs` extraction including a `Drop`-impl RAII guard for temp DB files. A-2 inherits the pattern; the gap accumulates orphan `.db` files in `/tmp/` on flaky CI runs. **Recommended:** when issue #102 lands, refactor `temp_db()` to return a `TempDb` struct with `Drop`. Multiple files.

- **A-2-iter1-DEF6 (Blind F18):** No explicit second-run idempotency test for `run_migrations` on a v007-ready DB. `test_run_migrations_idempotent` runs twice on a fresh DB which lands at v7 on first call and stays at v7 on second; that does pin idempotency for a single `run_migrations` instance, but doesn't pin behaviour across a fresh DB → v007 → restart → v007 chain. **Hypothetical defence;** the `if current_version < 7` guard is provably idempotent by construction. LOW. `src/storage/schema.rs::tests`.

- **A-2-iter1-DEF7 (Blind F26 + Edge F7):** Several A-2 tests open multiple `Connection` handles to the same SQLite file (`temp_db()` returns one, `SqliteBackend::new` opens another, raw projection opens a third). SQLite's WAL mode handles this gracefully but the lifecycle is non-obvious. The `_conn` shadow binding in `test_v007_writers_still_populate_legacy_columns` and `test_v007_readers_still_read_legacy_columns` is dead weight — kept alive by Rust scoping until function exit. **Cleanup recommended:** split `temp_db()` into a "path only" variant or wrap in a `Drop` guard. Same issue #102 territory. `src/storage/schema.rs::test_v007_writers_*`.

- **A-2-iter1-DEF8 (Edge F8):** `create_v006_baseline_db` only checks `pre_version == 6` to verify the baseline matches a fresh v006 DB. It does not assert the full table shape (column inventory, index inventory, constraint inventory) matches what `MIGRATION_V001`..`V006` would produce on a fresh DB. Today this is moot because v007 is column-only DDL — but if a future v007-equivalent migration adds an index or trigger, the rollback via `DROP COLUMN` won't undo those and the baseline diverges. **Mitigation when needed:** swap the DROP-COLUMN-rollback to manual v001-v006 SQL execution. `src/storage/schema.rs::create_v006_baseline_db`.

- **A-2-iter1-DEF9 (Blind F22):** SQL comment in `v007_typed_value_columns.sql` says "Pre-A-2 rows are tagged value_type='legacy' via the column default — no UPDATE statement is required." This is true only because the default is a literal constant (`'legacy'`). If a future maintainer copies this pattern with a non-constant default (e.g. `DEFAULT (datetime('now'))`), SQLite handles them differently. The comment doesn't warn. **Cosmetic; document the constant-default constraint when the next migration is authored.** `migrations/v007_typed_value_columns.sql`.

- **A-2-iter1-DEF10 (Blind F19):** Test `test_v007_adds_all_typed_columns_to_metric_values` and `test_v007_adds_all_typed_columns_to_metric_history` include a comment `// PRAGMA table_info returns: cid, name, type, notnull, dflt_value, pk` — the comment is informational but `cid`, `dflt_value`, and `pk` are not used by the query (which projects only `type` and `notnull`). Trim the comment or query the extra columns to match. **Cosmetic.** `src/storage/schema.rs::test_v007_adds_all_typed_columns_*`.

- **A-2-iter1-DEF11 (Blind F21):** `migrations/v007_typed_value_columns.sql` uses explicit `NULL` qualifier on nullable columns. SQLite treats this as redundant (NULL is the default for non-PK columns). Other migrations omit the `NULL` keyword. **Cosmetic style inconsistency;** harmonise when v008+ is authored. `migrations/v007_typed_value_columns.sql`.

- **A-2-iter1-DEF12 (Blind F5, Edge F5):** `pragma_table_info('metric_values')` queries in tests inline the column name into the SQL string via `format!()`. The inline pattern is consistent with the rest of the file but bypasses parameterization. Today safe (column names are hardcoded test literals); future refactor that derives column names from runtime would be SQL-injectable. **Cosmetic;** convert to `params!` when next touched. `src/storage/schema.rs::test_v007_adds_all_typed_columns_*`.

- **A-2-iter1-DEF13 (Auditor Cross-AC AC#13):** The new `migrations/v007_typed_value_columns.sql` carries SPDX + copyright headers per the AC#13 example and CLAUDE.md project-wide convention ("SPDX license headers ... in each source file"). However, `migrations/v002_add_command_fields.sql`, `v003_make_payload_optional.sql`, `v005_gateway_status.sql`, and `v006_gateway_status_health_metrics.sql` lack SPDX headers — they predate the convention. AC#13's "Matches the format used by v005/v006" cross-reference is inconsistent with its own SPDX-header example block; the user explicitly accepted the dev's CLAUDE.md-conforming choice. **Backfill v002/v003/v005/v006 with SPDX headers in a focused housekeeping commit** to make all migration files uniform. `migrations/v002_*.sql + v003_*.sql + v005_*.sql + v006_*.sql`.

- **A-2-iter1-DEF14 (Blind F28):** Migration filename versioning relies on coordinator-side awareness — if another open PR introduces `v007_*.sql` for an unrelated feature, both branches would conflict on `LATEST_VERSION`, the runner block, and the user_version pragma. No conflict detected at A-2's commit time (`c31cad5` is the latest commit; no other v007 in flight). **Operational concern; document the migration-numbering coordination protocol if a multi-developer rhythm emerges.** Process-level, not source-level.

- **A-2-iter1-DEF15 (Blind F29):** No rollback migration provided. The `create_v006_baseline_db` test helper documents the inverse DDL pattern (DROP COLUMN per column + `PRAGMA user_version = 6`), but no operator-facing rollback script. **A-7 migration runbook covers this.** `docs/deployment-guide.md` (A-7 territory).

- **A-2-iter1-DEF16 (Blind F30):** `src/storage/schema.rs::tests` imports `crate::storage::sqlite::SqliteBackend` for the writer/reader contract tests. The schema unit-test module is coupling to higher-level trait implementations. **Architectural cleanup:** when issue #102 (`tests/common/mod.rs` extraction) lands, consider relocating writer-contract tests to `src/storage/sqlite.rs::tests`. `src/storage/schema.rs::test_v007_writers_*` and `::test_v007_readers_*`.

- **A-2-iter1-DEF17 (Blind F31):** `info!(version = 7, "Applied migration v007_typed_value_columns")` logs success but there is no `warn!` or `error!` between `execute_batch` and `pragma_update`. If `execute_batch` succeeds and `pragma_update` fails, the operator sees the success log line plus the wrapped `OpcGwError::Database` — no explicit "partially-applied, manual recovery required" message. **Convergent with DEF-IH1; handle in the runner-hardening story.** `src/storage/schema.rs::run_migrations`.

- **A-2-iter1-DEF18 (Edge F2):** `value_type = NULL` on an explicit INSERT triggers the NOT NULL constraint, not the CHECK constraint (because `NULL IN (...)` evaluates to NULL). Today both layers reject NULL; if a future migration drops NOT NULL, the CHECK alone would silently accept NULL. **Hypothetical;** no test pins this two-constraint interaction. LOW. `migrations/v007_typed_value_columns.sql + tests`.

- **A-2-iter1-DEF19 (Edge F6):** `test_v007_migration_under_5s_for_10k_rows` couples to wall-clock time without a retry/median window. 5 s ceiling is generous (metadata-only DDL completes in <100 ms), so practical flake risk is near zero — but the test could be marked `#[ignore]` per the `test_pool_throughput_under_load` precedent (commit `2c5a6b1`). **Defensive only;** monitor CI for flake reports. `src/storage/schema.rs::test_v007_migration_under_5s_for_10k_rows`.

- **A-2-iter1-DEF20 (Edge F9):** `src/opc_ua.rs:1923` has a local binding `_value_type` (unrelated to the new schema column) that creates a grep-confusion overlap. Pre-existing; A-2 promotes `value_type` from a one-off local into a project-wide identifier without renaming the OPC UA local. **Cosmetic;** rename in A-4 or later. `src/opc_ua.rs:1923`.

- **A-2-iter1-DEF21 (Edge F10):** `if current_version < 7` guard does not detect schema-drift where `user_version = 7` but typed columns are missing (e.g. operator manually `PRAGMA user_version = 7` on a v006 DB). Pre-existing pattern for all migrations. **Hypothetical operator-error scenario; bundle into runner-hardening story.** `src/storage/schema.rs::run_migrations`.

- **A-2-iter1-DEF22 (Blind F27):** `test_v007_preserves_pre_a2_rows` seeds 8 rows total (3 + 5). AC#6 says "≥3 metric_values + ≥5 metric_history covering all 4 data_type variants" — satisfied per the combined cross-table coverage. Per-table String coverage is not in `metric_values` (only Float/Int/Bool); it exists in `metric_history` (line "label/OK/String"). **Edge case test economy;** spec floor is met, recommend adding a String row to metric_values seed in a follow-up if every variant per table becomes a requirement.

- **A-2-iter1-DEF23 (Blind F24):** `metric_values` seeds use timestamp `'2024-01-01T00:00:00Z'` repeated three times across different `(device_id, metric_name)` pairs. The `metric_values` PRIMARY KEY is `(device_id, metric_name)` — timestamp duplication is not a uniqueness concern. `metric_history` seeds vary timestamps within `dev1.temperature` rows. **Schema assumption documented;** no test failure.

## Deferred from: code review of A-4-opc-ua-read-value-payload-pipeline — iter-1 (2026-05-16)

- **DEF-iter1-A4-1 (Blind B-H7):** `convert_variant_to_metric` returns `(String, MetricType)` tuple where both halves are constructed from the inbound Variant. Cross-version readers (one half from pre-A-4 zero-default era, other half from post-A-4 real-value era) could see inconsistent pairs — hypothetical migration concern, not a regression. Defer. `src/opc_ua.rs:2030-2046`.
- **DEF-iter1-A4-2 (Blind B-H10 + Edge F11):** Integration test `tests/opc_ua_read_typed_payload.rs:124-189` seeds legacy rows via bare `rusqlite::Connection::open(&db.path)`, bypassing the `SqliteBackend` connection pool's per-connection PRAGMAs. Passes today; future per-connection CHECK-enforcement PRAGMAs (e.g. `defer_foreign_keys`, `integrity_check`) would create asymmetry between insert-via-bare-conn and read-via-pool. Recommended fix: switch to `backend.pool.checkout()` for legacy-row seeding when issue #102 (tests/common extraction) lands.
- **DEF-iter1-A4-3 (Blind B-H13):** Chirpstack monotonic legacy fallback `prev_metric.value.parse::<i64>().ok()` is unreachable for SqliteBackend `legacy` rows post-A-4 (those rows surface as `Ok(None)` from `get_metric_value` before the fallback can examine `.value`). The fallback's actual reach is narrower than the chirpstack.rs:1699-1706 comment claims. Update the comment to acknowledge the legacy-row pre-filter when the chirpstack monotonic check is next touched. Cosmetic. `src/chirpstack.rs:1699-1706`.
- **DEF-iter1-A4-4 (Blind B-H14):** `metric_type_from_typed_columns` takes `device_id: &str` + `metric_name: &str` purely to embed in error messages via `format!`. Minor API smell — helper is coupled to caller's identifier-naming convention. Defer; consider extracting `OpcGwError::SchemaDrift { value_type, expected_column }` variant when next touched. `src/storage/sqlite.rs:126-171`.
- **DEF-iter1-A4-5 (Blind B-H17):** `OpcUa::convert_metric_to_variant` no longer returns `Variant::Int32(0)` fallback for Int parse-failure (the pre-A-4 caller could rely on it as a sentinel). Post-A-4 the Int arm always returns the real value. Behavioral change with no version bump on the function signature. Defer — the pre-A-4 sentinel was a parse-error indicator that's structurally impossible under the new typed-payload contract. `src/opc_ua.rs:1842-1855`.
- **DEF-iter1-A4-6 (Blind B-H18):** `prev_metric.value` parse + `&prev_metric.data_type` borrow at chirpstack.rs:1707-1709 reference fields of the same owned struct snapshot — semantically consistent within the function. The comment at 1699-1704 admits the two halves may historically be inconsistent (different writers), but the fallback arm produces silently inconsistent results in that case. Already covered by IR4 (decision-needed) — defer separately as a documentation-only concern.
- **DEF-iter1-A4-7 (Blind B-H19):** Test naming convention asymmetry — in-mod tests use `test_` prefix, integration tests in `tests/opc_ua_read_typed_payload.rs` don't. Cosmetic. Defer.
- **DEF-iter1-A4-8 (Blind B-H20):** `make_metric_value` test helper at src/opc_ua.rs:2926-2933 hides "value: ignored" magic string in a fixture. Future refactor could replace with `value: ""` (empty) which would silently break the regression contract for `test_convert_metric_to_variant_does_not_read_value_field`. Add a `// LOAD-BEARING: must remain non-empty AND non-numeric — proves convert_metric_to_variant doesn't parse metric.value` comment above the helper when next touched.
- **DEF-iter1-A4-9 (Blind B-H21):** `__op.ok()` not called on the schema-drift `Err` path inside `get_metric` / `get_metric_value`. Semantically correct (Err = failure for storage op-log telemetry); the StorageOpLog Drop impl emits `success=false`. Defer. `src/storage/sqlite.rs:542-552, 612-622`.
- **DEF-iter1-A4-10 (Blind B-H22):** `metric_read` event name is generic for a single-reason closed-enum (`reason="narrowing_overflow"` after iter-1; `reason="narrowing_underflow"` after IR7 patch). Future expansion (precision_loss, etc.) would fit; the umbrella name is intentional. Defer.
- **DEF-iter1-A4-11 (Blind B-H23):** Doc warning at `convert_metric_to_variant` (src/opc_ua.rs:1810-1826) about not re-introducing `metric.value.parse::<…>()` is preserved only in docstring + `test_convert_metric_to_variant_does_not_read_value_field`. A `git grep` check in CI for `metric\.value\.parse` in `convert_metric_to_variant`-adjacent code would harden the contract. Cross-cutting tooling concern. Defer.
- **DEF-iter1-A4-12 (Blind B-H24):** `value_type` is stringly-typed; no compile-time enum. A typed Rust enum `SqliteValueType { Float, Int, Bool, String, Legacy }` would make the closed-enum nature compile-time enforced and eliminate the stringly-typed match risks. Defer to A-7 cleanup migration. `src/storage/sqlite.rs:135-156`.
- **DEF-iter1-A4-13 (Blind B-H25):** `MetricType::Bool(b)` (Copy) vs `MetricType::String(s)` (move) asymmetry in `convert_metric_to_variant` — language idiom, not a behavior concern. Defer. `src/opc_ua.rs:1872-1873`.
- **DEF-iter1-A4-14 (Blind B-H26):** `let new_int = raw_value as i64` at chirpstack.rs:1712 lacks LOCAL defensive guard. Upstream `int_overflow` guard at chirpstack.rs:1633-1646 catches NaN/Inf/saturating-cast for Counter — current line is safe under invariant. Future refactor risk only. Defer.
- **DEF-iter1-A4-15 (Edge F3):** Schema-drift `value_type='legacy'` with orphaned `value_real=Some(...)` silently dropped by `metric_type_from_typed_columns`. v008 CHECK forbids this combination; only reachable via manual SQL / restored backup. Hypothetical. Defer.
- **DEF-iter1-A4-16 (Edge F5 / Blind B-H6):** Silent f64→f32 precision loss for finite in-range values (e.g. 1.234567890_f64 → 1.2345679_f32). Pre-existing OPC UA spec design — Variant::Float = f32 by definition. Not A-4-specific; widening to Variant::Double would be a separate decision. Defer.
- **DEF-iter1-A4-17 (Edge F7):** `load_all_metrics` schema-drift skip increments `skipped_count` and returns `Ok(result)` with no caller-signal that the cache is incomplete. Per IR5 patch, legacy-skip will surface at info-level — but schema-drift-skip remains at error-level only. Caller (startup-restore) doesn't see a count of failed rows. Defer.
- **DEF-iter1-A4-18 (Edge F9):** `get_metric_value` timestamp-parse error returns `OpcGwError::Database` (`BadInternalError` at OPC UA boundary) while `load_all_metrics` tolerates with `Utc::now()` fallback (`Good` status). Asymmetric tolerance. Pre-existing behavior; A-4 didn't change the timestamp parse logic. Defer.
- **DEF-iter1-A4-19 (Auditor AC#7):** Missing `test_get_value_uses_post_reload_stale_threshold` test promised by spec line ~356. 9-7 carry-forward test at `tests/config_hot_reload.rs::ac10_stale_threshold_seconds_is_hot_reload_safe` exists and covers the contract — no A-4-specific test needed. Defer.
- **DEF-iter1-A4-20 (Auditor load-bearing-L6 / A-1-iter1-DEF10 carry-forward):** README "Current Version" line ~5000+ chars on a single line. Reviewability degrades; A-1-iter1-DEF10 already flagged this pattern. Pre-existing housekeeping concern. Defer.

## Deferred from: code review of A-5-opc-ua-historyread-value-payload-pipeline (2026-05-17)

- **DEF-iter1-A5-W1 (Blind L2):** `query_metric_history` SELECT no longer reads `data_type` column [src/storage/sqlite.rs:1709-1710]. If a future migration drops it (per the inline comment), the v008 CHECK referencing the table-recreate pattern would need to be reapplied — easy to miss. Defer to A-7 (migration runbook + version-gated migration script).
- **DEF-iter1-A5-W2 (Blind L6):** Int32 → Int64 silent widening on round-trip [src/opc_ua.rs:2074-2075]. A client that writes `Variant::Int32(42)` reads back `Variant::Int64(42)` — silent type widening with no contract record. Pre-existing OPC UA design; not A-5 scope.
- **DEF-iter1-A5-W3 (Blind L7):** `test_batch_write_metrics_populates_typed_columns_all_variants` assertion bakes in soon-to-rot fact about the legacy `value` column carrying the discriminant string [src/storage/sqlite.rs:5337]. If A-7 drops the column the assertion will need retirement. Transitional; defer to A-7.
- **DEF-iter1-A5-W4 (Blind L9):** `InMemoryBackend::load_all_metrics` stamps `Utc::now()` for every recovered metric's timestamp [src/storage/memory.rs:218]. Pre-A-5 the legacy `value: metric_type.to_string()` was at least an obvious sentinel; post-A-5 the bogus `Utc::now()` looks like a fresh measurement. Pre-existing semantic, not A-5-specific. Defer (likely closes when InMemoryBackend gains ingest-timestamp tracking — out of Epic A scope).
- **DEF-iter1-A5-W5 (Blind L17 / Edge):** `convert_variant_to_metric` rejects `UInt32`/`UInt64`/`Byte`/`SByte`/`Int16`/`UInt16` variants as `BadTypeMismatch` [src/opc_ua.rs:2072-2085]. Per OPC UA Part 4 §7.13, implicit type conversion would upcast these to Int. Pre-existing interop hazard, unchanged by A-5; revisit when a SCADA-client integration surfaces the issue.

### User-explicit acceptances (A-5 iter-1 decisions)

- **DEF-iter1-A5-D1 (Auditor AMBIGUOUS-HIGH on AC#14):** spec MUST-NOT list at AC#14 includes `src/web/api.rs` + `src/storage/schema.rs`, but A-4's AC#4 structural field removal made compile-cascade touches mandatory. User accepted on 2026-05-17 with rationale: "the strict-zero list was authored before the structural field-removal cascade of AC#4 was fully simulated; both touches are minimal (~13 lines + 1 assertion drop), scoped, documented in the dev log, and the api.rs touch defers full resolution to Story A-6 which is already on the roadmap." Future stories should revise the MUST-NOT list when the spec scopes a structural field removal.
- **DEF-iter1-A5-D2 (Blind Hunter MEDIUM, mirrors A-4):** narrowing-overflow / narrowing-underflow in `build_data_values` (and A-4's `convert_metric_to_variant`) returns `Variant::Float(0.0)` with `StatusCode::Good`. OPC UA Part 8 §A.4.3 suggests `BadOutOfRange` or `UncertainSubNormal` would be more honest. User accepted on 2026-05-17 with rationale: "mirrors the A-4 behavior by design; the warn log is the operator-side signal; back-port a status-code fix to BOTH A-4 and A-5 in a future cleanup story once the OPC UA Part 8 §A.4.3 mapping is verified end-to-end." Track as a paired A-4 + A-5 follow-up.
- **DEF-iter1-A5-D3 (Edge Case Hunter MEDIUM):** `query_metric_history` schema-drift rows hit `continue;` and disappear from the response stream, while legacy rows surface as `BadDataUnavailable` (visible per epic AC#1). Asymmetry between legacy and schema-drift visibility. User accepted on 2026-05-17 with rationale: "schema-drift is structurally unreachable post-v008 CHECK (defensive guard only); the existing Story 8-3 partial-success contract is silent-skip for schema-drift; preserving that contract is correct. The defensive emission warn (event=metric_history_read reason=schema_drift) provides the operator-side signal." Revisit if a real schema-drift incident surfaces operationally.

## Deferred from: code review of A-6-web-ui-live-metrics-value-display — iter-1 (2026-05-18)

- **W1 (Blind):** `std::mem::forget(dir)` tempdir leak pattern at `tests/web_dashboard.rs::build_test_app_state` + new integration test — deferred, pre-existing helper pattern long before A-6; refactoring is a sweep across the whole web-dashboard test file and out of A-6 scope. Real CI-disk-accumulation concern but bounded by per-test-process lifetime.
- **W2 (Blind):** `Some("")` vs `None` distinction on `MetricSpec.metric_unit` has no JS consumer — deferred, deliberate per spec AC#3 paragraph 5 ("snapshot preserves the distinction at the type level; renderer coalesces"). User-confirmed design choice. Revisit if a future consumer needs the distinction (e.g., per-row "no unit configured" badge vs "unit explicitly empty").
- **W3 (Blind):** `MetricSpec` PartialEq + empty-string `metric_unit` ⇒ unnecessary topology rebuild on `""` ↔ absent toggle (`src/web/mod.rs`). Deferred; the rebuild cost is one snapshot reconstruction (sub-ms at typical scales) with no user-visible artifact. W2 closure (normalising empty to `None` at boundary) would also close this; defer until W2 is acted on.
- **W4 (Blind):** `metrics_js_references_unit_and_data_type_fields` content-grep passes if literals appear in a comment (`tests/web_dashboard.rs`). Deferred, same brittleness as the Story 9-3 precedent test `metrics_js_is_served_and_references_api_devices`. Headless-browser test infrastructure would be a separate epic.
- **W5 (Blind):** `MetricView` doc-comment carries migration history (`src/web/api.rs`). Deferred, consistent house-style across A-1/A-2/A-3/A-4/A-5 spec migration narratives; rewriting all of them is a documentation pass, not A-6 scope.
- **W6 (Edge):** Float subnormal precision unguarded in `metric_type_to_json_value` Float arm (`src/web/api.rs`). Deferred, subnormal Float behaviour is well-defined at the f64 ⇒ JSON boundary; operator-facing impact is nil unless a sensor genuinely reports values below 1.4e-45. Out of A-6 scope.
- **W7 (Edge):** `row.data_type` discriminant ≠ `spec.metric_type` during hot-reload window (`src/web/api.rs::api_devices` Some(row) arm). Deferred, a legitimate transitional state during a metric-type hot-reload (Story 9-7). Worth a tracking issue for a future story to either (a) emit a `type_drift` warn, or (b) suppress the unit when the discriminants disagree. Out of A-6 scope.
- **W8 (Edge):** `metric.unit === "0"` falsy-coalesces in JS truthy check (`static/metrics.js`). Deferred, extremely rare (a unit identifier literally `"0"`); the existing fix at P10 (treat `""` as "no unit") plus the standard truthy-check is the standard pattern. Operator-visible impact: a single weird unit string is dropped. Acceptable.
- **W9 (Edge):** sNaN treated as plain `non_finite` (forensic detail lost) at `metric_type_to_json_value` Float arm (`src/web/api.rs`). Deferred, sNaN signalling is a forensic-grade distinction that the metric pipeline never preserves anyway (Rust f64 routinely normalises sNaN → qNaN at any arithmetic op). Out of A-6 scope.
- **W10 (Edge):** `metric_unit` length cap missing (`src/web/mod.rs` / `src/web/api.rs validate path`). Deferred, length validation is Story 9-5 territory (PUT-replace-device validator). 9-5-iter1-D5 already deferred the byte-vs-char budget; A-6 is the read path.

## Deferred from: code review of A-6-web-ui-live-metrics-value-display — iter-2 (2026-05-18)

- **DEF-iter2-A6-1 (Auditor LOW):** AC#1 / AC#5 / AC#6 spec table text not updated for the iter-1+iter-2 contract refinements (post-A-6 closed-enum widening to `{non_finite, int_precision_lossy, f32_overflow, f32_underflow}`; AC#1 silent f32-precision narrowing; AC#6 default-arm switched from `String(value)` to `"?"` + `console.warn`). The Dev Agent Record / Completion Notes + Review Findings block document the post-iter-2 contract; a future documentation-pass story should pull these into the spec AC tables proper.
- **DEF-iter2-A6-2 (Auditor LOW):** P11 latent persisted-vs-snapshot whitespace drift at `src/web/mod.rs::from_config`. The K4 ASCII-only trim normalises the in-memory snapshot but does NOT mutate the persisted TOML on disk; a future story that compares snapshot-vs-persisted (no such code exists today) would see false-positive drift on `metric_unit = "  °C  "` vs in-memory `Some("°C")`. Track at issue-filing time when the snapshot-vs-persisted comparison is introduced.
- **DEF-iter2-A6-3 (Auditor LOW):** P6 promised dedicated `#[test]` pinning the wire string for `REASON_NON_FINITE` not delivered as a standalone test. The P4 `logs_contain("reason=\"non_finite\"")` assertions serve as de facto pins via the tracing-test capture; a dedicated string-pin test is nice-to-have but not load-bearing.
- **DEF-iter2-A6-4 (Blind iter-2 #4):** aggregate warn label is "Float row(s) emitted as JSON null" without sample identifiers in the message. Per-row `debug!` (`non_finite`) / `warn!` (`f32_overflow`) lines carry full device_id+metric_name correlation; aggregate is a count signal only. Adding sample identifiers to the aggregate would duplicate the forensic surface for marginal triage value.
- **DEF-iter2-A6-5 (Blind iter-2 #13):** P9 client-side `console.warn` per render is unthrottled at `static/metrics.js::formatValue`. Would log-flood on a sustained forward-incompat data_type, but the structural condition is "the wire schema is ahead of the deployed JS bundle" which is a release-discipline problem, not an A-6 concern. Client-side throttling would add module-level state for a release-edge case.
- **DEF-iter2-A6-6 (Edge iter-2 #11):** `valueMissing` future-variant coupling at `static/metrics.js::renderMetricRow`. A future MetricType variant (`Bytes` etc.) would need the renderer updated to recognise the new data_type; the unit suffix would render correctly today only by accident. Out of A-6 scope; tied to a hypothetical future wire-contract expansion.


## Deferred from: code review of B-1-docker-hub-publishing-and-user-manual — iter-1 (2026-05-19)

- **DEF-iter1-B1-T8 (RESOLVED 2026-05-20):** Bump `Cargo.toml` version from `2.0.0-rc` to `2.0.0` BEFORE pushing the `v2.0.0` tag. Also update README badge from `version-2.0.0--rc-blue` to `version-2.0.0-blue`. Required as a separate small commit (touches Cargo.toml + Cargo.lock which are strict-zero in B-1; this commit explicitly carries the AC#20 violation in service of the tag-time version flip). User-confirmed deferral 2026-05-19. **Iter-2 escalation 2026-05-20 (resolves decision U2):** This is now a HARD pre-tag gate, NOT a soft deferral. The user-facing doc set (README × 4, docker-compose.yml × 1, docs/dockerhub-description.md × 2, docs/manual/opcgw-user-manual.xml × 5) pins to `:2.0`, but `docker/metadata-action@v5`'s `type=semver,pattern={{major}}.{{minor}}` SUPPRESSES the bare `:2.0` tag emission for pre-release versions (semver rule). A `v2.0.0-rc` tag push therefore produces only `:2.0.0-rc` and `:sha-…` — leaving every doc-pinned `:2.0` reference broken with `manifest unknown` on hub.docker.com and ghcr.io. **Sequencing rule:** the Cargo bump commit MUST land BEFORE the first `v*` tag push, OR all `:2.0` doc references break. Recommended commit message stub: `chore(release): bump Cargo to 2.0.0 (pre-v2.0.0-tag gate per DEF-iter1-B1-T8)`.
- **DEF-iter1-B1-T11 (decision-needed):** `docker/build-push-action@v5` is one major behind latest stable `@v6`. Other Docker actions in the same workflow are pinned to current latest majors (`setup-qemu-action@v3`, `setup-buildx-action@v3`, `login-action@v3`, `metadata-action@v5`). Bumping requires verifying v6 multi-arch manifest behavior on a real tag push — defer to a v2.x follow-up issue. Workflow now carries inline comment documenting the intentional v5 pin. User-confirmed deferral 2026-05-19.
- **DEF-iter1-B1-T23 (Edge LOW):** Config comment dropped the OPC UA standard-port-4840 reference. Minor information loss; operators familiar with OPC UA may wonder why opcgw uses 4855 specifically (project override of library default). No functional impact.
- **DEF-iter1-B1-T25 (Edge LOW):** `docs/logo/opcgw-logo-pack.zip` is tracked in git, but `docs/manual/out/` rendered outputs are gitignored. Policy inconsistency on regeneratable artifacts. Either gitignore the zip and produce on demand via a `make logo-pack` target, or document the zip as an intentionally-tracked release artifact. No functional impact.
- **DEF-iter1-B1-T26 (Edge LOW):** Three docs/dockerhub-description.md links point to raw DocBook XML at `https://github.com/guycorbaz/opcgw/blob/main/docs/manual/opcgw-user-manual.xml`. Docker Hub Overview readers click through and get syntax-highlighted source instead of rendered manual. Fix requires a CI step that publishes the rendered HTML somewhere durable (GitHub Pages, release artifact, etc.); out-of-scope v2.x doc-pass story.
- **DEF-iter1-B1-AC23 (Auditor AMBIGUOUS):** GitHub tracking issue for Story B-1 — commit `e7931fe` carries placeholder `Refs #__`. Explicitly anticipated by the spec (line 184-186) and by Dev Notes; Guy will open the issue out-of-band and provide the number for a future commit-message amendment or follow-up commit. Per CLAUDE.md "Issue Management" rule, this is the established Epic A precedent (gh CLI not authenticated for write).


## Deferred from: code review of B-1-docker-hub-publishing-and-user-manual — iter-2 (2026-05-20)

- **DEF-iter2-B1-U18 (Edge MED — process gap):** `Refs #__` commit-message trap. The placeholder `#__` is now in three commit messages (`e7931fe`, `1cf3ba7`, and the forthcoming iter-2 patches commit). Resolving via `git rebase -i` is forbidden by CLAUDE.md "Never use git commands with the `-i` flag". Resolution path: once the GitHub tracking issue is opened (Guy out-of-band), land a follow-up commit titled `Story B-1: Resolve tracking-issue placeholder (Refs #N — resolves placeholder from e7931fe, 1cf3ba7, <iter-2 SHA>)` rather than rewriting history. Documented here so future iter readers don't propose interactive-rebase fixes.
- **DEF-iter2-B1-U20 (Edge LOW — Makefile UX):** `docs/manual/Makefile` uses `LOGO_SRC := ../logo` which only resolves correctly when `make` is invoked with cwd = `docs/manual/` (e.g., `cd docs/manual && make html`, or `make -C docs/manual html` from repo root). Running `make -f docs/manual/Makefile html` from repo root leaves cwd at repo root and `../logo` resolves to the *parent of the repo* — a silent path bug. The Makefile header already documents `make html` from `docs/manual/` as the canonical invocation; consider robustifying with `LOGO_SRC := $(dir $(abspath $(firstword $(MAKEFILE_LIST))))../logo` in a follow-up if anyone hits this.
- **DEF-iter2-B1-U21 (Edge LOW — rolls in with U2/T8):** `docs/dockerhub-description.md` Supported-tags table claims `:2.0` "Auto-updates on patch releases". Accurate post-Cargo-bump (DEF-iter1-B1-T8 escalated above resolves the prerequisite). Re-verify after `2.0.0` cut and the first `v2.0.x` tag confirms the floating `:2.0` updates as advertised.

## Deferred from: Epic C scope decision (2026-05-21)

- **CR-EPIC-C-MQTT (LOW — change request, not bound to any story):** MQTT-based real-time path for uplink ingestion. Originally proposed as Story C-5 — captured under the working name "D-5" on 2026-05-20 and renamed to C-5 on 2026-05-21 when the parent epic was renamed from D to C (`project_epic_c_auto_discovery_vision.md`). Concept: opcgw subscribes to ChirpStack's MQTT `up` events for tracked devices and emits the same internal storage-write the polling path does, passing through the name-translation layer. Operator-visible behaviour unchanged (still named data in the OPC UA browse tree); staleness drops from "poll interval" to "<1s after uplink hits ChirpStack." Polling remains as a fallback for ChirpStack instances without MQTT enabled. **Removed from Epic C scope** by Guy 2026-05-21 ("I'm not willing to implement MQTT, at least now") so the epic can flip to done after C-0/1/2/3/4/6 without an orphaned story. Tracked here as a low-priority CR — re-open as a new story (in a future epic) only if Guy explicitly asks for real-time updates. The name-translation rationale that drives Epic C applies equally to C-5 if/when revived: MQTT events carry UUIDs, so the MQTT subscriber MUST go through the same `chirpstack_metric_name` → OPC UA `metric_name` mapping layer.


## Deferred from: code review of C-0-empty-config-bootstrap — iter-2 (2026-05-21)

- **DEF-iter2-C0-BH-H8 (Blind HIGH — theoretical):** chmod-before-rename ordering in `write_secrets_toml`. Linux `rename(2)` preserves perms on same-fs; the tempfile is created in the same parent dir as the destination so cross-fs scenario is unlikely. Risk is theoretical. If hit, defence-in-depth would be: chmod the destination AFTER persist + verify mode.
- **DEF-iter2-C0-BH-H10 (Blind HIGH — out-of-scope):** No rate-limit on wizard POST. Covered by `docs/security.md` trusted-operator-network assumption ("first-run mode requires a network on which the operator is the only/first client"). Future hardening story should add per-IP rate-limit (e.g. `tower-governor` 10 attempts/minute) once operator network model evolves.
- **DEF-iter2-C0-EH-H3 (Edge HIGH — refactor):** `AppConfig::is_first_run()` and `AppConfig::validate()` use asymmetric env-var "set" semantics. M3 patch fixed `is_first_run` to require non-empty/non-whitespace; `validate()` still uses bare `env::var(...).is_ok()`. Both paths agree today on the rejection ordering but are vulnerable to refactor drift. Future refactor story: extract `fn opcua_password_envvar_set() -> bool` in `crate::utils` and have both paths call it; add a parity unit test.
- **DEF-iter2-C0-BH-M5 (Blind MED — UX):** Hand-editing `secrets.toml` at runtime without restart leaves wizard reachable. Operator who partially configures via TOML edit but forgets to restart could be racing an attacker who finishes via the wizard. Mitigation deferred: on first POST, re-read secrets.toml and short-circuit to 410 Gone if populated. Document the failure mode in `docs/security.md`.
- **DEF-iter2-C0-BH-M6 (Blind MED — refactor):** `toml_escape_string` has hand-rolled char-by-char escaping; non-BMP characters and surrogate pairs are emitted raw (TOML accepts them per spec but our hand-rolled escaper is fragile). EH-H1 patch added DEL coverage; full robustness would replace the 21-line function with `toml::Value::String(s).to_string()` (toml crate already a dep). Future cleanup story.
- **DEF-iter2-C0-BH-M7 (Blind MED — UX):** JS setInterval countdown + setTimeout reload assume supervisor restart completes within ~7 seconds. On slow supervisors (RPi, network FS, `RestartSec=30s`), browser auto-reload hits connection refused. Documented as deferred follow-up #3 in the Completion Note. Future UX-polish story: implement retry-until-200 loop with exponential backoff (max 30s) in the wizard's success script. Same scope swallows EH-M7.
- **DEF-iter2-C0-BH-M10 (Blind MED — durability):** No fsync of parent directory after `tempfile.persist()`. On power-loss between rename and dir-entry flush, the persisted secrets.toml could vanish (operator sees "Password set" but next boot re-enters first-run mode). Linux rename is metadata-only; dirent fsync via `File::open(parent).sync_all()` would close the gap. Document the durability contract and revisit.
- **DEF-iter2-C0-EH-M2 (Edge MED — cross-path consistency):** `validate_password` (wizard) rejects whitespace-bracketed but allows embedded whitespace mid-string; `validate()` (env-var) has the same trim-bracketed rule. Today both pass `"my secret"` (single internal space). Risk is future drift if either path adds new rules without the other. Future refactor story: extract `fn is_valid_password(s: &str) -> Result<(), &'static str>` and call from both sites.
- **DEF-iter2-C0-BH-L1 (Blind LOW — doc):** `is_first_run` doc-comment promises "source 3: secrets.toml absent or doesn't have the field" — implementation only checks the figment-merged `self.opcua.user_password` value, not the filesystem. Update the doc to reflect the actual behaviour ("all three sources are checked via the figment-merged result").
- **DEF-iter2-C0-BH-L3 (Blind LOW — i18n):** Wizard HTML hardcoded English. Out of scope for v2.x; future i18n story.
- **DEF-iter2-C0-BH-L7 (Blind LOW — test hardening):** `post_first_run_setup_get_returns_410_gone` test doesn't FIRST verify auth would reject a bad password. If auth was silently bypassed, the test still passes via the GONE assertion. Add a sub-assertion: bad password → 401, then good password → 410.
- **DEF-iter2-C0-BH-L8 (Blind LOW — doc):** README's first-run-wizard subsection says "WITHOUT `OPCGW_OPCUA__USER_PASSWORD` set" — validator rejects empty env-var with a whitespace error, so the README's binary "set vs. unset" framing is incomplete. Align with validator behaviour.
- **DEF-iter2-C0-BH-L11 (Blind LOW — CSP):** `static/setup.html` has inline `<style>` block. No CSP enforced today; revisit when security headers are tightened in a future hardening story.
- **DEF-iter2-C0-BH-L13 (Blind LOW — debuggability):** No `trace!` log between the two `Toml::file()` figment providers in `from_path`. Operator hand-editing secrets.toml that doesn't get picked up has no signal in logs. Add a `trace!(secrets_path = %..., exists = ?...);` line.
- **DEF-iter2-C0-EH-L3 (Edge LOW — doc):** 256-char password length cap is undocumented. Extract as named constant `MAX_WIZARD_PASSWORD_LEN` or document the rationale inline.
- **DEF-iter2-C0-EH-L6 (Edge LOW — operational):** `tempfile::NamedTempFile` may leak files in `config/` if the process is `kill -9`'d between create and persist. Cosmetic — figment ignores the tempfile pattern names on next boot. Document the litter in the user manual or sweep on boot.
- **DEF-iter2-C0-EH-L7 (Edge LOW — dev-UX):** Operator running `cargo run` locally without a supervisor (`Restart=on-failure` in systemd, Docker restart policy, etc.) completes the wizard, gateway exits, doesn't restart, browser sees connection refused. Emit a `warn` log at first-run-mode-entry: "First-run mode active; supervisor not detected — auto-restart after wizard submit is your responsibility."
- **DEF-iter2-C0-AA-L2 (Auditor LOW — copy drift):** Dashboard empty-state copy in `static/dashboard.js` says `'No applications configured yet. Add one to start polling.'` (inline hyperlink); AC#13 specified `"No applications configured — click below to add one"` (button CTA). Functional empty state present; cosmetic drift only.
- **DEF-iter2-C0-AA-L3 (Auditor LOW — issue-tracker precedent):** `Refs #__` placeholder in commit messages `c200089`, `c5cdea1`, `7ec2fc1`. Per Epic A/B precedent (gh CLI not authenticated for write), Guy opens the issue out-of-band and a follow-up commit can reference the resolved number. Same resolution path as `DEF-iter2-B1-U18`.
- **DEF-iter2-C0-AA-L4 (Auditor LOW — spec drift):** AC#21 cites `≥ 56 ignored` doctests; actual pre-C-0 baseline at HEAD is 55. Acknowledged in the Completion Note. Spec-body amendment optional.
- **DEF-iter2-C0-BH-L10 (Blind LOW — test brittleness):** Integration test `wizard_post_persists_password_and_signals_shutdown` asserts on substring match of `secrets.toml` body. Brittle if `toml_escape_string` whitespace changes. Same fix shape as P10 on the unit-test side: parse via `toml` crate, assert `user_password` value equals input. P10 covers the unit-test version; integration test deferred to a hardening pass.


## Deferred from: code review of C-0-empty-config-bootstrap — iter-3 (2026-05-22)

- **DEF-iter3-C0-BH-H1 (Blind HIGH — defence-in-depth, defensible):** Origin header check on `POST /api/setup/password` accepts requests with NO Origin header. The threat model documented in `docs/security.md` § "First-run wizard" is "browser drive-by from a malicious LAN page" — browsers ALWAYS send Origin on POST. curl-style attackers omitting Origin still face the Content-Type strict check (iter-2 P9, `application/json` required) that a `<form>` element cannot forge. Tightening to a 403 on missing Origin would close the residual gap but break legitimate scripting/automation (curl, unattended provisioning) on the wizard. Documented in `src/web/setup.rs::setup_post` comments. Guy-accepted 2026-05-22 with documented reason: defence-in-depth-only; primary defence is trusted-network + Content-Type.



## Deferred from: code review of C-1-chirpstack-inventory-query-layer — iter-1 (2026-05-22)

- **DEF-iter1-C1-P2 (HIGH — 3-of-3 reviewer convergence; refactor scope):** `InventoryCache` mutex per scope serializes ALL keys, not per-key. Doc-comment claims "race-free fetch-and-insert" + "two concurrent callers on the SAME key" coalescing, but the single `Mutex<HashMap<...>>` per scope means concurrent fetches for DIFFERENT applications also serialize. Under picker UI load (operator opens app A, then app B before A's fetch returns), B blocks on A's full ChirpStack RTT. Functionally correct (no double-fetch) but over-serialised. **Fix scope:** rewrite the cache to per-key Mutex via `DashMap<Key, Arc<Mutex<Option<CacheEntry>>>>` or `tokio::sync::OnceCell` pattern. ~150 LOC refactor + new tests. Out of iter-1 scope; tracked here for iter-2 or a hardening follow-up. Operator-observable impact is bounded by `max_wait_seconds` × concurrent picker sessions.

- **DEF-iter1-C1-P4 (Edge HIGH — shutdown latency):** `stream_recent_device_uplinks` does NOT accept a `CancellationToken`. Gateway shutdown waits up to `max_wait` (≤60s) per in-flight uplink request before axum's graceful-shutdown returns. Same shape as the Story B-1 2026-05-20 main-deadlock-incident class. **Fix scope:** add a `cancel_token: &CancellationToken` arg to `stream_recent_device_uplinks` + thread it through; same fix shape for `fetch_applications` / `fetch_devices`'s per-page `client.list()` await (currently checks `cancel_token.is_cancelled()` only between pages — a slow 30s page still blocks shutdown). ~50 LOC across 3 functions + signature changes at handler callsites. Deferred to iter-2 (handler signature change is invasive enough to warrant a fresh review round on its own).

- **DEF-iter1-C1-BlindMED9 (chirpstack_failure_reason substring brittle):** `chirpstack_failure_reason` in `src/web/inventory.rs` matches on lowercase substrings of the formatted error message. Brittle to tonic Status-display format changes between versions; misclassifies some tonic Code variants. **Proper fix:** keep the `tonic::Status` typed in `OpcGwError::ChirpStack` and match on `Status::code()` directly. Requires extending `OpcGwError::ChirpStack(String)` to carry a typed code OR introducing a new variant. Out of iter-1 scope; tracked for the next OpcGwError refactor pass.

- **DEF-iter1-C1-AC19 (12-test integration suite):** AC#19 promised 12 integration tests; iter-1 shipped 5 (HTTP-validation paths only). The remaining 7 (cache hit/miss/refresh state transitions; cache invalidation by CRUD writes; ChirpStack unreachable 502; cache scope per-(tenant,app); 3-uplink wire-type inference; zero uplinks; heterogeneous-key audit emission) need a mock ChirpStack tonic server. Already documented in spec sprint-status as deferred; explicitly tracked here for discoverability.

- **DEF-iter1-C1-AC16-smoke (real-ChirpStack smoke test):** Task 10.4 smoke test against Guy's real ChirpStack at 192.168.1.12:8080 deferred. The `.env` credentials are in place; needs operator-time to run + verify the AC#16 discriminator string ("up" vs alternatives) + AC's mention that `InternalService` auth semantics may differ from ApplicationService/DeviceService.

- **DEF-iter1-C1-CSRF (Blind HIGH H6 unverified):** Blind Hunter flagged that the inventory routes don't appear in any CSRF-skip allowlist in the diff. Verification needs reading the existing CSRF middleware behaviour. **The convention in opcgw is that all GET endpoints are CSRF-exempt** (CSRF only enforces on state-changing POST/PUT/DELETE per `src/web/csrf.rs` design); the inventory routes are GET-only so they inherit this exemption. Documented here for the iter-2 reviewer to verify with a direct grep of `src/web/csrf.rs`.

- **DEF-iter1-C1-LOWs (assorted):** README "C-1 done" claim before review concludes (premature, mirrors C-0 doctrine drift); applications-endpoint audit event always emits `chirpstack_response="ok"` even on empty list (devices + uplinks correctly emit "empty"); `received_at` sort uses RFC3339 string compare (works for chrono-formatted strings but brittle to format drift); `all-null` keys default to String with no audit emit (loses codec-bug signal); `log_item_to_uplink` silently drops malformed body JSON (no audit emit); test count claim in sprint-status overstates "62 new tests" (actual ~32 across new files + ~30 indirect from config-field literals across existing files); `chirpstack_failure_reason` called 3× per error path (minor refactor); `BearerInterceptor::call` produces non-actionable "invalid api token" Status for non-ASCII tokens (better diagnostic possible at config-validation time). All accepted as LOW-priority polish; defer to iter-2 batch or a follow-up cleanup story.


## Deferred from: code review of C-1-chirpstack-inventory-query-layer — iter-2 (2026-05-22)

iter-2 of bmad-code-review C-1 caught **2 HIGH + 2 MED + several LOW** in iter-1's own patches — 16th iter-N+1 doctrine validation. Blind+Edge convergent on the P3 regression (iter-1's i64::MAX fix overcorrected, classifying `42.0`, `1000.0`, `1e3` as Float when they should stay Int). Auditor missed it (the new tests passed but didn't exercise the regression vector). All 3 critical findings patched in iter-2 commit; LOW items deferred below.

- **DEF-iter2-C1-LOW-deadcomment (LOW — doc clutter):** The 4-line comment block at `src/chirpstack_inventory.rs:749-754` explaining why `InventoryBearerInterceptor` was deleted now references a struct that no longer exists. Future readers will hunt for the symbol and find nothing. Either delete the comment or rewrite as forward-looking ("Both helpers use `BearerInterceptor` defined above"). Cosmetic; defer.

- **DEF-iter2-C1-LOW-cancelmsg (LOW — error-string consistency):** `fetch_applications` / `fetch_devices` emit two distinct cancellation strings: `"cancelled during shutdown"` (entry-point arm) and `"cancelled mid-pagination during shutdown"` (mid-loop arm). Audit-log grep/aggregation becomes harder with two keys for the same condition. Pick one; defer.

- **DEF-iter2-C1-LOW-mutexcomment (LOW — comment-vs-code drift):** The doc-comment on `InventoryCache::get_or_fetch_applications` (line 207-209) claims "two concurrent callers on the SAME key coalesce" but the single-`Mutex<HashMap>` serialises ALL keys within a scope. Iter-1 didn't update this comment; iter-2 leaves it for the same reason. Honest fix needs the per-key locking refactor (DEF-iter1-C1-P2). Update the comment now to honestly state "ALL keys serialise within a scope" until the per-key refactor lands. Cosmetic; defer.

- **DEF-iter2-C1-LOW-shutdown-asymmetry (LOW promotion of DEF-iter1-C1-P4):** Now that iter-1 P5 made `fetch_applications` / `fetch_devices` cancel-aware but `stream_recent_device_uplinks` still doesn't accept a `CancellationToken`, shutdown latency is asymmetric per endpoint. Pre-iter-1 ALL three endpoints were equally loose; iter-1 P5 tightened two but not the third. Roll into the DEF-iter1-C1-P4 fix when it lands.

- **DEF-iter2-C1-LOW-builderConnectTimeout (LOW — tonic default):** `build_channel` doesn't set an explicit `.connect_timeout(...)` on the tonic Endpoint, so cancellation can be blocked on tonic's default connect timeout (typically 60s) when ChirpStack is unreachable. Set an explicit small timeout (e.g. 5s) so the gateway's shutdown SLO is project-controlled. Defer to a hardening pass.

- **DEF-iter2-C1-LOW-testcountclaim (LOW — process hygiene):** Iter-1 commit message claimed "1392/0/10 tests pass" — verified accurate (post-iter-2 is 1398/0/10 with +6 regression-guards from iter-2's P1 fix). Iter-2 commit will pin 1398/0/10. No action needed beyond this confirmation.

## Deferred from: code review of C-2-inventory-pickers-web-ui — iter-1 (2026-05-23)

- **DEF-iter1-C2-update-emit (MED):** `update_device` would emit `metric_wire_type_inferred` on PUT-replace for any pre-existing metric carrying a `picker_metadata` envelope. Today this is latent — the JS edit modal (`buildMetricRow` in static/devices-config.js) does NOT attach picker_metadata when re-rendering pre-existing rows, so the emit only fires on create. Once a future picker-driven edit modal lands, the audit semantic must either (a) strip picker_metadata from re-saved metrics client-side, or (b) be relaxed to "emit per metric carrying picker_metadata at write time" and documented in docs/inventory-api.md.

- **DEF-iter1-C2-key-charclass (MED — UX polish):** ChirpStack `observed_keys[].key` values containing spaces or non-ASCII chars (e.g. "Battery Voltage" from a permissive codec) will appear in the metric-picker checkbox list but fail the server-side validator with HTTP 400 on submit. Server returns a clear error today; client-side pre-validation (disable + tooltip on invalid keys) is UX polish for a future iteration.

- **DEF-iter1-C2-picker-opened-shape (LOW — refactor):** `audit_picker_event` `picker_opened` arm always emits `application_id` / `dev_eui` / `cache_status` even when they're empty for the resource scope (e.g. `picker_resource="application"` doesn't need `dev_eui`). Per-resource conditional emit is non-trivial in tracing (no fully-dynamic field names); current verbose-but-correct form is parseable by downstream consumers. Refactor to per-arm conditional fields if log volume becomes an issue.

- **DEF-iter1-C2-audit-rate-limit (LOW — carry-forward #88):** `auditEvent` JS helper has no client-side debounce; combined with no server-side per-IP rate limit (issue #88, still open), bursty picker UX (e.g. operator clicking through 5 devices in rapid succession) can flood the audit log. Shared concern with the entire `/api/*` surface; fix lands when #88 is addressed.

## Deferred from: code review of C-2-inventory-pickers-web-ui — iter-2 (2026-05-23)

- **DEF-iter2-C2-setbanner-race (LOW — UX race):** `loadPicker(opts)` in applications.js and `loadDevicePicker(opts)` in devices-config.js call their UI-clear helpers (`setPickerBanner(null)`, `setDevicePickerBanner('')`) BEFORE the `await` resolves. A stale prior fetch's synchronous setup can race-clobber a fresh fetch's intermediate state for ~one tick. The abort-check-after-await catches the dominant race window, and the post-await setup always reflects the live controller's state, so the bug is cosmetic only. Address with a sequence-number / generation guard if it becomes operator-visible.

- **DEF-iter2-C2-docbook-vocab (LOW — doc-maintenance):** The DocBook user manual documents `event="picker_audit_rejected"` as carrying `reason` values `unknown_event | csrf` — a closed vocabulary. Future iterations adding additional rejection reasons (e.g., rate-limited per issue #88, malformed-body) must remember to extend the manual. Add a "see docs/logging.md for full event vocabulary" pointer the next time the section is touched.

- **DEF-iter2-C2-metadata-cap-forward (LOW — forward-compat):** `PICKER_METADATA_STRING_CAP = 64` is justified in the docstring with reference to current legal `OpcMetricTypeConfig` values (3-6 chars). A future contributor might invite tightening to 16 bytes; the docstring should note "headroom for vocabulary growth". Address in a follow-up pass.

## Deferred from: code review of C-2-inventory-pickers-web-ui — iter-3 (2026-05-23)

- **DEF-iter3-C2-nfc-normalisation (LOW — extreme-edge):** `attachEditedFlag` divergence check compares `inputEl.value` to recorded baseline byte-for-byte. Operator names containing combining marks (NFD form) where autofill restores in NFC form (or vice versa) would false-positive the edited flag. Affects only Unicode strings with combining marks — Latin / ASCII / typical CJK is unaffected. Address with `.normalize('NFC')` on both sides if it ever becomes operator-visible.

- **DEF-iter3-C2-string-coercion (LOW — doctrine):** `recordPickerPopulation` uses `String(value || '')` which coerces 0 / false / NaN to ''. No current caller passes those types (the picker always passes a `String`), but a more defensive `String(value == null ? '' : value)` would future-proof. Address in a follow-up doctrine pass.

- **DEF-iter3-C2-domop-throw (LOW — improbable):** `loadMetricPicker` calls `metricPickerRows.replaceChildren()` and `setMetricPickerBanner('')` BEFORE the abort runs. If those DOM operations throw synchronously (only possible if the element was detached / removed from the DOM tree between renders), the prior in-flight uplinks fetch would never abort. Wrapping the pre-abort DOM ops in `try { ... } finally { abort }` would close the gap; deferred because the trigger is extremely unlikely (detached node would already break a dozen other unrelated paths first).

- **DEF-iter3-C2-finally-throw (LOW — improbable):** `loadPicker` / `loadDevicePicker` `finally` blocks call `setSubmitEnabled(true)` / `setFormSubmitEnabled(true)` unconditionally; if those throw (which they don't — they just set a DOM property), any awaiting caller's continuation would be corrupted. Wrap in `try { ... } catch (_) { /* log */ }` for paranoid defence. Deferred — `submitBtn.disabled = ...` is infallible on a valid DOM element.

- **DEF-iter3-C2-counter-bound (LOW — doc):** `_metricCheckboxIdSeq` is a page-lifetime monotonic counter (declared inside the IIFE). No overflow guard before `Number.MAX_SAFE_INTEGER` (~9 quadrillion increments). One-line comment documents the choice; deferred any explicit bound check until a SPA-style page that lives 10+ years sees realistic risk.

## Deferred from: code review of C-3-duplicate-prevention-validator (2026-05-23)

- No CI guard for `conflict_kind` coverage across all 30 audit emit sites in `src/web/api.rs` — pre-existing pattern; would need a `ConflictKind` enum + helper macro. Target: post-Epic-C cleanup.
- No test pinning `ErrorResponse` skip-serializing behaviour for legacy non-duplicate callers (`src/web/api.rs:65-80`) — defensive coverage; rely on serde-skip derive. Target: opportunistic.
- delete_application "empty_application_list" 409 path stale post-C-0 (validator now accepts empty `application_list`); `src/web/api.rs:1894-1911`. Pre-existing inconsistency; not C-3-introduced. Target: Epic C retrospective decides keep-as-UX-guardrail vs remove.
- [MEDIUM, user-approved 2026-05-23] tracing_test global_buf race risk in audit-taxonomy test `tests/web_duplicate_prevention.rs:1817` — `#[serial(captured_logs)]` only serialises against other `serial(captured_logs)` tests; a concurrent non-serial test emitting matching event names could let `assert!(logs.contains(...))` pass vacuously. Pre-existing pattern across web_*_crud.rs. Target: tests/common extraction cleanup alongside #102.
- [MEDIUM, user-approved 2026-05-23] No length cap on `value`/`scope` echo in `ErrorResponse::duplicate` (`src/web/api.rs:107-133`) — pathological large request fields produce amplified 409 bodies. Body-size guards belong at the axum/figment layer, not in ErrorResponse. Target: only if gateway adds a global body-size policy.
- [MEDIUM, user-approved 2026-05-23] No tests for 4 update/delete TOML-state duplicate paths in `src/web/api.rs:1746,1976,3016,3294` — only reachable on pre-existing TOML corruption; covered by existing per-file CRUD suites for audit-shape. Target: opportunistic when web_*_crud.rs gets a corruption-fixture helper.

## Deferred from: code review iter-3 of C-3-duplicate-prevention-validator (2026-05-23)

- [MEDIUM, user-approved 2026-05-23] update_command BH-H3 corruption-first guard ordering: the new malformed_existing_block guard at `src/web/api.rs:4391-4429` fires BEFORE the rename-target equality check, locking the operator out of the legitimate self-recovery workflow where they PUT to rename one of two corrupted-same-command_id siblings before fixing the corruption itself. Rare scenario; current ordering prioritises corruption-detection over rename-completion. Target: surface as a separate operator-runbook entry rather than a code change.
- [MEDIUM, user-approved 2026-05-23] config.rs empty-app_id branch (iter-2 BH-M2 patch at `src/config.rs:1846-1866`) silently un-deduplicates device_ids within an empty-app_id application. After the operator fixes the empty-id, the next reload pass surfaces a previously-hidden duplicate device_id, creating a multi-round iterative-fix workflow. Defensible (current empty-id error is more actionable than a noisy duplicate alongside it). Target: track for an "iterative-validation" feature that surfaces ALL latent errors at once.
- [MEDIUM, user-approved 2026-05-23] Cross-resource attribution leak in `reload_error_response`: a `command_crud_rejected` event can be paired with `duplicate_field=application_id` if the operator triggered a command CRUD that caused a reload-time validate to surface an unrelated application-level duplicate. Defensible by event-handler-attribution semantics (the operator action was a command CRUD); audit consumers grouping by event-name see the right event-handler. Target: future enhancement could validate `info.field` matches the resource family at `reload_error_response` and log a mismatch warning.

## Deferred from: code review of C-4-inventory-drift-view (2026-05-24)

- DEF1 (LOW): `renderRows` no null-guard on `row.opcgw`/`row.chirpstack` before class-specific field access — server invariants from `compute_drift` guarantee correctness today; defensive-only. `static/inventory-drift.js`.
- DEF2 (LOW): Modal children (`drift-confirm-title`, `drift-confirm-yes`, `drift-confirm-cancel`) accessed without null-check after outer dialog guard — all present in current HTML template. `static/inventory-drift.js:463-477`.
- DEF3 (LOW): Sequential uplink fetches, no `FuturesUnordered` — K × D × `inventory_uplink_max_wait_seconds` worst-case latency; architectural trade-off, spec-accepted operator-triggered-only design. `src/web/drift.rs:731-771`.
- DEF4 (LOW): `drift_view_opened` success path (line 788) not covered by integration tests — requires tonic mock, same deferral precedent as C-1's deferred 12-test suite. `tests/web_inventory_drift.rs`.
- DEF5 (LOW): `drift_view_opened` emits flat `summary_ok/stale/available/drifted/total` fields; AC#11 notation shows nested `summary: { ok: N, … }`; `docs/logging.md` documents the actual flat form. `src/web/drift.rs:801-814`.
- DEF6 (LOW): `drift_view_opened_audit_fires_with_summary` only asserts `summary_total`; per-class counts not pinned; AC#15 item 9 says "correct summary counts". `tests/web_inventory_drift.rs:397-407`.
- DEF7 (LOW): `drift_dismissed` emits `drift_reason` string; AC#13 spec says "drift_details snapshot" implying structured shape; information equivalent, `logging.md` documents actual field name. `src/web/api.rs`.

## Deferred from: code review iter-1 of C-6-toml-to-sqlite-config-migration (2026-05-25)

- F10 (LOW): `command_port` not range-validated during migration — `src/storage/sqlite.rs:3371-3383` inserts `cmd.command_port` without checking the 1-65535 spec range. Pre-existing gap in command validation predating C-6; not a C-6-introduced regression.
- F11 (LOW): `commands` config table missing `created_at`/`updated_at` columns — `migrations/v009_application_config_tables.sql`. The `applications`, `devices`, and `metrics` tables have audit timestamps; `commands` does not. Requires a v010 schema migration; out of C-6 scope.
- F12 (LOW): `config_reload.rs::reload()` unreachable and untested — moot if F5 (dead-code decision) resolves to deletion; else pre-existing coverage gap for the SIGHUP-era reload path.
- F13 (LOW): FK pragma must be enabled per-connection — `src/storage/sqlite.rs`. `PRAGMA foreign_keys = ON` is set on each connection via `connection_hook`. Pre-existing pattern across codebase; risk that a raw connection bypasses the hook. Not a C-6-specific regression.
- F15 (LOW): `duration_ms` includes pool-checkout time — `src/storage/migrate_config.rs:66`. Timer starts before `migrate_applications_config()` which internally does a pool checkout (up to 5 s timeout). Cosmetic accuracy issue; pool checkout is typically sub-millisecond.
- F17 (LOW): Unstructured `info!` startup log block in `main.rs` — pre-existing style inconsistency; not a C-6 regression.
