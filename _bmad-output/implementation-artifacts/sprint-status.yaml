# generated: 2026-04-02
# last_updated: 2026-05-06 (Story 9-7 done — 3 code-review iterations terminated under CLAUDE.md condition #2 (only LOW remains). Iter-1 surfaced 24 patches (5 HIGH + 11 MEDIUM + 5 LOW + 3 from 5 decisions resolved) — 22 applied, 2 deferred (P13 atomicity + P20 success-log knob trail, design calls). Iter-2 caught 2 HIGH-REGRESSIONs in iter-1's own patches (P25 tautological subscription test, P26 topology_device_diff disagreement with classify_diff on device_command_list) + 3 HIGH (P27 None≡Some([]), P28 destructure landmines on chirpstack_equal/opcua_equal, P29 NaN-safe device_schemas via to_bits()) + 9 MEDIUM. Iter-2 applied 11 (all HIGH/HIGH-REGRESSION + 6 MEDIUM); 3 MEDIUM deferred (P33 spawn_blocking, P36 API surface, P37 helpers compare — all with sound rationale). Iter-3 verified iter-2 clean; surfaced 1 MEDIUM (P42 param_type_equal _ => false → exhaustive cross-variant pairs) + 9 LOW; P42 applied. Final: 876 passed / 0 failed (was 870 baseline; +6 from new tests P12/P22/P23/P24×3); cargo clippy --all-targets -- -D warnings clean; AC#7 grep contract = 3 lines; AC#8 file invariants intact. GH issues filed: #112 (tracker), #113 (live-borrow refactor for AC#10 + credential rotation), #114 ([global] hot-reload), #115 ([command_validation] hot-reload), #116 ([logging] log-level hot-reload). 5 v1 limitations backfilled to deferred-work.md per AC#11. Memory feedback_iter3_validation.md precedent reinforced — Edge caught P42 in iter-3 that Blind/Auditor missed. PREVIOUS NARRATIVE — Story 9-7 review — bmad-dev-story implementation complete in a single execution; status flipped ready-for-dev → in-progress → review. New `src/config_reload.rs` module owns the `tokio::sync::watch::Sender<Arc<AppConfig>>` channel + `ConfigReloadHandle::reload()` validate-then-swap routine + knob-taxonomy classifier (hot-reload-safe vs restart-required vs address-space-mutating). `src/main.rs` spawns SIGHUP listener (3 new audit events `event="config_reload_attempted/succeeded/failed"`), web-config-listener (atomic swap of `AppState.dashboard_snapshot` + `stale_threshold_secs` — now `RwLock<Arc<...>>` + `AtomicU64`), OPC UA config-listener stub (logs `topology_change_detected` for Story 9-8). Poller `config_rx` outer-loop arm honours Story 4-4 read-at-entry semantics. AC#8 invariants intact: zero changes to src/web/auth.rs, src/opc_ua_auth.rs, src/opc_ua_session_monitor.rs, src/opc_ua_history.rs, src/security.rs, src/security_hmac.rs, src/main.rs::initialise_tracing. src/opc_ua.rs gains 6 lines (doc comment + #[allow(dead_code)] on the unchanged `pub async fn run` body — required to keep clippy `-D warnings` clean since main.rs now uses the build/run_handles split directly; zero behaviour change, the legacy `run` wrapper still works for lib callers). v1 limitations documented in docs/security.md § Configuration hot-reload: OPC UA address-space mutation stubbed (9-8 territory), credential rotation restart-required, OPC UA-side stale-threshold deferred (per-variable closures), no HTTP trigger, no filesystem watch. 13 new integration tests in tests/config_hot_reload.rs (AC#1/2/3/4/9/10 + io-reason + NoChange), 6 new src/config_reload.rs::tests unit tests, 1 poller hot-reload unit test, 3 clamp-helper unit tests. AC#7 grep contract `git grep -hoE 'event = "config_reload_[a-z]+"' src/ | sort -u` returns exactly 3 lines. Issue #110 (RunHandles missing Drop) verdict: keep open — listener tasks cooperate with cancel_token explicitly. GH tracking issue #112 opened. Next: bmad-code-review on a different LLM. Previous narrative — Story 9-7 ready-for-dev — bmad-create-story produced 9-7-configuration-hot-reload.md (~580 lines, 12 ACs); SIGHUP-only trigger, tokio::sync::watch per 4-4 pre-commitment, knob taxonomy as load-bearing call. Earlier today: Story 4-4 done — iter-3 code review tightened config.toml retry/delay to satisfy AC#4 + 4 patches; loop terminated under CLAUDE.md condition 1. See last_updated YAML field below for full narrative. Previous header narrative — Story 4-4 review — bmad-dev-story implementation complete in a single execution. ALL 12 ACs satisfied on first pass; loop terminates without iteration. See last_updated YAML field below for the full narrative; older narratives chained into the field's value preserve commit-time history. Previous header narrative — Story 4-4 ready-for-dev. bmad-create-story 9-2 produced _bmad-output/implementation-artifacts/9-2-gateway-status-dashboard.md (~700 lines, modelled on Story 9-1 shape). Story closes FR38 + advances FR41: new GET /api/status JSON endpoint reading get_gateway_health_metrics() via a new per-task SqliteBackend (5th call site of the Story 4-1/5-1/8-3 per-task pattern); new AppState struct lands the Story 9-1 deferred shape (auth + backend + dashboard_snapshot); new DashboardConfigSnapshot captures application_count + device_count from AppConfig.application_list at startup (Story 9-7 hot-reload will swap in tokio::sync::watch); new src/web/api.rs hosts the handler (api.rs lands on the directory slot architecture.md:417-421 reserved for "Stories 9-2 onwards"); new static/index.html replaces the 9-1 placeholder with real dashboard markup (5-6 status tiles); new static/dashboard.css ships mobile-first responsive layout + prefers-color-scheme dark; new static/dashboard.js fetches /api/status on load + 10s setInterval; new tests/web_dashboard.rs adds ≥4 integration tests (auth carry-forward + JSON shape + HTML markup + CSS responsive marker). AC count: 7 (AppState + JSON endpoint + auth carry-forward + dashboard HTML/CSS/JS + tests-green + carry-forward intact + sanity counts). Task count: 8. Story explicitly out-of-scope: live per-device metric values (Story 9-3), CRUD (9-4/9-5/9-6), SSE/WebSocket push, historical chart, CSP/HSTS headers, dashboard refresh-interval config knob. Test budget delta: ≈+10 lib+bins (was 333 baseline, target ≥343). Audit-event surface: ZERO new web_* events (success path is routine GETs); ONE new api_* event (api_status_storage_error warn) — total event= grep across src/web/ goes from 2 to 3. AC#6 file invariants: zero changes to src/opc_ua_*.rs production code; zero changes to src/web/auth.rs production code (only fixture imports for AppState shape). Sequencing per epics.md Phase-B polish: 9-2 follows 9-1; 9-3 (live metrics) and 9-2 can run in parallel after 9-1; 9-0 spike still queued before 9-7/9-8. Recommend running on a different LLM per CLAUDE.md "Code Review & Story Validation Loop Discipline" (story 9-2 implementation in one model, code review in another). Carry-forward GH issues unchanged: #88 (per-IP rate limiting), #100 (56 doctest ignores), #102 (tests/common reuse), #103 (story tracker), #104 (TLS hardening follow-up). One new GH issue to open at implementation start (Task 0): main story tracker for 9-2. Next: bmad-dev-story 9-2 to implement. Previous narrative — Story 9-1 done. bmad-code-review iter-1 + iter-2 complete; loop terminated per CLAUDE.md (no decision-needed, no HIGH, no MED unresolved; only LOW remains). Iter-1: 4 decisions resolved (a/a/c/c) + 30 patches applied + 2 deferred. Iter-2 caught 1 HIGH regression (GRACEFUL_SHUTDOWN_BUDGET_SECS timeout was wrapping the entire serve future, killing the server after 5s of normal operation) — fixed via tokio::select! restructure + new regression test. Final: 333 lib+bins / 0 fail / 3 ignored; 779 tests across all targets / 0 fail. clippy clean, doctest 0-fail (56 ignored — issue #100 baseline). AC#7 file invariants intact, AC#8 grep returns exactly 2 event names.)
# project: opcgw
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - SM typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-04-02
last_updated: "2026-05-06 (Story 9-7 ready-for-dev — bmad-create-story produced 9-7-configuration-hot-reload.md (~580 lines, 12 ACs). SIGHUP-only trigger; tokio::sync::watch::Sender<Arc<AppConfig>> per Story 4-4's pre-commitment; validate-then-swap with restart-required-knob rejection; knob taxonomy classifier as load-bearing design call (hot-reload-safe / restart-required / address-space-mutating-9-8 / auth-rotating); OPC UA address-space mutation seam stubbed for Story 9-8 (dashboard updates atomically on reload but OPC UA address space stays at startup state until 9-8 lands). New src/config_reload.rs module + poller/web/opcua-listener wiring + tests/config_hot_reload.rs. AC#8 file invariants extend the prior 9-1/9-2/9-3 zero-LOC pattern: src/web/auth.rs, src/opc_ua*.rs, src/security*.rs, src/main.rs::initialise_tracing all untouched. Issue #110 (RunHandles missing Drop impl) evaluated and explicitly kept deferred — 9-7's listener tasks cooperate with cancel_token via tokio::select! so RAII drop is redundant. Previous narrative — Story 4-4 done — bmad-code-review iter-3 (user-requested fresh adversarial pass after iter-2 PASS, per CLAUDE.md over-reviewing > under-reviewing standing preference) surfaced 1 HIGH decision-needed + 4 MED/LOW patches that iter-1/iter-2 missed. D2 [HIGH]: shipped `config/config.toml` defaults `retry=10, delay=10` (worst-case 100s) violated AC#4's verification clause `retry × delay ≤ 30s`. Resolved via P14: tightened defaults to `retry=30, delay=1` (worst-case 30s — matches AC#4 line 144 example verbatim) + added doc-comment block above the knobs documenting the AC#4 invariant for future operators. Spec dev-notes corrected (lines 69, 144, 225, 589 had referenced incorrect `retry=1, delay=1`). P10 [MED]: cancel-branch + `RecoveryOutcome::Exhausted::last_error` were losing the original gRPC outage cause after the first failed probe (the iter-1 P3 description called for it but the implementation used `last_attempt_error` which gets overwritten); fix introduces immutable `original_outage_cause: String` at function entry and surfaces both probe error AND original cause in cancel-branch message. Test `recovery_aborts_on_cancel_during_sleep` updated with new assertions for both substrings. P11 [MED]: clarified retry/attempt semantics in `docs/logging.md` (recovery_attempt row now explains `chirpstack.retry=N` means N total probes, not N retries after the original failure). P12 [LOW]: `chirpstack_outage` operator-action runbook revised to describe the `chirpstack.retry × chirpstack.delay` budget shape and the recovery_attempt → recovery_complete/recovery_failed sequence. P13 [LOW]: replaced `recovery_complete_carries_downtime_secs`'s fragile field-order-coupled `=10 ` trailing-space assertion with a token-boundary scan via `tracing_test::internal::global_buf()` — robust against future macro field-order changes. 1 deferred (cancel-during-probe path untested; existing `recovery_aborts_on_cancel_during_sleep` is timing-flaky on slow CI — added to deferred-work.md iter-3 section). 1 dismissed (Auditor AC#11 README/sprint-status concern — VERIFIED FALSE POSITIVE; both files were updated in commit `673f54b`, the `Partial` status was an artifact of the narrowed iter-3 diff scope). All four review layers + final cargo test (837 passed / 0 failed / 8 ignored across 17 suites) + cargo clippy --all-targets -- -D warnings are clean. Loop terminates under CLAUDE.md condition 1: zero open findings. Story 4-4 → done; Epic 4 → done (no retrospective re-run needed since Epic 4 retro 2026-04 already flagged 4-4 as soft-deferred). Previous narrative — Story 4-4 review — bmad-dev-story implementation complete in a single execution. ALL 12 ACs satisfied on first pass; loop terminates without iteration. Implementation summary: new `RecoveryOutcome` enum in src/chirpstack.rs (Recovered { attempts_used: u32 } / Exhausted { attempts_used: u32, last_error: String }) + new `ChirpstackPoller::recover_from_chirpstack_outage(error_count_at_entry, last_error) -> RecoveryOutcome` async method (~120 LOC) layered on top of the existing Story 6-3 `maybe_emit_chirpstack_outage` warn. Wire-in at the per-device error branch (src/chirpstack.rs:1052-1068) uses `maybe_emit_chirpstack_outage`'s boolean return value to gate the recovery call (fires at most once per cycle naturally). Helper reads `chirpstack.retry` + `chirpstack.delay` once at entry, calls `update_gateway_status(None, error_count_at_entry, false)` to surface the outage to OPC UA (Story 5-3) + web dashboard (Story 9-2) — `None` for last_poll_timestamp preserves the existing DB value per the trait contract documented at src/storage/mod.rs:685-688. Probes `check_server_availability` up to R times with cancel-token-paired `tokio::time::sleep(D)` budget (Ctrl+C aborts cleanly via tokio::select!). Three reserved log operations from docs/logging.md:240-242 promoted to implemented in the main events table: `recovery_attempt` (info per attempt with attempt + max_retries + delay_secs + last_error fields), `recovery_complete` (info on success with attempts_used + downtime_secs from `last_successful_poll` math, OR from_startup=true on cold start when last_successful_poll is None), `recovery_failed` (warn on budget exhaustion with attempts_used + last_error). The reserved-operations subsection at the bottom of docs/logging.md was removed (only Story 4-4 names were reserved there). Picked up the 6-3 carry-forward from deferred-work.md:86 (Channel::connect() has no explicit timeout — Story 4-4 territory): `create_channel` now wraps `builder.connect()` with `tokio::time::timeout(Duration::from_secs(5), ...)` — 5s budget chosen as smaller than NFR17's 30s SLA so a single channel rebuild doesn't blow the recovery budget, larger than the TCP probe's 1s timeout so transient slow-but-reachable servers don't get falsely flagged. New error path emits `chirpstack_connect` warn with `timeout_secs=5` field; deferred-work.md entry struck-through with RESOLVED note. Tests: 7 new unit tests in src/chirpstack.rs::tests covering all load-bearing ACs (recovery_succeeds_when_server_returns_available — AC#1 + AC#5 + AC#6 single-attempt success path; recovery_exhausts_when_server_stays_down — AC#1 + AC#2 budget exhaustion path; recovery_updates_gateway_status_to_unavailable — AC#3 verifies `update_gateway_status(None, 42, false)` propagates correctly via InMemoryBackend round-trip; recovery_complete_emits_from_startup_when_no_prior_poll — AC#6 cold-start path emits from_startup=true and NOT downtime_secs; recovery_complete_carries_downtime_secs — AC#6 with last_successful_poll set to 10s ago verifies downtime_secs=10 ±1 jitter; recovery_aborts_on_cancel_during_sleep — AC#1 cancel-safety with R=5 D=10 fires cancel after 50ms and asserts elapsed < 2s + Exhausted with cancelled last_error; recovery_single_retry_budget_makes_exactly_one_attempt — AC#2 R=1 budget edge case). All use real TCP listeners via `tokio::net::TcpListener::bind('127.0.0.1:0')` for the simulated outage/recovery state — bind+drop yields a (probabilistically) free port for the unbound case; bind-and-hold for the bound case. Field-shape divergence from spec (functionally equivalent — documented in Dev Agent Record): the spec proposed both unit tests AND a separate tests/chirpstack_recovery.rs integration test file; implementation chose unit-tests-only because the wire-in is a 4-line code change structurally simple enough that the unit tests' contract asserts + clippy provide sufficient verification. The integration test file would have driven the same helper through poll_metrics with extra TCP listener + gRPC stub overhead — duplicative coverage, no new failure modes pinned. Test counts: 322 lib + 7 new lib = **329 lib** + 345 bins = **674 / 0 fail / 5 ignored** (was 667 baseline; Δ=+7). Integration: 154 across 15 binaries / 0 fail (UNCHANGED — no new integration test file). cargo clippy --all-targets -- -D warnings: clean. cargo test --doc: 0 fail / 56 ignored (issue #100 baseline unchanged). AC#6 invariants reverified: git diff HEAD --stat src/web/ static/ tests/web_*.rs src/opc_ua_auth.rs src/opc_ua_session_monitor.rs src/opc_ua_history.rs src/opc_ua.rs all produce zero output. AC#7 grep contract: `git grep -hoE 'operation = \"recovery_[a-z]+\"' src/chirpstack.rs | sort -u | wc -l` returns 3 (recovery_attempt + recovery_complete + recovery_failed); same grep across src/ also returns 3 (no leakage). Documentation surface: docs/logging.md updated (3 operations promoted from reserved-table to main events table at line 147 region; reserved-operations subsection at bottom removed; 'polls keep failing' diagnostic recipe updated to mention recovery_attempt/complete/failed); README.md Epic 4 row updated to '🔄 in-progress (4-1/4-2/4-3 done · 4-4 review)' with full 4-4 narrative paragraph; Current Version line bumped to Story 4-4 review narrative; deferred-work.md 6-3 carry-forward entry struck through with RESOLVED note. NO changes to docs/security.md, config/config.toml, config/config.example.toml (per AC#11 — recovery loop adds no new operator-facing surface beyond log operations + the channel timeout, which doesn't need a new config knob since it's a fixed 5s sanity ceiling). Files: 0 new (wire-in fits in existing src/chirpstack.rs + existing test module); ~5 modified (src/chirpstack.rs, README.md, docs/logging.md, _bmad-output/implementation-artifacts/sprint-status.yaml, _bmad-output/implementation-artifacts/deferred-work.md, _bmad-output/implementation-artifacts/4-4-auto-recovery-from-chirpstack-outages.md). Task 0 GitHub tracker issue deferred to commit time per Story 9-2 precedent. Recommend running bmad-code-review on a different LLM per CLAUDE.md 'Code Review & Story Validation Loop Discipline'. Carry-forward GH issues unchanged: #88 (per-IP rate limiting), #100 (56 doctest ignores), #102 (tests/common reuse — done), #104 (TLS hardening), #107 (Story 9-3 KF — duplicate metric_name validation), #108 (Storage KF — payload-less MetricType, BLOCKS production deployment; orthogonal to 4-4), #110 (Story 9-0 KF — RunHandles missing Drop). Note on Epic 9 retro: still BLOCKED on #108 — Story 4-4 does NOT unblock the retro; #108 is the operational gap, 4-4 is a Phase A reliability story landing during Phase B. Next required action per CLAUDE.md BMad Workflow Commit & Push Discipline: commit Story 4-4 implementation-complete (single 'Story 4-4: ... - Implementation Complete' commit per the per-story commit rule), then run bmad-code-review on a different LLM. Previous narrative — Story 4-4 ready-for-dev — bmad-create-story 4-4 produced _bmad-output/implementation-artifacts/4-4-auto-recovery-from-chirpstack-outages.md as Phase A carry-forward (deferred at Epic 4 retro 2026-04 line 125 as soft dependency; picked up now to clear long-standing tech-debt before Story 9-7 reshapes poller reload semantics). Story 4-4 closes PRD FR6 (TCP connectivity check), FR7 (auto-reconnect without manual intervention), FR8 (configurable retry count + delay — knobs `chirpstack.retry` and `chirpstack.delay` already exist + validated at src/config.rs:931-936), and NFR17 (30s auto-recovery SLA). Spec: 12 ACs (AC#1-#7 from epics.md:532-549 BDD + AC#8 carry-forward invariants for Stories 5/6/7/8/9 + AC#9 tracing-event-name contract for the 3 reserved operations + AC#10 NFR12 carry-forward + AC#11 docs sync + AC#12 tests-pass-clippy-clean). 6 tasks (Task 0 GH issue at commit time per Story 9-2 precedent + Task 1 recovery loop helper in src/chirpstack.rs ~80-150 LOC + Task 2 gateway_status update during outage + Task 3 promote 3 reserved log operations from docs/logging.md:240-242 reserved-table to main implemented-table — recovery_attempt info / recovery_complete info with downtime_secs / recovery_failed warn — same operation= names verbatim + Task 4 integration tests in NEW tests/chirpstack_recovery.rs ~250-400 LOC with stub-backend pattern via tokio::net::TcpListener bound on pick_free_port + drop/rebind to simulate outage/recovery + Task 5 docs sync README/sprint-status/docs-logging.md + Task 6 final verification cargo-test + clippy + grep contract). Reserved log operations from docs/logging.md:240-242 (recovery_attempt/recovery_complete/recovery_failed) get promoted to implemented in Task 3. Existing chirpstack_outage warn (Story 6-3 AC#5) stays — recovery loop layers ON TOP, does not replace. Existing TCP probe retry loop at src/chirpstack.rs:1786-1847 stays — recovery loop wraps cycle-level recovery on top of intra-cycle probe retry. Picks up deferred-work.md:86 entry (Channel::connect() has no explicit timeout — adds 5s tokio::time::timeout wrap inside create_channel). Carry-forward invariants enumerated explicitly in AC#8 (Story 5-3 gateway_status table single-row id=1 + Story 5-2 stale-data StatusCode::Bad + Story 6-1/6-2/6-3 tracing-only logging + Story 7-2/7-3 zero changes to opc_ua_auth/opc_ua_session_monitor + Story 8-3 zero changes to opc_ua_history + Story 9-0 zero changes to opc_ua + RunHandles + Story 9-1/9-2/9-3 zero changes to web/static/tests/web_*.rs). Out of scope: per-IP throttling (#88), exponential backoff (Story 9-7 territory), recovery-aware OPC UA event emission (Story 5-2 already covers via stale-data flags), web dashboard live recovery status (Story 9-2 reads gateway_status surface naturally), hot-reload of retry/delay knobs (Story 9-7), commands queue path outage detection (already covered by Story 3-1), address-space mutation (9-7/9-8), multi-server failover (project supports single chirpstack.server_address), #108 storage payload-less (orthogonal — recovery loop doesn't touch metric values), doctest cleanup (#100). Test budget delta: ≈+3 unit + ≈+3-5 integration tests; target ≥670 lib+bins (was 667 baseline) + 16 integration binaries (was 15 + 1 NEW chirpstack_recovery.rs). Audit-event surface: ZERO new src/web/ event= names (still 4); +3 src/chirpstack.rs operation= names (recovery_attempt + recovery_complete + recovery_failed); src/ event= grep count grows from 16 to 19 (or stays 16 if the operation= names are scoped under a different field-name convention — Task 3 verifies). AC#6 file invariants: zero changes to src/opc_ua*.rs / src/web/* / static/* / tests/web_*.rs / docs/security.md / config/config.toml / config/config.example.toml. Sequencing: Phase A carry-forward; lands BEFORE Story 9-7 (configuration hot-reload) reshapes poller reload semantics. Carry-forward GH issues unchanged: #88, #100, #102, #104, #107, #108, #110. One new GH issue to open at implementation start (Task 0): main story tracker for 4-4 (deferred to commit time per Story 9-2 precedent). Recommend running bmad-code-review on a different LLM per CLAUDE.md 'Code Review & Story Validation Loop Discipline' (story 4-4 implementation in one model, code review in another). Next: bmad-dev-story 4-4 to implement. Previous narrative — Story 9-0 done — bmad-code-review iter-1 + iter-2 loop terminated per CLAUDE.md (zero decision-needed unresolved; zero HIGH unresolved; zero MED unresolved; remaining HIGH/MED items either patched or explicitly accepted as deferred per condition 3 with documented one-line reason in deferred-work.md). Iter-1 produced 17 (Blind Hunter) + 32 (Edge Case Hunter) + 6 (Acceptance Auditor) = 55 raw findings → ~42 unique after dedup; triage 1 decision-needed (D1 — AC#3 measurement substitution) + 11 patches (P1-P11 after D1→P11 conversion) + 14 deferred + 17 dismissed. D1 resolved with Option (a) — accept and document the AC#3 substitution per Guy's preference (lock-hold-duration measures the same physical concern as max-gap; under OPC UA's value-change semantics with static-sentinel callbacks the original max-gap measurement is meaningless). P1 (HIGH — RunHandles missing Drop impl) was attempted but blocked by rustc E0509 (cannot move out of struct fields when Drop impl is present, but run_handles consumes server: Server and gauge_handle: JoinHandle<()> by value via destructuring); P1 reclassified to deferred per Guy's explicit decision with GitHub KF tracker issue #110 filed (well-formed per #107/#108 precedent: KF classification banner + Summary + Why-cannot-add-Drop + Why-currently-theoretical + Failure-mode + Affected-future-surface + three-workaround-shapes + Why-classified-as-KF + References). Iter-1 patches applied (P2-P11): P2 reconciled spike report § 8 false claim about deferred-work.md by adding the stale-callback-leak entry; P3 added asserts on add_folder bool + add_variables Vec<bool> in Q3 bulk mutation; P4 removed CI-flake-prone strict <1s lock-hold assert (verdict tier classification preserved in eprintln); P5 restructured spike report § 7 from 'Throughput measurement (out of scope)' to 'Pattern reuse' with throughput note folded into final paragraph; P6 added sentinel-value warm-up assertion to Q2 (mirrors Q1 pattern, prevents false 'frozen-last-good' verdict on un-invoked callback); P7 replaced 'let _ = run_handles().await' with 'if let Err(e) = ... { eprintln!(...) }' for diagnostic visibility; P8 replaced 'wait_for_connection unwrap_or(false) + assert' with three-arm match on (Ok(true), Ok(false), Err(_)) with distinct panic messages; P9 ratified AC#1/AC#2 baseline-stream demotion in spec AC Amendments + spike report §§ 4/5 addenda; P10 corrected Dev Agent Record numerical inaccuracies (~110 LOC → ~147 LOC; 22 KB → 36 KB precedent); P11 ratified AC#3 substitution in spec AC Amendments + spike report § 6 addendum. Iter-2 re-ran all three reviewer layers per CLAUDE.md 'don't trust a single pass after a non-trivial patch round' — produced 16 (Blind Hunter) + 17 (Edge Case Hunter) + per-patch verdicts (Acceptance Auditor: 10 of 11 PASS, 1 PARTIAL on P10 second-occurrence at File List) = ~50 raw → ~12 unique. Iter-2 patches applied (IP1-IP6, all MED+LOW): IP1 fixed P10 residual '~110 LOC' at File List (Acceptance Auditor catch); IP2 restored a 30s sanity ceiling on lock_hold_duration (catches catastrophic regressions like deadlock without re-introducing iter-0's 1s flake source); IP3 corrected Review Findings header arithmetic (Patches '10 — P1 reclassified to defer' that visually listed P1-P11 → reorganised so Patches has P2-P11 and Deferred has 15 incl. P1; opening paragraph dedup count corrected from '~25' to '~42'); IP4 hardened P6 sentinel comparison from '(v - 42.0_f32).abs() < 1e-6' to 'assert_eq!(v, 42.0_f32, ...)' (42.0_f32 is exactly representable so 1e-6 was theatre; assert_eq! also rejects NaN by IEEE semantics — fixes the misleading-diagnostic concern); harmonised Q1 to use the same pattern; IP5 cleaned up 'wait no' stream-of-consciousness phrasing in RunHandles doc-comment (pre-existing iter-0 prose that the iter-1 patch had touched the region of); IP6 corrected wrong file-path bracket on stale-callback-leak entry in deferred-work.md (leak surface is async-opcua's SimpleNodeManagerImpl registry; src/opc_ua_history.rs is just a mitigation surface). Final test count: 322 lib + 345 bins = 667 / 0 fail / 5 ignored (UNCHANGED from Story 9-3 baseline — Shape B refactor + iter-1 + iter-2 patches are regression-free); 6 spike tests / 0 fail (3 spike + 3 inherited common helpers); cargo clippy --all-targets -- -D warnings clean; cargo test --doc 0 fail / 56 ignored (issue #100 baseline unchanged). AC#6 invariants reverified: git diff HEAD --stat src/web/ static/ tests/web_*.rs src/opc_ua_auth.rs src/opc_ua_session_monitor.rs src/opc_ua_history.rs all produce zero output. AC#7 grep contract holds: src/web/ has exactly 4 distinct event= names (web_server_started, web_auth_failed, api_status_storage_error, api_devices_storage_error); src/ has exactly 16. AC#8 NFR12 carry-forward intact. Q1 + Q2 + Q3 verdicts unchanged: Q1 RESOLVED FAVOURABLY (sentinel arrives within 5s budget on runtime-added variable); Q2 Behaviour B (frozen-last-good — Story 9-8 must emit BadNodeIdUnknown via manager.set_attributes before address_space.delete()); Q3 RESOLVED FAVOURABLY (lock-hold = 117.604 µs vs 100ms sampler tick = 850× headroom; sibling sampler cannot be starved). Spike report § 6 addendum + spec AC#3 amendment now ratify the AC#3 measurement substitution explicitly; the lesson for Story 9-7/9-8 spec authors is documented (assertions that depend on subscription-stream continuity require dynamic value-changing callbacks; sentinel-value read callbacks alone produce only a first notification per monitored item). New GH issue: #110 (Story 9-0 KF — RunHandles lacks Drop impl; gauge task leaks if cancel-token sharing convention is broken; tracked for Story 9-7 evaluation when designing hot-reload listener lifecycle). Carry-forward GH issues unchanged: #88 (per-IP rate limiting), #100 (56 doctest ignores), #102 (tests/common reuse — done), #103 (Story 9-1), #104 (TLS hardening), #105 (Story 9-2), #106 (Story 9-3 main), #107 (Story 9-3 KF — duplicate metric_name validation), #108 (Storage KF — payload-less MetricType, BLOCKS production deployment; orthogonal to 9-0 spike). Note on Epic 9 retro: still BLOCKED on #108. Story 9-0 does NOT unblock the retro — 9-8 must coordinate explicit cleanup (per Q2 finding) AND Epic A storage refactor must land (per #108) before Epic 9 can close cleanly. Next required action per CLAUDE.md BMad Workflow Commit & Push Discipline: commit Story 9-0 code-review-complete (single 'Story 9-0: ... - Code Review Complete' commit per the per-story commit rule, covering iter-1 + iter-2 patches together — both per-iteration and end-of-review patterns are acceptable per the 2026-04-30 CLAUDE.md clarification), then proceed to Story 9-7 OR run bmad-retrospective for Epic 9 milestone (Epic 9 still in-progress with 9-1, 9-2, 9-3 done + 9-0 done; retrospective optional and BLOCKED on #108). Previous narrative — Story 9-0 review — bmad-dev-story implementation complete in a single execution. ALL 10 ACs satisfied on first pass; loop terminates without iteration. Three load-bearing questions from epics.md:784-787 resolved empirically under live subscriptions against async-opcua 0.17.1: Q1 add path RESOLVED FAVOURABLY (a runtime-added variable's first subscription notification carries the registered sentinel `Variant::Float(42.0)` with `StatusCode::Good` — confirms SyncSampler honours post-init add_read_callback registrations without restart, no production code change needed beyond the AC#5 build/run_handles split); Q2 remove path Behaviour B (frozen-last-good — stream went silent post-delete; no observable client error in 3s observation window — Story 9-8 must emit explicit BadNodeIdUnknown via manager.set_attributes before address_space.delete() to give SCADA clients a clean status transition); Q3 sibling isolation RESOLVED FAVOURABLY (write-lock-hold for 11-node bulk mutation = 117.604 µs, ~850× shorter than the 100ms sampler tick — no sampler starvation risk at typical 100-1000-node hot-reload scales; bulk-added Metric05 received its first notification within the 5s budget, confirming bulk-add semantics match single-add). Implementation summary: Shape B chosen per AC#5 — split OpcUa::run into build (returns RunHandles { server, server_handle, manager, gauge_handle, state_guard, cancel_token }) + run_handles + backward-compat run wrapper. All 4 existing OPC UA integration test files (opcua_subscription_spike, opcua_history, opc_ua_security_endpoints, opc_ua_connection_limit) compile unchanged via the wrapper. Single src/ change confined to src/opc_ua.rs (~110 LOC: ~60 LOC RunHandles struct + ~50 LOC build/run_handles split, well under the 200 LOC Shape A fallback threshold). Files: 3 new (tests/opcua_dynamic_address_space_spike.rs at ~700 LOC including 3 spike tests + DynTestServer fixture + helpers; _bmad-output/implementation-artifacts/9-0-spike-report.md at 20 KB / 234 lines / 12 sections; this story file); ~3 modified (src/opc_ua.rs, README.md, sprint-status.yaml). Test counts: 322 lib + 345 bins = 667 / 0 fail / 5 ignored (UNCHANGED from Story 9-3 baseline — Shape B refactor is regression-free); 154 integration tests / 0 fail across 15 binaries (was 14, +1 spike file with 6 tests = 3 spike + 3 inherited common helpers); cargo clippy --all-targets -- -D warnings clean; cargo test --doc 0 fail / 56 ignored (issue #100 baseline unchanged). AC#6 invariants reverified: git diff HEAD --stat src/web/ static/ tests/web_*.rs src/opc_ua_auth.rs src/opc_ua_session_monitor.rs src/opc_ua_history.rs all produce zero output. AC#7 grep contract holds: src/web/ has exactly 4 distinct event= names (web_server_started 9-1, web_auth_failed 9-1, api_status_storage_error 9-2, api_devices_storage_error 9-3); src/ has exactly 16 (broader-scope unchanged). Note: the AC#7 verification command in the story file (-hE | sort -u) double-counts due to whitespace-prefix variation in matched lines (returns 17); the corrected -hoE form returns the accurate 16 — minor verification-command bug logged in Dev Agent Record Debug Log. Field-shape divergences from spec (all functionally equivalent — documented in Dev Agent Record): (1) original AC#3 sibling-stream max-gap measurement replaced with lock-hold-duration + fresh-subscription verdict because the OPC UA subscription model only emits notifications on value changes (DataChangeFilter default behaviour); with static-value sentinel callbacks, sibling subscriptions naturally produce only their first notification regardless of mutation activity — measuring inter-publish gaps would record sampler-noise not write-lock starvation. The revised metric (lock-hold < 100ms = FAVOURABLE) measures the actual concern. Documented in spike report § 6 'Note on the simplified Q3 verdict'. (2) AC#1 + AC#2 'baseline keeps producing notifications' soft-checks demoted from asserts to informational drains for the same value-change-semantics reason. (3) Spike report grew to 20 KB vs 6-12 KB target — within Story 8-1 precedent (22 KB) which explicitly accepted verbosity for architecture-reference value. Known limitation surfaced (logged in spike report § 8): SimpleNodeManagerImpl does not expose remove_read_callback (verified via source enumeration of pub fn symbols at simple.rs); deleting a node leaks the registered closure with its captured Arc<dyn StorageBackend> + StatusCache + device_id + metric_name + threshold (~120 bytes per closure). Operationally negligible at expected churn rates (~150 leaks over 30 days = ~18 KB) but strictly a leak. Story 9-8 mitigation: extend OpcgwHistoryNodeManager with a remove-callback wrap method (the wrap pattern from Story 8-3) OR file upstream FR to async-opcua (precedent: Story 8-1 issue #94 session-rejected callback FR). Documentation surface: README.md Planning row updated with full 9-0 narrative + Current Version line bumped to 2026-05-04. No changes to docs/security.md, docs/logging.md, config/config.toml, config/config.example.toml (per AC#9 — spike adds no new operator surface). Task 0 GitHub tracker issue deferred to commit time per Story 9-2 precedent. Recommend running bmad-code-review on a different LLM per CLAUDE.md 'Code Review & Story Validation Loop Discipline' — over-reviewing > under-reviewing per Guy's stated preference (memory: feedback_review_iterations.md). Carry-forward GH issues unchanged: #88 per-IP rate limiting, #100 56 doctest ignores, #102 tests/common reuse (done), #103 Story 9-1, #104 TLS hardening, #105 Story 9-2, #106 Story 9-3 main, #107 Story 9-3 KF (duplicate metric_name validation), #108 Storage KF (payload-less MetricType — BLOCKS production deployment; orthogonal to 9-0 spike since spike uses sentinel values). Note on #108: Epic 9 retrospective remains BLOCKED on #108. Story 9-0 does NOT unblock the retro — 9-8 must coordinate explicit cleanup (per Q2 finding) AND Epic A storage refactor must land (per #108) before Epic 9 can close cleanly. Next required action per CLAUDE.md BMad Workflow Commit & Push Discipline: commit Story 9-0 implementation-complete (single 'Story 9-0: ... - Implementation Complete' commit per the per-story commit rule), then run bmad-code-review on a different LLM. Previous narrative — Story 9-0 ready-for-dev. bmad-create-story 9-0 produced _bmad-output/implementation-artifacts/9-0-async-opcua-runtime-address-space-mutation-spike.md (~500 lines, modelled on Story 8-1 spike shape since both are spikes with reference-test deliverables — smaller than 8-1 because 9-0 has 3 questions vs 8-1's 6 and no manual-SCADA / load-probe surfaces). Story closes the Phase B carry-forward decision at epics.md:780-793 (Epic 8 retro 2026-05-02 action item #5): empirical confirmation of async-opcua 0.17.1 runtime address-space mutation contract under live subscriptions. Three load-bearing questions: Q1 add path (fresh subscription on a runtime-added variable receives DataChangeNotifications), Q2 remove path (subscription on deleted variable observes which of three plausible status-code behaviours), Q3 sibling isolation (unaffected subscriptions continue uninterrupted under bulk write-lock-held mutation). New test infrastructure: single new integration-test file tests/opcua_dynamic_address_space_spike.rs (~250-400 LOC, 3 #[serial_test::serial] tests) modelled on tests/opcua_subscription_spike.rs shape with mod common; reuse + per-file-divergent init_test_subscriber/setup_dyn_test_server/DynTestServer struct (manager-handle holder is the divergence). Single small production-code change: one new pub accessor on OpcUa exposing Arc<OpcgwHistoryNodeManager> + ServerHandle to the spike test (Shape A oneshot channel ~30-50 LOC OR Shape B split run into build+run_handles ~80-150 LOC; Shape B recommended because Story 9-7 hot-reload will need the same shape — landing it in 9-0 is lifting forward, not speculative). NOT #[cfg]-gated because 9-7 will consume it as production code. AC count: 10 (Q1 + Q2 + Q3 + spike report + reference-test discipline + carry-forward invariants from 7-2/7-3/8-3/9-1/9-2/9-3 + zero new event= rows + NFR12 carry-forward intact + docs sync + tests-green). Task count: 6 (GH issue + accessor + spike test file + run-and-report + docs sync + final verification). Story explicitly out-of-scope: hot-reload mechanism (9-7), apply_config_diff algorithm (9-8), web-UI CRUD (9-4/9-5/9-6), NodeId collision regression test for #99 (9-5's territory), issue #108 storage payload-less (orthogonal — spike uses sentinel values), TLS-bound clients, throughput/load-probe (8-1 owns), manual FUXA/UaExpert (carry-forward to issue #93), per-IP rate limiting (#88), Plan B implementation (deferred to 9-7 or 9-8 if a question fails), tests/common extension (already done at issue #102), doctest cleanup (#100). Test budget delta: +3 integration tests on 1 new test binary (target ≥670 lib+bins, was 667 baseline; integration binaries 16→17). Audit-event surface: ZERO new event= names in src/ (AC#7 grep contract: still exactly 4 distinct names — web_server_started, web_auth_failed, api_status_storage_error, api_devices_storage_error). AC#6/AC#8 file invariants: zero changes to src/web/*, static/*, tests/web_*.rs, src/opc_ua_auth.rs, src/opc_ua_session_monitor.rs, src/opc_ua_history.rs; only allowed change in src/ is one accessor in src/opc_ua.rs (≤150 LOC for Shape B; ≤50 LOC for Shape A). New deferral candidate (likely): stale read-callback closure leak when a variable is deleted (SimpleNodeManagerImpl probably doesn't expose a remove-callback API; spike report § 8 documents this for 9-7/9-8 to mitigate via OpcgwHistoryNodeManager wrap extension OR upstream FR). Sequencing per epics.md:793: 9-0 follows 9-1/9-2/9-3 (which are done); must complete BEFORE 9-7 (hot-reload depends on the spike's findings) and 9-8 (dynamic mutation depends on the spike's findings). Recommended commit order: bmad-dev-story 9-0 → bmad-code-review on a different LLM per CLAUDE.md 'Code Review & Story Validation Loop Discipline'. Carry-forward GH issues unchanged: #88 (per-IP rate limiting), #100 (56 doctest ignores), #102 (tests/common reuse — done), #103 (Story 9-1), #104 (TLS hardening), #105 (Story 9-2), #106 (Story 9-3 main), #107 (Story 9-3 KF — duplicate metric_name validation), #108 (Storage KF — payload-less MetricType, BLOCKS production deployment; orthogonal to the spike). One new GH issue to open at implementation start (Task 0): main story tracker for 9-0 (deferred to commit time per Story 9-2 precedent). Note on Epic 9 retro: still BLOCKED on #108 — 9-0 spike does not unblock the retro; #108 is the operational gap, 9-0 is a Phase B architectural prep. Next: bmad-dev-story 9-0 to implement. Previous narrative — Story 9-3 done — bmad-code-review iter-1 + iter-2 loop terminated per CLAUDE.md (zero decision-needed / HIGH / unresolved MED; H3 + M5 explicitly deferred via KF GitHub issues #107 + #108; only LOW remains). Iter-1: 3 parallel adversarial layers (Blind Hunter / Edge Case Hunter / Acceptance Auditor). Auditor PASS on all 7 ACs in both iterations. 17 (Blind) + 9 (Edge) = 26 raw findings → ~24 after dedup → 6 patches + 2 KF deferred + ~16 LOW deferred. Iter-1 patches: H1 (replaced parallel Vec<String>+Vec<OpcMetricTypeConfig> on DeviceSummary with single Vec<MetricSpec> — type-enforces 1-to-1 invariant), H2 (server-side clamp+warn on stale_threshold_secs in main.rs — rejects 0 + values > BAD_THRESHOLD_SECS), M3 (FailingBackend → FailingBackendForApiTests rename + extended doc-comment), M6 (tighter MIME assertion: text/javascript || application/javascript), L1 (JS != null guard), L6 (as_of recency assertion). KF deferrals: H3 → #107 (duplicate metric_name within device collapses silently — pre-existing config validation gap), M5 → #108 (MetricType is payload-less, value column always equals data_type string — pre-existing project-wide bug, BLOCKS production deployment, needs Epic-1-scale storage trait refactor). Iter-2 re-review per CLAUDE.md 'don't trust a single pass after a non-trivial patch round': 4 (Blind) + 3 (Edge) = 7 raw findings → 6 after dedup → all LOW; 5 patched (L1 stale doc comments at 4 sites, L3 better clamp value to DEFAULT not BAD-1, L4 30s recency window, L5 boundary > vs >=, L6 BAD_THRESHOLD_SECS pub const dedup) + 3 accepted LOW (device_count↔devices.len() invariant carry-forward, spec Field-shape divergence section closed via in-place addendum, H2 clamp behaviour undocumented in operator docs). Final test count: 322 lib + 345 bins = 667 total / 0 fail / 5 ignored (unchanged from iter-0); 16 integration binaries / 0 fail; tests/web_dashboard.rs reports 12 passed. cargo clippy --all-targets -- -D warnings: clean. cargo test --doc: 0 fail (56 ignored — issue #100 baseline). AC#6 invariants reverified: git diff HEAD --stat src/opc_ua{.,_history,_session_monitor,_auth}.rs src/web/auth.rs static/index.html static/dashboard.js produces zero output across all 7 invariant files; src/main.rs modified +30 LOC inside the existing if web_enabled { ... } block (documented divergence — see story file). AC#7 grep contract: exactly 4 distinct event= names in src/ (web_server_started 9-1 info, web_auth_failed 9-1 warn audit, api_status_storage_error 9-2 warn diag, api_devices_storage_error 9-3 warn diag) with one emit site each. Story 9-3 closed via main story tracker issue #106 in the implementation commit (0187dc8); 2 new KF tracker issues #107 + #108 opened during iter-1 review. Carry-forward GH issues: #88 (per-IP rate limiting), #100 (56 doctest ignores), #102 (tests/common reuse), #103 (Story 9-1), #104 (TLS hardening), #105 (Story 9-2), #106 (Story 9-3 main), #107 (Story 9-3 KF — duplicate metric_name validation), #108 (Storage KF — payload-less MetricType, BLOCKS production). Next required action per CLAUDE.md BMad Workflow Commit & Push Discipline: commit Story 9-3 code-review-complete, then proceed to Story 9-0 (async-opcua runtime address-space mutation spike — per epics.md Phase-B sequencing 9-1 → 9-2 → 9-3 → 9-0 spike → 9-7 → 9-8 → 9-4/9-5/9-6) OR run bmad-retrospective for Epic 9 milestone if epic-scope guidance is needed (Epic 9 in-progress with 9-1, 9-2, 9-3 done; retrospective optional until all 9 stories complete). Previous narrative — Story 9-3 review — bmad-dev-story implementation complete in a single execution. ALL 7 ACs satisfied on first pass; loop terminates without iteration. Implementation summary: new src/web/api.rs::api_devices handler reads StorageBackend::load_all_metrics() via the existing 9-2 web SqliteBackend (no new task-claimer; pool size 5 unchanged), builds an O(N) HashMap lookup keyed by (device_id, metric_name), then walks the snapshot to produce application-grouped JSON; new DevicesResponse / ApplicationView / DeviceView / MetricView structs ship the 4-field top-level shape (as_of, stale_threshold_secs, bad_threshold_secs, applications); ApplicationSummary (Story 9-2) extended with devices: Vec<DeviceSummary> field where DeviceSummary { device_id, device_name, metric_names, metric_types: Vec<OpcMetricTypeConfig> } mirrors the configured topology (the metric_types field beyond the spec — needed for the configured-fallback data_type when a metric has no row in metric_values); DashboardConfigSnapshot::from_config walk deepens by one level; new AppState.stale_threshold_secs: u64 field resolved at construction in main.rs.unwrap_or(DEFAULT_STALE_THRESHOLD_SECS=120); new event='api_devices_storage_error' warn diagnostic on storage-read failure (NFR7-aligned: log inner error to operator log, return generic body to client); new static/metrics.html (NOT static/devices.html — devices.html stays reserved for Story 9-5 device CRUD per Story 9-1 placeholder reservation; course-correction documented in Dev Agent Record); new static/metrics.js mirrors dashboard.js iter-2 hardening (in-flight guard + AbortController feature-detect + Content-Type sniff + generic error banner + stale-render guard + shared parseTimestamp); CSS bundled into static/dashboard.css (additions only, scoped to .metrics-grid-container so 9-2 dashboard tile selectors are byte-identical) rather than separate static/devices.css; tests/web_dashboard.rs reports 12 passed (was 8 — +4 new Story 9-3 tests: auth_required_for_api_devices + api_devices_returns_json_with_expected_shape_when_authed + metrics_html_contains_viewport_meta_and_grid_markup + metrics_js_is_served_and_references_api_devices). Field-shape divergences from spec (all functionally equivalent — documented in Dev Agent Record): (1) added metric_types: Vec<OpcMetricTypeConfig> to DeviceSummary beyond spec, (2) created NEW static/metrics.html instead of replacing static/devices.html (devices.html reserved for 9-5), (3) new AppState.stale_threshold_secs scalar field instead of Arc<AppConfig> threading — minor src/main.rs +12 LOC divergence from AC#6, (4) CSS bundled into dashboard.css with .metrics-grid-container scoping, (5) Cargo.toml unchanged. Test count: 322 lib + 345 bins = 667 total (was 657 baseline; Δ=+10 matches spec target). Integration test count: 16 binaries / 0 failures including tests/web_dashboard.rs at 12 passed (+4 from 8). cargo clippy --all-targets -- -D warnings: clean. cargo test --doc: 0 failed (56 ignored — issue #100 baseline). AC#6 invariants reverified: git diff HEAD --stat src/opc_ua{.,_history,_session_monitor,_auth}.rs src/web/auth.rs static/index.html static/dashboard.js produces zero output across all 7 invariant files. AC#7 grep contract: exactly 4 distinct event= names introduced (web_server_started 9-1 info, web_auth_failed 9-1 warn audit, api_status_storage_error 9-2 warn diag, api_devices_storage_error 9-3 warn diag), each with one emit site. Documentation surface: docs/security.md § Web UI authentication → API endpoints subsection extended with /api/devices paragraph (server-side as_of + dual threshold fields + missing-metric semantics); docs/logging.md event= registry +1 row; README.md Web UI subsection +Story 9-3 paragraph + Epic 9 Planning row updated to '9-1 done · 9-2 done · 9-3 review' + last_updated bumped. Files: 3 new (static/metrics.html, static/metrics.js, this story file); ~10 modified (src/web/mod.rs, src/web/api.rs, src/main.rs, static/dashboard.css, tests/web_auth.rs, tests/web_dashboard.rs, docs/security.md, docs/logging.md, README.md, sprint-status.yaml). Task 0 GitHub tracker issue deferred to commit time per Story 9-2 precedent. Recommend running bmad-code-review on a different LLM per CLAUDE.md 'Code Review & Story Validation Loop Discipline'. Carry-forward GH issues unchanged: #88 (per-IP rate limiting), #100 (56 doctest ignores), #102 (tests/common reuse), #103 (Story 9-1), #104 (TLS hardening), #105 (Story 9-2). Previous narrative — Story 9-3 ready-for-dev — bmad-create-story 9-3 produced _bmad-output/implementation-artifacts/9-3-live-metric-values-display.md (~750 lines, modelled on Story 9-2 shape). Story closes FR37 + advances FR41: new GET /api/devices JSON endpoint reading load_all_metrics() via the existing 9-2 web SqliteBackend (no new task-claimer; pool size 5 unchanged); ApplicationSummary struct (Story 9-2) extended with devices: Vec<DeviceSummary> field where DeviceSummary { device_id, device_name, metric_names: Vec<String> } mirrors the configured [[application.device]] + [[application.device.read_metric]] structure; DashboardConfigSnapshot::from_config walk deepens by one level; new src/web/api.rs::api_devices handler returns DevicesResponse { as_of, stale_threshold_secs, bad_threshold_secs, applications: Vec<ApplicationView { application_id, application_name, devices: Vec<DeviceView { device_id, device_name, metrics: Vec<MetricView { metric_name, data_type, value, timestamp }> }> }> } JSON; per-row staleness computed client-side via (as_of - timestamp) vs the two threshold fields (consistent with Story 9-2's 'server returns facts, client interprets' pattern); static/devices.html replaces the 9-1 placeholder with real per-device grid markup; static/devices.js mirrors dashboard.js iter-2 hardening (in-flight guard + AbortController feature-detect + Content-Type sniff + generic error banner + stale-render guard); CSS bundled into static/dashboard.css OR new static/devices.css (dev agent picks). Tests: ≥4 new unit tests for api_devices (success / 500-with-generic-body / null-on-unpolled-metric / data_type fallback) + ≥4 new integration tests in tests/web_dashboard.rs (auth carry-forward + JSON shape + HTML markup + JS file existence). Total event= grep grows from 3 → 4 (new event='api_devices_storage_error' warn diag, mirrors 9-2 api_status_storage_error shape). Test budget delta: ~+10 lib+bins (target ≥350; was 340 baseline). AC#6 file invariants: zero changes to src/opc_ua_*.rs / src/web/auth.rs / src/main.rs / static/index.html / static/dashboard.js production code; src/web/api.rs gets additions only (api_status invariant); static/dashboard.css EITHER zero changes (new-file path) OR additions-only. Sequencing per epics.md Phase-B polish: 9-3 follows 9-2; 9-0 spike still queued before 9-7/9-8. Recommend running on a different LLM per CLAUDE.md 'Code Review & Story Validation Loop Discipline'. Carry-forward GH issues unchanged: #88 (per-IP rate limiting), #100 (56 doctest ignores), #102 (tests/common reuse), #103 (Story 9-1), #104 (TLS hardening), #105 (Story 9-2). One new GH issue to open at implementation start (Task 0): main story tracker for 9-3 (deferred to commit time per Story 9-2 precedent). Carry-forward LOWs from Story 9-2 review acknowledged in Dev Notes: L1 (Content-Type case sensitivity — applies symmetrically to new devices.js), L3 (pool size literal — 9-3 doesn't add a task-claimer), L5 (tracing-test poison-mutex — pre-existing), L6 (buffer-clear pattern not extracted — duplication grows from 1 to 2 sites, still under 3-site DRY threshold). Next: bmad-dev-story 9-3 to implement. Previous narrative — Story 9-2 done — bmad-code-review iter-1 + iter-2 loop terminated per CLAUDE.md (zero decision-needed / HIGH / unresolved MED; only LOW remains). Iter-1: 3 parallel adversarial layers (Blind Hunter / Edge Case Hunter / Acceptance Auditor); Acceptance Auditor reported PASS across all 7 ACs on iter-1 + iter-2. 17 (Blind) + 14 (Edge) = 31 raw findings → 29 after dedup → 1 decision-needed (D1: pool size 3 → 5; user chose option-a) + 13 patches + 9 deferred + 5 dismissed. Iter-1 patches: B2 (HTML test pins all 10 DOM IDs not 5), B3 (FailingBackend descriptive panic), B5+E10 (clear tracing-test buf), B6 (handle.await.expect on 4 sites), B9 (no err.message in dashboard banner), B10 (build_state slice signature), B11 (CARGO_MANIFEST_DIR for static dir), E2+E5 (in-flight guard + AbortController), E3 (new test for None-after-Some semantic), E7 (chirpstack_available 3-way branch), E8 (Content-Type sniff), E9 (uptime <= 5 not <= 1), E13 (shared parseTimestamp). Iter-2 re-review per CLAUDE.md 'don't trust a single pass after a non-trivial patch round' produced 3 MED follow-ups (all patched): M1 (dashboard.js stale-render race — guard the second .then with inflightToken === thisCallToken), M2 (feature-detect AbortController — older browsers got ReferenceError), M3 (E3 test docstring re-scoped to honestly cover only InMemoryBackend; reference src/storage/sqlite.rs::tests::test_null_timestamp_preserves_last_successful_poll for the SQL path). Iter-2 also surfaced 6 LOW accepted as LOW (Content-Type case sensitivity, panic message specificity, pool size const, parseTimestamp numeric input, tracing-test poison hazard, buffer-clear helper extraction). Final test count: 340 lib+bins / 0 fail / 3 ignored (unchanged from iter-0); 16 integration binaries / 0 fail; tests/web_dashboard.rs reports 8 passed (was 7 — +E3). cargo clippy --all-targets -- -D warnings: clean. cargo test --doc: 0 fail (56 ignored — issue #100 baseline). AC#6 invariants reverified: git diff HEAD --stat src/opc_ua{.,_history,_session_monitor,_auth}.rs src/web/auth.rs produces zero output — all 5 files completely unchanged across iter-0 + iter-1 + iter-2 patch rounds. AC#7 grep contract reverified: exactly 3 distinct event= names in src/ (web_server_started 9-1 info, web_auth_failed 9-1 warn audit, api_status_storage_error 9-2 warn diag) with one emit site each. Story 9-2 closed via main story tracker issue #105 in the implementation commit (e1a42be); no new tracker issues opened in iter-1 or iter-2 (deferred LOW items inline in story file). Carry-forward GH issues unchanged: #88 (per-IP rate limiting), #100 (56 doctest ignores), #102 (tests/common reuse), #103 (Story 9-1), #104 (TLS hardening), #105 (Story 9-2). Next required action per CLAUDE.md BMad Workflow Commit & Push Discipline: commit Story 9-2 code-review-complete (per-iteration OR single-end-of-review pattern, both acceptable per the 2026-04-30 clarification), then proceed to Story 9-3 (Live Metric Values Display) OR run bmad-retrospective for Epic 9 milestone if epic-scope guidance is needed (Epic 9 is in-progress with 9-1 + 9-2 done; retrospective is optional until all stories complete). Previous narrative — Story 9-2 review — bmad-dev-story implementation complete in a single execution. ALL 7 ACs satisfied on first pass; loop terminates without iteration. Implementation summary: new src/web/api.rs module hosts the GET /api/status handler reading get_gateway_health_metrics() via a new per-task SqliteBackend (5th call site of the Story 4-1/5-1/8-3 per-task pattern in src/main.rs); new AppState struct in src/web/mod.rs lands the Story 9-1 deferred shape (auth + backend + dashboard_snapshot + start_time); new DashboardConfigSnapshot::from_config walks AppConfig.application_list once and freezes (application_count, device_count, applications: Vec<ApplicationSummary>); StatusResponse JSON ships 6 fields (chirpstack_available, last_poll_time, error_count, application_count, device_count, uptime_secs) with last_poll_time as RFC 3339 string or null; ErrorResponse is fixed {error: 'internal server error'} body (NFR7 — never leaks e.to_string() to clients); new event='api_status_storage_error' warn diagnostic on storage-read failure; static/index.html replaces the 9-1 placeholder with a 6-tile dashboard backed by static/dashboard.css (mobile-first responsive grid + prefers-color-scheme dark) and static/dashboard.js (fetch on load + 10s setInterval + inline error banner on 401/5xx); tests/web_dashboard.rs adds 4 integration tests (auth carry-forward + JSON shape pin + HTML markup + CSS responsive marker); 4 new unit tests for api_status (success / 500-with-generic-body / first-startup defaults / null-serialisation pin); 3 new unit tests for DashboardConfigSnapshot::from_config. Field-shape divergences from spec (all functionally equivalent — documented in Dev Agent Record): (1) handler return type uses Response rather than (StatusCode, Json<ErrorResponse>) — same wire behaviour, more idiomatic Axum 0.8. (2) Added 6th 'Uptime' tile beyond spec's '5-6 tiles' (data already in JSON, ~20 LOC total). (3) Used Arc<AppState> directly (not Axum's FromRef substate pattern) — keeps basic_auth_middleware signature unchanged for AC#6. (4) tests/web_auth.rs gained a wrap_in_app_state helper rather than inline fixture changes. (5) Cargo.toml not modified — serde_json was already a direct dep. Test count: 340 lib+bins (was 333 baseline; Δ=+7 within spec's '≈10 ± minor variance' budget). Integration test count: 16 binaries (was 15; +tests/web_dashboard.rs with 4 tests). cargo clippy --all-targets -- -D warnings: clean (after 2 iterations: doc-list-continuation lint on '+ how many' phrasing, await-holding-lock lint on tracing-test MutexGuard scope). cargo test --doc: 0 failed (56 ignored — issue #100 baseline). AC#6 invariants verified by zero-output git diff: src/opc_ua.rs, src/opc_ua_history.rs, src/opc_ua_session_monitor.rs, src/opc_ua_auth.rs, src/web/auth.rs all show 0 changes. AC#7 grep contract: exactly 3 distinct event names across src/ — web_server_started (9-1 info), web_auth_failed (9-1 warn audit), api_status_storage_error (9-2 warn diag). Documentation surface: docs/security.md § Web UI authentication → API endpoints (Story 9-2+) sub-section (no-anonymous-probe contract + storage-failure error shape), docs/logging.md event= registry +1 row, README.md Web UI subsection extended with dashboard paragraph + Epic 9 Planning row updated. Files: 4 new (src/web/api.rs, static/dashboard.css, static/dashboard.js, tests/web_dashboard.rs); ~8 modified (src/web/mod.rs, src/main.rs, static/index.html, tests/web_auth.rs, docs/security.md, docs/logging.md, README.md, this story file). Task 0 GitHub tracker issue deferred to commit time per user's standing policy on shared-state actions. Recommend running bmad-code-review on a different LLM per CLAUDE.md 'Code Review & Story Validation Loop Discipline' (over-reviewing > under-reviewing per user feedback). Previous narrative — Story 9-2 ready-for-dev — bmad-create-story 9-2 produced _bmad-output/implementation-artifacts/9-2-gateway-status-dashboard.md (~700 lines, modelled on Story 9-1 shape). Story closes FR38 + advances FR41: new GET /api/status JSON endpoint reading get_gateway_health_metrics() via a new per-task SqliteBackend (5th call site of the Story 4-1/5-1/8-3 per-task pattern); new AppState struct lands the Story 9-1 deferred shape (auth + backend + dashboard_snapshot); new DashboardConfigSnapshot captures application_count + device_count from AppConfig.application_list at startup (Story 9-7 hot-reload will swap in tokio::sync::watch); new src/web/api.rs hosts the handler (lands on the directory slot architecture.md:417-421 reserved for 'Stories 9-2 onwards'); new static/index.html replaces the 9-1 placeholder with real dashboard markup (5-6 status tiles); new static/dashboard.css ships mobile-first responsive layout + prefers-color-scheme dark; new static/dashboard.js fetches /api/status on load + 10s setInterval; new tests/web_dashboard.rs adds >=4 integration tests (auth carry-forward + JSON shape + HTML markup + CSS responsive marker). AC count: 7. Task count: 8. Story explicitly out-of-scope: live per-device metric values (Story 9-3), CRUD (9-4/9-5/9-6), SSE/WebSocket push, historical chart, CSP/HSTS headers, dashboard refresh-interval config knob. Test budget delta: ~+10 lib+bins (was 333 baseline, target >=343). Audit-event surface: ZERO new web_* events; ONE new api_* event (api_status_storage_error warn) — total event= grep across src/web/ goes from 2 to 3. AC#6 file invariants: zero changes to src/opc_ua_*.rs production code; zero changes to src/web/auth.rs production code (only fixture imports for AppState shape). Sequencing per epics.md Phase-B polish: 9-2 follows 9-1; 9-3 (live metrics) and 9-2 can run in parallel after 9-1; 9-0 spike still queued before 9-7/9-8. Recommend running on a different LLM per CLAUDE.md 'Code Review & Story Validation Loop Discipline'. Carry-forward GH issues unchanged: #88 (per-IP rate limiting), #100 (56 doctest ignores), #102 (tests/common reuse), #103 (story tracker), #104 (TLS hardening follow-up). One new GH issue to open at implementation start (Task 0): main story tracker for 9-2. Next: bmad-dev-story 9-2 to implement. Previous narrative — Story 9-1 done — bmad-code-review iter-1 + iter-2 loop terminated per CLAUDE.md. Iter-1 surfaced 4 decision-needed + 27 patch + 2 defer + 15 dismiss; user resolved decisions a/a/c/c and chose batch-apply; all 30 patches landed (4 decision-resolutions inline). Iter-2 re-ran the three reviewers per CLAUDE.md 'don't trust a single pass after a non-trivial patch round' and caught 1 HIGH regression: the iter-1 D4 patch wrapped axum::serve(...).with_graceful_shutdown(...) in tokio::time::timeout(5s, serve_future), which fired regardless of whether cancel had fired — making the server self-terminate after exactly 5 seconds of normal operation. Iter-2 fix restructures via tokio::select! between (a) the serve future and (b) a post-cancel deadline future (cancel.cancelled() + sleep(5s)) so the timeout only bounds the post-cancel drain. Pinned by new tests/web_auth.rs::test_server_stays_bound_past_shutdown_budget_when_idle (idles 6s, asserts port still bound — would have failed at the 5s mark on iter-1 code). Iter-2 also patched: 6 MED (test rename + const-assert for AC#1 default-off enforcement, sanitise_path with 256-char budget for audit-event path field, explicit control-char branch in auth_realm validator, fail-closed empty/whitespace-only credentials in WebAuthState::new, spec narrative cleanup deleting stale from_opcua_auth references + adding follow_symlinks(false) divergence entry + adding GRACEFUL_SHUTDOWN_BUDGET_SECS divergence entry), 6 LOW patched. 8 LOW dismissed (test boilerplate duplication, Drop::drop no longer awaits, Dockerfile path conventional, realm validator wording superseded, etc.). Final test count: 333 lib+bins / 0 fail / 3 ignored (Δ +24 from iter-0 baseline of 309). Total across all targets: 779 tests / 0 fail. cargo clippy --all-targets -- -D warnings clean. cargo test --doc 0 fail (56 ignored — issue #100 baseline). AC#7 invariants reverified: src/opc_ua_session_monitor.rs / src/opc_ua.rs / src/opc_ua_history.rs all show 0 changes; src/opc_ua_auth.rs only the allowed extraction + #[allow(dead_code)] pub fn hmac_key() accessor + test fixture import. AC#8 invariant reverified: git grep 'event=\"web_' src/ returns exactly 2 distinct values (web_auth_failed at src/web/auth.rs:294 + web_server_started at src/web/mod.rs:206). Next required action per CLAUDE.md BMad Workflow Commit & Push Discipline: commit Story 9-1 code-review-complete (per-iteration commit pattern OR single-end-of-review commit, both acceptable per the 2026-04-30 clarification), then proceed to Story 9-2 OR run bmad-retrospective for Epic 9 milestone if epic-scope guidance is needed (Epic 9 is in-progress with 9-1 done; retrospective is optional until all stories complete). Carry-forward GH issues unchanged: #88 (per-IP rate limiting), #100 (56 doctest ignores), #103 (story tracker), #104 (TLS hardening follow-up). Previous narrative — Story 9-1 review — bmad-dev-story implementation complete in a single execution. ALL 8 ACs satisfied on first pass; loop terminates without iteration. Implementation summary: new src/security_hmac.rs module extracts hmac_sha256 from src/opc_ua_auth.rs (Phase-B carry-forward rule per epics.md:782 — share the HMAC primitive between OPC UA and web auth surfaces); new src/web/{mod,auth}.rs hosts the Axum 0.8 web server + Basic-auth middleware (10 unit tests in src/web/auth.rs::tests covering all 6 failure modes + success + is_configured-false defence + sanitiser + AuthFailureReason::as_str stability); new tests/web_auth.rs runs 7 integration tests against a real spawned server (auth-fail audit pinned via tracing-test global buffer + reqwest client). Field-shape divergences from spec (all functionally equivalent — documented in Dev Agent Record): (1) used WebAuthState::new (Shape 1, fresh per-process key) instead of from_opcua_auth (Shape 2, shared key) because OpcgwAuthManager is constructed inside OpcUa::run rather than main.rs and AC#6 forbids modifying src/opc_ua.rs to surface it; from_opcua_auth is exposed but #[allow(dead_code)] for symmetry. (2) used Option<JoinHandle> + conditional await after the existing 4-handle try_join! instead of a literal 5th tokio::select! arm — same CancellationToken fan-out, correctly models 'this handle may not exist when [web].enabled = false'. (3) initial implementation pass added web_server_stopped + web_server_disabled events; AC#8's strict 'exactly two new event names' rule trimmed those to plain info! lines without event= field. (4) used constant_time_eq (existing direct dep from Story 7-2) instead of the spec's subtle::ConstantTimeEq for consistency with the OPC UA path. (5) AppState (spec Task 3) deferred to Stories 9-2+ — Story 9-1 doesn't read storage. (6) tower-http 0.6 instead of spec's 0.5 (axum 0.8 expects 0.6.x). Test count: 326 lib+bins (was 309 baseline; Δ=+17 within spec's '≈14 ± minor variance' budget). Integration test count: 15 binaries / 0 failures including new tests/web_auth.rs (7 web + 3 reused common helpers). cargo clippy --all-targets -- -D warnings: clean. cargo test --doc: 0 failed (56 ignored — issue #100 baseline). AC#7 invariants: src/opc_ua_session_monitor.rs / src/opc_ua.rs / src/opc_ua_history.rs all show 0 changes; src/opc_ua_auth.rs only the allowed extraction + pub fn hmac_key() accessor + WebConfig::default() in test fixture (no logic changes). AC#8 grep contract: exactly 2 distinct event names introduced (web_auth_failed audit warn, web_server_started diagnostic info), each with one emit site. Documentation surface: docs/security.md § Web UI authentication (full What/Configuration/Logs/Anti-patterns/Tuning shape mirroring Story 7-2 + 7-3 sections), docs/logging.md § Audit and diagnostic events (event=) catalogues all event= names introduced from Story 7-2 onward, README.md Configuration + Planning row updated, deferred-work.md +4 entries (user-manual chapter, TLS hardening at #104, per-IP rate limiting follow-up of #88, CSRF-for-9-4+ note). Tracking issues: #103 main, #104 TLS follow-up. Dependencies added: axum=0.8, tower-http=0.6 (with fs feature), base64=0.22 as direct deps; reqwest=0.12 + tower=0.5 as dev-deps. Files: 5 new (src/security_hmac.rs, src/web/mod.rs, src/web/auth.rs, 4 static/*.html, tests/web_auth.rs); ~14 modified (Cargo.toml, src/utils.rs, src/config.rs, src/opc_ua_auth.rs test-fixture only, src/lib.rs, src/main.rs, tests/common/mod.rs, 4 test fixtures with WebConfig::default() addition, config/config.toml, config/config.example.toml, docs/security.md, docs/logging.md, README.md, deferred-work.md, this story file, this sprint-status.yaml). Recommend running bmad-code-review on a different LLM per CLAUDE.md 'Code Review & Story Validation Loop Discipline' (over-reviewing > under-reviewing per user feedback). Previous narrative — Story 9-1 ready-for-dev. Comprehensive spec created via bmad-create-story 9-1 at _bmad-output/implementation-artifacts/9-1-axum-web-server-and-basic-authentication.md (~700 lines, modelled on Story 8-3 shape). Story closes FR50/NFR11/NFR12 + partial FR41: Axum 0.8 web server, HMAC-SHA-256 keyed basic auth (reused from Story 7-2 OpcgwAuthManager — extracted hmac_sha256 to a shared primitive), event=\"web_auth_failed\" audit + event=\"web_server_started\" diagnostic with source IP via Axum's ConnectInfo<SocketAddr> (no two-event correlation needed unlike async-opcua), tower_http::ServeDir mount, opt-in [web].enabled = false default, integration with existing CancellationToken + tokio::select! shutdown sequence. ~250-400 LOC production + ~250-400 LOC tests + ~100 LOC docs. Story explicitly out-of-scope: TLS (operator-deploys reverse proxy), per-IP rate limiting (#88 carry-forward), CSRF (Story 9-4+ will need it), session cookies, mTLS, multi-user RBAC, Story 9-7 hot-reload of [web] config. AC count: 8 (config + middleware + audit event + lifecycle + static files + tests-green + carry-forward intact + sanity counts). Task count: 8. Epic 9 flipped backlog → in-progress (first story in epic). Per Epic 8 retro action items #2-7, all carry-forward prep COMPLETE before this story creation: doctest cleanup (#100, commit 4445cc8) + NodeId fix (#99, commit 9f823cc) + spike-test productionisation (#101, commit c175a17) + tests/common extraction (#102, commit 16b071d) + CLAUDE.md per-iteration commit clarification (commit 45026f1) + Story 9-0 spike decision (commit f7946db). 6 GH tracker issues for completed stories closed (issues #75/#76/#77/#78/#84/#96). Test baseline post-Epic-8-carry-forward: 309 lib+bins / 0 fail / 3 ignored; 56 ignored doctests; clippy clean. Next: bmad-dev-story 9-1 to implement. Recommend running on a different LLM per CLAUDE.md \"Code Review & Story Validation Loop Discipline\" (story 9-1 implementation in one model, code review in another). Sequencing per epics.md Phase-B polish: 9-1 → 9-2 → 9-3 → 9-0 spike → 9-7 → 9-8 → 9-4 / 9-5 / 9-6 (CRUD depends on 9-7 hot-reload). Story 9-5 was annotated 'blocked by issue #99' — that's now resolved (commit 9f823cc landed the NodeId fix). Previous narrative — Epic 8 retrospective complete (2026-05-01). Stories 8-1, 8-2, 8-3 shipped under Phase B subscription + historical-data baseline; 8-4 classified as Known Failure per Story 6-3 convention with operator-visible diagnostics treatment (SCADA-side alarm-threshold configuration is the recommended replacement). Retrospective doc at _bmad-output/implementation-artifacts/epic-8-retro-2026-05-01.md. Key Epic 8 discoveries: (1) access-control enforced before node-manager overrides — metric variables now carry HISTORY_READ access level + historizing=true; without this async-opcua's session dispatch returns BadUserAccessDenied before the override is reached. (2) NodeId metric-name-only collision is a pre-existing latent HIGH-severity bug Epic 8 surfaced via HistoryRead — must fix before Epic 9 ships multi-device CRUD (Story 9-5). (3) Spike-test productionisation overdue — 7 findings deferred during 8-2 iter-1 review carry forward. Action items decided in retro: (a) tag 8-4 as KF in deferred-work.md (DONE in this commit), (b) open GH issue for NodeId collision before Epic 9 spec polish, (c) fix test_concurrent_write_read_isolation flake before Epic 9 starts, (d) doctest cleanup is BLOCKING before Epic 9 (4th epic in a row), (e) clarify CLAUDE.md per-iteration commit rule (8-2 was a combined commit slip), (f) open spike-test productionisation cleanup story before Epic 9, (g) open tests/common/mod.rs extraction story before 5th integration-test file. Carry-forward GH issues: #88 #93 #94 #95 #98. Loop terminated per CLAUDE.md (only LOW remains; auditor clean across all stories). Test count: ~700+ workspace tests pass / 0 fail / 7 ignored. Pre-existing flake test_concurrent_write_read_isolation under heavy parallel load documented. Next required action: commit Epic 8 retrospective + git push (visibility checkpoint per CLAUDE.md). Then schedule Epic 9 prep work (NodeId fix / doctest cleanup / spike-test productionisation / tests/common extraction / Epic 9 spec polish) BEFORE invoking bmad-create-story 9-1. Previous narrative — Story 8-3 done. Code review iter-1 + iter-2 (3 adversarial layers each: Blind Hunter, Edge Case Hunter, Acceptance Auditor) complete. Iter-1 surfaced 2 decision-needed + 28 patch + 12 defer + 13 dismiss; user resolved D1 → implement AC#2.6 concurrent-subscription-same-session test, D2 → accept retention_days field-extension over spec's history_retention_days new field. Iter-1 patch round applied 31 patches (11 HIGH correctness: DataType match per metric_type, null-DateTime guards, is_read_modified/return_bounds/continuation_point/index_range rejection, fail-closed startup, retention upgrade error message, RwLock snapshot-then-iterate, Variant::Float instead of Double, BadInternalError instead of BadHistoryOperationInvalid; 14 MED: client-cap branch test, InMemoryBackend warn, OpcGwError::Storage unification, traced_test capture, doc clarification, README boundary fix, max_results saturate, Status-Good/timestamp-resolution docs, num_values_per_node=0 log, spec consistency, config.example.toml, audit-event narrative, test count narrative; 3 LOW: bench debug-build panic, plus 2 left-as-is). Iter-2 review found 3 LOW spec/doc inconsistencies (AC#3 heading, Arc<HashMap> spec text, test count math) + 2 MED follow-up patches (info!→debug! for num_values_per_node=0 log noise, throttle InMemoryBackend warn to once-per-process via AtomicBool) — all addressed. Loop terminates per CLAUDE.md (only LOW remains; auditor clean). Final tally: 309 lib+bins tests pass / 7 opcua_history integration tests pass / clippy --all-targets -- -D warnings exits 0. Pre-existing flake test_concurrent_write_read_isolation under heavy parallel load is documented in deferred-work.md from Story 8-2 — not Story 8-3 regression. Files modified by iter-1+iter-2 patch rounds: src/opc_ua.rs (per-metric-type initial Variant), src/opc_ua_history.rs (history_read_raw_modified guards + RwLock snapshot + Variant::Float + BadInternalError + log demotion), src/main.rs (fail-closed retention startup), src/storage/{sqlite,memory,mod}.rs (saturated cast + tracing-test + once-per-process warn + doc clarifications), src/config.rs (upgrade-aware retention error), README.md (boundary semantics), docs/security.md (Status-Good limitation, timestamp resolution, retention paragraph), config/config.example.toml (max_history_data_results_per_node block), tests/opcua_history.rs (P-D1 concurrent test + P12 client-cap branch test), tests/opcua_history_bench.rs (panic on debug build), this story file (Review Findings + spec text fixes), this sprint-status.yaml, deferred-work.md (12 deferred items + iter-2 narrative). Next: commit Story 8-3 code-review-complete then run bmad-retrospective for Epic 8 (8-4 deferred to backlog per epic plan; epic-8-retrospective is now required per CLAUDE.md "treat optional as required when all stories done"). Previous narrative (kept for traceability) — bmad-dev-story execution complete. Implemented in a single execution under the dev-story continue-until-complete contract. AC#1 (StorageBackend::query_metric_history with half-open interval, partial-success on bad rows, 11 unit tests across SqliteBackend + InMemoryBackend) + AC#2 (OpcgwHistoryNodeManager wrap of SimpleNodeManagerImpl in new src/opc_ua_history.rs ~390 LOC overriding history_read_raw_modified, NodeId→(device_id,metric_name) reverse-lookup map populated in OpcUa::add_nodes, metric variables now carry HISTORY_READ access level + historizing=true — without the access-level fix async-opcua's session dispatch returns BadUserAccessDenied before the override is reached, 5 integration tests in tests/opcua_history.rs + 3 module-level unit tests) + AC#3 (extended existing [storage].retention_days field validation FR22 floor 7 / hard cap 365 + new [opcua].max_history_data_results_per_node Option<usize> default 10000 hard cap 1_000_000, retention written to retention_config table at startup via INSERT OR REPLACE in new SqliteBackend::set_metric_history_retention_days method, 11 unit tests + 1 retention-config UPSERT integration test) + AC#5 (continuation points NOT implemented — manual-paging recipe documented in docs/security.md, grep confirms zero set_next_continuation_point calls) + AC#6 (702 tests pass / 0 fail / 8 ignored; clippy --all-targets -- -D warnings exits 0) + AC#7 (NFR12 carry-forward intact: src/opc_ua_session_monitor.rs unchanged; src/opc_ua_auth.rs has 1-line test-fixture change only — production code untouched, documented in deferred-work.md) + AC#8 (zero new audit events; +59 default test count). AC#4 (NFR15 release-build benchmark) wired in tests/opcua_history_bench.rs targeting 600k-row 7-day query <2s but not run during this story (would take ~35s); listed in deferred-work.md for first release-build CI lane that includes --ignored tests. Field-shape divergence from spec: implementation extended existing StorageConfig.retention_days rather than adding a duplicate history_retention_days field — strictly cleaner one-source-of-truth design. Test-harness extraction to tests/common/mod.rs deferred (4th-file threshold trigger but the four files diverge in subtle ways — clean-up story tracked in deferred-work.md). Files: src/storage/{mod,sqlite,memory}.rs, src/opc_ua.rs, src/opc_ua_history.rs (NEW), src/config.rs, src/utils.rs, src/main.rs, src/lib.rs, tests/opcua_history.rs (NEW, 5 tests), tests/opcua_history_bench.rs (NEW, 1 ignored test), tests/{opc_ua_connection_limit,opc_ua_security_endpoints,opcua_subscription_spike}.rs (test-fixture updates only), config/config.toml, docs/security.md (+120 LOC new section), README.md, _bmad-output/implementation-artifacts/deferred-work.md, _bmad-output/implementation-artifacts/8-3-historical-data-access-via-opc-ua.md, this file. Next: commit Story 8-3 with the implementation, then run code-review (recommended via different LLM per CLAUDE.md). Previous narrative — Story 8-3 ready-for-dev. Comprehensive spec created via bmad-create-story 8-3 covering: new `query_metric_history` storage method (AC#1, 9 unit tests), OPC UA HistoryRead service handler via `OpcgwHistoryNodeManager` wrap of SimpleNodeManagerImpl (AC#2, 6 integration tests), `[storage].history_retention_days` + `[opcua].max_history_data_results_per_node` config knobs (AC#3, 11 unit tests), NFR15 release-build performance benchmark targeting 600k-row 7-day query in <2s (AC#4, 1 ignored bench test), continuation-points explicit-non-implementation with documented manual-paging recipe (AC#5), NFR12 carry-forward (AC#7, zero changes to opc_ua_auth.rs / opc_ua_session_monitor.rs, +0 events). Spec identifies a misleading code comment at src/storage/sqlite.rs:952-955 to fix as part of Task 1 (the comment references this story by its old number 7-3). Test-harness extraction to tests/common/mod.rs flagged as the 4th-file threshold trigger but allowed to defer if invasive. Estimated diff: ~1000 LOC including ~250 LOC new opc_ua_history module + ~350 LOC tests. Two follow-up GitHub issues to open at implementation start (Task 0): main story tracker + carry-forward bucket for HistoryReadProcessed/AtTime + continuation-points + per-metric retention. Previous narrative: Story 8-2 done — code review iterations 1+2 complete. Iter-2 (autonomous-loop): re-ran review per CLAUDE.md after iter-1 14-patch round; auditor declared clean; adversarial layers surfaced 1 MED (cross-knob coherence silently skipped when only one knob set) + 7 LOWs. Iter-2 patched: cross-knob now resolves unset knobs via unwrap_or(default) + skips when either is Some(0); aligned OPCUA_MAX_MESSAGE_SIZE_HARD_CAP = 4096 × 65535 = 268_431_360 with chunk-count cap (was 256 MiB literal which was 4096-byte off); +2 cross-knob unit tests; OPCUA_MIN_CHUNK_SIZE_BYTES doc-comment corrected (65535 is async-opcua's per-chunk ceiling, not OPC UA Part 6's TransportProfile minimum of 8192); BadTooManySubscriptions || TooManySubscriptions simplified; AC#3.2 dead assert removed; generated-source line numbers dropped from docs; startup-log poll budget 5s → 10s for loaded CI; updated 2 pre-existing tests broken by cross-knob default-fallback change. All 14 opcua_subscription_spike tests pass; all 60 config tests pass; clippy clean. One pre-existing SQLite-concurrency flake (test_concurrent_write_read_isolation) under heavy parallel load — deferred to storage-test productionisation pass. Loop terminates per CLAUDE.md (only LOW remains; auditor clean). Story flipped review → done. Iter-1 baseline: 3 decision-needed (all resolved Option 1) + 11 patches applied + 8 deferred (pre-existing 8-1 baseline + NFR7 carry-forward) + ~14 dismissed. Applied: status-code pinning on AC#3.1/3.2 (BadTooManySubscriptions + total_successes==3), cross-knob coherence validation (max_chunk_count × 65535 < max_message_size + OPCUA_MIN_CHUNK_SIZE_BYTES + 2 unit tests), unfiltered DataChangeFilter regression test pinning async-opcua's value-only-dedup contract (#94), AC#3.3 outage-loop flake fix (12s budget + per-iteration timeout consumes remaining), AC#2 startup-log shape regression test (event=opcua_limits_configured field-name pinning), DataChangeTrigger Part 4 §7.22.2 citation correction (library default is Status not StatusValue), ..Default::default() on DataChangeFilter literal, README sync + Epic 8 row compression, limits_mut() ordering invariant comment, dv.status==Good recovery assertion tightened. Final test count: 641 pass / 0 fail / 7 ignored on cargo test --lib --bins --tests; clippy clean. NFR12 carry-forward still intact. Tracker issue #96; Closes #89 at the code-review-complete commit. Carry-forward issues #88/#90/#93/#94/#95 unchanged. Next: commit Story 8-2 code-review-complete then run bmad-retrospective for Epic 8 if 8-3/8-4 are deferred or proceed to 8-3.)"
project: opcgw
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # Epic 1: Crash-Free Gateway Foundation (Phase A)
  epic-1: done
  1-1-update-dependencies-and-rust-toolchain: done
  1-2-migrate-logging-from-log4rs-to-tracing: done
  1-3-comprehensive-error-handling: done
  1-4-graceful-shutdown-with-cancellationtoken: done
  1-5-configuration-validation-and-clean-startup: done
  epic-1-retrospective: done

  # Epic 2: Data Persistence (Phase A) — Refined: 5 stories → 15 stories (tight granularity, 1-3 days each; includes 1 legacy file)
  epic-2: done

  # Phase 2-1: StorageBackend Trait & Core Types (Foundational)
  2-1a-storagebackend-trait-definition: done
  2-1b-core-storage-data-types: done
  2-1c-inmemorybackend-implementation: done

  # Phase 2-2: SQLite Schema & Infrastructure (+ Type System Alignment Bridge)
  2-2-type-system-sql-alignment: done
  2-2a-sqlite-schema-design: done
  2-2b-schema-creation-and-migration: done
  2-2x-per-task-connections: done
  2-2c-sqlitebackend-basic-crud-implementation: done
  2-2d-prepared-statements-and-sql-safety: done

  # Phase 2-3: Metric Persistence & Optimization
  2-3a-metric-values-persistence-upsert: done
  2-3b-historical-metrics-append-only: done
  2-3c-batch-write-optimization: done

  # Phase 2-4: Startup Restore & Degradation
  2-4a-metric-restore-on-startup: done
  2-4b-graceful-degradation: done

  # Phase 2-5: Historical Data Management
  2-5a-historical-data-pruning-task: done
  2-5b-pruning-integration-testing: done

  epic-2-retrospective: done

  # Epic 3: Reliable Command Execution (Phase A)
  epic-3: done
  3-1-sqlite-backed-fifo-command-queue: done
  3-2-command-parameter-validation: done
  3-3-command-delivery-status-reporting: done
  epic-3-retrospective: done

  # Epic 4: Scalable Data Collection (Phase A)
  epic-4: done  # 2026-05-06 — Story 4-4 done after iter-3 code review (loop terminated under CLAUDE.md condition 1: zero open findings; cargo test 837/0/8 + cargo clippy clean). Epic 4 returned to done. Retrospective remains done (Epic 4 retro 2026-04 flagged 4-4 as soft-deferred — not re-running on 4-4 completion).
  4-1-poller-refactoring-to-sqlite-backend: done
  4-2-support-all-chirpstack-metric-types: done
  4-3-api-pagination-for-large-deployments: done
  # 2026-05-06: Story 4-4 done. Iter-3 review (2026-05-06) surfaced 1 HIGH (D2: shipped retry/delay defaults violated AC#4) + 4 MED/LOW patches. All resolved: P14 tightened config.toml retry=10,delay=10 → retry=30,delay=1 (worst-case 30s budget; satisfies AC#4 retry × delay ≤ 30); P10 preserves original gRPC outage cause separate from last probe error in cancel branch + Exhausted return; P11 clarified retry/attempt semantics in docs/logging.md; P12 updated chirpstack_outage operator-action runbook; P13 replaced fragile field-order-coupled downtime_secs assertion with token-boundary scan via tracing_test::internal::global_buf().
  4-4-auto-recovery-from-chirpstack-outages: done
  epic-4-retrospective: done

  # Epic 5: Operational Visibility (Phase A)
  epic-5: done
  5-1-opc-ua-server-refactoring-to-sqlite-backend: done
  5-2-stale-data-detection-and-status-codes: done
  5-3-gateway-health-metrics-in-opc-ua: done
  epic-5-retrospective: done

  # Epic 6: Production Observability & Diagnostics (Phase A)
  epic-6: done
  6-1-comprehensive-logging-infrastructure: done
  6-2-configurable-log-verbosity: done
  6-3-remote-diagnostics-for-known-failures: done
  epic-6-retrospective: done

  # Epic 7: Security Hardening (Phase A)
  epic-7: done
  7-1-credential-management-via-environment-variables: done
  7-2-opc-ua-security-endpoints-and-authentication: done
  7-3-connection-limiting: done
  epic-7-retrospective: done

  # Epic 8: Real-Time Subscriptions & Historical Data (Phase B)
  epic-8: in-progress
  8-1-async-opcua-subscription-spike: done
  8-2-opc-ua-subscription-support: done
  8-3-historical-data-access-via-opc-ua: done
  8-4-threshold-based-alarm-conditions: backlog  # KF — Known Failure per epic-8 retro 2026-05-01; see deferred-work.md
  epic-8-retrospective: done

  # Epic 9: Web Configuration & Hot-Reload (Phase B)
  # Sequencing per Epic 8 retro action item #5 (2026-05-02): 9-0 spike runs
  # after 9-1/9-2/9-3 (those don't touch OPC UA address space) and before
  # 9-7 (hot-reload depends on the spike's findings about address-space
  # mutation under live subscriptions).
  #
  # PRODUCTION-DEPLOYMENT BLOCKER (Story 9-3 review iter-1, 2026-05-03):
  # Issue #108 — MetricType is payload-less, every metric_values row has
  # value == data_type string ("Float" / "Int" / etc.) instead of the
  # actual measurement. Affects 4 epics (2, 5, 8, 9-3). opcgw has never
  # actually persisted real metric values. Fix is an Epic-1-scale storage
  # trait refactor (proposed: "Epic A — Storage Payload Migration").
  # Epic 9 retrospective is BLOCKED on #108: any "lessons learned" pass
  # over Epic 9 needs to acknowledge that Stories 9-2 + 9-3 are
  # surface-correct but data-incorrect until #108 lands.
  epic-9: in-progress
  9-0-async-opcua-runtime-address-space-mutation-spike: done
  9-1-axum-web-server-and-basic-authentication: done
  9-2-gateway-status-dashboard: done
  9-3-live-metric-values-display: done
  9-4-application-crud-via-web-ui: backlog
  9-5-device-and-metric-mapping-crud-via-web-ui: backlog  # blocked by issue #99 (NodeId collision); fixed in commit 9f823cc
  9-6-command-crud-via-web-ui: backlog
  # 2026-05-06: bmad-create-story 9-7 produced 9-7-configuration-hot-reload.md (~580 lines, 12 ACs: 6 from epics.md:899-914 + 6 carry-forward from Stories 4-4 / 5-2 / 7-2 / 9-0 / 9-1 / 9-2 / 9-3). SIGHUP-only trigger surface (web POST deferred to 9-4+ when first POST/CSRF lands). Validate-then-swap with restart-required-knob rejection; tokio::sync::watch::Sender<Arc<AppConfig>> per Story 4-4's pre-commitment. Knob taxonomy (load-bearing design call): hot-reload-safe vs restart-required vs address-space-mutating (9-8 territory) vs auth-rotating (rebuild WebAuthState digests). 9-7 stubs the OPC UA address-space mutation seam for 9-8; dashboard updates atomically on reload but OPC UA address space stays at startup state until 9-8 lands. ~80-120 LOC in new src/config_reload.rs + ~30 LOC poller wiring + ~30 LOC web wiring + ~20 LOC OPC UA listener stub + ~150-250 LOC integration tests in new tests/config_hot_reload.rs.
  9-7-configuration-hot-reload: done
  9-8-dynamic-opc-ua-address-space-mutation: backlog  # depends on 9-0 spike findings
  epic-9-retrospective: optional  # BLOCKED on issue #108 (storage payload-less MetricType — production-deployment blocker)

---

## Next Session Checklist for Story 5-3

Before marking complete, verify:
- [ ] Manual smoke test with fresh database
- [ ] OPC UA client can browse Gateway folder
- [ ] Migration v006 works on production-like data
- [ ] cargo clippy clean
- [ ] No regressions in other OPC UA variables

Ready to continue anytime. All code changes are in place and tested.
