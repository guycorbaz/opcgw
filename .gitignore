# Rust
/target/
Cargo.lock
**/*.rs.bk
*.pdb

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Build artifacts
*.o
*.a
*.so
*.dylib

# Logs
/log/
*.log

# Config & Secrets
config/config.local.toml
# Epic C C-0 (2026-05-21): the first-run web wizard at /setup writes
# the OPC UA user_password to config/secrets.toml (file mode 0600).
# This file is machine-specific and must NEVER be committed.
config/secrets.toml
.env
.env.local

# Test artifacts
.coverage/

# PKI (can be regenerated)
pki/

# User-manual build artifacts — regeneratable via `make pdf` in docs/manual/
# (LaTeX edition; per-dir rules also in docs/manual/latex/.gitignore)
docs/manual/out/
docs/manual/latex/figures/
docs/manual/latex/version.tex
docs/manual/latex/*.aux
docs/manual/latex/*.log
docs/manual/latex/*.toc
docs/manual/latex/*.out
docs/manual/latex/*.fls
docs/manual/latex/*.fdb_latexmk
docs/manual/latex/main.pdf
docs/manual/latex/opcgw-user-manual.pdf
